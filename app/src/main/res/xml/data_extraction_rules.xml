<?xml version="1.0" encoding="utf-8"?>
<!-- Privacidade: whitelist, historico e configuracoes NAO saem do aparelho via
     backup em nuvem nem transferencia device-to-device (docs/PRIVACIDADE.md).
     `path="."` e exigido pela documentacao oficial em include/exclude; a exclusao
     de diretorio e recursiva, entao -wal e -shm ficam cobertos sem serem listados
     (nomes transientes nao devem virar contrato). -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="datastore" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="datastore" />
    </device-transfer>
</data-extraction-rules>
