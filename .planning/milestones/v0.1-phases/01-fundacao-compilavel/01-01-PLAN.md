---
phase: 01-fundacao-compilavel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/build.gradle.kts
  - app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml
  - app/src/main/res/mipmap-anydpi-v26/ic_launcher_round.xml
  - app/src/main/res/mipmap-anydpi/ic_launcher.xml
  - app/src/main/res/mipmap-anydpi/ic_launcher_round.xml
autonomous: true
requirements: [QLT-02]

must_haves:
  truths:
    - "O relatorio de lint fica com zero issues (nao apenas zero errors), fechando a letra de QLT-02"
    - "A unica issue de lint corrigida de verdade (ObsoleteSdkInt) desaparece por remocao do qualificador -v26, nao por supressao"
    - "Nenhuma string ou cor de recurso e apagada para calar o lint"
  artifacts:
    - path: "app/build.gradle.kts"
      provides: "Bloco lint { } com abortOnError = true e disable justificado por comentario"
      contains: "lint {"
    - path: "app/src/main/res/mipmap-anydpi/ic_launcher.xml"
      provides: "Icone adaptativo sem qualificador de API obsoleto (minSdk 29 > 26)"
  key_links:
    - from: "app/build.gradle.kts"
      to: "app/build/reports/lint-results-debug.xml"
      via: "task ./gradlew lint aplicando a politica declarada"
      pattern: "lint \\{"
    - from: "app/src/main/AndroidManifest.xml"
      to: "app/src/main/res/mipmap-anydpi/ic_launcher.xml"
      via: "android:icon=\"@mipmap/ic_launcher\" continua resolvendo apos o rename"
      pattern: "@mipmap/ic_launcher"
---

<objective>
Fechar QLT-02 na letra: transformar os 137 warnings do lint em zero issues por meio de uma
politica declarada em um unico lugar (`lint { }` no `app/build.gradle.kts`) mais UMA correcao
real (remocao do qualificador `-v26` do mipmap adaptativo).

Purpose: hoje `./gradlew lint` termina com 0 errors mas 137 warnings. O criterio 1 da fase
("termina sem erro") passa; QLT-02 ("lint sem issues") NAO passa. Sem essa politica a fase
fecha ambigua e QLT-02 reabre como gate na Phase 9.
Output: `app/build.gradle.kts` com bloco `lint {}` comentado e `mipmap-anydpi/` sem qualificador.

NAO faz parte deste plano: apagar qualquer string ou cor de `res/values/`, criar
`lint-baseline.xml`, bumpar AGP, tocar em `CallDecisionEngine`, tema ou catalogo de versoes.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fundacao-compilavel/01-CONTEXT.md
@.planning/phases/01-fundacao-compilavel/01-RESEARCH.md
@CLAUDE.md
@app/build.gradle.kts

<interfaces>
<!-- Estado atual verificado em 2026-07-29. O executor NAO precisa explorar o codebase. -->

Composicao exata dos 137 warnings de `app/build/reports/lint-results-debug.xml`:

| id | qtd | Natureza | Tratamento DECIDIDO |
|----|-----|----------|---------------------|
| `UnusedResources` | 132 | strings/cores pre-escritas para as telas das Fases 5-9 (`welcome_*`, `history_*`, `support_*`, `R.color.sentinela_primary`, `ic_launcher_round`) | `disable` com comentario. NUNCA deletar |
| `Typos` | 3 | falso positivo de dicionario en-US sobre pt-BR ("'momento' ... did you mean 'memento'?") | `disable` |
| `AndroidGradlePluginVersion` | 1 | informa AGP 9.3.1 (sem release notes publicadas; pin 9.3.0 e decisao de projeto) | `disable` |
| `ObsoleteSdkInt` | 1 | `mipmap-anydpi-v26` e desnecessaria com minSdk 29 | CORRECAO REAL: rename da pasta |

`app/build.gradle.kts` hoje: bloco `android { }` das linhas 21 a 64, terminando com
`testOptions { unitTests { isIncludeAndroidResources = true; isReturnDefaultValues = true } }`.
NAO existe bloco `lint`. No AGP 9 o DSL e `lint { }` — `lintOptions { }` foi REMOVIDO.

Conteudo de `app/src/main/res/mipmap-anydpi-v26/`: `ic_launcher.xml` e `ic_launcher_round.xml`.
`AndroidManifest.xml` linha 14 referencia `android:icon="@mipmap/ic_launcher"` (nao ha
`android:roundIcon`). O rename da pasta NAO altera o nome do recurso, so o qualificador.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declarar a politica de lint no app/build.gradle.kts</name>
  <files>app/build.gradle.kts</files>
  <read_first>
    - app/build.gradle.kts (arquivo a modificar — bloco `android { }`, linhas 21-64)
    - .planning/phases/01-fundacao-compilavel/01-CONTEXT.md (secao "Politica de lint (decidida em 2026-07-29)" — decisao travada)
    - .planning/phases/01-fundacao-compilavel/01-RESEARCH.md (Code Examples §4 — bloco exato; Pitfall 2 — composicao dos warnings)
  </read_first>
  <action>
Adicionar, DENTRO do bloco `android { }` de `app/build.gradle.kts` (logo apos o bloco
`testOptions { }`, antes do fechamento do `android { }` na linha 64), exatamente este bloco,
com os comentarios preservados verbatim (eles SAO a justificativa auditavel exigida pelo
CONTEXT.md):

```kotlin
    lint {
        abortOnError = true // explicito: erro de lint quebra o build

        disable += setOf(
            // Strings e cores pre-escritas para as telas das Fases 5-9
            // (docs/design/TELAS.md). Sao ativos legitimos, nao lixo:
            // apagar destroi trabalho e recria custo nas fases de UI.
            // Reavaliar na Phase 9, quando as telas reais consumirem as strings.
            "UnusedResources",
            // O dicionario do lint e en-US e acusa falso positivo em conteudo
            // pt-BR ("momento" != "memento"). O idioma padrao do app e pt-BR.
            "Typos",
            // A versao do AGP e decisao de projeto (.planning/research/STACK.md):
            // 9.3.0 e a linha de base validada de ponta a ponta. Nao ha release
            // notes publicadas para a 9.3.1.
            "AndroidGradlePluginVersion",
        )
    }
```

Regras nao negociaveis desta task:
- Usar `lint { }`, NUNCA `lintOptions { }` (removido no AGP 9).
- NAO adicionar `baseline = file("lint-baseline.xml")` — proibido pelo CONTEXT.md
  ("Nao adotar lint-baseline.xml — exigiria regeneracao a cada fase que adiciona strings").
- NAO adicionar `ObsoleteSdkInt` ao `disable` — ele e corrigido de verdade na Task 2.
- NAO apagar nenhuma entrada de `app/src/main/res/values/strings.xml` ou `colors.xml`.
- NAO alterar nenhuma outra linha do arquivo (versoes, dependencias, sdk, signingConfigs).
  </action>
  <verify>
    <automated>export ANDROID_HOME="${ANDROID_HOME:-$HOME/Library/Android/sdk}"; ./gradlew lint && grep -c "id=\"UnusedResources\"\|id=\"Typos\"\|id=\"AndroidGradlePluginVersion\"" app/build/reports/lint-results-debug.xml; test "$(grep -c 'id="UnusedResources"' app/build/reports/lint-results-debug.xml)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "lint {" app/build.gradle.kts` retorna `1`
    - `grep -c "lintOptions" app/build.gradle.kts` retorna `0`
    - `grep -c "lint-baseline" app/build.gradle.kts` retorna `0`
    - `grep -n "abortOnError = true" app/build.gradle.kts` encontra a linha dentro do bloco lint
    - `grep -c "UnusedResources\|Typos\|AndroidGradlePluginVersion" app/build.gradle.kts` retorna `3`
    - `grep -c "ObsoleteSdkInt" app/build.gradle.kts` retorna `0`
    - Apos `./gradlew lint`: `grep -c 'id="UnusedResources"' app/build/reports/lint-results-debug.xml` retorna `0`
    - Apos `./gradlew lint`: `grep -c 'id="Typos"' app/build/reports/lint-results-debug.xml` retorna `0`
    - `git diff --stat app/src/main/res/values/` nao mostra nenhuma linha removida (nenhum recurso apagado)
  </acceptance_criteria>
  <done>O bloco `lint {}` existe com os 3 `disable` comentados, `abortOnError = true`, sem baseline, e o relatorio de lint nao contem mais issues de `UnusedResources`, `Typos` nem `AndroidGradlePluginVersion`.</done>
</task>

<task type="auto">
  <name>Task 2: Corrigir ObsoleteSdkInt removendo o qualificador -v26 do mipmap</name>
  <files>app/src/main/res/mipmap-anydpi/ic_launcher.xml, app/src/main/res/mipmap-anydpi/ic_launcher_round.xml</files>
  <read_first>
    - app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml (arquivo a mover)
    - app/src/main/res/mipmap-anydpi-v26/ic_launcher_round.xml (arquivo a mover)
    - app/src/main/AndroidManifest.xml (linha 14 — `android:icon="@mipmap/ic_launcher"`, confirmar que o rename nao quebra a referencia)
    - .planning/phases/01-fundacao-compilavel/01-RESEARCH.md (Pitfall 2, linha `ObsoleteSdkInt`)
  </read_first>
  <action>
`minSdk` do projeto e **29**. O qualificador `-v26` so faz sentido quando o minSdk e menor que
26, entao o lint emite `ObsoleteSdkInt`. A correcao decidida no CONTEXT.md e REAL (nao
supressao): renomear o diretorio de recursos preservando o historico do git.

Executar exatamente:

```bash
git mv app/src/main/res/mipmap-anydpi-v26 app/src/main/res/mipmap-anydpi
```

O nome dos recursos (`@mipmap/ic_launcher`, `@mipmap/ic_launcher_round`) NAO muda — apenas o
qualificador de configuracao do diretorio. `AndroidManifest.xml:14` continua valido sem edicao.

NAO fazer:
- NAO adicionar `ObsoleteSdkInt` ao `disable` do bloco lint.
- NAO editar o conteudo XML dos dois arquivos.
- NAO apagar `ic_launcher_round.xml` mesmo que o lint o reporte como recurso nao usado (o
  `disable "UnusedResources"` da Task 1 ja cobre isso; o arquivo e ativo legitimo).
- NAO renomear para `mipmap-anydpi-v29` nem criar copia em outro qualificador.
  </action>
  <verify>
    <automated>export ANDROID_HOME="${ANDROID_HOME:-$HOME/Library/Android/sdk}"; test ! -d app/src/main/res/mipmap-anydpi-v26 && test -f app/src/main/res/mipmap-anydpi/ic_launcher.xml && ./gradlew assembleDebug lint && test "$(grep -c 'id="ObsoleteSdkInt"' app/build/reports/lint-results-debug.xml)" = "0" && test "$(grep -c '<issue' app/build/reports/lint-results-debug.xml)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `test -d app/src/main/res/mipmap-anydpi-v26` falha (diretorio nao existe mais)
    - `test -f app/src/main/res/mipmap-anydpi/ic_launcher.xml` passa
    - `test -f app/src/main/res/mipmap-anydpi/ic_launcher_round.xml` passa
    - `git status --porcelain app/src/main/res/` mostra os arquivos como renomeados/movidos (nao deletados + criados soltos)
    - `grep -c "@mipmap/ic_launcher" app/src/main/AndroidManifest.xml` continua retornando `1`
    - Apos `./gradlew assembleDebug lint`: `grep -c 'id="ObsoleteSdkInt"' app/build/reports/lint-results-debug.xml` retorna `0`
    - Apos `./gradlew assembleDebug lint`: `grep -c '<issue' app/build/reports/lint-results-debug.xml` retorna `0` — QLT-02 fechado na letra
    - `test -f app/build/outputs/apk/debug/app-debug.apk` passa (o APK continua sendo produzido)
  </acceptance_criteria>
  <done>`mipmap-anydpi-v26` nao existe mais, `mipmap-anydpi` contem os dois XMLs, o APK debug ainda e gerado e `lint-results-debug.xml` tem exatamente 0 ocorrencias de `<issue`.</done>
</task>

</tasks>

<verification>
```bash
export ANDROID_HOME="${ANDROID_HOME:-$HOME/Library/Android/sdk}"

# 1. Politica declarada num lugar so, sem baseline
grep -n "lint {" app/build.gradle.kts
grep -c "lint-baseline" app/build.gradle.kts        # 0
grep -c "lintOptions" app/build.gradle.kts          # 0

# 2. Correcao real, nao supressao
test ! -d app/src/main/res/mipmap-anydpi-v26 && echo "OK: qualificador removido"
grep -c "ObsoleteSdkInt" app/build.gradle.kts       # 0

# 3. Nenhum recurso apagado
git diff --numstat app/src/main/res/values/         # sem linhas removidas

# 4. QLT-02 fechado na letra
./gradlew lint
grep -c "<issue" app/build/reports/lint-results-debug.xml   # 0
```
</verification>

<success_criteria>
- `app/build.gradle.kts` contem um unico bloco `lint { }` com `abortOnError = true` e
  `disable += setOf("UnusedResources", "Typos", "AndroidGradlePluginVersion")`, cada um
  precedido de comentario justificando.
- Nenhum `lint-baseline.xml` criado; nenhum recurso de `res/values/` apagado.
- `mipmap-anydpi-v26` renomeado para `mipmap-anydpi` via `git mv`; icone continua resolvendo.
- `app/build/reports/lint-results-debug.xml` tem 0 ocorrencias de `<issue`.
- `./gradlew assembleDebug lint` termina `BUILD SUCCESSFUL`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundacao-compilavel/01-01-SUMMARY.md`
</output>
