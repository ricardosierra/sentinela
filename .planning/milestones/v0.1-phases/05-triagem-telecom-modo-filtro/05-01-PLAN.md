---
phase: 05-triagem-telecom-modo-filtro
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/org/sentinela/app/domain/RepeatedCall.kt
  - app/src/main/java/org/sentinela/app/domain/CallDecisionEngine.kt
  - app/src/main/java/org/sentinela/app/domain/DecisionReason.kt
  - app/src/main/java/org/sentinela/app/domain/ScreenedCall.kt
  - app/src/main/java/org/sentinela/app/settings/ScreeningSettings.kt
  - app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt
  - app/src/main/java/org/sentinela/app/data/local/BlockedCallRepository.kt
  - app/src/main/java/org/sentinela/app/data/local/RoomBlockedCallRepository.kt
  - app/src/main/java/org/sentinela/app/data/local/db/BlockedCallDao.kt
  - app/src/main/java/org/sentinela/app/SentinelaApp.kt
  - app/src/main/java/org/sentinela/app/ui/MainActivity.kt
  - app/src/test/java/org/sentinela/app/domain/DecisionMatrixTest.kt
  - app/src/test/java/org/sentinela/app/domain/CallDecisionEngineTest.kt
  - app/src/test/java/org/sentinela/app/domain/DecisionReasonTest.kt
  - app/src/test/java/org/sentinela/app/settings/DataStoreSettingsRepositoryTest.kt
  - app/src/test/java/org/sentinela/app/data/local/RoomBlockedCallRepositoryTest.kt
  - app/src/test/java/org/sentinela/app/data/local/FakeBlockedCallDao.kt
  - app/src/test/java/org/sentinela/app/settings/AppOpenCounterTest.kt
autonomous: true
requirements: [SCR-12, QLT-01]

must_haves:
  truths:
    - "Um numero bloqueado que liga de novo dentro da janela configurada toca, em vez de ser bloqueado"
    - "A excecao de chamada repetida vem habilitada por padrao e pode ser desligada"
    - "A regra de chamada repetida vive no motor puro, nao no Service"
    - "Abrir uma chamada recebida nao conta como abertura do app"
  artifacts:
    - path: "app/src/main/java/org/sentinela/app/domain/RepeatedCall.kt"
      provides: "enum RepeatedCallLookup + constante nomeada da janela"
      contains: "REPEATED_CALL_WINDOW_MILLIS"
    - path: "app/src/main/java/org/sentinela/app/domain/CallDecisionEngine.kt"
      provides: "precedencia com o bypass de chamada repetida entre whitelist e desconhecidos"
      contains: "RepeatedCallLookup"
    - path: "app/src/main/java/org/sentinela/app/data/local/db/BlockedCallDao.kt"
      provides: "consulta de bloqueio recente por numero"
      contains: "countBlockedSince"
  key_links:
    - from: "app/src/main/java/org/sentinela/app/data/local/RoomBlockedCallRepository.kt"
      to: "BlockedCallDao.countBlockedSince"
      via: "hasRecentBlock"
      pattern: "countBlockedSince"
    - from: "app/src/main/java/org/sentinela/app/ui/MainActivity.kt"
      to: "AppContainer.onAppOpened"
      via: "onCreate da Activity"
      pattern: "onAppOpened"
---

<objective>
Entregar, em codigo puro e testado, tudo que a triagem da Fase 5 precisa **antes** de existir
qualquer Service: a nova regra de decisao SCR-12 (chamada repetida toca), a configuracao que a
liga/desliga, a consulta de historico que a alimenta, e a correcao do defeito do contador de
aberturas descoberto na pesquisa.

Purpose: o `CLAUDE.md` proibe condicao de bloqueio fora do `CallDecisionEngine`. Se a regra de
chamada repetida nascer no Service, ela nasce no lugar errado e nao tem como ser coberta pela
matriz parametrizada. Este plano e Wave 0 de toda a fase.

Output: motor com 8 niveis de precedencia, `RepeatedCallLookup`, `REPEATED_CALL_WINDOW_MILLIS`,
`repeatedCallBypassEnabled` persistido, `BlockedCallRepository.hasRecentBlock`, e
`onAppOpened()` disparado por Activity em vez de por start de processo.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-triagem-telecom-modo-filtro/05-CONTEXT.md
@.planning/phases/05-triagem-telecom-modo-filtro/05-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Contratos reais lidos do codigo em 2026-07-29. Use estes; nao explore o repo. -->

domain/CallDecisionEngine.kt (assinatura ATUAL, a ser estendida):
```kotlin
fun decide(
    call: ScreenedCall,
    settings: ScreeningSettings,
    contact: ContactLookup,
    whitelist: WhitelistLookup,
): CallDecision
```
Precedencia atual: saida -> protecao off -> privado -> contato -> whitelist -> falha de
consulta -> desconhecido/invalido.

domain/ScreenedCall.kt:
```kotlin
enum class ContactLookup { HIT, MISS, UNAVAILABLE }
enum class WhitelistLookup { HIT, MISS, LOOKUP_FAILED }
```

data/local/BlockedCallRepository.kt:
```kotlin
interface BlockedCallRepository {
    suspend fun record(entry: BlockedCallEntry)
    fun observeRecent(): Flow<List<BlockedCallEntry>>
    fun observeTotalCount(): Flow<Long>
    suspend fun deleteById(id: Long)
    suspend fun clearAll()
    suspend fun pruneOlderThan(utcMillis: Long)
}
data class BlockedCallEntry(
    val id: Long = 0, val maskedNumber: String, val numberE164: String?,
    val timestampUtcMillis: Long, val reason: DecisionReason,
    val notificationShown: Boolean,
    val classification: CallClassification = CallClassification.UNCLASSIFIED,
)
```
`RoomBlockedCallRepository(dao, settings, clock: () -> Long = System::currentTimeMillis, io)` —
o relogio ja e injetado (licao da Fase 3).

settings/ScreeningSettings.kt — data class com defaults; as chaves do DataStore ficam em
`DataStoreSettingsRepository.Keys` e o mapeamento em `toScreeningSettings()` / `writeInto()`.

AppContainer.onAppOpened() ja existe e faz `incrementAppOpenCount()` + `pruneNow()`.
SentinelaApp.onCreate() hoje chama `container.onAppOpened()` — este e o defeito.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Regra SCR-12 no motor de decisao</name>
  <read_first>
    - app/src/main/java/org/sentinela/app/domain/CallDecisionEngine.kt
    - app/src/main/java/org/sentinela/app/domain/ScreenedCall.kt
    - app/src/main/java/org/sentinela/app/domain/DecisionReason.kt
    - app/src/main/java/org/sentinela/app/settings/ScreeningSettings.kt
    - app/src/test/java/org/sentinela/app/domain/DecisionMatrixTest.kt
    - app/src/test/java/org/sentinela/app/domain/CallDecisionEngineTest.kt
    - app/src/test/java/org/sentinela/app/domain/DecisionReasonTest.kt
  </read_first>
  <files>
    app/src/main/java/org/sentinela/app/domain/RepeatedCall.kt,
    app/src/main/java/org/sentinela/app/domain/CallDecisionEngine.kt,
    app/src/main/java/org/sentinela/app/domain/DecisionReason.kt,
    app/src/main/java/org/sentinela/app/settings/ScreeningSettings.kt,
    app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt,
    app/src/test/java/org/sentinela/app/domain/DecisionMatrixTest.kt,
    app/src/test/java/org/sentinela/app/domain/CallDecisionEngineTest.kt,
    app/src/test/java/org/sentinela/app/domain/DecisionReasonTest.kt,
    app/src/test/java/org/sentinela/app/settings/DataStoreSettingsRepositoryTest.kt
  </files>
  <behavior>
    - `RepeatedCallLookup.HIT` + `repeatedCallBypassEnabled = true` + `unknownPolicy = BLOCK`
      => `CallDecision.Allow(DecisionReason.REPEATED_CALL)`
    - `RepeatedCallLookup.HIT` + `repeatedCallBypassEnabled = false` => decisao normal (bloqueia)
    - `RepeatedCallLookup.LOOKUP_FAILED` => tratado como MISS: politica normal, **nunca** fallback
    - `ContactLookup.HIT` vence o bypass (contato ja toca; reason continua `CONTACT`)
    - `WhitelistLookup.HIT` vence o bypass (reason continua `PERSONAL_WHITELIST`)
    - `protectionEnabled = false` e chamada de saida continuam vencendo tudo
    - `ScreenedNumber.Private` com `blockPrivateNumbers = true` vence o bypass (bloqueia)
    - bypass **nunca** transforma um `Allow` em bloqueio (e excecao que faz tocar, so isso)
  </behavior>
  <action>
    1. Criar `app/src/main/java/org/sentinela/app/domain/RepeatedCall.kt` com, exatamente:
       - `enum class RepeatedCallLookup { HIT, MISS, LOOKUP_FAILED }`
       - `const val REPEATED_CALL_WINDOW_MILLIS: Long = 5L * 60L * 1000L` (5 minutos) no
         nivel de arquivo, com KDoc explicando que emergencia real insiste e spam automatizado
         normalmente nao, e que o valor e constante nomeada por exigencia do CONTEXT.
       Arquivo sem nenhum `import android.` (o Bloco 3 de `verify-invariants.sh` cobra isso).
    2. Em `ScreeningSettings`, acrescentar `val repeatedCallBypassEnabled: Boolean = true`
       (habilitado por padrao, decisao do usuario) com KDoc curto. Atualizar o KDoc da data class.
    3. Em `DataStoreSettingsRepository`: chave `REPEATED_CALL_BYPASS =
       booleanPreferencesKey("repeated_call_bypass")`, leitura em `toScreeningSettings()` com
       fallback no default e escrita em `writeInto()`.
    4. Em `DecisionReason`, acrescentar a entrada `REPEATED_CALL` com `code` estavel e sem
       nenhum dado pessoal. Atualizar `DecisionReasonTest`, que hoje trava a contagem em 9,
       para 10, mantendo a assercao de que nenhum `code` contem digito de telefone.
    5. Em `CallDecisionEngine.decide`, acrescentar o parametro
       `repeatedCall: RepeatedCallLookup = RepeatedCallLookup.MISS` como ULTIMO parametro, e
       inserir o novo nivel **exatamente entre** o `if (whitelist == WhitelistLookup.HIT)` e o
       `if (contact == UNAVAILABLE || whitelist == LOOKUP_FAILED)`:
       ```kotlin
       if (settings.repeatedCallBypassEnabled && repeatedCall == RepeatedCallLookup.HIT) {
           return CallDecision.Allow(DecisionReason.REPEATED_CALL)
       }
       ```
       Atualizar o KDoc de precedencia da classe para 8 niveis, citando que a excecao so faz
       tocar e nunca bloquear.
    6. Testes: acrescentar ao menos 8 casos novos a `DecisionMatrixTest` (tabela esperada
       escrita a mao, nunca derivada do motor — regra da Fase 2) cobrindo a lista de
       `<behavior>`, e em `CallDecisionEngineTest` um teste que chama `decide` sem passar
       `repeatedCall` e afirma que o default se comporta como `MISS`.
    7. `DataStoreSettingsRepositoryTest`: +2 testes — default `true` em arquivo vazio, e
       round-trip de `false`.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*DecisionMatrixTest" --tests "*CallDecisionEngineTest" --tests "*DecisionReasonTest" --tests "*DataStoreSettingsRepositoryTest" detekt lint && bash scripts/verify-invariants.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/src/main/java/org/sentinela/app/domain/RepeatedCall.kt`
    - `[ "$(grep -c 'REPEATED_CALL_WINDOW_MILLIS' app/src/main/java/org/sentinela/app/domain/RepeatedCall.kt)" -ge 1 ]`
    - `[ "$(grep -rc '5L \* 60L \* 1000L' app/src/main/java/org/sentinela/app/domain/RepeatedCall.kt)" -eq 1 ]`
    - `[ "$(grep -c 'repeatedCallBypassEnabled: Boolean = true' app/src/main/java/org/sentinela/app/settings/ScreeningSettings.kt)" -eq 1 ]`
    - `[ "$(grep -c 'repeated_call_bypass' app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt)" -eq 1 ]`
    - `[ "$(grep -c 'REPEATED_CALL' app/src/main/java/org/sentinela/app/domain/DecisionReason.kt)" -ge 1 ]`
    - `[ "$(grep -c 'RepeatedCallLookup' app/src/main/java/org/sentinela/app/domain/CallDecisionEngine.kt)" -ge 2 ]`
    - `[ "$(grep -rc 'import android\.' app/src/main/java/org/sentinela/app/domain/RepeatedCall.kt)" -eq 0 ]`
    - `DecisionMatrixTest` ganha >= 8 casos novos; a suite JVM inteira fica verde
    - Prova de vermelho obrigatoria: inverter a condicao do novo `if` para
      `repeatedCall != RepeatedCallLookup.HIT`, rodar `--tests "*DecisionMatrixTest"`, observar
      falha, restaurar. Registrar a saida vermelha no SUMMARY
  </acceptance_criteria>
  <done>Motor decide RING para chamada repetida com a excecao ligada, e os testes falham se a regra for removida ou movida de posicao na precedencia.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consulta de bloqueio recente no historico existente</name>
  <read_first>
    - app/src/main/java/org/sentinela/app/data/local/db/BlockedCallDao.kt
    - app/src/main/java/org/sentinela/app/data/local/db/BlockedCallEntity.kt
    - app/src/main/java/org/sentinela/app/data/local/BlockedCallRepository.kt
    - app/src/main/java/org/sentinela/app/data/local/RoomBlockedCallRepository.kt
    - app/src/test/java/org/sentinela/app/data/local/FakeBlockedCallDao.kt
    - app/src/test/java/org/sentinela/app/data/local/RoomBlockedCallRepositoryTest.kt
    - app/src/main/java/org/sentinela/app/domain/RepeatedCall.kt
  </read_first>
  <files>
    app/src/main/java/org/sentinela/app/data/local/db/BlockedCallDao.kt,
    app/src/main/java/org/sentinela/app/data/local/BlockedCallRepository.kt,
    app/src/main/java/org/sentinela/app/data/local/RoomBlockedCallRepository.kt,
    app/src/test/java/org/sentinela/app/data/local/FakeBlockedCallDao.kt,
    app/src/test/java/org/sentinela/app/data/local/RoomBlockedCallRepositoryTest.kt
  </files>
  <behavior>
    - numero com registro dentro da janela => `RepeatedCallLookup.HIT`
    - numero com registro fora da janela (mais antigo que o corte) => `MISS`
    - corte ESTRITO no limite: registro com timestamp exatamente igual ao corte conta como
      dentro da janela (`>=`), coerente com a poda da Fase 3 que usa `<`
    - numero sem nenhum registro => `MISS`
    - numero diferente com registro recente => `MISS`
    - `numberE164` nulo/vazio => `MISS` sem tocar no DAO
    - excecao do DAO => `LOOKUP_FAILED` (a Fase 5 nao pode deixar o historico derrubar a decisao)
  </behavior>
  <action>
    1. `BlockedCallDao`: acrescentar
       ```kotlin
       @Query("SELECT COUNT(*) FROM blocked_call WHERE number_e164 = :numberE164 AND timestamp_utc_millis >= :sinceUtcMillis")
       suspend fun countBlockedSince(numberE164: String, sinceUtcMillis: Long): Int
       ```
       Conferir os nomes reais das colunas em `BlockedCallEntity` antes de escrever o SQL.
       **Nenhum indice novo e nenhuma alteracao de schema** — isso exigiria migracao v2 e a
       tabela e pequena por causa da retencao. Registrar essa decisao em KDoc, em prosa.
    2. `BlockedCallRepository`: acrescentar
       `suspend fun hasRecentBlock(numberE164: String?, nowUtcMillis: Long): RepeatedCallLookup`.
    3. `RoomBlockedCallRepository`: implementar usando `REPEATED_CALL_WINDOW_MILLIS`
       (`nowUtcMillis - REPEATED_CALL_WINDOW_MILLIS` como corte), dentro de `withContext(io)`,
       envolto em `runCatching` que devolve `LOOKUP_FAILED` na falha. Numero nulo ou em branco
       devolve `MISS` sem chamar o DAO.
       Atencao: esta e a UNICA excecao ao contrato da Fase 3 de "propagar excecao do DAO", e o
       KDoc precisa dizer por que (a decisao nao pode falhar por causa do historico).
    4. `FakeBlockedCallDao`: implementar `countBlockedSince` e um interruptor de falha.
    5. `RoomBlockedCallRepositoryTest`: +7 testes cobrindo a lista de `<behavior>`, com relogio
       fixo injetado (`clock = { 1_700_000_000_000L }`).
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*RoomBlockedCallRepositoryTest" --tests "*SchemaExportTest" detekt lint && bash scripts/verify-invariants.sh</automated>
  </verify>
  <acceptance_criteria>
    - `[ "$(grep -c 'countBlockedSince' app/src/main/java/org/sentinela/app/data/local/db/BlockedCallDao.kt)" -ge 1 ]`
    - `[ "$(grep -c 'hasRecentBlock' app/src/main/java/org/sentinela/app/data/local/BlockedCallRepository.kt)" -eq 1 ]`
    - `[ "$(grep -c 'REPEATED_CALL_WINDOW_MILLIS' app/src/main/java/org/sentinela/app/data/local/RoomBlockedCallRepository.kt)" -ge 1 ]`
    - `SchemaExportTest` continua verde **sem** nenhuma edicao: o schema v1 nao muda (consulta
      nova nao altera schema). Se ele ficar vermelho, a task introduziu mudanca de schema e
      precisa ser revertida
    - `[ "$(ls app/schemas/org.sentinela.app.data.local.db.SentinelaDatabase/ | wc -l | tr -d ' ')" -eq 1 ]`
      (continua so o schema v1; nenhuma versao nova gerada)
    - Prova de vermelho: trocar `>=` por `>` no SQL e observar o teste do limite exato falhar;
      restaurar
  </acceptance_criteria>
  <done>O repositorio responde HIT/MISS/LOOKUP_FAILED para bloqueio recente sem mudar o schema e sem propagar excecao.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Contador de aberturas so conta abertura de verdade</name>
  <read_first>
    - app/src/main/java/org/sentinela/app/SentinelaApp.kt
    - app/src/main/java/org/sentinela/app/ui/MainActivity.kt
    - app/src/main/java/org/sentinela/app/AppContainer.kt
    - app/src/test/java/org/sentinela/app/settings/AppOpenCounterTest.kt
  </read_first>
  <files>
    app/src/main/java/org/sentinela/app/SentinelaApp.kt,
    app/src/main/java/org/sentinela/app/ui/MainActivity.kt,
    app/src/test/java/org/sentinela/app/settings/AppOpenCounterTest.kt
  </files>
  <behavior>
    - `SentinelaApp.onCreate` nao incrementa nada e nao dispara poda
    - a contagem so acontece quando existe Activity (`MainActivity.onCreate`)
    - rotacao de tela nao conta duas vezes: incrementar apenas quando
      `savedInstanceState == null`
  </behavior>
  <action>
    1. Em `SentinelaApp`: remover a chamada `container.onAppOpened()` de `onCreate`. Substituir
       o comentario existente por um KDoc explicando, em prosa portuguesa, que o processo tambem
       e criado pelo vinculo do Telecom numa chamada recebida, e que contar isso como abertura
       corromperia o contador de ENG-01 e o convite de avaliacao da Fase 9. **Nao escrever nomes
       de permissao nem identificadores vigiados neste comentario** (armadilha recorrente das
       Fases 3 e 4).
    2. Em `MainActivity.onCreate`, apos `super.onCreate`, acrescentar:
       ```kotlin
       if (savedInstanceState == null) {
           (application as SentinelaApp).container.onAppOpened()
       }
       ```
    3. `AppOpenCounterTest`: acrescentar um teste que documenta o contrato — o incremento e do
       ciclo de vida da Activity, nao do processo. Se o teste existente presumia a chamada em
       `Application`, corrigi-lo.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*AppOpenCounterTest" --tests "*DataStoreSettingsRepositoryTest" detekt lint && bash scripts/verify-invariants.sh</automated>
  </verify>
  <acceptance_criteria>
    - `[ "$(grep -c 'onAppOpened' app/src/main/java/org/sentinela/app/SentinelaApp.kt)" -eq 0 ]`
    - `[ "$(grep -c 'onAppOpened' app/src/main/java/org/sentinela/app/ui/MainActivity.kt)" -eq 1 ]`
    - `[ "$(grep -c 'savedInstanceState == null' app/src/main/java/org/sentinela/app/ui/MainActivity.kt)" -eq 1 ]`
    - `./gradlew assembleDebug` verde
  </acceptance_criteria>
  <done>Nenhum start de processo disparado pelo Telecom incrementa o contador de aberturas.</done>
</task>

</tasks>

<verification>
```bash
./gradlew testDebugUnitTest koverLog lint detekt && bash scripts/verify-invariants.sh
```
Registrar o percentual do `koverLog` no SUMMARY (o `koverVerify` so volta a ser cobrado no
plano 05-07).
</verification>

<success_criteria>
- Motor com 8 niveis de precedencia e >= 8 casos novos na matriz parametrizada
- `repeatedCallBypassEnabled` default `true`, persistido e testado nos dois sentidos
- `hasRecentBlock` responde sem alterar o schema v1 e sem propagar excecao
- Contador de aberturas desacoplado do start de processo
- Duas provas de vermelho registradas (regra do bypass e corte da janela)
</success_criteria>

<output>
Criar `.planning/phases/05-triagem-telecom-modo-filtro/05-01-SUMMARY.md`.
</output>
