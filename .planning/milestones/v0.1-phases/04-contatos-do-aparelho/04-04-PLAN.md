---
phase: 04-contatos-do-aparelho
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-03"]
files_modified:
  - app/src/androidTest/java/org/sentinela/app/data/contacts/ContactLookupSourceTest.kt
  - app/src/androidTest/java/org/sentinela/app/data/contacts/ContactsObserverTest.kt
  - app/src/androidTest/java/org/sentinela/app/data/contacts/ContactLookupPerformanceTest.kt
autonomous: true
requirements: [CTT-02]

must_haves:
  truths:
    - "Contato gravado em formato nacional e reconhecido como HIT (a sonda unica por E.164 falharia)"
    - "Numero fora da agenda devolve MISS contra o provider real"
    - "Uma mudanca na agenda invalida o cache e o contato novo passa a ser reconhecido"
    - "Com 5.000 contatos, a mediana do lookup cabe folgado no orcamento da decisao"
  artifacts:
    - path: "app/src/androidTest/java/org/sentinela/app/data/contacts/ContactLookupSourceTest.kt"
      provides: "HIT/MISS reais contra o provider, incluindo formato nacional"
      min_lines: 60
    - path: "app/src/androidTest/java/org/sentinela/app/data/contacts/ContactsObserverTest.kt"
      provides: "invalidacao por observer + debounce"
    - path: "app/src/androidTest/java/org/sentinela/app/data/contacts/ContactLookupPerformanceTest.kt"
      provides: "percentis com 5.000 contatos, assert primario na mediana"
  key_links:
    - from: "ContactLookupSourceTest"
      to: "ContactsTestFixture"
      via: "adoptShellPermissionIdentity + applyBatch"
      pattern: "ContactsTestFixture"
    - from: "ContactsObserverTest"
      to: "ContactKeyCache"
      via: "onChange -> debounce -> reconstrucao"
      pattern: "observeChanges|onChange"
---

<objective>
Provar contra o `ContactsContract` real (emulador `Medium_Phone_API_35`) que a sonda dupla
funciona, que o observer invalida o cache e que a mediana do lookup cabe no orcamento.

Purpose: o teste da sonda dupla e o unico que **falha** numa implementacao ingenua de sonda
unica — e a falha que o CONTEXT chama de "MISS silencioso e perigoso". Sem ele, CTT-02 e uma
afirmacao, nao uma prova.
Output: tres classes instrumentadas verdes com percentis arquivados em logcat.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-contatos-do-aparelho/04-CONTEXT.md
@.planning/phases/04-contatos-do-aparelho/04-RESEARCH.md
@app/src/androidTest/java/org/sentinela/app/data/local/db/WhitelistPerformanceTest.kt

<interfaces>
Disponivel do plano 04-01:
```kotlin
// app/src/androidTest/java/org/sentinela/app/data/contacts/ContactsTestFixture.kt
fun adoptShell(); fun dropShell()
fun insert(cr: ContentResolver, displayName: String, number: String)
fun insertMany(cr: ContentResolver, count: Int, numberAt: (Int) -> String)
fun wipe(cr: ContentResolver): Int
```

Disponivel do plano 04-03:
```kotlin
internal interface ContactNumberSource {
    fun hasPermission(): Boolean
    fun probe(e164: String, nationalDigits: String?): Boolean
    fun allRawNumbers(): List<String>
    fun observeChanges(onChange: () -> Unit)
    fun close()
}
class ContactsContractLookupSource(...) : ContactNumberSource
class ContactKeyCache(...)                       // get(): Set<String>?  / warmInBackground()
class DefaultContactLookupRepository(...) : ContactLookupRepository
```

Numeros de referencia MEDIDOS na pesquisa (mesmo AVD), para calibrar os limites:
- lookup quente em HashSet: p50 1,08 µs
- sonda direta, 5.000 contatos: p50 1,95 ms (HIT) / 2,45 ms (MISS); p95 6,9–8,2 ms; max 73,9 ms
- construcao do cache normalizando: 1.494–1.764 ms
- inserir 5.000 contatos: 7,1–14,0 s
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContactLookupSourceTest — sonda dupla contra o provider real</name>
  <files>app/src/androidTest/java/org/sentinela/app/data/contacts/ContactLookupSourceTest.kt</files>
  <read_first>
    - app/src/androidTest/java/org/sentinela/app/data/contacts/ContactsTestFixture.kt
    - .planning/phases/04-contatos-do-aparelho/04-RESEARCH.md matriz do PhoneLookup
    - app/src/main/java/org/sentinela/app/data/contacts/ContactsContractLookupSource.kt
  </read_first>
  <action>
Criar `ContactLookupSourceTest` instrumentado. `@Before`: `adoptShell()` + `wipe`.
`@After`: `wipe` + `close()` da source + `dropShell()`.

Casos obrigatorios (>= 7 `@Test`):
1. contato gravado em E.164 (`+5511912345678`) -> `lookup` devolve HIT
2. **contato gravado em formato nacional (`(11) 91234-5678`) -> HIT** — este e o teste que
   falha com sonda unica por E.164; o SUMMARY deve registrar que ele foi visto falhando
   quando a segunda sonda e desligada temporariamente (prova de vermelho)
3. contato gravado com mascara e espacos (`+55 11 91234-5678`) -> HIT
4. numero fora da agenda -> MISS (nunca UNAVAILABLE quando ha permissao)
5. agenda vazia -> MISS
6. fixo BR (`(21) 3216-5498` / `+552132165498`) -> HIT nas duas grafias de consulta
7. `allRawNumbers()` com 4 contatos devolve 4 valores e o conjunto de chaves construido tem
   **tamanho 4** — assertar o tamanho do CONJUNTO, nunca confiar na contagem do cursor
   (a pesquisa viu uma corrida devolver contagem divergente)

Nenhum `assert` deste arquivo pode depender de tempo.
  </action>
  <verify>
    <automated>bash scripts/run-instrumented-tests.sh --tests "*ContactLookupSourceTest"</automated>
  </verify>
  <acceptance_criteria>
    - `[ "$(grep -c '@Test' app/src/androidTest/java/org/sentinela/app/data/contacts/ContactLookupSourceTest.kt)" -ge 7 ]`
    - O arquivo contem a string literal `(11) 91234-5678` (contato em formato nacional)
    - `[ "$(grep -cE 'System.nanoTime|measureTime' app/src/androidTest/java/org/sentinela/app/data/contacts/ContactLookupSourceTest.kt)" -eq 0 ]`
    - `app/build/outputs/androidTest-results/connected/debug/TEST-*.xml` traz `failures="0"` e `errors="0"` para `ContactLookupSourceTest`
    - SUMMARY registra a execucao com a segunda sonda desativada mostrando o caso 2 VERMELHO, e depois restaurada verde
  </acceptance_criteria>
  <done>HIT/MISS provados contra o provider real, incluindo a grafia que quebra a sonda unica.</done>
</task>

<task type="auto">
  <name>Task 2: ContactsObserverTest — invalidacao e debounce</name>
  <files>app/src/androidTest/java/org/sentinela/app/data/contacts/ContactsObserverTest.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/data/contacts/ContactKeyCache.kt
    - app/src/main/java/org/sentinela/app/data/contacts/ContactsContractLookupSource.kt
    - .planning/phases/04-contatos-do-aparelho/04-RESEARCH.md secao "ContentObserver"
  </read_first>
  <action>
Criar `ContactsObserverTest` instrumentado com >= 4 `@Test`:
1. observer registrado em `AUTHORITY_URI` recebe callback apos uma insercao (aguardar por
   `CountDownLatch` com timeout de 10 s — nunca `Thread.sleep` fixo como unico sincronizador)
2. apos a insercao e o debounce, uma nova consulta ao repositorio reconhece o contato novo
   como HIT (o cache foi de fato invalidado)
3. apos remover o contato e o debounce, o mesmo numero volta a MISS
4. rajada de N insercoes (N >= 10) resulta em **uma unica** reconstrucao do conjunto — contar
   as reconstrucoes com um contador instrumentado no proprio teste, nao com tempo
5. `close()` desregistra: apos fecha-lo, uma insercao nova nao incrementa o contador de callbacks

Registrar em logcat, com a tag padrao do repo (`SENTINELA|contacts|...`), a contagem de
callbacks observada — numero de diagnostico, **nao** assert (a coalescencia do provider foi
medida variando entre 1 e 51 callbacks e nao e garantida).
  </action>
  <verify>
    <automated>bash scripts/run-instrumented-tests.sh --tests "*ContactsObserverTest"</automated>
  </verify>
  <acceptance_criteria>
    - `[ "$(grep -c '@Test' app/src/androidTest/java/org/sentinela/app/data/contacts/ContactsObserverTest.kt)" -ge 4 ]`
    - `grep -q "CountDownLatch" app/src/androidTest/java/org/sentinela/app/data/contacts/ContactsObserverTest.kt`
    - `TEST-*.xml` com `failures="0"` e `errors="0"` para `ContactsObserverTest`
    - O logcat do teste em `app/build/outputs/androidTest-results/connected/debug/*/logcat-ContactsObserverTest-*.txt` contem a contagem de callbacks
    - Nenhum assert sobre numero de callbacks do provider (so sobre numero de reconstrucoes)
  </acceptance_criteria>
  <done>A invalidacao por observer e o debounce sao comportamento provado, nao promessa.</done>
</task>

<task type="auto">
  <name>Task 3: ContactLookupPerformanceTest — 5.000 contatos, assert na mediana</name>
  <files>app/src/androidTest/java/org/sentinela/app/data/contacts/ContactLookupPerformanceTest.kt</files>
  <read_first>
    - app/src/androidTest/java/org/sentinela/app/data/local/db/WhitelistPerformanceTest.kt (padrao de percentis e warmup do repo)
    - .planning/phases/04-contatos-do-aparelho/04-RESEARCH.md secao "Numeros medidos"
    - .planning/STATE.md decisao da Phase 3 sobre p95 movido para a Phase 9
  </read_first>
  <action>
Criar `ContactLookupPerformanceTest` instrumentado, com o fixture pesado em `@BeforeClass`
(inserir 5.000 contatos custa 7–14 s; em `@Before` a suite viraria minutos) e `@AfterClass`
com `wipe` + `dropShell`.

Medicoes, com warmup de >= 100 e >= 500 amostras, todas logadas com a tag
`SENTINELA|contacts|...`:
- **Assert primario (mediana, sinal estavel):**
  - lookup com cache quente: `p50 < 10 ms` (medido 1,08 µs — folga de ~4 ordens de grandeza)
  - sonda direta (cache frio, permissao concedida): `p50 < 50 ms` (medido 1,95–2,45 ms)
- **Reportado, NAO afirmado:** p95 e max das duas medicoes. A Phase 3 tirou um p95 do assert do
  emulador exatamente por instabilidade do scheduler do host; repetir esse erro aqui derrubaria
  o build ~1 em 5 execucoes. Os numeros vao para logcat e viram cenario da Phase 9.
- **Assert estrutural (nao cronometro):** apos o cache aquecer, N lookups consecutivos nao
  incrementam o contador de consultas ao provider. Se a implementacao perder o cache, este
  assert fica vermelho mesmo com o tempo bom.
- Registrar tambem o tempo de construcao do cache (esperado 1,5–1,8 s) em logcat, sem assert.
  </action>
  <verify>
    <automated>bash scripts/run-instrumented-tests.sh --tests "*ContactLookupPerformanceTest"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "@BeforeClass" app/src/androidTest/java/org/sentinela/app/data/contacts/ContactLookupPerformanceTest.kt`
    - `[ "$(grep -c '@Before$' app/src/androidTest/java/org/sentinela/app/data/contacts/ContactLookupPerformanceTest.kt)" -eq 0 ]` (fixture pesado nunca por teste)
    - O arquivo contem `5_000` ou `5000` como tamanho da agenda de teste
    - `[ "$(grep -cE 'assert.*p95|assert.*P95' app/src/androidTest/java/org/sentinela/app/data/contacts/ContactLookupPerformanceTest.kt)" -eq 0 ]` (p95 reportado, nunca afirmado)
    - `TEST-*.xml` com `failures="0"` e `errors="0"` para `ContactLookupPerformanceTest`
    - `logcat-ContactLookupPerformanceTest-*.txt` contem linhas com `SENTINELA|contacts|` trazendo p50, p95 e max
    - A suite instrumentada completa (`bash scripts/run-instrumented-tests.sh`) permanece verde
  </acceptance_criteria>
  <done>A mediana esta dentro do orcamento e o uso do cache e provado por contador, com a cauda reportada para a Phase 9.</done>
</task>

</tasks>

<verification>
```bash
bash scripts/run-instrumented-tests.sh
grep -h "SENTINELA|contacts|" app/build/outputs/androidTest-results/connected/debug/*/logcat-*.txt
```
</verification>

<success_criteria>
- Sonda dupla provada contra o provider real, com vermelho demonstrado na sonda unica
- Invalidacao por observer e debounce provados
- Mediana dentro do orcamento; p95/max reportados e diferidos para a Phase 9
</success_criteria>

<output>
Criar `.planning/phases/04-contatos-do-aparelho/04-04-SUMMARY.md`
</output>
