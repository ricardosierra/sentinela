---
phase: 06-modo-discador-opcional
plan: 08
type: execute
wave: 5
depends_on: ["06-07"]
files_modified:
  - docs/LIMITACOES.md
  - docs/design/TELAS.md
  - docs/TESTE-FISICO-SAMSUNG.md
  - docs/INDEX.md
  - CHANGELOG.md
  - app/build.gradle.kts
  - .planning/phases/06-modo-discador-opcional/06-EVIDENCE.md
autonomous: true
requirements: [DIA-05, QLT-06]

must_haves:
  truths:
    - "Nenhum documento do projeto afirma mais que o modo discador destrava numero privado sem prova"
    - "A secao de interface de chamada da documentacao de telas descreve as telas que existem de verdade"
    - "O roteiro fisico cobre o modo discador sem cenario duplicado, com os itens antigos revisados no lugar"
    - "O gate de cobertura volta a ser cobrado e continua acima do minimo"
    - "A evidencia da fase foi produzida com limpeza e sem cache"
  artifacts:
    - path: "docs/LIMITACOES.md"
      provides: "item de numero privado corrigido e limitacao de uma chamada por vez"
      contains: "uma chamada por vez"
    - path: "docs/design/TELAS.md"
      provides: "secao 11 reescrita a partir do contrato de design"
    - path: "docs/TESTE-FISICO-SAMSUNG.md"
      provides: "cenarios 23-30 revisados no lugar e novos de 52 em diante"
      contains: "| 60 |"
    - path: ".planning/phases/06-modo-discador-opcional/06-EVIDENCE.md"
      provides: "saida integral da suite pos-limpeza sem cache"
  key_links:
    - from: "app/build.gradle.kts"
      to: "app/src/main/java/org/sentinela/app/telecom/call"
      via: "denominador da cobertura ampliado com exclude por nome de classe"
      pattern: "kover"
---

<objective>
Fechar a fase com honestidade e prova: corrigir o que os documentos afirmam sem base, reescrever a
secao de interface de chamada da documentacao de telas a partir do contrato de design, revisar no
lugar os cenarios fisicos do modo discador que JA existem e acrescentar so o que e novo, religar o
gate de cobertura e arquivar a evidencia pos-limpeza.

Purpose: a fase de maior risco do MVP nao pode terminar com documento mentindo. A pesquisa encontrou
tres defeitos de documento: a matriz de permissoes estava incompleta (corrigido no plano 06-03), a
secao de interface de chamada e um esboco de seis linhas, e o roteiro fisico ja tem cenarios de modo
discador que a instrucao original mandaria duplicar. E ha uma afirmacao SEM PROVA a corrigir: hoje um
item diz que o modo discador destrava numero privado, e isso nao foi verificado.

Output: quatro documentos, o changelog, o bloco de cobertura e o arquivo de evidencia.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-modo-discador-opcional/06-RESEARCH.md
@.planning/phases/06-modo-discador-opcional/06-UI-SPEC.md
@.planning/phases/06-modo-discador-opcional/06-VALIDATION.md
@.planning/phases/06-modo-discador-opcional/06-07-SUMMARY.md
@docs/LIMITACOES.md
@docs/TESTE-FISICO-SAMSUNG.md
@CLAUDE.md

<interfaces>
Estado atual do roteiro fisico: os cenarios **23 a 30** ja existem numa secao "Modo discador
(executar apos a Fase 6)" e cobrem ativacao, chamada de contato com cada politica, desconhecido,
discagem propria, tela bloqueada e reversao. As Fases 3 a 5 acrescentaram 31 a 51 em secoes
herdadas. A numeracao atual termina em **51**. **Revisar 23-30 no lugar** e usar **52 em diante**
somente para o que e novo — duplicar cobertura ja escrita seria defeito.

Cenarios NOVOS a acrescentar, de 52 a 60 (o unico ponto genuinamente impossivel no aparelho virtual e
o roteamento de audio, mais o comportamento da interface do fabricante):

| # | Cenario | Acao | Esperado |
|---|---------|------|----------|
| 52 | Viva-voz e roteamento real | Em chamada ativa, alternar viva-voz, fone e Bluetooth | Audio troca de rota e a interface reflete a rota ativa. Unico ponto de DIA-02 impossivel no aparelho virtual, que expoe somente a rota de alto-falante |
| 53 | Tela cheia sobre a tela bloqueada do fabricante | Aparelho bloqueado, receber chamada de contato | A tela de chamada do Sentinela aparece por cima; atender e recusar funcionam com o aparelho travado |
| 54 | Permissao de tela cheia revogada | Revogar em Configuracoes e receber chamada | Degrada para aviso com acoes de atender e recusar; nunca fica em silencio |
| 55 | Morte no meio da chamada em aparelho real | Com chamada ativa, forcar a parada do aplicativo | A chamada continua; o sistema assume com o discador do fabricante e avisa. Confirma no aparelho o que foi medido no aparelho virtual |
| 56 | Dois chips e escolha de conta de telefone | Com dois chips, originar chamada sem chip padrao definido | Estado tratado com tela informativa e botao de encerrar funcional — nunca tela em branco |
| 57 | Papel tomado por atualizacao do sistema | Apos atualizacao da interface do fabricante ou instalacao de outro discador, reabrir o aplicativo | A tela detecta a perda na retomada e degrada para modo filtro sem alarme e sem quebrar |
| 58 | Otimizacao de bateria agressiva | Colocar o aplicativo em suspensao de atividade do fabricante e receber chamada | Registrar se o servico de chamada ainda e vinculado. Se nao for, e limitacao de fabricante, nao defeito |
| 59 | Numero privado no modo discador | Ligar com identificacao bloqueada | **Registrar** se a chamada chega a triagem ou somente a interface de chamada. Resolve a questao aberta e decide o texto do item 8 das limitacoes |
| 60 | Historico do fabricante como telefone padrao | Bloquear uma chamada com o modo discador ativo e abrir o historico do fabricante | A chamada APARECE como bloqueada. Confirma no aparelho que ser telefone padrao nao destrava o pulo do registro |

Precedente de cobertura: `koverVerify` com minimo de 80 e cobertura atual de 97,64%. Excluir SEMPRE
por nome de classe, nunca por pacote, e ampliar o filtro somente neste ultimo plano. As classes de
`telecom.call` que sao PURAS entram no denominador sem exclude — e por serem puras devem SUBIR a
cobertura, como o coordenador de triagem fez na Fase 5. Candidatas legitimas a exclude nomeado: a
implementacao da costura que fala com a plataforma e o servico de chamada, se e somente se algum
caminho seu so rodar instrumentado. Nenhuma classe pura pode aparecer em exclude.

Formato do changelog: Release Notes do usuario — titulo `# Release Notes`, secoes com os emojis
exatos, itens SEMPRE em caixa de selecao.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Honestidade nos documentos e a secao 11 reescrita</name>
  <read_first>
    - docs/LIMITACOES.md
    - docs/design/TELAS.md
    - docs/design/DESIGN.md
    - docs/INDEX.md
    - .planning/phases/06-modo-discador-opcional/06-UI-SPEC.md
    - .planning/phases/06-modo-discador-opcional/06-07-SUMMARY.md
  </read_first>
  <files>
    docs/LIMITACOES.md,
    docs/design/TELAS.md,
    docs/INDEX.md
  </files>
  <action>
    1. Corrigir o item 8 das limitacoes. Hoje ele AFIRMA que a opcao de bloquear numeros privados "so
       tem efeito real no modo discador". Isso NAO foi verificado: nao se sabe se a triagem passa a
       receber essas chamadas por segurarmos o papel, ou se apenas a interface de chamada as ve — o
       que seria tarde demais para bloquear. Se o cenario 59 tiver sido executado e registrado, usar o
       resultado; caso contrario, reescrever o item como NAO VERIFICADO, dizendo o que se sabe (no
       modo filtro a chamada sem identificacao nao e entregue a triagem) e o que nao se sabe, e
       apontando para o cenario 59. A interface nao promete isso em nenhum texto.
    2. Acrescentar as limitacoes de escopo desta fase: **uma chamada por vez** — chamada em espera,
       segunda chamada, conferencia e transferencia nao sao suportadas nesta versao —, ausencia de
       videochamada, ausencia de mensagem rapida ao recusar, ausencia de gesto de arrastar para
       atender, e o fato de chamada de emergencia ser sempre atendida pelo discador que vem no
       aparelho, mesmo com o papel de telefone padrao em nossas maos.
    3. Reforcar no item 3 que a medicao desta fase RECONFIRMOU em execucao, com o papel de telefone
       padrao ativo, que a chamada bloqueada continua entrando no historico do telefone.
    4. Reescrever a secao 11 de `docs/design/TELAS.md` a partir do contrato de design: as seis telas
       (recebida, saida, ativa, teclado de tons, discagem, ativacao/reversao), as quatro variantes de
       identidade, os tamanhos travados (atender/recusar 72dp, encerrar 64dp, teclas 72dp, controles
       secundarios 56dp, avatar 96dp), as cores funcionais fixas fora da cor dinamica e a razao disso,
       a regra do numero completo na tela contra mascarado em todo o resto, e os dez requisitos de
       acessibilidade. Substituir o esboco atual de seis linhas por inteiro, sem deixar a versao
       antiga.
    5. Conferir `docs/INDEX.md`: nenhum documento novo foi criado nesta task, mas se o plano 06-02
       criou o arquivo de pendencia de fontes, ele precisa estar indexado.
  </action>
  <verify>
    <automated>bash scripts/verify-invariants.sh && ./gradlew testDebugUnitTest --tests "*CallStringsTest" --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `[ "$(grep -c 'uma chamada por vez' docs/LIMITACOES.md)" -ge 1 ]`
    - `[ "$(grep -c 'nao verificado\|não verificado\|nao foi verificado' docs/LIMITACOES.md)" -ge 1 ]` OU
      o item 8 cita o resultado registrado do cenario 59
    - A secao 11 de `docs/design/TELAS.md` tem >= 40 linhas e menciona as seis telas
    - `[ "$(grep -c 'sem mockup' docs/design/TELAS.md)" -le 3 ]` — a secao 11 deixou de ser esboco
    - `bash scripts/verify-invariants.sh` verde e `CallStringsTest` verde
  </acceptance_criteria>
  <done>Nenhum documento afirma o que nao foi medido, e a secao de telas descreve o que existe.</done>
</task>

<task type="auto">
  <name>Task 2: Roteiro fisico — revisar 23-30 no lugar, acrescentar 52 a 60</name>
  <read_first>
    - docs/TESTE-FISICO-SAMSUNG.md
    - .planning/phases/06-modo-discador-opcional/06-RESEARCH.md
    - .planning/phases/06-modo-discador-opcional/06-07-SUMMARY.md
  </read_first>
  <files>
    docs/TESTE-FISICO-SAMSUNG.md
  </files>
  <action>
    1. **Revisar os cenarios 23 a 30 no lugar**, sem renumerar e sem duplicar: atualizar cada um com o
       que a medicao desta fase mudou. Em particular, o cenario 23 passa a dizer que a elegibilidade
       ao papel foi provada em automacao e o que resta e a confirmacao do dialogo do fabricante; os
       cenarios 25 e 26 passam a dizer que a politica por contato ja e provada em automacao e o que
       resta e a percepcao real em chamada; o cenario 30 passa a apontar que a reversao ja e provada em
       automacao e o que resta e o comportamento da interface do fabricante.
    2. Acrescentar os cenarios **52 a 60** exatamente com o conteudo da tabela do bloco de interfaces,
       numa secao "Pendencias da Phase 6 (modo discador)".
    3. Na secao de registro de comportamento do fabricante, acrescentar as tres questoes abertas: se o
       fabricante insere confirmacao extra ao trocar o telefone padrao, se o aplicativo nativo reassume
       o papel apos atualizacao do sistema, e se a otimizacao de bateria mata o servico de chamada.
       **Nenhum ajuste preventivo de fabricante entra no codigo** antes de um item falhar
       comprovadamente ali.
    4. Atualizar a secao de criterios de aceite cobertos com os requisitos desta fase.
  </action>
  <verify>
    <automated>[ "$(grep -cE '^\| (5[2-9]|60) \|' docs/TESTE-FISICO-SAMSUNG.md)" -eq 9 ] && [ "$(grep -cE '^\| 2[3-9] \|' docs/TESTE-FISICO-SAMSUNG.md)" -eq 7 ] && [ "$(grep -cE '^\| 30 \|' docs/TESTE-FISICO-SAMSUNG.md)" -eq 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - Os nove cenarios novos (52 a 60) existem exatamente uma vez cada
    - Os cenarios 23 a 30 continuam existindo exatamente uma vez cada — nenhum duplicado, nenhum
      renumerado
    - O total de cenarios do documento e 60
    - Nenhum cenario novo repete o assunto de um cenario de 23 a 30, conferido por leitura
  </acceptance_criteria>
  <done>O roteiro fisico cobre o modo discador uma vez so, com o que foi automatizado ja registrado.</done>
</task>

<task type="auto">
  <name>Task 3: Gate de cobertura religado, changelog e evidencia pos-limpeza</name>
  <read_first>
    - app/build.gradle.kts
    - CHANGELOG.md
    - .planning/phases/05-triagem-telecom-modo-filtro/05-EVIDENCE.md
    - .planning/phases/06-modo-discador-opcional/06-07-SUMMARY.md
  </read_first>
  <files>
    app/build.gradle.kts,
    CHANGELOG.md,
    .planning/phases/06-modo-discador-opcional/06-EVIDENCE.md
  </files>
  <action>
    1. Rodar `./gradlew koverLog` e ler o percentual real. Ajustar o bloco de cobertura acrescentando
       exclude por NOME DE CLASSE apenas para classes cujo caminho so roda instrumentado — candidatas
       sao a implementacao da costura que fala com a plataforma e o servico de chamada. Nenhuma classe
       pura pode aparecer em exclude; se alguma pura estiver com cobertura baixa, a resposta e escrever
       teste, nao excluir. Registrar no comentario do bloco, em prosa, o percentual antes e depois.
    2. Rodar a suite completa com limpeza e sem cache e arquivar a saida integral em
       `06-EVIDENCE.md`, no formato do arquivo equivalente da Fase 5: comando, cabecalho de tarefas
       executadas com `M > 0`, resumo de testes JVM e instrumentados, percentual de cobertura, saida do
       script de invariantes com os 8 blocos, e a lista das provas de vermelho da fase com quem as
       executou.
    3. Atualizar `CHANGELOG.md` no bloco nao lancado, no formato Release Notes: em Novidades, o modo
       discador opcional com as telas de chamada e discagem; em Tecnico, as quatro permissoes novas com
       a matriz atualizada, o Bloco 8 de invariantes, e a nota de que o requisito de politica por
       contato foi provado sem alterar o motor de decisao. Itens sempre em caixa de selecao.
    4. Nao tocar em `.planning/STATE.md` nem no roadmap — isso e do fluxo de fechamento de fase.
  </action>
  <verify>
    <automated>./gradlew clean && ./gradlew --no-build-cache assembleDebug testDebugUnitTest koverVerify lint detekt && bash scripts/verify-invariants.sh && bash scripts/run-instrumented-tests.sh</automated>
  </verify>
  <acceptance_criteria>
    - `koverVerify` passa e o percentual registrado em `06-EVIDENCE.md` e >= 80
    - O bloco de cobertura NAO ganhou nenhum exclude de pacote: `[ "$(grep -A20 'excludes {' app/build.gradle.kts | grep -cE 'classes\("org\.sentinela\.app\.(telecom|domain|phone|notifications|permissions)\.\*"\)')" -eq 0 ]`
    - `06-EVIDENCE.md` contem a linha de tarefas executadas com valor maior que zero e a saida dos 8
      blocos do script
    - `CHANGELOG.md` tem os itens desta fase em caixa de selecao, sob os cabecalhos com os emojis
      exatos do formato Release Notes
    - A suite instrumentada completa esta verde na evidencia; emulador que nao sobe e blocker
      reportado, nunca omitido
  </acceptance_criteria>
  <done>A fase tem gate de cobertura cobrado, changelog atualizado e evidencia reproduzivel.</done>
</task>

</tasks>

<verification>
```bash
./gradlew clean
./gradlew --no-build-cache assembleDebug testDebugUnitTest koverVerify lint detekt
bash scripts/verify-invariants.sh
bash scripts/run-instrumented-tests.sh
```
</verification>

<success_criteria>
- Item de numero privado corrigido; limitacao de uma chamada por vez documentada
- Secao 11 da documentacao de telas reescrita a partir do contrato de design
- 60 cenarios fisicos, sem duplicata, com 23-30 revisados no lugar
- `koverVerify` verde acima de 80, sem exclude de pacote
- `06-EVIDENCE.md` com a suite completa pos-limpeza e sem cache
</success_criteria>

<output>
Criar `.planning/phases/06-modo-discador-opcional/06-08-SUMMARY.md`
</output>
