---
phase: 03-dados-locais
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/build.gradle.kts
  - scripts/run-instrumented-tests.sh
  - app/src/androidTest/java/org/sentinela/app/InstrumentationSmokeTest.kt
autonomous: true
requirements: [QLT-06]

must_haves:
  truths:
    - "Um comando unico sobe o emulador headless, espera o boot de verdade e roda connectedDebugAndroidTest"
    - "O emulador e derrubado mesmo quando a suite instrumentada falha"
    - "MigrationTestHelper consegue enxergar app/schemas/ como asset do androidTest"
  artifacts:
    - path: "scripts/run-instrumented-tests.sh"
      provides: "Boot headless + polling sys.boot_completed + trap emu kill"
      contains: "sys.boot_completed"
    - path: "app/src/androidTest/java/org/sentinela/app/InstrumentationSmokeTest.kt"
      provides: "Prova de que o runner instrumentado executa"
    - path: "app/build.gradle.kts"
      provides: "androidTestImplementation room-testing + test-core e assets srcDir de schemas"
      contains: "androidTest\").assets.srcDir"
  key_links:
    - from: "scripts/run-instrumented-tests.sh"
      to: ":app:connectedDebugAndroidTest"
      via: "chamada ao gradlew apos BOOTED"
      pattern: "connectedDebugAndroidTest"
    - from: "app/build.gradle.kts"
      to: "app/schemas"
      via: "sourceSets androidTest assets"
      pattern: "assets\\.srcDir"
---

<objective>
Montar a infraestrutura de teste instrumentado da fase: dependencias de androidTest,
schemas visiveis como asset e um script que sobe/derruba o emulador sozinho.

Purpose: sem isto nenhuma task de DAO, indice, performance ou migracao das waves
seguintes tem como produzir evidencia automatizada. Este e o Wave 0 da fase.
Output: `scripts/run-instrumented-tests.sh` verde com um teste instrumentado real.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dados-locais/03-CONTEXT.md
@.planning/phases/03-dados-locais/03-RESEARCH.md
@app/build.gradle.kts
@gradle/libs.versions.toml

<interfaces>
<!-- Ja existe em app/build.gradle.kts — NAO recriar: -->
<!--   defaultConfig.testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner" -->
<!--   ksp { arg("room.schemaLocation", "$projectDir/schemas") } -->
<!--   androidTestImplementation(libs.androidx.junit.ext / androidx.test.runner / androidx.test.rules) -->
<!-- Ja existe em gradle/libs.versions.toml (nao adicionar versao nova): -->
<!--   room-testing = androidx.room:room-testing:2.8.4 -->
<!--   androidx-test-core = androidx.test:core-ktx:1.7.0 -->
<!-- AVD confirmado: Medium_Phone_API_35 (usar o AvdId, nao o nome do diretorio) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Habilitar androidTest para Room (deps + schemas como asset)</name>
  <files>app/build.gradle.kts</files>
  <read_first>
    - app/build.gradle.kts (linhas 22-99 do bloco `android {}` e 116-153 de `dependencies {}`)
    - gradle/libs.versions.toml (linhas 43-46 e 59)
    - .planning/phases/03-dados-locais/03-RESEARCH.md secao "Migracao e schemas"
  </read_first>
  <action>
Em `app/build.gradle.kts`:

1. Dentro do bloco `android { }`, logo apos o bloco `testOptions { ... }` (linha ~64),
   adicionar:

```kotlin
    // MigrationTestHelper le o JSON exportado pelo Room a partir dos assets do
    // androidTest. Sem este srcDir ele falha com "Cannot find the schema file".
    sourceSets {
        getByName("androidTest").assets.srcDir("$projectDir/schemas")
    }
```

2. No bloco `dependencies { }`, junto das linhas `androidTestImplementation` existentes
   (apos `androidTestImplementation(libs.androidx.test.rules)`), adicionar:

```kotlin
    androidTestImplementation(libs.room.testing)
    androidTestImplementation(libs.androidx.test.core)
```

NAO remover as declaracoes `testImplementation(libs.room.testing)` e
`testImplementation(libs.androidx.test.core)` existentes — `androidx.test.core` continua
sendo usado por testes JVM.

NAO alterar `testInstrumentationRunner` (ja correto), NAO tocar no bloco `ksp { }`
(ja exporta o schema) e NAO adicionar o plugin `androidx.room` — o `ksp arg` legado
foi verificado funcionando com AGP 9.3.0 + Room 2.8.4.

NAO adicionar versao nova ao catalogo: `libs.room.testing` e `libs.androidx.test.core`
ja existem em `gradle/libs.versions.toml`.
  </action>
  <acceptance_criteria>
    - `[ "$(grep -c 'androidTestImplementation(libs.room.testing)' app/build.gradle.kts)" -eq 1 ]`
    - `[ "$(grep -c 'androidTestImplementation(libs.androidx.test.core)' app/build.gradle.kts)" -eq 1 ]`
    - `grep -q 'getByName("androidTest").assets.srcDir("$projectDir/schemas")' app/build.gradle.kts`
    - `[ "$(grep -c 'org.jetbrains.kotlin.android' app/build.gradle.kts)" -eq 0 ]`
    - `[ "$(grep -c 'fallbackToDestructiveMigration' app/build.gradle.kts)" -eq 0 ]`
    - `./gradlew :app:assembleDebugAndroidTest` termina com `BUILD SUCCESSFUL`
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :app:assembleDebugAndroidTest</automated>
  </verify>
  <done>APK de androidTest compila com room-testing e core-ktx no classpath e o diretorio `schemas` registrado como asset.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Script de emulador headless com boot polling e trap</name>
  <files>scripts/run-instrumented-tests.sh</files>
  <read_first>
    - .planning/phases/03-dados-locais/03-RESEARCH.md secao "Emulador — procedimento verificado ponta a ponta"
    - scripts/verify-invariants.sh (linhas 1-22, para o estilo de script do projeto)
    - .planning/phases/03-dados-locais/03-CONTEXT.md secao "Testes instrumentados"
  </read_first>
  <behavior>
    - Sem device conectado: sobe `Medium_Phone_API_35` headless e espera `sys.boot_completed=1`
    - Com device ja conectado: NAO sobe emulador e NAO derruba nada no final
    - `connectedDebugAndroidTest` falhando: script sai com codigo != 0 e AINDA assim derruba o emulador que ele mesmo subiu
    - Argumentos extras sao repassados ao gradlew (ex.: `--tests "*WhitelistDaoTest"`)
  </behavior>
  <action>
Criar `scripts/run-instrumented-tests.sh` (e `chmod +x`) com exatamente este conteudo:

```bash
#!/usr/bin/env bash
# Sobe o emulador headless, roda a suite instrumentada e derruba o emulador.
# Decisao da Fase 3: testes de DAO/migracao rodam DE VERDADE em emulador; subir
# emulador e automatizavel, entao nao existe checkpoint humano para isto.
#
# Uso: bash scripts/run-instrumented-tests.sh [args extras do gradlew]
#   bash scripts/run-instrumented-tests.sh --tests "*WhitelistDaoTest"
set -uo pipefail

export ANDROID_HOME="${ANDROID_HOME:-$HOME/Library/Android/sdk}"
ADB="$ANDROID_HOME/platform-tools/adb"
EMULATOR="$ANDROID_HOME/emulator/emulator"
AVD="${SENTINELA_AVD:-Medium_Phone_API_35}"
STARTED_BY_US=0
EMU_SERIAL=""

cleanup() {
  if [ "$STARTED_BY_US" -eq 1 ] && [ -n "$EMU_SERIAL" ]; then
    echo "== derrubando $EMU_SERIAL =="
    "$ADB" -s "$EMU_SERIAL" emu kill >/dev/null 2>&1
  fi
}
trap cleanup EXIT

[ -x "$ADB" ] || { echo "FAIL: adb ausente em $ADB" >&2; exit 2; }

# `grep -c` sai 1 quando conta 0 — capturar em variavel descarta o status.
DEVICES=$("$ADB" devices | grep -cw "device")
if [ "${DEVICES:-0}" -ge 1 ]; then
  echo "== device ja conectado; reaproveitando =="
else
  [ -x "$EMULATOR" ] || { echo "FAIL: emulator ausente em $EMULATOR" >&2; exit 2; }
  if ! "$EMULATOR" -list-avds | grep -qx "$AVD"; then
    echo "FAIL: AVD '$AVD' nao encontrado (emulator -list-avds)" >&2
    exit 2
  fi
  echo "== subindo $AVD headless =="
  nohup "$EMULATOR" -avd "$AVD" \
    -no-window -no-audio -no-boot-anim -no-snapshot -gpu swiftshader_indirect \
    > /tmp/sentinela-emulator.log 2>&1 &
  STARTED_BY_US=1

  # `adb wait-for-device` NAO basta: o device fica "offline" por varios segundos.
  # A unica prova de boot e sys.boot_completed (medido na pesquisa da Fase 3).
  echo "== esperando boot (ate 600 s) =="
  if ! timeout 600 "$ADB" wait-for-device shell \
      'while [ "$(getprop sys.boot_completed)" != "1" ]; do sleep 2; done; echo BOOTED'; then
    echo "FAIL: emulador nao completou o boot em 600 s (log: /tmp/sentinela-emulator.log)" >&2
    EMU_SERIAL=$("$ADB" devices | awk '/^emulator-/ {print $1; exit}')
    exit 3
  fi
fi

EMU_SERIAL=$("$ADB" devices | awk '/^emulator-/ {print $1; exit}')

echo "== ./gradlew :app:connectedDebugAndroidTest $* =="
./gradlew :app:connectedDebugAndroidTest "$@"
STATUS=$?

echo "== relatorios =="
ls -1 app/build/outputs/androidTest-results/connected/debug/TEST-*.xml 2>/dev/null
exit "$STATUS"
```

Notas obrigatorias ao escrever:
- `timeout` sem `-` no macOS vem do coreutils; se `timeout` nao existir no PATH,
  o executor deve substituir a linha por `"$ADB" wait-for-device shell '...'` sem
  timeout e registrar isso no SUMMARY — nao trocar o polling por `wait-for-device` puro.
- O nome do XML de resultado contem parenteses e espacos
  (`TEST-Medium_Phone_API_35(AVD) - 15-_app-.xml`) — por isso o `ls` usa glob `TEST-*.xml`.
  Nunca montar esse caminho a mao sem aspas.
  </action>
  <acceptance_criteria>
    - `[ -x scripts/run-instrumented-tests.sh ]`
    - `bash -n scripts/run-instrumented-tests.sh` sai 0 (sintaxe valida)
    - `grep -q 'sys.boot_completed' scripts/run-instrumented-tests.sh`
    - `grep -q 'trap cleanup EXIT' scripts/run-instrumented-tests.sh`
    - `grep -q 'Medium_Phone_API_35' scripts/run-instrumented-tests.sh`
    - `[ "$(grep -c 'checkpoint' scripts/run-instrumented-tests.sh)" -eq 0 ]`
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/run-instrumented-tests.sh &amp;&amp; grep -q sys.boot_completed scripts/run-instrumented-tests.sh &amp;&amp; grep -q 'trap cleanup EXIT' scripts/run-instrumented-tests.sh</automated>
  </verify>
  <done>Script executavel, sintaticamente valido, com polling de boot e trap de desligamento.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Teste instrumentado de fumaca provando o ciclo ponta a ponta</name>
  <files>app/src/androidTest/java/org/sentinela/app/InstrumentationSmokeTest.kt</files>
  <read_first>
    - scripts/run-instrumented-tests.sh (criado na Task 2)
    - app/build.gradle.kts (bloco `sourceSets` criado na Task 1)
    - .planning/phases/03-dados-locais/03-RESEARCH.md tabela "Onde ficam as evidencias"
  </read_first>
  <behavior>
    - O runner instrumentado executa e enxerga o applicationId real `org.sentinela.app`
    - O diretorio `schemas` esta acessivel como asset do androidTest (list() nao lanca)
  </behavior>
  <action>
Criar `app/src/androidTest/java/org/sentinela/app/InstrumentationSmokeTest.kt`:

```kotlin
package org.sentinela.app

import androidx.test.ext.junit.runners.AndroidJUnit4
import androidx.test.platform.app.InstrumentationRegistry
import org.junit.Assert.assertEquals
import org.junit.Assert.assertNotNull
import org.junit.Test
import org.junit.runner.RunWith

/**
 * Prova de que o ciclo instrumentado da Fase 3 esta de pe: runner, emulador e
 * assets do androidTest (onde vivem os schemas do Room para o MigrationTestHelper).
 */
@RunWith(AndroidJUnit4::class)
class InstrumentationSmokeTest {

    @Test
    fun runnerInstrumentadoEnxergaOAppReal() {
        val ctx = InstrumentationRegistry.getInstrumentation().targetContext
        assertEquals("org.sentinela.app", ctx.packageName)
    }

    @Test
    fun assetsDoAndroidTestExistem() {
        val assets = InstrumentationRegistry.getInstrumentation().context.assets
        assertNotNull("assets do androidTest inacessiveis", assets.list(""))
    }
}
```

Rodar `bash scripts/run-instrumented-tests.sh --tests "*InstrumentationSmokeTest"` e
conferir a saida real. Se o emulador nao subir, isso e **blocker reportado no SUMMARY**
(com o conteudo de `/tmp/sentinela-emulator.log`) — NUNCA converter estes testes em JVM
nem em Robolectric (4.16.1 nao suporta compileSdk 37).
  </action>
  <acceptance_criteria>
    - `bash scripts/run-instrumented-tests.sh --tests "*InstrumentationSmokeTest"` termina com `BUILD SUCCESSFUL`
    - Existe ao menos um XML: `[ "$(ls -1 app/build/outputs/androidTest-results/connected/debug/TEST-*.xml | wc -l)" -ge 1 ]`
    - O XML tem `failures="0"` e `errors="0"`
    - `[ "$(grep -c 'org.robolectric' app/src/androidTest/java/org/sentinela/app/InstrumentationSmokeTest.kt)" -eq 0 ]`
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-instrumented-tests.sh --tests "*InstrumentationSmokeTest"</automated>
  </verify>
  <done>Suite instrumentada roda verde no emulador via script unico, com XML de resultado gerado.</done>
</task>

</tasks>

<verification>
```bash
./gradlew :app:assembleDebugAndroidTest
bash -n scripts/run-instrumented-tests.sh
bash scripts/run-instrumented-tests.sh --tests "*InstrumentationSmokeTest"
./gradlew testDebugUnitTest lint detekt
```
</verification>

<success_criteria>
- `androidTestImplementation(libs.room.testing)` e `libs.androidx.test.core` presentes
- `sourceSets.getByName("androidTest").assets.srcDir("$projectDir/schemas")` presente
- `scripts/run-instrumented-tests.sh` sobe, espera `sys.boot_completed`, roda e derruba
- Ao menos um `TEST-*.xml` com `failures="0"` em `app/build/outputs/androidTest-results/connected/debug/`
- Nenhum `checkpoint:human-*` emitido; emulador que nao sobe vira blocker reportado
</success_criteria>

<output>
Criar `.planning/phases/03-dados-locais/03-01-SUMMARY.md`
</output>
