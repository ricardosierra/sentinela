---
phase: 03-dados-locais
plan: 05
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - app/src/main/java/org/sentinela/app/settings/RetentionPolicy.kt
  - app/src/main/java/org/sentinela/app/settings/ScreeningSettings.kt
  - app/src/main/java/org/sentinela/app/data/local/RoomBlockedCallRepository.kt
  - app/src/test/java/org/sentinela/app/settings/RetentionPolicyTest.kt
  - app/src/test/java/org/sentinela/app/data/local/FakeBlockedCallDao.kt
  - app/src/test/java/org/sentinela/app/data/local/RoomBlockedCallRepositoryTest.kt
  - app/src/androidTest/java/org/sentinela/app/data/local/db/BlockedCallDaoTest.kt
autonomous: true
requirements: [HST-01, HST-02, HST-03, HST-04, HST-05, QLT-01, QLT-06]

must_haves:
  truths:
    - "Historico registra o minimo (mascara, E.164, timestamp UTC, motivo, notificacao, classificacao)"
    - "Historico desligado ou retencao NEVER_STORE fazem record() nao gravar nada"
    - "A poda apaga o que passou da retencao e preserva o que esta dentro dela"
    - "MANUAL nunca poda automaticamente"
    - "Usuario limpa tudo ou exclui um registro individual"
    - "Usuario marca um registro como legitimo ou indesejado"
    - "O E.164 completo fica disponivel no registro para a Fase 8 adicionar a whitelist"
  artifacts:
    - path: "app/src/main/java/org/sentinela/app/settings/RetentionPolicy.kt"
      provides: "Enum nunca/7/30/90/manual + calculo puro do cutoff"
      contains: "enum class RetentionPolicy"
    - path: "app/src/main/java/org/sentinela/app/data/local/RoomBlockedCallRepository.kt"
      provides: "Implementacao Room do historico com poda apos cada gravacao"
      min_lines: 45
    - path: "app/src/test/java/org/sentinela/app/settings/RetentionPolicyTest.kt"
      provides: "Cobertura JVM pura das 5 politicas"
  key_links:
    - from: "RoomBlockedCallRepository.record"
      to: "SettingsRepository.snapshot"
      via: "consulta de historyEnabled e retentionPolicy antes de gravar"
      pattern: "snapshot\\(\\)"
    - from: "RoomBlockedCallRepository.record"
      to: "BlockedCallDao.pruneOlderThan"
      via: "poda apos gravacao bem-sucedida (sem WorkManager)"
      pattern: "pruneOlderThan"
---

<objective>
Implementar o historico local: retencao como regra pura testavel, repositorio Room com
poda sem WorkManager, limpeza total/individual e classificacao.

Purpose: o historico e o que da auditabilidade ao bloqueio (HST-01..05) e e o unico
lugar onde o E.164 completo e persistido — sob backup excluido e mascara em todo log.
Output: `RetentionPolicy` puro, `RoomBlockedCallRepository` e suites JVM + instrumentada.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dados-locais/03-CONTEXT.md
@.planning/phases/03-dados-locais/03-RESEARCH.md
@.planning/phases/03-dados-locais/03-03-SUMMARY.md
@docs/PROMPT-MVP.md
@app/src/main/java/org/sentinela/app/data/local/BlockedCallRepository.kt
@app/src/main/java/org/sentinela/app/settings/ScreeningSettings.kt
@app/src/main/java/org/sentinela/app/settings/SettingsRepository.kt
@app/src/main/java/org/sentinela/app/data/local/db/BlockedCallDao.kt

<interfaces>
<!-- Contratos JA EXISTENTES — implementar sem alterar assinaturas: -->
interface BlockedCallRepository {
    suspend fun record(entry: BlockedCallEntry)
    fun observeRecent(): Flow<List<BlockedCallEntry>>
    fun observeTotalCount(): Flow<Long>
    suspend fun deleteById(id: Long)
    suspend fun clearAll()
    suspend fun pruneOlderThan(utcMillis: Long)
}
interface SettingsRepository {
    val settings: Flow<ScreeningSettings>
    suspend fun snapshot(): ScreeningSettings
    suspend fun update(transform: (ScreeningSettings) -> ScreeningSettings)
}
<!-- ScreeningSettings ja tem 9 campos (protectionEnabled, unknownPolicy,
     contactsPolicy, whitelistPolicy, blockPrivateNumbers, blockMode,
     hideFromNativeCallLog, showOwnNotification, fallbackPolicy).
     Este plano ACRESCENTA historyEnabled e retentionPolicy — sem remover nada. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RetentionPolicy pura + extensao do ScreeningSettings</name>
  <files>app/src/main/java/org/sentinela/app/settings/RetentionPolicy.kt, app/src/main/java/org/sentinela/app/settings/ScreeningSettings.kt, app/src/test/java/org/sentinela/app/settings/RetentionPolicyTest.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/settings/ScreeningSettings.kt (integral — 9 campos atuais)
    - docs/PROMPT-MVP.md secao 7 (retencao)
    - .planning/phases/03-dados-locais/03-CONTEXT.md secao "Historico"
    - .planning/phases/03-dados-locais/03-RESEARCH.md "Pattern 4"
  </read_first>
  <behavior>
    - `NEVER_STORE.cutoff(now)` sinaliza "nao gravar" e `MANUAL.cutoff(now)` sinaliza "nunca podar" — os dois retornam `null`, distinguidos por `shouldStore`
    - `DAYS_7.cutoff(now) == now - 7 * 86_400_000L`; idem 30 e 90
    - `ScreeningSettings().retentionPolicy == RetentionPolicy.DAYS_30` (padrao travado)
    - `ScreeningSettings().historyEnabled == true` (padrao travado)
    - A lista de politicas tem exatamente 5 entradas com `id` estavel — reordenar nao corrompe dado persistido
  </behavior>
  <action>
Criar `app/src/main/java/org/sentinela/app/settings/RetentionPolicy.kt`:

```kotlin
package org.sentinela.app.settings

/**
 * Retencao do historico local (HST-02). O `id` e o valor PERSISTIDO — nunca o
 * `ordinal`, que mudaria de significado ao reordenar o enum e corromperia
 * silenciosamente a configuracao do usuario.
 *
 * Regra pura e sem Android: e assim que ela entra no gate de cobertura.
 */
enum class RetentionPolicy(val id: String, val days: Int?) {
    /** Nao guardar nada: record() retorna cedo. */
    NEVER_STORE("never", 0),
    DAYS_7("7d", 7),
    DAYS_30("30d", 30),
    DAYS_90("90d", 90),
    /** Guarda ate o usuario excluir: nunca poda automaticamente. */
    MANUAL("manual", null);

    /** O historico deve gravar novos registros com esta politica? */
    val shouldStore: Boolean get() = this != NEVER_STORE

    /**
     * Instante-limite: registros com timestamp ANTERIOR a este valor sao podados.
     * `null` = nao podar (MANUAL) ou nada a podar (NEVER_STORE nem grava).
     */
    fun cutoffUtcMillis(nowUtcMillis: Long): Long? =
        days?.takeIf { it > 0 }?.let { nowUtcMillis - it * MILLIS_PER_DAY }

    companion object {
        const val MILLIS_PER_DAY: Long = 86_400_000L
        fun fromId(id: String?): RetentionPolicy =
            entries.firstOrNull { it.id == id } ?: DAYS_30
    }
}
```

Em `app/src/main/java/org/sentinela/app/settings/ScreeningSettings.kt`, ACRESCENTAR dois
campos ao final da `data class` (sem remover nem reordenar os 9 existentes, para nao
quebrar chamadas nomeadas da Phase 2):

```kotlin
    /** Historico ligado por padrao: e o que da auditabilidade ao bloqueio. */
    val historyEnabled: Boolean = true,
    /** Retencao padrao 30 dias (decisao travada no CONTEXT da Fase 3). */
    val retentionPolicy: RetentionPolicy = RetentionPolicy.DAYS_30,
```
Atualizar o KDoc da classe mencionando historico ligado e retencao de 30 dias.

Criar `app/src/test/java/org/sentinela/app/settings/RetentionPolicyTest.kt` (JVM puro),
cobrindo os 5 comportamentos, minimo 8 `@Test`, incluindo:
- teste que trava a lista em 5 entradas e afirma os 5 `id` literais
  (`"never"`, `"7d"`, `"30d"`, `"90d"`, `"manual"`) — mesma disciplina do
  `DecisionReasonTest` da Phase 2;
- `fromId(null)` e `fromId("valor_desconhecido")` caem em `DAYS_30` sem lancar.

Nenhuma string de UI aqui: rotulos de tela sao da Fase 8, em `strings.xml`.
  </action>
  <acceptance_criteria>
    - `./gradlew testDebugUnitTest --tests "*RetentionPolicyTest"` verde
    - `[ "$(grep -c '@Test' app/src/test/java/org/sentinela/app/settings/RetentionPolicyTest.kt)" -ge 8 ]`
    - `grep -q 'val retentionPolicy: RetentionPolicy = RetentionPolicy.DAYS_30' app/src/main/java/org/sentinela/app/settings/ScreeningSettings.kt`
    - `grep -q 'val historyEnabled: Boolean = true' app/src/main/java/org/sentinela/app/settings/ScreeningSettings.kt`
    - `[ "$(grep -c 'import android' app/src/main/java/org/sentinela/app/settings/RetentionPolicy.kt)" -eq 0 ]`
    - `[ "$(grep -c 'ordinal' app/src/main/java/org/sentinela/app/settings/RetentionPolicy.kt)" -eq 0 ]`
    - `./gradlew testDebugUnitTest` continua verde (nenhum teste da Phase 2 quebrado)
  </acceptance_criteria>
  <verify>
    <automated>./gradlew testDebugUnitTest detekt</automated>
  </verify>
  <done>Retencao e regra pura com 5 politicas travadas por teste, defaults do MVP no ScreeningSettings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RoomBlockedCallRepository com poda apos cada gravacao</name>
  <files>app/src/main/java/org/sentinela/app/data/local/RoomBlockedCallRepository.kt, app/src/test/java/org/sentinela/app/data/local/FakeBlockedCallDao.kt, app/src/test/java/org/sentinela/app/data/local/RoomBlockedCallRepositoryTest.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/data/local/BlockedCallRepository.kt
    - app/src/main/java/org/sentinela/app/data/local/db/BlockedCallDao.kt
    - app/src/main/java/org/sentinela/app/settings/RetentionPolicy.kt (Task 1)
    - app/src/main/java/org/sentinela/app/data/local/RoomWhitelistRepository.kt (padrao de mapper/withContext do plano 03-04, se ja existir)
  </read_first>
  <behavior>
    - `historyEnabled = false` -> `record()` nao chama o DAO
    - `retentionPolicy = NEVER_STORE` -> `record()` nao chama o DAO
    - `historyEnabled = true` + `DAYS_30` -> grava e chama `pruneOlderThan(now - 30 dias)`
    - `MANUAL` -> grava e NAO chama `pruneOlderThan`
    - `deleteById` e `clearAll` delegam ao DAO
    - `observeRecent` mapeia entidade->dominio com `reason` e `classification` corretos e `numberE164` preservado (HST-04)
    - `updateClassification` persiste LEGITIMATE/UNWANTED (HST-05)
    - Excecao do DAO em `record()` propaga — o Service da Fase 5 e quem decide engolir, o repositorio nao mente
  </behavior>
  <action>
Criar `app/src/main/java/org/sentinela/app/data/local/RoomBlockedCallRepository.kt`:

```kotlin
class RoomBlockedCallRepository(
    private val dao: BlockedCallDao,
    private val settings: SettingsRepository,
    private val clock: () -> Long = System::currentTimeMillis,
    private val io: CoroutineDispatcher = Dispatchers.IO,
) : BlockedCallRepository {

    /**
     * HST-01/HST-02. Sem WorkManager: a poda roda logo apos cada gravacao
     * bem-sucedida (e na abertura do app, via AppContainer). A tabela e local e
     * pequena; uma dependencia nova custaria cold start do Service sem ganho.
     */
    override suspend fun record(entry: BlockedCallEntry) = withContext(io) {
        val current = settings.snapshot()
        if (!current.historyEnabled || !current.retentionPolicy.shouldStore) return@withContext
        dao.insert(entry.toEntity())
        pruneAccordingTo(current.retentionPolicy)
    }

    /** Chamada tambem na abertura do app (ENG/AppContainer, plano 03-07). */
    suspend fun pruneNow() = withContext(io) {
        pruneAccordingTo(settings.snapshot().retentionPolicy)
    }

    private suspend fun pruneAccordingTo(policy: RetentionPolicy) {
        policy.cutoffUtcMillis(clock())?.let { dao.pruneOlderThan(it) }
    }

    suspend fun updateClassification(id: Long, classification: CallClassification) =
        withContext(io) { dao.updateClassification(id, classification.name) }
    // observeRecent / observeTotalCount / deleteById / clearAll / pruneOlderThan: delegacao + mapper
}
```

Mais os mappers `BlockedCallEntity.toDomain()` / `BlockedCallEntry.toEntity()` usando os
`Converters` de codigo/nome (nunca `ordinal`).

Criar `app/src/test/java/org/sentinela/app/data/local/FakeBlockedCallDao.kt` (JVM puro,
lista + `MutableStateFlow`, contador de chamadas de `pruneOlderThan` para os asserts) e
`app/src/test/java/org/sentinela/app/data/local/RoomBlockedCallRepositoryTest.kt` cobrindo
os 8 comportamentos, com `clock = { FIXED_NOW }` (relogio injetado, nunca
`System.currentTimeMillis()` real no teste) e um fake de `SettingsRepository`.
Minimo 12 `@Test`.

PROIBIDO: `Log.` ou `println` com `numberE164`; adicionar coluna/campo de nome de contato;
usar WorkManager; usar `ordinal`.
  </action>
  <acceptance_criteria>
    - `./gradlew testDebugUnitTest --tests "*RoomBlockedCallRepositoryTest"` verde
    - `[ "$(grep -c '@Test' app/src/test/java/org/sentinela/app/data/local/RoomBlockedCallRepositoryTest.kt)" -ge 12 ]`
    - `grep -q 'shouldStore' app/src/main/java/org/sentinela/app/data/local/RoomBlockedCallRepository.kt`
    - `grep -q 'cutoffUtcMillis' app/src/main/java/org/sentinela/app/data/local/RoomBlockedCallRepository.kt`
    - `[ "$(grep -ci 'workmanager' app/src/main/java/org/sentinela/app/data/local/RoomBlockedCallRepository.kt)" -eq 0 ]`
    - `[ "$(grep -cE 'Log\.|println' app/src/main/java/org/sentinela/app/data/local/RoomBlockedCallRepository.kt)" -eq 0 ]`
    - `bash scripts/verify-invariants.sh` continua verde (invariante de nome de contato)
  </acceptance_criteria>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*RoomBlockedCallRepositoryTest" detekt</automated>
  </verify>
  <done>Historico grava condicionalmente, poda pela politica com relogio injetado, 12+ testes JVM verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: BlockedCallDaoTest instrumentado — poda, limpeza e classificacao</name>
  <files>app/src/androidTest/java/org/sentinela/app/data/local/db/BlockedCallDaoTest.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/data/local/db/BlockedCallDao.kt
    - app/src/main/java/org/sentinela/app/data/local/db/BlockedCallEntity.kt
    - app/src/androidTest/java/org/sentinela/app/InstrumentationSmokeTest.kt (padrao do runner)
    - scripts/run-instrumented-tests.sh
  </read_first>
  <behavior>
    - Insert + `observeRecent()` devolve o registro com mascara, E.164, motivo e notificacao intactos (HST-01)
    - `observeRecent()` vem ordenado por timestamp decrescente
    - `pruneOlderThan(cutoff)` apaga so o que e anterior ao cutoff e preserva o resto (HST-02)
    - `deleteById` remove um; `clearAll` zera a tabela e `observeTotalCount()` vai a 0 (HST-03)
    - `updateClassification` persiste UNWANTED e sobrevive a releitura (HST-05)
    - Round-trip do `reason_code` devolve o `DecisionReason` original pelos conversores
  </behavior>
  <action>
Criar `app/src/androidTest/java/org/sentinela/app/data/local/db/BlockedCallDaoTest.kt`
com `@RunWith(AndroidJUnit4::class)` e `Room.inMemoryDatabaseBuilder`, mesmo padrao de
setup/teardown do `WhitelistDaoTest`.

Cobrir os 6 comportamentos, minimo 8 `@Test`. Para a poda, inserir 3 registros com
timestamps explicitos (`cutoff - 1`, `cutoff`, `cutoff + 1`), chamar
`pruneOlderThan(cutoff)` e afirmar que sobraram exatamente 2 — o limite e estritamente
`< cutoff`, e esse limite precisa estar travado por assert.

Flows coletados com `.first()` dentro de `runBlocking`. Sem `allowMainThreadQueries`.

Rodar `bash scripts/run-instrumented-tests.sh --tests "*BlockedCallDaoTest"`.
Emulador que nao sobe = blocker reportado, nunca conversao para JVM.
  </action>
  <acceptance_criteria>
    - `bash scripts/run-instrumented-tests.sh --tests "*BlockedCallDaoTest"` termina com `BUILD SUCCESSFUL`
    - O `TEST-*.xml` correspondente tem `failures="0"`, `errors="0"` e `tests` >= 8
    - `[ "$(grep -c '@Test' app/src/androidTest/java/org/sentinela/app/data/local/db/BlockedCallDaoTest.kt)" -ge 8 ]`
    - `grep -q 'pruneOlderThan' app/src/androidTest/java/org/sentinela/app/data/local/db/BlockedCallDaoTest.kt`
    - `grep -q 'UNWANTED' app/src/androidTest/java/org/sentinela/app/data/local/db/BlockedCallDaoTest.kt`
    - `[ "$(grep -c 'allowMainThreadQueries' app/src/androidTest/java/org/sentinela/app/data/local/db/BlockedCallDaoTest.kt)" -eq 0 ]`
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-instrumented-tests.sh --tests "*BlockedCallDaoTest"</automated>
  </verify>
  <done>Poda, limpeza total/individual e classificacao verdes no emulador, com o limite do cutoff travado.</done>
</task>

</tasks>

<verification>
```bash
./gradlew testDebugUnitTest lint detekt
bash scripts/run-instrumented-tests.sh --tests "*BlockedCall*"
bash scripts/verify-invariants.sh
```
</verification>

<success_criteria>
- HST-01: registro minimo persistido e relido intacto
- HST-02: 5 politicas puras testadas + poda efetiva medida no banco, sem WorkManager
- HST-03: limpeza total e individual verdes
- HST-04: `numberE164` preservado no registro para a Fase 8
- HST-05: classificacao legitimo/indesejado persistida
- QLT-01: historico desligado, NEVER_STORE, MANUAL e falha de repositorio cobertos
- Nenhum nome de contato na camada de dados; nenhum numero completo em log
</success_criteria>

<output>
Criar `.planning/phases/03-dados-locais/03-05-SUMMARY.md`
</output>
