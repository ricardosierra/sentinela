---
phase: 03-dados-locais
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/res/xml/data_extraction_rules.xml
  - app/src/main/res/xml/full_backup_content.xml
  - app/src/test/java/org/sentinela/app/privacy/BackupRulesTest.kt
autonomous: true
requirements: [HST-06, PRV-03]

must_haves:
  truths:
    - "Backup em nuvem e device-transfer excluem banco, sharedpref e o arquivo do DataStore"
    - "Remover uma linha de <exclude> deixa a suite JVM vermelha"
    - "O manifest aponta para as duas regras (API 31+ e API 29-30)"
  artifacts:
    - path: "app/src/main/res/xml/data_extraction_rules.xml"
      provides: "Exclusoes de cloud-backup e device-transfer com path explicito"
      contains: "<exclude domain=\"file\" path=\"datastore\" />"
    - path: "app/src/main/res/xml/full_backup_content.xml"
      provides: "Equivalente legado para API 29-30"
      contains: "<exclude domain=\"file\" path=\"datastore\" />"
    - path: "app/src/test/java/org/sentinela/app/privacy/BackupRulesTest.kt"
      provides: "Teste JVM que le e afirma o XML"
      min_lines: 40
  key_links:
    - from: "app/src/test/java/org/sentinela/app/privacy/BackupRulesTest.kt"
      to: "app/src/main/res/xml/data_extraction_rules.xml"
      via: "DocumentBuilderFactory sobre caminho relativo ao modulo app/"
      pattern: "src/main/res/xml"
    - from: "app/src/main/AndroidManifest.xml"
      to: "@xml/data_extraction_rules e @xml/full_backup_content"
      via: "atributos do <application>"
      pattern: "android:(dataExtractionRules|fullBackupContent)"
---

<objective>
Fechar o criterio 5 do ROADMAP ("backup comprovadamente exclui os dados") com XML correto
e um teste JVM que le o XML — nao inspecao manual.

Purpose: PRV-03 e HST-06 sao afirmacoes de privacidade; so valem se existir um teste que
fica vermelho quando a propriedade quebra.
Output: dois XMLs corrigidos e `BackupRulesTest` verde, com a falha demonstrada.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dados-locais/03-RESEARCH.md
@docs/PRIVACIDADE.md
@app/src/main/res/xml/data_extraction_rules.xml
@app/src/main/res/xml/full_backup_content.xml
@app/src/main/AndroidManifest.xml

<interfaces>
<!-- Caminhos reais confirmados em runtime na API 35 (pesquisa da Fase 3): -->
<!--   /data/data/<pkg>/databases/           -> domain="database"  (recursivo, cobre -wal e -shm) -->
<!--   /data/data/<pkg>/files/datastore/*.preferences_pb -> domain="file" path="datastore" -->
<!--   /data/data/<pkg>/shared_prefs/        -> domain="sharedpref" -->
<!-- O working dir dos testes JVM e o diretorio do modulo app/ (fato da Phase 2, TestMetadata). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir os dois XMLs de backup com path explicito</name>
  <files>app/src/main/res/xml/data_extraction_rules.xml, app/src/main/res/xml/full_backup_content.xml</files>
  <read_first>
    - app/src/main/res/xml/data_extraction_rules.xml (estado atual: excludes de database/sharedpref SEM path; datastore com barra final)
    - app/src/main/res/xml/full_backup_content.xml (datastore com barra final)
    - app/src/main/AndroidManifest.xml (confirmar que os dois atributos ja apontam para os arquivos)
    - .planning/phases/03-dados-locais/03-RESEARCH.md secao "Backup — configuracao exata e verificacao"
  </read_first>
  <action>
Substituir integralmente `app/src/main/res/xml/data_extraction_rules.xml` por:

```xml
<?xml version="1.0" encoding="utf-8"?>
<!-- Privacidade: whitelist, historico e configuracoes NAO saem do aparelho via
     backup em nuvem nem transferencia device-to-device (docs/PRIVACIDADE.md).
     `path="."` e exigido pela documentacao oficial em include/exclude; a exclusao
     de diretorio e recursiva, entao -wal e -shm ficam cobertos sem serem listados
     (nomes transientes nao devem virar contrato). -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="datastore" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="datastore" />
    </device-transfer>
</data-extraction-rules>
```

Substituir integralmente `app/src/main/res/xml/full_backup_content.xml` por:

```xml
<?xml version="1.0" encoding="utf-8"?>
<!-- Equivalente legado (API 29-30) do data_extraction_rules.xml. minSdk = 29,
     entao os dois formatos precisam estar corretos. -->
<full-backup-content>
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
    <exclude domain="file" path="datastore" />
</full-backup-content>
```

Mudancas exatas: `path="."` explicito nos excludes de `database`/`sharedpref` do
`data_extraction_rules.xml`, e `datastore` **sem barra final** nos dois arquivos
(igualdade exata facilita o assert do teste).

NAO adicionar nenhum elemento `<include>` — um include reintroduziria o que foi excluido.
NAO tocar no `AndroidManifest.xml`: ele ja declara `android:dataExtractionRules` e
`android:fullBackupContent`.
  </action>
  <acceptance_criteria>
    - `[ "$(grep -c '<exclude domain="database" path="." />' app/src/main/res/xml/data_extraction_rules.xml)" -eq 2 ]`
    - `[ "$(grep -c '<exclude domain="file" path="datastore" />' app/src/main/res/xml/data_extraction_rules.xml)" -eq 2 ]`
    - `[ "$(grep -c '<exclude domain="file" path="datastore" />' app/src/main/res/xml/full_backup_content.xml)" -eq 1 ]`
    - `[ "$(grep -c '<include' app/src/main/res/xml/data_extraction_rules.xml)" -eq 0 ]`
    - `[ "$(grep -c '<include' app/src/main/res/xml/full_backup_content.xml)" -eq 0 ]`
    - `[ "$(grep -c 'datastore/' app/src/main/res/xml/full_backup_content.xml)" -eq 0 ]`
    - `./gradlew :app:processDebugMainManifest lint` termina com `BUILD SUCCESSFUL`
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :app:assembleDebug lint</automated>
  </verify>
  <done>Os dois XMLs tem os tres excludes com `path` explicito, sem nenhum `<include>`, e o build/lint passa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BackupRulesTest — teste JVM que le e afirma os XMLs</name>
  <files>app/src/test/java/org/sentinela/app/privacy/BackupRulesTest.kt</files>
  <read_first>
    - app/src/main/res/xml/data_extraction_rules.xml (apos a Task 1)
    - app/src/main/res/xml/full_backup_content.xml (apos a Task 1)
    - app/src/main/AndroidManifest.xml
    - app/src/test/java/org/sentinela/app/phone/TestMetadata.kt (confirma que o working dir do teste e `app/`)
    - .planning/phases/03-dados-locais/03-RESEARCH.md secao "Teste JVM que le o XML"
  </read_first>
  <behavior>
    - `cloud-backup` contem os 3 pares (database,.), (sharedpref,.), (file,datastore)
    - `device-transfer` contem os mesmos 3 pares
    - `full-backup-content` contem os mesmos 3 pares
    - Nenhum elemento `<include>` existe em nenhum dos dois arquivos
    - O manifest referencia `@xml/data_extraction_rules` e `@xml/full_backup_content`
    - Removendo qualquer `<exclude>`, ao menos um destes testes fica VERMELHO
  </behavior>
  <action>
Criar `app/src/test/java/org/sentinela/app/privacy/BackupRulesTest.kt`:

```kotlin
package org.sentinela.app.privacy

import java.io.File
import javax.xml.parsers.DocumentBuilderFactory
import org.junit.Assert.assertEquals
import org.junit.Assert.assertTrue
import org.junit.Test
import org.w3c.dom.Element

/**
 * PRV-03 / HST-06: whitelist, historico e configuracoes nunca saem do aparelho
 * por backup em nuvem ou device-transfer. Este teste LE o XML — declarar nao basta.
 *
 * Working dir do teste JVM = diretorio do modulo `app/` (fato fixado na Phase 2).
 */
class BackupRulesTest {

    private fun element(file: String, tag: String): Element {
        val doc = DocumentBuilderFactory.newInstance()
            .newDocumentBuilder()
            .parse(File("src/main/res/xml/$file"))
        val nodes = doc.getElementsByTagName(tag)
        assertEquals("elemento <$tag> nao encontrado em $file", 1, nodes.length)
        return nodes.item(0) as Element
    }

    private fun excludes(file: String, tag: String): Set<Pair<String, String>> {
        val nodes = element(file, tag).getElementsByTagName("exclude")
        return (0 until nodes.length).map { i ->
            val e = nodes.item(i) as Element
            e.getAttribute("domain") to e.getAttribute("path")
        }.toSet()
    }

    private fun includeCount(file: String): Int {
        val doc = DocumentBuilderFactory.newInstance()
            .newDocumentBuilder()
            .parse(File("src/main/res/xml/$file"))
        return doc.getElementsByTagName("include").length
    }

    @Test
    fun cloudBackupExcluiDadosSensiveis() {
        assertTrue(
            "cloud-backup nao exclui tudo: ${excludes(RULES, "cloud-backup")}",
            excludes(RULES, "cloud-backup").containsAll(REQUIRED),
        )
    }

    @Test
    fun deviceTransferExcluiDadosSensiveis() {
        assertTrue(
            "device-transfer nao exclui tudo: ${excludes(RULES, "device-transfer")}",
            excludes(RULES, "device-transfer").containsAll(REQUIRED),
        )
    }

    @Test
    fun fullBackupLegadoExcluiDadosSensiveis() {
        assertTrue(
            "full-backup-content nao exclui tudo: ${excludes(LEGACY, "full-backup-content")}",
            excludes(LEGACY, "full-backup-content").containsAll(REQUIRED),
        )
    }

    @Test
    fun nenhumIncludeReintroduzDadoExcluido() {
        assertEquals("<include> em $RULES reintroduziria dado excluido", 0, includeCount(RULES))
        assertEquals("<include> em $LEGACY reintroduziria dado excluido", 0, includeCount(LEGACY))
    }

    @Test
    fun manifestApontaParaAsDuasRegras() {
        val manifest = File("src/main/AndroidManifest.xml").readText()
        assertTrue(
            "manifest sem android:dataExtractionRules",
            manifest.contains("android:dataExtractionRules=\"@xml/data_extraction_rules\""),
        )
        assertTrue(
            "manifest sem android:fullBackupContent (necessario em API 29-30)",
            manifest.contains("android:fullBackupContent=\"@xml/full_backup_content\""),
        )
    }

    private companion object {
        const val RULES = "data_extraction_rules.xml"
        const val LEGACY = "full_backup_content.xml"
        val REQUIRED = setOf(
            "database" to ".",
            "sharedpref" to ".",
            "file" to "datastore",
        )
    }
}
```

**Prova obrigatoria de que o teste falha de verdade** (mesma disciplina do `koverVerify`
da Phase 2): antes de commitar, remover temporariamente a linha
`<exclude domain="file" path="datastore" />` do `<cloud-backup>`, rodar
`./gradlew testDebugUnitTest --tests "*BackupRulesTest"`, confirmar VERMELHO, restaurar
a linha e confirmar VERDE. Registrar as duas saidas no SUMMARY.

Se o manifest usar aspas simples ou formatacao diferente, ajustar o assert ao texto real
do arquivo — **nunca** afrouxar para um `contains("dataExtractionRules")` generico.
  </action>
  <acceptance_criteria>
    - `./gradlew testDebugUnitTest --tests "*BackupRulesTest"` termina com `BUILD SUCCESSFUL`
    - `[ "$(grep -c '@Test' app/src/test/java/org/sentinela/app/privacy/BackupRulesTest.kt)" -ge 5 ]`
    - O XML `app/build/test-results/testDebugUnitTest/TEST-org.sentinela.app.privacy.BackupRulesTest.xml` tem `failures="0"` e `errors="0"` e `tests="5"`
    - `[ "$(grep -c 'DocumentBuilderFactory' app/src/test/java/org/sentinela/app/privacy/BackupRulesTest.kt)" -ge 1 ]` (parse real, nao regex)
    - `[ "$(grep -c 'Regex\|toRegex' app/src/test/java/org/sentinela/app/privacy/BackupRulesTest.kt)" -eq 0 ]`
    - SUMMARY registra a saida vermelha da remocao temporaria de um `<exclude>`
  </acceptance_criteria>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*BackupRulesTest" detekt</automated>
  </verify>
  <done>5 testes verdes lendo os XMLs por DOM, com a falha demonstrada ao remover um exclude.</done>
</task>

</tasks>

<verification>
```bash
./gradlew assembleDebug testDebugUnitTest --tests "*BackupRulesTest" lint detekt
bash scripts/verify-invariants.sh
```
</verification>

<success_criteria>
- Os tres pares de exclusao presentes em `cloud-backup`, `device-transfer` e `full-backup-content`
- Zero elementos `<include>` nos dois arquivos
- `BackupRulesTest` com 5 testes verdes e falha demonstrada
- Nenhuma permissao nova; `verify-invariants.sh` continua verde
</success_criteria>

<output>
Criar `.planning/phases/03-dados-locais/03-02-SUMMARY.md`
</output>
