---
phase: 07-ui-onboarding-e-home
plan: 11
type: execute
wave: 5
depends_on: ["07-10"]
files_modified:
  - app/build.gradle.kts
  - app/lint.xml
  - docs/design/TELAS.md
  - docs/TESTE-FISICO-SAMSUNG.md
  - docs/backlog/capacidades-prometidas-nos-mockups.md
  - .planning/phases/07-ui-onboarding-e-home/07-EVIDENCE.md
autonomous: true
requirements: [UIX-07, UIX-09, UIX-11]

must_haves:
  truths:
    - "A supressão de lint da fase 1 deixou de ser um bloco cego e passou a listar nominalmente as fases que a justificam"
    - "Uma string nova desta fase que ninguém use volta a aparecer como achado de lint"
    - "O documento de telas descreve as telas que existem, e nenhuma afirmação dele é maior do que o que foi medido"
    - "O que só se prova em aparelho está no roteiro físico como cenário numerado, nunca como lacuna"
  artifacts:
    - path: "app/lint.xml"
      provides: "estreitamento da supressão por prefixo, cada prefixo comentado com a fase que o consome"
      contains: "UnusedResources"
    - path: ".planning/phases/07-ui-onboarding-e-home/07-EVIDENCE.md"
      provides: "evidência pós-limpeza sem cache, com contagens reais"
      min_lines: 60
  key_links:
    - from: "app/lint.xml"
      to: "app/build.gradle.kts"
      via: "a regra sai do bloco de desabilitados e passa a ser estreitada por expressão"
      pattern: "UnusedResources"
---

<objective>
Fechamento da fase: reabilitar e estreitar a política de lint com os números medidos, fazer a faxina
dos dois defeitos incidentais restantes, reescrever o documento de telas a partir do contrato de
design, acrescentar os cenários físicos a partir de 61, cobrar o gate de cobertura e arquivar a
evidência pós-limpeza.

Purpose: precedente de 06-08 e 06-09 — a política de lint, o gate de cobertura e a documentação só
fazem sentido depois de as telas existirem e os testes dizerem o que de fato foi verificado.
Output: `app/lint.xml`, `app/build.gradle.kts` ajustado, `TELAS.md` reescrito, cenários 61 em diante,
`07-EVIDENCE.md`.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-onboarding-e-home/07-RESEARCH.md
@.planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md
@.planning/phases/06-modo-discador-opcional/06-EVIDENCE.md
@docs/design/TELAS.md
@docs/TESTE-FISICO-SAMSUNG.md
@CLAUDE.md

<interfaces>
Números MEDIDOS na pesquisa desta fase, a usar e a citar:
- com a regra de recursos não usados reabilitada, o lint de depuração saiu com código 0 — a
  severidade é de aviso e a conversão de aviso em erro não está ligada. **A supressão nunca foi o que
  mantinha o build verde**, e o plano deve dizer isso com franqueza em vez de repetir a premissa
  antiga
- 133 achados no total: 131 de texto, 1 de cor, 1 de ícone redondo do lançador
- estreitamento por expressão nos prefixos das fases 8 e 9 testado: **133 vira 81**, silenciando
  exatamente 52
- cerca de 79 dos 133 pertencem a esta fase e devem DESAPARECER porque as telas agora os consomem
- bloco de dependências de teste DUPLICADO em `app/build.gradle.kts`, as mesmas três linhas duas
  vezes, com dois comentários diferentes da Fase 6
- partida a frio hoje: mediana 680 ms em oito execuções, faixa de 612 a 917 ms
- cobertura atual do gate: 96,69%

Mecanismo verificado funcionando:
```xml
<?xml version="1.0" encoding="utf-8"?>
<lint>
    <issue id="UnusedResources">
        <ignore regexp="R\.string\.(whitelist|history|about|support|review)_" />
    </issue>
</lint>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reabilitar e estreitar a política de lint, e a faxina dos dois defeitos incidentais</name>
  <files>app/build.gradle.kts, app/lint.xml</files>
  <read_first>
    - app/build.gradle.kts (o bloco de lint e os blocos de dependências de teste)
    - .planning/phases/07-ui-onboarding-e-home/07-RESEARCH.md (§Q7 inteira)
    - app/src/main/res/values/colors.xml
    - .planning/STATE.md (a pendência do ícone)
  </read_first>
  <action>
    1. Remover a regra de recursos não usados do bloco de desabilitados em `app/build.gradle.kts`.
       Substituir o comentário antigo por um que diga a verdade medida: a supressão nunca foi
       necessária para manter o build verde — a severidade é de aviso e a conversão de aviso em erro
       não está ligada; ela servia para manter o relatório limpo, e agora que as telas consomem as
       chaves o relatório pode voltar a ser útil.

    2. Criar `app/lint.xml` com o estreitamento por expressão restrito aos prefixos das Fases 8 e 9,
       **cada prefixo em sua própria linha e cada linha comentada com a fase que o consome**, para que
       a Phase 8 saiba exatamente qual linha apagar. Os prefixos são os cinco medidos: whitelist,
       history, about, support e review.

    3. Resolver os dois achados que NÃO são texto de fase futura, porque são resíduo real do
       esqueleto: a cor primária declarada em recurso e o ícone redondo do lançador. Para cada um,
       escolher uma das três condutas e JUSTIFICAR nominalmente no comentário: usar, apagar ou ignorar
       com motivo. O ícone redondo se relaciona com a pendência de arte final registrada no estado do
       projeto, e por isso ignorar com motivo nomeado é conduta legítima aqui; apagá-lo não é, porque
       recriaria trabalho.

    4. Faxina do bloco duplicado: as mesmas três linhas de dependência de teste aparecem duas vezes
       em `app/build.gradle.kts`, com dois comentários diferentes da Fase 6. Remover a SEGUNDA cópia,
       preservando o comentário mais informativo dos dois.

    5. **NÃO** ligar a conversão de aviso em erro. Está fora de escopo e transformaria os achados
       remanescentes em build vermelho.

    Registrar no SUMMARY a contagem real de achados de recursos não usados APÓS esta fase, lida do
    relatório em formato de análise estática, e comparar com os 133 e os 81 medidos na pesquisa. Se o
    número final for maior que o esperado, LISTAR NOMINALMENTE as chaves remanescentes e a fase a que
    pertencem — nunca suprimir em bloco.
  </action>
  <verify>
    <automated>./gradlew :app:lintDebug --rerun-tasks && ./gradlew --rerun-tasks assembleDebug testDebugUnitTest detekt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"UnusedResources"' app/build.gradle.kts` devolve 0
    - `app/lint.xml` existe, contém `UnusedResources` e exatamente cinco expressões de prefixo, cada uma numa linha com comentário citando a fase
    - `grep -c 'compose-ui-test-junit4' app/build.gradle.kts` devolve 1
    - `grep -c 'warningsAsErrors' app/build.gradle.kts` devolve 0
    - `./gradlew :app:lintDebug --rerun-tasks` sai com código 0
    - PROVA DE VERMELHO (estreitamento não silencia demais): acrescentar em `strings.xml` uma chave com prefixo DESTA fase e não usá-la ⇒ o achado de recurso não usado aparece no relatório; remover a chave e reexecutar. Transcrever no SUMMARY a linha do relatório. Fazer o bloco de desabilitados voltar NÃO é prova — a medição já mostrou que ele nunca segurava o build
  </acceptance_criteria>
  <done>A supressão cega virou estreitamento nominal por fase, os dois resíduos do esqueleto têm conduta justificada, o bloco duplicado saiu, e o estreitamento foi provado não silenciar as chaves desta fase.</done>
</task>

<task type="auto">
  <name>Task 2: Documento de telas reescrito e cenários físicos a partir de 61</name>
  <files>docs/design/TELAS.md, docs/TESTE-FISICO-SAMSUNG.md, docs/backlog/capacidades-prometidas-nos-mockups.md</files>
  <read_first>
    - docs/design/TELAS.md (inteiro; a §11 é o modelo de contrato que a Fase 6 produziu)
    - docs/TESTE-FISICO-SAMSUNG.md (os 60 cenários existentes, para não duplicar)
    - docs/backlog/capacidades-prometidas-nos-mockups.md
    - docs/INDEX.md
    - .planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md
    - todos os SUMMARY de 07-01 a 07-10
  </read_first>
  <action>
    1. Reescrever as seções 1 a 6 e a seção 10 de `docs/design/TELAS.md` a partir do contrato de
       design desta fase, no MESMO padrão da seção 11 — que a Fase 6 converteu de esboço em contrato.
       Descrever as telas que EXISTEM, com os componentes e as chaves reais. Nenhuma afirmação pode
       ser maior do que o que os testes desta fase provaram: onde algo só se prova em aparelho, apontar
       o cenário numerado do roteiro físico, no molde do que 06-08 fez ao devolver o item de número
       privado à condição de não verificado.

    2. Registrar explicitamente, nas seções que descrevem boas-vindas e a home, as substituições de
       copy decididas pelo usuário: as cinco capacidades que os mockups prometiam e o MVP não entrega,
       cada uma apontando o arquivo de registro pós-lançamento; e as duas imagens remotas substituídas
       por superfície tonal, com o motivo (o aplicativo não declara acesso à internet).

    3. Acrescentar a `docs/TESTE-FISICO-SAMSUNG.md` os cenários desta fase, começando em **61** e
       numerados sem lacuna. Cobrir exatamente o que a automação NÃO alcança e que a pesquisa nomeou:
       locução, ritmo e verbosidade reais do leitor de tela; gestos de exploração por toque e de
       deslizamento; ordem de foco EFETIVA (a declarada já é automatizada); contraste medido sob cor
       dinâmica com papel de parede real; escala de fonte a 200% em aparelho; e a percepção da partida
       a frio em hardware, já que a mediana de 680 ms foi medida em aparelho virtual e a cauda ali mede
       o hospedeiro tanto quanto o aplicativo. Cada cenário com passos, resultado esperado e coluna de
       resultado em branco.

    4. Conferir que todo `.md` novo ou renomeado está em `docs/INDEX.md`.

    Regra de honestidade documental herdada de 06-08: afirmação de documento sem medição É defeito.
    Se algum item deste plano não tiver medição, ele entra como não verificado apontando o cenário.
  </action>
  <verify>
    <automated>bash scripts/verify-invariants.sh && [ "$(grep -cE '^\| 6[1-9] \|' docs/TESTE-FISICO-SAMSUNG.md)" -ge 6 ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## ' docs/design/TELAS.md` continua igual ou maior que antes, e as seções 1 a 6 e 10 contêm as chaves de recurso reais das telas implementadas
    - `[ "$(grep -cE '^\| 6[1-9] \|' docs/TESTE-FISICO-SAMSUNG.md)" -ge 6 ]` verdadeiro, e a numeração não tem lacuna a partir de 61
    - `grep -c 'capacidades-prometidas-nos-mockups' docs/design/TELAS.md` é maior que 0
    - `grep -ciE '100% garantido|bloqueio total|base global|criptografad' docs/design/TELAS.md` devolve 0
    - Todo arquivo `.md` novo referenciado em `docs/INDEX.md`
    - `bash scripts/verify-invariants.sh` verde
  </acceptance_criteria>
  <done>O documento de telas descreve o que existe com as chaves reais, as substituições de copy estão registradas, e o que só se prova em aparelho está numerado a partir de 61.</done>
</task>

<task type="auto">
  <name>Task 3: Gate de cobertura e evidência pós-limpeza sem cache</name>
  <files>.planning/phases/07-ui-onboarding-e-home/07-EVIDENCE.md, app/build.gradle.kts, .planning/phases/07-ui-onboarding-e-home/07-VALIDATION.md</files>
  <read_first>
    - app/build.gradle.kts (o bloco de cobertura)
    - .planning/phases/06-modo-discador-opcional/06-EVIDENCE.md (o formato a seguir)
    - todos os SUMMARY de 07-01 a 07-10
    - .planning/phases/07-ui-onboarding-e-home/07-VALIDATION.md
  </read_first>
  <action>
    1. Cobrar `koverVerify` de novo. A fase acrescentou a chave de onboarding concluído em
       `settings.*`, que É medido — e os donos de estado e as telas vivem em `ui.*`, que NÃO é medido.
       Verificar o número real por `./gradlew koverLog`.

    2. Decidir sobre o filtro de cobertura, e este é o ÚNICO plano da fase autorizado a mexer nele:
       - se a cobertura estiver acima do piso, **não alargar nada**. Alargar o filtro para `ui.*`
         seria escopo novo e traria composta e pré-visualização para o denominador
       - se algum arquivo novo de `settings.*` ou `data.*` derrubar o número, o remédio é TESTE, não
         exclusão
       - se for realmente necessário excluir, excluir por **NOME DE CLASSE**, nunca por pacote, com
         comentário dizendo o que prova aquele código no lugar do Kover — precedente de 04-05, 06-08 e
         06-09
       - o gate só é aceito depois de VISTO VERMELHO: subir o piso temporariamente para 99, ver o
         build quebrar, transcrever, e restaurar para 80

    3. Criar `.planning/phases/07-ui-onboarding-e-home/07-EVIDENCE.md` no formato de 06-EVIDENCE,
       com a saída REAL de:
       ```bash
       ./gradlew clean
       ./gradlew --no-build-cache assembleDebug testDebugUnitTest koverVerify lint detekt
       bash scripts/verify-invariants.sh
       ```
       Evidência só vale com limpeza E sem cache de build: reaproveitamento de cache tem exatamente o
       mesmo defeito probatório que tarefa considerada atualizada. Registrar a linha de resumo de
       tarefas mostrando que houve tarefas EXECUTADAS, não só atualizadas.

       Registrar também: contagem total de casos em JVM antes e depois da fase (a de antes é 603 mais
       o que a Fase 6 acrescentou — ler do relatório, não do estado); número de achados de recursos não
       usados após o estreitamento; cobertura do gate; e a lista de provas de vermelho da fase inteira,
       plano por plano, com uma linha cada.

    4. A suíte instrumentada NÃO ganha caso novo nesta fase, e isto é decisão registrada, não lacuna: a
       pesquisa mediu que o fluxo multi-tela inteiro roda em JVM sob leitor de configuração de
       dispositivo, com o grafo real, e o que resta é hardware — que virou cenário numerado na Task 2.
       Executar a suíte instrumentada EXISTENTE uma vez para provar que a fase não a quebrou, e
       arquivar a saída. Emulador que não sobe é blocker reportado no SUMMARY, nunca troca silenciosa
       por teste em JVM.

    5. Atualizar `07-VALIDATION.md`: marcar cada linha do mapa de verificação por task como verde, e
       preencher a data de aprovação.
  </action>
  <verify>
    <automated>./gradlew clean && ./gradlew --no-build-cache assembleDebug testDebugUnitTest koverVerify lint detekt && bash scripts/verify-invariants.sh && bash scripts/run-instrumented-tests.sh</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew --no-build-cache ... koverVerify ...` sai com código 0, e a evidência transcreve a linha de resumo de tarefas com número de tarefas executadas maior que zero
    - `.planning/phases/07-ui-onboarding-e-home/07-EVIDENCE.md` existe e contém: contagem de casos em JVM antes e depois, cobertura do gate, achados de recursos não usados após o estreitamento, e a lista de provas de vermelho da fase
    - Se houver exclusão nova no bloco de cobertura, ela é um nome de classe e nunca um pacote: `grep -c 'classes("org.sentinela.app.ui' app/build.gradle.kts` devolve 0
    - PROVA DE VERMELHO (gate): piso temporário em 99 quebra o build; saída transcrita; piso restaurado em 80
    - `bash scripts/run-instrumented-tests.sh` verde, com a saída arquivada
    - `07-VALIDATION.md` com `status: approved` e todas as linhas do mapa marcadas
  </acceptance_criteria>
  <done>O gate de cobertura volta a ser cobrado e foi visto vermelho; a evidência pós-limpeza sem cache está arquivada com números reais; a suíte instrumentada existente continua verde.</done>
</task>

</tasks>

<verification>
```bash
./gradlew clean
./gradlew --no-build-cache assembleDebug testDebugUnitTest koverVerify lint detekt
bash scripts/verify-invariants.sh
bash scripts/run-instrumented-tests.sh
```
</verification>

<success_criteria>
- Supressão de lint estreitada nominalmente por fase, com prova de que não silencia esta fase
- Dois defeitos incidentais e os dois resíduos do esqueleto resolvidos
- Documento de telas reescrito sem afirmação não medida; cenários físicos a partir de 61
- `koverVerify` verde e visto vermelho; `07-EVIDENCE.md` pós-limpeza sem cache
- `07-VALIDATION.md` aprovado
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-ui-onboarding-e-home/07-11-SUMMARY.md`
</output>
