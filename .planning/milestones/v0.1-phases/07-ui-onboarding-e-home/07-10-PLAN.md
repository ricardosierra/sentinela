---
phase: 07-ui-onboarding-e-home
plan: 10
type: execute
wave: 4
depends_on: ["07-05", "07-06", "07-07", "07-08", "07-09"]
files_modified:
  - app/src/main/java/org/sentinela/app/ui/navigation/SentinelaNavHost.kt
  - app/src/main/java/org/sentinela/app/ui/onboarding/OnboardingRoute.kt
  - app/src/main/java/org/sentinela/app/ui/home/HomeRoute.kt
  - app/src/main/java/org/sentinela/app/ui/settings/SettingsRoute.kt
  - app/src/main/java/org/sentinela/app/ui/dialer/DialerActivationRoute.kt
  - app/src/main/java/org/sentinela/app/ui/MainActivity.kt
  - scripts/verify-invariants.sh
  - app/src/test/java/org/sentinela/app/ui/onboarding/OnboardingFlowTest.kt
  - app/src/test/java/org/sentinela/app/ui/settings/DialerRouteTest.kt
autonomous: true
requirements: [SCR-01, UIX-01, UIX-03, UIX-07]

must_haves:
  truths:
    - "O aplicativo abre no onboarding na primeira abertura e na home depois, sem nunca decidir isso bloqueando a thread principal"
    - "Pular o onboarding leva à home e o gesto de voltar não devolve o usuário a ele"
    - "O papel é reconsultado a cada retomada da home e da Proteção"
    - "A tela de ativação do modo discador é alcançável a partir de Proteção nos cinco ramos de estado"
    - "Nenhum literal de texto de interface existe nas telas desta fase"
  artifacts:
    - path: "app/src/main/java/org/sentinela/app/ui/navigation/SentinelaNavHost.kt"
      provides: "o grafo com os dez destinos e o descarte inclusivo do onboarding"
      min_lines: 100
    - path: "app/src/main/java/org/sentinela/app/ui/MainActivity.kt"
      provides: "hospedeira do grafo com estado de carregamento explícito para o destino inicial"
      min_lines: 60
    - path: "scripts/verify-invariants.sh"
      provides: "Bloco 9: zero literal de texto de interface e fronteira do número nas telas da fase"
      contains: "Bloco 9"
  key_links:
    - from: "app/src/main/java/org/sentinela/app/ui/MainActivity.kt"
      to: "app/src/main/java/org/sentinela/app/ui/navigation/SentinelaNavHost.kt"
      via: "grafo composto somente depois de o destino inicial estar decidido"
      pattern: "SentinelaNavHost"
    - from: "app/src/main/java/org/sentinela/app/ui/home/HomeRoute.kt"
      to: "app/src/main/java/org/sentinela/app/ui/home/HomeViewModel.kt"
      via: "reconsulta do papel na retomada da tela"
      pattern: "LifecycleResumeEffect"
    - from: "app/src/main/java/org/sentinela/app/ui/settings/SettingsRoute.kt"
      to: "app/src/main/java/org/sentinela/app/ui/dialer/DialerActivationScreen.kt"
      via: "destino de navegação com os quatro retornos de chamada existentes"
      pattern: "DialerActivationScreen"
---

<objective>
A fiação: o grafo real, as quatro camadas de rota finas que conhecem o container, a hospedeira sem
tela de espera falsa, o Bloco 9 de invariantes e o teste de fluxo de ponta a ponta em JVM.

Purpose: é onde o aplicativo deixa de ser esqueleto para o usuário, e onde a pendência registrada em
06-05 se fecha — a tela de ativação do modo discador ganha ponto de entrada.
Output: `SentinelaNavHost`, quatro rotas, `MainActivity` reescrita, Bloco 9, dois testes.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-onboarding-e-home/07-RESEARCH.md
@.planning/phases/07-ui-onboarding-e-home/07-CONTEXT.md
@.planning/phases/07-ui-onboarding-e-home/07-02-SUMMARY.md
@.planning/phases/07-ui-onboarding-e-home/07-04-SUMMARY.md
@.planning/phases/07-ui-onboarding-e-home/07-08-SUMMARY.md
@.planning/phases/07-ui-onboarding-e-home/07-09-SUMMARY.md
@CLAUDE.md

<interfaces>
```kotlin
// ui/navigation/SentinelaRoutes.kt (07-02) — dez constantes de texto
internal object Rotas { const val BOAS_VINDAS = "..."; /* … */ }

// AppContainer.kt — colaboradores relevantes, todos por lazy
val settingsRepository: DataStoreSettingsRepository
val blockedCallRepository: RoomBlockedCallRepository
val maskNumber: (String) -> String
fun onAppOpened()

// PROIBIDO nesta fase: container.contactLookupRepository (constrói cache medido em 2,57 s)

// telecom/SystemRoleGate.kt
fun isRoleAvailable(): Boolean; fun isRoleHeld(): Boolean; fun buildRequestIntent(): Intent?
class ScreeningRoleManager(context: Context) : SystemRoleGate(...)

// platform/ContactsPermissionChecker.kt
fun isGranted(context: Context): Boolean
fun state(activity: Activity, alreadyAsked: Boolean): ContactsPermissionState
fun appSettingsIntent(packageName: String): Intent

// ui/dialer/DialerActivationScreen.kt — assinatura FECHADA, não muda
fun DialerActivationScreen(state: DialerModeState, onRequestRole: () -> Unit, onRevert: () -> Unit,
                           onGrantContacts: () -> Unit, modifier: Modifier = Modifier,
                           onBack: (() -> Unit)? = null)
```

Formas verificadas a compilar neste repositório:
```kotlin
val vm: HomeViewModel = viewModel(factory = HomeViewModel.factory(container))
val estado by vm.estado.collectAsStateWithLifecycle()
LifecycleResumeEffect(Unit) { vm.reconsultarPapel(); onPauseOrDispose { } }
val seletor = rememberLauncherForActivityResult(ActivityResultContracts.StartActivityForResult()) { vm.reconsultarPapel() }
val pedido = rememberLauncherForActivityResult(ActivityResultContracts.RequestPermission()) { vm.respondeu(it) }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: As quatro camadas de rota e o grafo</name>
  <files>app/src/main/java/org/sentinela/app/ui/onboarding/OnboardingRoute.kt, app/src/main/java/org/sentinela/app/ui/home/HomeRoute.kt, app/src/main/java/org/sentinela/app/ui/settings/SettingsRoute.kt, app/src/main/java/org/sentinela/app/ui/dialer/DialerActivationRoute.kt, app/src/main/java/org/sentinela/app/ui/navigation/SentinelaNavHost.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/ui/navigation/SentinelaRoutes.kt
    - app/src/main/java/org/sentinela/app/ui/home/HomeViewModel.kt
    - app/src/main/java/org/sentinela/app/ui/onboarding/OnboardingViewModel.kt
    - app/src/main/java/org/sentinela/app/ui/settings/SettingsViewModel.kt
    - app/src/main/java/org/sentinela/app/ui/dialer/DialerActivity.kt (o precedente em produção de lançadores e de marca gravada ao disparar)
    - app/src/main/java/org/sentinela/app/AppContainer.kt
    - .planning/phases/07-ui-onboarding-e-home/07-RESEARCH.md (§Q1, §Q2, §Q3, §Q4)
  </read_first>
  <action>
    Regra desta task, e ela é o que mantém tudo testável em JVM: **composta de TELA recebe estado e
    retornos de chamada e nunca recebe container; só a camada de rota, fina, conhece o container.** É
    o padrão que a tela de chamada e a de ativação do discador já seguem, e a Fase 5 mediu que um
    segundo container no mesmo processo derruba a aplicação.

    **`ui/onboarding/OnboardingRoute.kt`.** `OnboardingRoute(container: AppContainer, nav: NavController)`.
    Constrói o dono de estado pela fábrica; monta os lançadores de leitura da agenda e de notificação
    por `rememberLauncherForActivityResult` com o contrato de pedido de permissão; monta o lançador do
    seletor de papel com o contrato de início de Activity por resultado, cujo retorno APENAS
    reconsulta — o código de resultado nunca é a fonte da verdade, porque perder um papel encerra o
    processo e o retorno não roda no caminho que importa; reconsulta o papel por `LifecycleResumeEffect`;
    lê o estado da permissão da agenda pelo verificador de plataforma, que exige `Activity` e por isso
    é lido aqui e desce como valor — o dono de estado NUNCA chama a plataforma; e despacha os seis
    passos por um `when` sobre o passo corrente, chamando as seis compostas puras já existentes.
    `pular()` e `concluir()` navegam para a home com descarte inclusivo do início e entrada única.

    **`ui/home/HomeRoute.kt`.** `HomeRoute(container: AppContainer, nav: NavController)`. Dono de
    estado pela fábrica; `LifecycleResumeEffect` reconsultando o papel a cada retomada; lançador do
    seletor de papel cujo retorno reconsulta — redundância deliberada, no molde das duas redes
    permissivas da Fase 5 e das duas defesas do vigia da Fase 6, porque a retomada cobriria o caso
    sozinha; lançador de pedido de leitura da agenda; e o botão de correção que só dispara quando a
    intenção de pedido não é nula. Navegação para Proteção, whitelist, histórico e ativação do
    discador.

    **`ui/settings/SettingsRoute.kt`.** Dono de estado pela fábrica; `LifecycleResumeEffect`
    reconsultando os dois papéis; e os 19 retornos de chamada da tela ligados às funções do dono de
    estado.

    **`ui/dialer/DialerActivationRoute.kt`.** Liga a composta `DialerActivationScreen`, que já existe
    e cuja assinatura NÃO muda, aos quatro retornos de chamada: pedir o papel de telefone padrão pelo
    seletor do sistema, reverter abrindo o seletor do sistema — desabilitar componente próprio é
    proibido para sempre e é checagem 8.2 do script —, conceder a leitura da agenda, e voltar. Esta
    é a pendência registrada em 06-05 e é aqui que ela se fecha.

    **`ui/navigation/SentinelaNavHost.kt`.**
    `SentinelaNavHost(container: AppContainer, startDestination: String, modifier: Modifier = Modifier)`.
    `NavHost` com os dez destinos por rota de TEXTO, a partir das constantes. Os destinos de whitelist
    e histórico, que são da Phase 8, recebem uma composta de espera que comunica o estado — nunca tela
    em branco, o que `UIX-10` proíbe. O destino de privacidade e sobre, da Phase 9, recebe o mesmo
    tratamento.

    A transição entre passos do onboarding vive aqui, como animação do destino: deslizamento
    horizontal mais dissolução, 250 ms, espelhada no retorno, suprimida com a redução de movimento
    ligada.

    NENHUMA rota anotada, e nenhum literal de rota fora das constantes.
  </action>
  <verify>
    <automated>./gradlew assembleDebug lint detekt --rerun-tasks && ./gradlew testDebugUnitTest --tests "*NavGraphContractTest" --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'LifecycleResumeEffect' app/src/main/java/org/sentinela/app/ui/home/HomeRoute.kt` é 1 e o mesmo em `SettingsRoute.kt` e em `OnboardingRoute.kt`
    - `grep -rc 'contactLookupRepository' app/src/main/java/org/sentinela/app/ui/` devolve 0 em todos os arquivos
    - `grep -rc 'runBlocking' app/src/main/java/org/sentinela/app/ui/` devolve 0 em todos os arquivos
    - `grep -c 'DialerActivationScreen' app/src/main/java/org/sentinela/app/ui/dialer/DialerActivationRoute.kt` é 1, e `git diff app/src/main/java/org/sentinela/app/ui/dialer/DialerActivationScreen.kt` está vazio — a tela existente não é tocada
    - `grep -rciE 'serializable' app/src/main/java/org/sentinela/app/ui/navigation/` devolve 0
    - `grep -c 'composable(' app/src/main/java/org/sentinela/app/ui/navigation/SentinelaNavHost.kt` devolve 10
    - `./gradlew assembleDebug lint detekt --rerun-tasks` verde
  </acceptance_criteria>
  <done>As quatro camadas de rota conhecem o container e as telas não; o grafo tem dez destinos por texto; a tela de ativação do discador está alcançável sem ter sido reescrita.</done>
</task>

<task type="auto">
  <name>Task 2: MainActivity hospedando o grafo, com destino inicial decidido antes de compor</name>
  <files>app/src/main/java/org/sentinela/app/ui/MainActivity.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/ui/MainActivity.kt
    - app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt (a chave de onboarding concluído, de 07-04)
    - app/src/main/java/org/sentinela/app/ui/navigation/SentinelaNavHost.kt
    - .planning/phases/07-ui-onboarding-e-home/07-RESEARCH.md (Pitfall 2 e §Q8)
    - scripts/verify-invariants.sh (Bloco 2 — a checagem de rebranding)
  </read_first>
  <action>
    Reescrever `ui/MainActivity.kt`: a composta de espera atual sai, e o grafo entra. A guarda de
    estado salvo em torno da contagem de abertura PERMANECE — início de processo pelo sistema de
    telecomunicações não é abertura, e rotação não é abertura nova.

    O destino inicial vem da chave de onboarding concluído. **Resolvê-lo ANTES de compor o grafo**,
    com um estado explícito de carregamento na própria Activity: trocar o destino inicial depois de o
    grafo já composto NÃO re-navega, e a primeira leitura do repositório custa disco. Enquanto carrega,
    mostrar `state_loading`. PROIBIDO decidir isso com bloqueio da thread principal: é a forma mais
    provável de estragar a partida a frio medida, e a solução correta do defeito é também a solução de
    desempenho.

    Registrar em KDoc, em PROSA PORTUGUESA e usando importações em vez de nome totalmente qualificado,
    que o identificador do aplicativo e o nome visível nunca aparecem como literal em Kotlin —
    rebranding não pode exigir tocar em código, e a checagem de rebranding do script já derrubou três
    executores que escreveram o literal em comentário ou em nome qualificado.

    Nada é construído na thread principal além do que já era: o container continua com todo
    colaborador preguiçoso, e `Application.onCreate` continua sem I/O síncrono.
  </action>
  <verify>
    <automated>./gradlew assembleDebug lint detekt --rerun-tasks && bash scripts/verify-invariants.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'PlaceholderScreen' app/src/main/java/org/sentinela/app/ui/MainActivity.kt` devolve 0
    - `grep -c 'savedInstanceState == null' app/src/main/java/org/sentinela/app/ui/MainActivity.kt` é 1
    - `grep -c 'runBlocking' app/src/main/java/org/sentinela/app/ui/MainActivity.kt` devolve 0
    - `grep -c 'state_loading' app/src/main/java/org/sentinela/app/ui/MainActivity.kt` é maior que 0
    - `bash scripts/verify-invariants.sh` verde, incluindo o Bloco 2
    - `./gradlew assembleDebug lint detekt --rerun-tasks` verde
  </acceptance_criteria>
  <done>O aplicativo abre no grafo, o destino inicial é decidido sem bloquear a thread principal e a contagem de abertura mantém a guarda.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bloco 9 de invariantes e o teste de fluxo de ponta a ponta</name>
  <files>scripts/verify-invariants.sh, app/src/test/java/org/sentinela/app/ui/onboarding/OnboardingFlowTest.kt, app/src/test/java/org/sentinela/app/ui/settings/DialerRouteTest.kt</files>
  <read_first>
    - scripts/verify-invariants.sh (inteiro; os Blocos 6, 7 e 8 são os moldes de escopo e de aritmética de grep)
    - app/src/main/java/org/sentinela/app/ui/navigation/SentinelaNavHost.kt
    - app/src/test/java/org/sentinela/app/ui/navigation/NavGraphContractTest.kt
    - .planning/phases/07-ui-onboarding-e-home/07-RESEARCH.md (Pitfall 9 e Pitfall 10)
  </read_first>
  <behavior>
    - Boas-vindas leva ao passo 1; os seis passos avançam em ordem até a home
    - Pular em qualquer passo de 1 a 5 leva à home, e a pilha fica com um único elemento
    - O gesto de voltar na home não devolve o usuário ao onboarding
    - Papel negado no passo 1 permite avançar até a home sem travar
    - Da home, o ícone de ajustes leva à Proteção, e voltar retorna à home
    - De Proteção, o item do modo discador leva à tela de ativação nos cinco ramos de estado
    - O caminho inteiro é percorrível tocando SOMENTE em nós que têm descrição ou texto de recurso
  </behavior>
  <action>
    **Bloco 9 de `scripts/verify-invariants.sh`**, no molde dos Blocos 6, 7 e 8. Três checagens, cada
    uma restrita a `app/src/main/java/org/sentinela/app/ui/onboarding`, `.../ui/home` e
    `.../ui/settings`:

    9.1 — nenhum literal de texto de interface em Kotlin nessas três pastas. Casar aspas contendo duas
    ou mais letras seguidas de espaço, EXCLUINDO linhas de comentário e de KDoc por padrão de linha, e
    excluindo os arquivos de pré-visualização se estiverem separados. Este é o ponto onde a
    auto-sabotagem já pegou seis executores: a proibição é descrita em prosa portuguesa e o padrão de
    grep não pode casar o próprio comentário que a descreve.

    9.2 — a fronteira do número: nenhum arquivo dessas três pastas menciona o campo de número em
    formato internacional do registro de bloqueio. A máscara única é o único caminho de exibição fora
    das pastas de chamada e de discagem, e é o dono de estado que aplica a máscara.

    9.3 — nenhuma dessas três pastas menciona o repositório de consulta de contatos. Ele registra
    observador da agenda e dispara construção de cache medida em mais de dois segundos e meio, e
    nenhuma tela desta fase precisa dele.

    Aritmética obrigatória, armadilha na terceira encarnação neste repositório: `grep -c` sai com
    código 1 quando conta zero. Usar `[ "$(grep -c ... )" -eq 0 ]`, NUNCA `|| echo 0`, e o modo de
    abortar do shell continua proibido no script.

    **`app/src/test/java/org/sentinela/app/ui/onboarding/OnboardingFlowTest.kt`**,
    `@RunWith(RobolectricTestRunner::class)`,
    `@Config(sdk = [35], qualifiers = "w411dp-h891dp-xxhdpi")`, `createComposeRule`. Compõe o grafo
    REAL com donos de estado construídos de dublês — nunca com container. Capturar o controlador num
    `lateinit var`. `compose.waitForIdle()` depois de cada toque que navega. Asserts de pilha sempre
    com filtragem de nulo, porque a entrada do próprio grafo tem rota nula.

    Cobrir os sete comportamentos, no mínimo 10 casos. O caso do caminho percorrível só por nós com
    descrição é o que automatiza a parte com dentes do critério de leitor de tela: percorrer o fluxo
    localizando cada controle por texto ou descrição vindos do recurso, nunca por posição na tela.

    **`app/src/test/java/org/sentinela/app/ui/settings/DialerRouteTest.kt`**: mesma configuração, um
    caso por ramo de `DialerModeState`, afirmando que em indisponível a linha é anunciada desabilitada
    e não navega, e que nos outros quatro ela navega para o destino de ativação.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*OnboardingFlowTest" --tests "*DialerRouteTest" --rerun-tasks && bash scripts/verify-invariants.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify-invariants.sh` verde, e a saída contém o cabeçalho do Bloco 9 com três checagens
    - `grep -c 'set -e' scripts/verify-invariants.sh` devolve 0
    - `grep -c '|| echo 0' scripts/verify-invariants.sh` devolve 0
    - `./gradlew testDebugUnitTest --tests "*OnboardingFlowTest" --tests "*DialerRouteTest" --rerun-tasks` verde com no mínimo 15 `@Test` somados
    - PROVA DE VERMELHO 1 (9.1): escrever um literal de texto de interface numa tela JÁ COMMITADA desta fase ⇒ Bloco 9 vermelho; restaurar por edição manual, nunca por descarte de arquivo
    - PROVA DE VERMELHO 2 (9.2): fazer uma tela desta fase referenciar o campo de número em formato internacional ⇒ Bloco 9 vermelho; restaurar
    - PROVA DE VERMELHO 3 (9.3): referenciar o repositório de consulta de contatos numa rota desta fase ⇒ Bloco 9 vermelho; restaurar
    - PROVA DE VERMELHO 4 (pilha): remover o descarte inclusivo do caminho de pular ⇒ o caso de pilha do fluxo fica vermelho; restaurar
    - PROVA DE VERMELHO 5 (auto-sabotagem verificada): confirmar que o Bloco 9 continua VERDE com todos os comentários e KDoc desta fase no lugar, e registrar isso no SUMMARY — é a prova de que o critério não casa a própria prosa
    - As cinco provas transcritas no SUMMARY com a saída real
  </acceptance_criteria>
  <done>O Bloco 9 vigia texto embutido, fronteira do número e o repositório proibido; o fluxo inteiro é provado em JVM, incluindo pular, voltar e a rota do modo discador.</done>
</task>

</tasks>

<verification>
```bash
./gradlew clean
./gradlew --no-build-cache assembleDebug testDebugUnitTest lint detekt
bash scripts/verify-invariants.sh
./gradlew koverLog
```
</verification>

<success_criteria>
- O aplicativo abre no grafo real; o destino inicial não bloqueia a thread principal
- Fluxo de ponta a ponta verde em JVM, sem emulador
- Bloco 9 com três checagens e quatro provas de vermelho, mais a prova de não auto-sabotagem
- Pendência de 06-05 fechada: a tela de ativação do modo discador tem ponto de entrada
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-ui-onboarding-e-home/07-10-SUMMARY.md`
</output>
