---
phase: 07-ui-onboarding-e-home
plan: 09
type: execute
wave: 3
depends_on: ["07-03", "07-04"]
files_modified:
  - app/src/main/java/org/sentinela/app/ui/settings/SettingsUiState.kt
  - app/src/main/java/org/sentinela/app/ui/settings/SettingsGroup.kt
  - app/src/main/java/org/sentinela/app/ui/settings/SettingsScreen.kt
  - app/src/test/java/org/sentinela/app/ui/settings/ProtectionScreenTest.kt
autonomous: true
requirements: [UIX-03, UIX-09, UIX-10, UIX-11]

must_haves:
  truths:
    - "Cada uma das 16 configurações da tela Proteção é alterável e tem explicação curta e permanente sob ela"
    - "Nenhuma troca de política pede confirmação; só o que perde dado confirma"
    - "A tela repete as três honestidades provadas: registro no telefone sempre acontece, Não Perturbe não é contornável, mensageiro não é filtrado"
    - "O item do modo discador abre a tela de ativação existente sem reescrevê-la, e fica desabilitado com motivo quando o aparelho não oferece o papel"
  artifacts:
    - path: "app/src/main/java/org/sentinela/app/ui/settings/SettingsScreen.kt"
      provides: "os 16 itens da tela Proteção agrupados, com efeito imediato e sem botão salvar"
      min_lines: 300
    - path: "app/src/test/java/org/sentinela/app/ui/settings/ProtectionScreenTest.kt"
      provides: "um caso por item, mais os dois diálogos de confirmação e os ramos do modo discador"
      min_lines: 200
  key_links:
    - from: "app/src/main/java/org/sentinela/app/ui/settings/SettingsScreen.kt"
      to: "app/src/main/java/org/sentinela/app/ui/components/SettingSwitchRow.kt"
      via: "cada interruptor com explicação permanente irmã, nunca mesclada"
      pattern: "SettingSwitchRow"
    - from: "app/src/main/java/org/sentinela/app/ui/settings/SettingsScreen.kt"
      to: "app/src/main/java/org/sentinela/app/ui/components/HonestyCard.kt"
      via: "cartão de limitações com as quatro frases da ativação do discador"
      pattern: "dialer_activation_unchanged_"
---

<objective>
A tela Proteção: 16 itens em grupos, efeito imediato sem botão salvar, explicação permanente sob cada
opção, confirmação apenas no que perde dado, e o cartão de limitações com fonte única de verdade.

Purpose: é o critério de sucesso 3 da fase — cada configuração, incluindo o modo discador, com
explicação clara e efeito imediato na triagem.
Output: `SettingsUiState`, `SettingsGroup`, `SettingsScreen` e `ProtectionScreenTest`.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md
@.planning/phases/07-ui-onboarding-e-home/07-CONTEXT.md
@.planning/phases/07-ui-onboarding-e-home/07-03-SUMMARY.md
@.planning/phases/07-ui-onboarding-e-home/07-04-SUMMARY.md
@docs/design/TELAS.md
@CLAUDE.md

<interfaces>
```kotlin
data class ScreeningSettings(protectionEnabled, unknownPolicy, contactsPolicy, whitelistPolicy,
    blockPrivateNumbers, blockMode, hideFromNativeCallLog, showOwnNotification, fallbackPolicy,
    historyEnabled, retentionPolicy, repeatedCallBypassEnabled, notificationIdentification)
enum class OriginPolicy { RING, BLOCK, SILENCE, NEVER_SILENCE }
enum class BlockMode { REJECT, SILENT_VOICEMAIL }
enum class FallbackPolicy { ALLOW, BLOCK }
enum class NotificationIdentification { MASKED, ANONYMOUS }
enum class RetentionPolicy { NEVER_STORE, DAYS_7, DAYS_30, DAYS_90, MANUAL }   // conferir os nomes reais no arquivo
enum class DialerModeState { UNAVAILABLE, BLOCKED_BY_CONTACTS, OFFERED, ACTIVE, ROLE_LOST }
```
Componentes de 07-03: `OptionCard`, `SettingSwitchRow`, `SentinelaTopBar`. Da Fase 6: `HonestyCard`,
`InfoBanner`. Da Fase 6, a ligar em 07-10 e NÃO reescrever:
```kotlin
fun DialerActivationScreen(state: DialerModeState, onRequestRole: () -> Unit, onRevert: () -> Unit,
                           onGrantContacts: () -> Unit, modifier: Modifier = Modifier,
                           onBack: (() -> Unit)? = null)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SettingsUiState e SettingsGroup</name>
  <files>app/src/main/java/org/sentinela/app/ui/settings/SettingsUiState.kt, app/src/main/java/org/sentinela/app/ui/settings/SettingsGroup.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/settings/ScreeningSettings.kt
    - app/src/main/java/org/sentinela/app/settings/RetentionPolicy.kt (conferir o nome real do arquivo e das constantes)
    - app/src/main/java/org/sentinela/app/ui/settings/SettingsViewModel.kt (de 07-04)
    - .planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md (§9)
  </read_first>
  <action>
    **`ui/settings/SettingsUiState.kt`.** `data class SettingsUiState` com `settings: ScreeningSettings`,
    `screeningRoleHeld: Boolean`, `screeningRoleAvailable: Boolean`, `dialerMode: DialerModeState`,
    `historyRecordCount: Long`, `loading: Boolean`. O número de registros existe porque o diálogo de
    limpeza tem de dizer QUANTOS serão apagados.

    **`ui/settings/SettingsGroup.kt`.** Componente de agrupamento:
    `SettingsGroup(title: String, modifier: Modifier = Modifier, tinted: Boolean = false,
    content: @Composable ColumnScope.() -> Unit)`. Cartão `SurfaceContainer`, `ShapeMedium`, padding
    16dp, cabeçalho `labelLarge` `Primary` com `semantics { heading() }`, itens separados por 16dp.
    Quando `tinted`, o cartão recebe `StatusAttention` a 15% de alfa — é o tratamento do grupo de
    proteção quando ela está desligada.

    O cabeçalho como cabeçalho semântico não é enfeite: o leitor de tela navega por cabeçalho, e sem
    isso uma tela de 16 itens é intransitável.

    No mesmo arquivo, um componente de linha navegável:
    `SettingsNavRow(label: String, description: String, onClick: () -> Unit,
    modifier: Modifier = Modifier, enabled: Boolean = true, unavailableReason: String? = null,
    valueText: String? = null)`. Altura mínima 56dp, seta ao fim. Quando desabilitada, o
    `enabled = false` vai no NÓ DA LINHA e o motivo em descrição de estado — nunca só no filho.

    Pré-visualizações de grupo normal, grupo tingido e linha desabilitada.
  </action>
  <verify>
    <automated>./gradlew assembleDebug lint detekt --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'heading()' app/src/main/java/org/sentinela/app/ui/settings/SettingsGroup.kt` é maior que 0
    - `grep -c 'historyRecordCount' app/src/main/java/org/sentinela/app/ui/settings/SettingsUiState.kt` é 1
    - `grep -c 'stateDescription' app/src/main/java/org/sentinela/app/ui/settings/SettingsGroup.kt` é maior que 0
    - Nenhum texto de interface embutido em Kotlin
    - `./gradlew assembleDebug lint detekt --rerun-tasks` verde
  </acceptance_criteria>
  <done>O estado da tela carrega a contagem de registros e o agrupamento tem cabeçalho semântico e linha navegável com desabilitado no nó certo.</done>
</task>

<task type="auto">
  <name>Task 2: SettingsScreen — os 16 itens, efeito imediato e as duas confirmações</name>
  <files>app/src/main/java/org/sentinela/app/ui/settings/SettingsScreen.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/ui/settings/SettingsUiState.kt
    - app/src/main/java/org/sentinela/app/ui/settings/SettingsGroup.kt
    - app/src/main/java/org/sentinela/app/ui/components/SettingSwitchRow.kt
    - app/src/main/java/org/sentinela/app/ui/components/OptionCard.kt
    - app/src/main/java/org/sentinela/app/ui/components/HonestyCard.kt
    - docs/design/TELAS.md (§10)
    - .planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md (§9, §9.1, §9.2, §9.3 — a tabela de 16 itens é o contrato)
  </read_first>
  <action>
    Criar `ui/settings/SettingsScreen.kt`, composta PURA. Assinatura com um retorno de chamada por
    item, mais os dois de navegação e os dois de ação destrutiva:
    `SettingsScreen(state: SettingsUiState, onBack: () -> Unit, onProtectionChange: (Boolean) -> Unit,
    onFixRole: () -> Unit, onUnknownPolicy: (OriginPolicy) -> Unit, onContactsPolicy: (OriginPolicy) -> Unit,
    onWhitelistPolicy: (OriginPolicy) -> Unit, onBlockPrivateChange: (Boolean) -> Unit,
    onBlockMode: (BlockMode) -> Unit, onHideNativeLogChange: (Boolean) -> Unit,
    onNotificationChange: (Boolean) -> Unit, onNotificationIdentification: (NotificationIdentification) -> Unit,
    onOpenDialerActivation: () -> Unit, onRepeatedCallChange: (Boolean) -> Unit,
    onHistoryEnabledChange: (Boolean) -> Unit, onRetention: (RetentionPolicy) -> Unit,
    onClearHistory: () -> Unit, onFallback: (FallbackPolicy) -> Unit, onOpenAbout: () -> Unit,
    modifier: Modifier = Modifier)`.

    Barra superior de 64dp com voltar e `settings_title`; conteúdo rolável, margem lateral 16dp,
    24dp entre grupos.

    **Implementar os 16 itens exatamente como a tabela da §9 os define**, cada um com a explicação
    permanente ditada ali, na chave ditada ali. Em resumo, e sem substituir a leitura da tabela:
    1 interruptor de proteção com `settings_protection_toggle_desc`, e o grupo fica tingido quando
    desligada; 1b linha informativa do estado do papel com `dashboard_role_missing` e ação
    `dashboard_fix_configuration` — a mesma verdade da home, no lugar onde se conserta, e a ação só
    aparece quando o aparelho oferece o papel; 2 três cartões de opção para desconhecidos com as três
    descrições; 3 quatro cartões para contatos com as quatro descrições mais a nota
    `settings_contacts_policy_note`; 4 quatro cartões para whitelist; 5 interruptor de privados com
    `settings_block_private_desc`; 6 dois cartões de modo de bloqueio com `settings_block_mode_desc`,
    que descreve o efeito PARA QUEM LIGA; 7 interruptor de registro no histórico do telefone com
    `settings_hide_native_log_desc` LITERAL e sem cortar — é o texto que impede a promessa falsa, e
    encurtar aqui é regressão; 8 interruptor de notificação própria com as duas sub-opções visíveis
    só com ele ligado; 9 linha navegável do modo discador com `settings_dialer_mode_desc`; 10
    interruptor de chamada repetida; 11 interruptor de histórico ligado; 12 cinco cartões de retenção;
    13 botão de texto `Error` de limpar histórico; 14 dois cartões de política de erro com
    `settings_fallback_desc`; 15 cartão de limitações; 16 linha navegável de privacidade e sobre.

    **Item 9, o modo discador.** A linha navega para a tela de ativação que JÁ EXISTE — não
    reimplementar nada dela, e a assinatura dela não muda. Ramos: em indisponível a linha fica
    desabilitada com `dialer_activation_unavailable` como motivo; em bloqueado pela agenda a linha
    fica HABILITADA, porque a própria tela de destino explica o pré-requisito; em ativo, oferecido e
    papel perdido a linha navega normalmente e mostra o estado como valor.

    **Item 15, o cartão de limitações.** `HonestyCard` com os quatro itens
    `dialer_activation_unchanged_1` a `_4`, as MESMAS frases do passo 1 do onboarding e da tela de
    ativação do discador. Fonte única de verdade, texto único: registrar em KDoc que reescrever
    qualquer uma delas aqui criaria uma segunda versão da verdade, e que a Fase 5 existiu em parte
    para corrigir exatamente isso.

    **Item 16.** O destino é da Phase 9. Nesta fase o item existe e navega para `onOpenAbout`, que o
    envelope de navegação de 07-10 aponta para um destino de espera com estado comunicado — nunca para
    tela em branco.

    **Efeito imediato, §9.1.** Cada controle chama seu retorno de chamada na hora. NÃO existe botão
    salvar, NÃO existe aviso de "salvo" a cada toque, e nenhuma troca de política pede confirmação.
    Registrar em KDoc que confirmação excessiva ensina o usuário a tocar em sim sem ler.

    **Confirmações, §9.2 — só o que perde dado.** Dois diálogos:
    - limpar histórico: título `history_clear_all`, corpo `settings_clear_history_confirm` formatado
      com `state.historyRecordCount`, confirmar `action_confirm` em `Error`, cancelar `action_cancel`,
      e o FOCO INICIAL no cancelar
    - escolher não guardar na retenção: corpo `settings_retention_never_confirm`, porque a escolha
      muda o comportamento E poda o que existe
    Desligar o histórico NÃO tem diálogo — não apaga nada; um aviso temporário informa
    `settings_history_off_kept`. Desligar a proteção não tem diálogo. Ativar ou reverter o modo
    discador não tem diálogo próprio: o seletor do sistema é a confirmação, contrato da Fase 6.

    **Semântica, §9.3.** Cada linha de interruptor tem três nós e o interruptor NUNCA entra em
    container com semântica de mesclagem. Cada grupo de cartões tem agrupamento selecionável e o
    rótulo do grupo é cabeçalho. Ordem de travessia igual à ordem visual, nenhuma decoração focável.

    Pré-visualizações: proteção ligada, proteção desligada, modo discador indisponível, diálogo de
    limpeza aberto.
  </action>
  <verify>
    <automated>./gradlew assembleDebug lint detekt --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'settings_hide_native_log_desc' app/src/main/java/org/sentinela/app/ui/settings/SettingsScreen.kt` é 1, e `git diff app/src/main/res/values/strings.xml` não altera o valor dessa chave
    - `for i in 1 2 3 4; do grep -c "dialer_activation_unchanged_$i" .../SettingsScreen.kt; done` devolve 1 em cada
    - `grep -ciE 'salvar|salvo|Snackbar de salvamento' app/src/main/java/org/sentinela/app/ui/settings/SettingsScreen.kt` devolve 0
    - `grep -c 'AlertDialog' app/src/main/java/org/sentinela/app/ui/settings/SettingsScreen.kt` devolve exatamente 2 usos — limpar histórico e retenção nunca guardar
    - `grep -c 'DialerActivationScreen' app/src/main/java/org/sentinela/app/ui/settings/SettingsScreen.kt` devolve 0 — a tela de ativação é destino de navegação, não filha desta tela
    - `grep -cE 'AppContainer|ViewModel' app/src/main/java/org/sentinela/app/ui/settings/SettingsScreen.kt` devolve 0
    - Nenhum texto de interface embutido em Kotlin
    - `./gradlew assembleDebug lint detekt --rerun-tasks` verde
  </acceptance_criteria>
  <done>Os 16 itens existem com explicação permanente, efeito imediato, exatamente dois diálogos de confirmação e o cartão de limitações com as quatro frases originais.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ProtectionScreenTest</name>
  <files>app/src/test/java/org/sentinela/app/ui/settings/ProtectionScreenTest.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/ui/settings/SettingsScreen.kt
    - app/src/test/java/org/sentinela/app/ui/TouchTargetAsserts.kt
    - app/src/test/java/org/sentinela/app/ui/settings/SettingsViewModelTest.kt (de 07-04, para não duplicar o efeito imediato)
  </read_first>
  <behavior>
    - Cada um dos 16 itens aparece na tela, com sua explicação permanente visível
    - Tocar num cartão de política emite exatamente uma vez a política escolhida, sem nenhum diálogo
    - Limpar histórico abre diálogo cujo corpo cita o número de registros, e o cancelar tem o foco inicial
    - Escolher não guardar na retenção abre diálogo antes de emitir a escolha
    - Desligar o histórico não abre diálogo algum
    - Desligar a proteção não abre diálogo, e tinge o grupo de proteção
    - Modo discador indisponível deixa a linha anunciada como desabilitada com o motivo em texto
    - Modo discador bloqueado pela agenda deixa a linha habilitada
    - Papel ausente exibe a linha informativa com ação de correção; papel indisponível no aparelho não exibe a ação
    - As sub-opções de notificação só aparecem com o interruptor ligado
    - Cada grupo tem cabeçalho semântico, e cada interruptor anuncia papel e estado
    - Cada controle passa os dois eixos de toque
  </behavior>
  <action>
    Criar `app/src/test/java/org/sentinela/app/ui/settings/ProtectionScreenTest.kt`,
    `@RunWith(RobolectricTestRunner::class)`,
    `@Config(sdk = [35], qualifiers = "w411dp-h891dp-xxhdpi")`, `createComposeRule`. No mínimo 22
    casos. Todo texto lido do recurso em tempo de teste.

    Um caso de COMPLETUDE que vale por si: percorrer a lista das chaves de rótulo dos 16 itens,
    lidas do recurso, e afirmar que cada uma existe como nó na tela — depois de rolar até ela. É o
    caso que pega item esquecido, e sem ele os outros 21 não provam cobertura da tabela.

    Um caso de ausência de confirmação: trocar cada uma das quatro políticas por origem e afirmar que
    nenhum diálogo apareceu e que o retorno de chamada foi emitido uma única vez.

    Os dois eixos de toque afirmados em pelo menos um interruptor, um cartão de opção, uma linha
    navegável e o botão destrutivo.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*ProtectionScreenTest" --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew testDebugUnitTest --tests "*ProtectionScreenTest" --rerun-tasks` verde com no mínimo 22 `@Test`
    - `grep -c 'assertLayoutHeightIsAtLeast' app/src/test/java/org/sentinela/app/ui/settings/ProtectionScreenTest.kt` é no mínimo 4, cada um com `assertTouchHeightIsAtLeast` no mesmo caso
    - PROVA DE VERMELHO 1 (item esquecido): remover o item de chamada repetida da tela ⇒ o caso de completude fica vermelho; restaurar
    - PROVA DE VERMELHO 2 (confirmação excessiva): acrescentar diálogo à troca da política de desconhecidos ⇒ o caso de ausência de confirmação fica vermelho; restaurar
    - PROVA DE VERMELHO 3 (perda de dado sem confirmar): remover o diálogo de limpar histórico ⇒ vermelho; restaurar
    - PROVA DE VERMELHO 4 (desabilitado no filho): mover o desabilitado da linha do modo discador para o filho ⇒ o caso de anúncio de desabilitado fica vermelho; restaurar
    - As quatro provas transcritas no SUMMARY
  </acceptance_criteria>
  <done>Os 16 itens estão travados por um caso de completude, a fronteira entre confirmar e não confirmar está travada nos dois sentidos, e o desabilitado está no nó certo.</done>
</task>

</tasks>

<verification>
```bash
./gradlew --rerun-tasks assembleDebug testDebugUnitTest lint detekt
bash scripts/verify-invariants.sh
./gradlew koverLog
```
</verification>

<success_criteria>
- 16 itens com explicação permanente e efeito imediato, sem botão salvar
- Exatamente duas confirmações, ambas por perda de dado
- Cartão de limitações com as quatro frases originais, sem reescrita
- Vinte e dois casos verdes e quatro provas de vermelho transcritas
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-ui-onboarding-e-home/07-09-SUMMARY.md`
</output>
