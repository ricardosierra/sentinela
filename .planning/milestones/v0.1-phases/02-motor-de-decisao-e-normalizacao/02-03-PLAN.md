---
phase: 02-motor-de-decisao-e-normalizacao
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/src/main/java/org/sentinela/app/phone/RegionProvider.kt
  - app/src/main/java/org/sentinela/app/phone/CascadingRegionProvider.kt
  - app/src/main/java/org/sentinela/app/platform/AndroidRegionProvider.kt
  - app/src/test/java/org/sentinela/app/phone/CascadingRegionProviderTest.kt
autonomous: true
requirements: [NRM-01, NRM-02]

must_haves:
  truths:
    - "O app resolve a região do usuário sem travar em BR e sem pedir permissão nova"
    - "Região vinda do aparelho em minúsculo ou vazia não quebra a normalização"
    - "O domínio e o pacote phone/ continuam sem importar Android"
  artifacts:
    - path: "app/src/main/java/org/sentinela/app/phone/RegionProvider.kt"
      provides: "Interface pura de resolução de região"
      exports: ["RegionProvider"]
    - path: "app/src/main/java/org/sentinela/app/phone/CascadingRegionProvider.kt"
      provides: "Cascata aparelho → preferência do usuário → BR"
      contains: "CascadingRegionProvider"
    - path: "app/src/main/java/org/sentinela/app/platform/AndroidRegionProvider.kt"
      provides: "Único ponto que toca TelephonyManager"
      contains: "simCountryIso"
  key_links:
    - from: "app/src/main/java/org/sentinela/app/platform/AndroidRegionProvider.kt"
      to: "android.telephony.TelephonyManager"
      via: "simCountryIso com fallback networkCountryIso dentro de runCatching"
      pattern: "networkCountryIso"
    - from: "app/src/main/java/org/sentinela/app/phone/CascadingRegionProvider.kt"
      to: "org.sentinela.app.phone.RegionProvider"
      via: "implementa a interface, delegando aos dois degraus"
      pattern: ": RegionProvider"
---

<objective>
Implementar a cascata de resolução de região decidida pelo usuário — SIM/rede do aparelho →
preferência informada pelo usuário → `"BR"` como último recurso — sem violar a pureza de
`phone/` e sem adicionar permissão alguma.

Purpose: o app precisa funcionar no mundo todo; travar em `"BR"` quebraria a normalização de
qualquer usuário fora do Brasil. O risco real da fase está aqui e na normalização.
Output: `RegionProvider` (puro), `CascadingRegionProvider` (puro, testado com fakes),
`AndroidRegionProvider` (único arquivo com Android).
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-motor-de-decisao-e-normalizacao/02-CONTEXT.md
@.planning/phases/02-motor-de-decisao-e-normalizacao/02-RESEARCH.md
@docs/PERMISSOES.md
@app/src/main/java/org/sentinela/app/phone/PhoneNumberNormalizer.kt
@scripts/verify-invariants.sh

<interfaces>
<!-- Contratos criados por ESTE plano; o plano 02-04 (normalizer) e o 02-05 (wiring) consomem -->
```kotlin
package org.sentinela.app.phone

/** ISO-3166-1 alpha-2 MAIÚSCULO, ou null se indisponível. */
fun interface RegionProvider { fun currentRegion(): String? }

class CascadingRegionProvider(
    private val device: RegionProvider,
    private val userPreference: RegionProvider,
    private val fallback: String = DEFAULT_REGION,
) : RegionProvider {
    override fun currentRegion(): String   // nunca null
    companion object { const val DEFAULT_REGION = "BR" }
}
```
```kotlin
package org.sentinela.app.platform
class AndroidRegionProvider(private val telephonyManager: android.telephony.TelephonyManager?) : RegionProvider
```
<!-- Fatos provados na pesquisa (AOSP android-15.0.0_r1): getSimCountryIso() e
     getNetworkCountryIso() têm apenas @RequiresFeature — NENHUM @RequiresPermission.
     READ_PHONE_STATE NÃO é necessária e NÃO deve ser adicionada.
     Ambos devolvem string VAZIA (não null) quando indisponíveis, em MINÚSCULO,
     e lançam UnsupportedOperationException em tablet Wi-Fi-only. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RegionProvider + CascadingRegionProvider (puros) com testes de fake</name>
  <files>app/src/main/java/org/sentinela/app/phone/RegionProvider.kt, app/src/main/java/org/sentinela/app/phone/CascadingRegionProvider.kt, app/src/test/java/org/sentinela/app/phone/CascadingRegionProviderTest.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/phone/PhoneNumberNormalizer.kt (convenção do pacote)
    - .planning/phases/02-motor-de-decisao-e-normalizacao/02-RESEARCH.md §"Pattern 2" e §"Pitfall 4"
    - .planning/phases/02-motor-de-decisao-e-normalizacao/02-CONTEXT.md §"Região padrão"
    - scripts/verify-invariants.sh (Bloco 3 — `phone/` não pode importar android.*)
  </read_first>
  <behavior>
    `CascadingRegionProvider.currentRegion()` (nunca devolve null):
    - device `"br"` → `"BR"` (o Android devolve minúsculo; libphonenumber exige maiúsculo)
    - device `"US"` → `"US"`
    - device `" pt "` → `"PT"` (trim)
    - device `""` → cai para userPreference
    - device `"  "` → cai para userPreference
    - device `null` → cai para userPreference
    - device `"ZZZ"` (3 letras) → inválido, cai para userPreference
    - device `"1A"` (com dígito) → inválido, cai para userPreference
    - device null + userPreference `"pt"` → `"PT"`
    - device null + userPreference null → `"BR"` (DEFAULT_REGION)
    - device null + userPreference `""` → `"BR"`
    - fallback customizado: `CascadingRegionProvider(nullDevice, nullPref, fallback = "US")` → `"US"`
    - `CascadingRegionProvider.DEFAULT_REGION == "BR"`
  </behavior>
  <action>
    Criar `phone/RegionProvider.kt`:
    ```kotlin
    package org.sentinela.app.phone

    /** Resolve a regiao padrao (ISO-3166-1 alpha-2 MAIUSCULO) usada quando o numero vem sem DDI. */
    fun interface RegionProvider {
        /** Regiao em duas letras maiusculas, ou null quando indisponivel. */
        fun currentRegion(): String?
    }
    ```
    Criar `phone/CascadingRegionProvider.kt` exatamente como o §Pattern 2 da pesquisa, com
    `companion object { const val DEFAULT_REGION = "BR" }` e o `fallback` default apontando para
    a constante. A normalização de cada degrau: `trim().uppercase().takeIf { it.length == 2 && it.all(Char::isLetter) }`.
    NADA de `runCatching` aqui — este arquivo é puro e determinístico; exceção de plataforma é
    responsabilidade do `AndroidRegionProvider` (Task 2). NENHUM `import android.` neste pacote.
    KDoc obrigatório explicando a ordem da cascata e que o degrau 2 (preferência do usuário) só
    ganha persistência na Fase 3 — aqui existe apenas o contrato e o fallback em memória.
    Criar `CascadingRegionProviderTest.kt` com fakes escritos à mão (`RegionProvider { "br" }`,
    lambdas de SAM), sem MockK, um `@Test` por linha do bloco `<behavior>`.
  </action>
  <verify>
    <automated>export ANDROID_HOME="${ANDROID_HOME:-$HOME/Library/Android/sdk}"; ./gradlew testDebugUnitTest --tests "*CascadingRegionProviderTest" detekt</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/src/main/java/org/sentinela/app/phone/RegionProvider.kt` e `.../CascadingRegionProvider.kt`
    - `grep -rq '^import android' app/src/main/java/org/sentinela/app/phone/` retorna 1 (ausente)
    - `grep -q 'DEFAULT_REGION = "BR"' app/src/main/java/org/sentinela/app/phone/CascadingRegionProvider.kt`
    - `grep -q 'runCatching' app/src/main/java/org/sentinela/app/phone/CascadingRegionProvider.kt` retorna 1 (ausente)
    - `[ "$(grep -c '@Test' app/src/test/java/org/sentinela/app/phone/CascadingRegionProviderTest.kt)" -ge 12 ]`
    - `TEST-org.sentinela.app.phone.CascadingRegionProviderTest.xml` com `failures="0"` `errors="0"`
    - `./gradlew detekt` termina `BUILD SUCCESSFUL`
  </acceptance_criteria>
  <done>A cascata devolve sempre uma região de 2 letras maiúsculas válida, provada em 12+ casos incluindo `""`, `"  "`, `"br"` e `null`.</done>
</task>

<task type="auto">
  <name>Task 2: AndroidRegionProvider — único ponto de contato com TelephonyManager</name>
  <files>app/src/main/java/org/sentinela/app/platform/AndroidRegionProvider.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/phone/RegionProvider.kt (criado na Task 1)
    - docs/PERMISSOES.md (lista fechada — nenhuma permissão nova nesta fase)
    - .planning/phases/02-motor-de-decisao-e-normalizacao/02-RESEARCH.md §"Pattern 2", §"Pitfall 5" e §"State of the Art" (depreciação de `getSimCountryIso(int)`)
    - app/src/main/AndroidManifest.xml
  </read_first>
  <action>
    Criar `app/src/main/java/org/sentinela/app/platform/AndroidRegionProvider.kt`:
    ```kotlin
    package org.sentinela.app.platform

    import android.telephony.TelephonyManager
    import org.sentinela.app.phone.RegionProvider

    /**
     * Le a regiao do aparelho (SIM, depois rede). Unico arquivo do app que toca TelephonyManager.
     *
     * Nao exige permissao: no AOSP (android-15.0.0_r1) getSimCountryIso() e getNetworkCountryIso()
     * carregam apenas @RequiresFeature — nenhum @RequiresPermission. READ_PHONE_STATE continua
     * proibida por docs/PERMISSOES.md.
     *
     * runCatching e obrigatorio: em aparelho sem FEATURE_TELEPHONY_* os metodos lancam
     * UnsupportedOperationException (tablet Wi-Fi-only). Ambos tambem devolvem string VAZIA,
     * nao null, quando indisponiveis — a validacao de formato fica no CascadingRegionProvider.
     */
    class AndroidRegionProvider(
        private val telephonyManager: TelephonyManager?,
    ) : RegionProvider {
        override fun currentRegion(): String? = runCatching {
            telephonyManager?.simCountryIso?.takeIf { it.isNotBlank() }
                ?: telephonyManager?.networkCountryIso?.takeIf { it.isNotBlank() }
        }.getOrNull()
    }
    ```
    Usar SEMPRE as sobrecargas sem argumento — `getSimCountryIso(int subId)` está depreciado
    desde a API 30 e exige permissão em algumas variantes.
    NÃO adicionar permissão alguma ao manifest. NÃO instanciar aqui: o wiring é do plano 02-05.
    Este arquivo não recebe teste unitário JVM (dependeria de Robolectric, proibido) — a garantia
    é a pureza da cascata (testada) somada ao invariante do `verify-invariants.sh`; a validação
    em aparelho é diferida para a Phase 9.
  </action>
  <verify>
    <automated>export ANDROID_HOME="${ANDROID_HOME:-$HOME/Library/Android/sdk}"; ./gradlew assembleDebug testDebugUnitTest lint detekt && bash scripts/verify-invariants.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/src/main/java/org/sentinela/app/platform/AndroidRegionProvider.kt`
    - `grep -q 'simCountryIso' app/src/main/java/org/sentinela/app/platform/AndroidRegionProvider.kt` e `grep -q 'networkCountryIso' ...`
    - `grep -q 'runCatching' app/src/main/java/org/sentinela/app/platform/AndroidRegionProvider.kt`
    - `grep -q 'getSimCountryIso(' app/src/main/java/org/sentinela/app/platform/AndroidRegionProvider.kt` retorna 1 (sobrecarga com subId ausente)
    - `grep -q 'READ_PHONE_STATE' app/src/main/AndroidManifest.xml` retorna 1 (ausente)
    - `bash scripts/verify-invariants.sh` sai 0 e imprime `== todos os invariantes OK ==` (inclui allowlist de permissões e pureza de `phone/`)
    - `./gradlew lint detekt` termina `BUILD SUCCESSFUL`
  </acceptance_criteria>
  <done>Leitura da região do aparelho existe, isolada em `platform/`, sem permissão nova e sem crash em aparelho sem telefonia.</done>
</task>

</tasks>

<verification>
```bash
export ANDROID_HOME="${ANDROID_HOME:-$HOME/Library/Android/sdk}"
./gradlew assembleDebug testDebugUnitTest lint detekt && bash scripts/verify-invariants.sh
```
</verification>

<success_criteria>
- Cascata SIM → preferência do usuário → `"BR"` implementada e testada com fakes
- `phone/` continua 100% livre de `import android.`
- Nenhuma permissão adicionada ao manifest (allowlist do `verify-invariants.sh` intacta)
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/02-motor-de-decisao-e-normalizacao/02-03-SUMMARY.md`
</output>
