---
phase: 01-fundacao-compilavel
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - .planning/phases/01-fundacao-compilavel/01-CONTEXT.md
  - .planning/phases/01-fundacao-compilavel/01-EVIDENCE.md
  - docs/TESTE-FISICO-SAMSUNG.md
  - docs/backlog/manutencao-toolchain.md
  - docs/INDEX.md
  - CHANGELOG.md
autonomous: true
requirements: [PRV-01, QLT-02, UIX-08, UIX-12]

must_haves:
  truths:
    - "Existe evidencia arquivada de um build POS-clean mostrando 'N actionable tasks: M executed' — nao um log UP-TO-DATE que nao prova nada"
    - "O conflito POST_NOTIFICATIONS entre CONTEXT.md e docs/PERMISSOES.md esta resolvido por escrito a favor da fonte canonica, entao o verifier nao marca falso gap"
    - "Todo item que exige aparelho fisico esta registrado como pendencia da Phase 9, nao pedido ao usuario agora"
    - "Itens de manutencao adiados (gradlew.bat, AGP 9.3.1, reavaliar os disable de lint) estao em docs/backlog, nao esquecidos"
  artifacts:
    - path: ".planning/phases/01-fundacao-compilavel/01-EVIDENCE.md"
      provides: "Log e metricas do build limpo + saida do verify-invariants.sh como prova dos 5 criterios"
      contains: "actionable tasks"
    - path: "docs/TESTE-FISICO-SAMSUNG.md"
      provides: "Pendencias fisicas da Phase 1 registradas para execucao na Phase 9"
      contains: "Phase 1"
    - path: "docs/backlog/manutencao-toolchain.md"
      provides: "Itens de manutencao de toolchain adiados com justificativa"
  key_links:
    - from: ".planning/phases/01-fundacao-compilavel/01-EVIDENCE.md"
      to: "scripts/verify-invariants.sh"
      via: "saida do script colada como evidencia dos criterios 3/4/5"
      pattern: "verify-invariants"
    - from: ".planning/phases/01-fundacao-compilavel/01-CONTEXT.md"
      to: "docs/PERMISSOES.md"
      via: "reconciliacao explicita da autorizacao de POST_NOTIFICATIONS"
      pattern: "POST_NOTIFICATIONS"
    - from: "docs/backlog/manutencao-toolchain.md"
      to: "docs/INDEX.md"
      via: "entrada no indice (exigencia do CLAUDE.md para todo .md novo)"
      pattern: "manutencao-toolchain"
---

<objective>
Fechar a Phase 1 com evidencia auditavel: rodar a suite completa APOS `clean`, arquivar a prova,
reconciliar por escrito o conflito documental de `POST_NOTIFICATIONS` e registrar como pendencia
tudo que depende de aparelho fisico ou de decisao futura.

Purpose: sem esses dois passos a fase fecha com duas armadilhas conhecidas — (a) um log
`BUILD SUCCESSFUL` UP-TO-DATE que nao compilou, nao testou e nao analisou nada; (b) um verifier
marcando `POST_NOTIFICATIONS` como violacao de permissao por causa de uma frase imprecisa do
CONTEXT.md que contradiz a fonte canonica `docs/PERMISSOES.md`.
Output: `01-EVIDENCE.md`, CONTEXT.md corrigido, pendencias fisicas e de manutencao registradas.

NAO faz parte deste plano: qualquer alteracao de codigo Kotlin, Gradle, manifest ou recursos.
Este plano so escreve documentacao e coleta evidencia.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fundacao-compilavel/01-CONTEXT.md
@.planning/phases/01-fundacao-compilavel/01-RESEARCH.md
@.planning/phases/01-fundacao-compilavel/01-01-SUMMARY.md
@.planning/phases/01-fundacao-compilavel/01-02-SUMMARY.md
@CLAUDE.md
@docs/PERMISSOES.md
@docs/TESTE-FISICO-SAMSUNG.md
@docs/INDEX.md
@CHANGELOG.md

<interfaces>
<!-- Fatos verificados em 2026-07-29. -->

**Conflito documental a resolver (RESEARCH Open Question 1):**
- `01-CONTEXT.md` linha ~34 diz que nesta fase o manifest pode conter `BIND_SCREENING_SERVICE`
  e `POST_NOTIFICATIONS` — mas o `01-RESEARCH.md` cita uma versao anterior do texto que dizia
  "so pode conter `BIND_SCREENING_SERVICE`".
- `docs/PERMISSOES.md` linha 13 (FONTE CANONICA segundo o `CLAUDE.md`) registra:
  `android.permission.POST_NOTIFICATIONS | Runtime (API 33+) | Fase 1 (manifest) / Fase 5 (pedido)`.
- Resolucao DECIDIDA: a favor de `docs/PERMISSOES.md`. MANTER a declaracao. Remover quebraria
  a Phase 5. Nada e solicitado em runtime nesta fase (NTF-02 poe o pedido na Phase 5).

**Baseline de build medido em 2026-07-29 (pos-`clean`, ANTES do plano 01):**
`BUILD SUCCESSFUL in 42s` — `57 actionable tasks: 56 executed, 1 up-to-date`; 20/20 testes;
detekt 0 issues; lint 0 errors / 137 warnings; APK debug 33,8 MB.
Apos os planos 01 e 02 o esperado e: lint 0 issues, testes >= 27.

**Pitfall 3 (critico):** um run incremental devolve `BUILD SUCCESSFUL` com tudo UP-TO-DATE e
NAO prova nada. A evidencia so vale se vier de `./gradlew clean` seguido da suite, e o log
tem de mostrar `N actionable tasks: M executed` com M > 0.

**Pitfall 4:** nao existe `local.properties`; o SDK e localizado por `ANDROID_HOME` do shell.
Exportar explicitamente antes de rodar.

**Estrutura de `docs/TESTE-FISICO-SAMSUNG.md`:** roteiro com "Preparacao", "Matriz de cenarios"
(22 linhas numeradas), secao "Modo discador (executar apos a Fase 6)" (cenarios 23-30),
"Registro de comportamento OEM" e "Criterios de aceite cobertos".

**Estrutura de `CHANGELOG.md`:** formato Release Notes — `# Release Notes`, `---`,
`## [Futuro]`, `## [Unreleased](...)`, secoes `### ✨ Novidades` / `### 🎨 Melhorias` /
`### 🐛 Correcoes` / `### 🔧 Tecnico`, itens SEMPRE em checkbox `- [x]` / `- [ ]`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconciliar POST_NOTIFICATIONS e registrar pendencias fisicas e de manutencao</name>
  <files>.planning/phases/01-fundacao-compilavel/01-CONTEXT.md, docs/TESTE-FISICO-SAMSUNG.md, docs/backlog/manutencao-toolchain.md, docs/INDEX.md, CHANGELOG.md</files>
  <read_first>
    - .planning/phases/01-fundacao-compilavel/01-CONTEXT.md (secao "Escopo travado antes da fase" — texto a ajustar)
    - docs/PERMISSOES.md (fonte canonica; linha 13 autoriza POST_NOTIFICATIONS na Fase 1)
    - .planning/phases/01-fundacao-compilavel/01-RESEARCH.md (Open Questions 1, 2, 3 e 4; Pitfall 5)
    - docs/TESTE-FISICO-SAMSUNG.md (arquivo a modificar — estrutura do roteiro)
    - docs/INDEX.md (linha 33 — padrao de entrada de backlog)
    - CHANGELOG.md (secao `## [Unreleased]` -> `### 🔧 Tecnico`)
    - .planning/ROADMAP.md (secao "Politica de validacao fisica" no Progress)
  </read_first>
  <action>
**1) `01-CONTEXT.md` — reconciliacao explicita.** Na secao `<decisions>` > "Escopo travado
antes da fase", garantir que o texto sobre permissoes esteja inequivoco e adicionar, logo
apos, este paragrafo de reconciliacao (texto literal):

> **Reconciliacao POST_NOTIFICATIONS (2026-07-29):** `docs/PERMISSOES.md` (linha 13) e a fonte
> canonica de permissoes segundo o `CLAUDE.md` e autoriza explicitamente a **declaracao** de
> `POST_NOTIFICATIONS` no manifest na Fase 1, com o **pedido em runtime** apenas na Fase 5
> (NTF-02). A permissao PERMANECE declarada. Remove-la quebraria a Phase 5. O verifier nao deve
> tratar sua presenca como violacao de permissao.

**2) `docs/TESTE-FISICO-SAMSUNG.md` — pendencias fisicas da Phase 1.** Adicionar uma secao nova
`## Pendencias herdadas da Phase 1 (fundacao)`, ANTES de "Registro de comportamento OEM", com
uma tabela no mesmo formato das outras (`| # | Cenario | Passos | Esperado | Resultado |`) e a
numeracao continuando a partir de 30:

| # | Cenario | Passos | Esperado |
|---|---------|--------|----------|
| 31 | APK debug instala | `adb install sentinela-debug.apk` | Instala sem erro; icone aparece na gaveta |
| 32 | Tema dark Silent Guardian renderiza | Abrir o app com o sistema em dark mode | Fundo `#081425`, texto legivel, contraste correto; nenhum flash branco no splash |
| 33 | Dynamic Color sob One UI | Trocar o papel de parede/paleta do sistema (Android 12+) e reabrir | O app adota a paleta dinamica sem quebrar legibilidade |
| 34 | Tema light forcado | Forcar light mode no sistema | App permanece utilizavel e legivel (produto e dark-first) |

Acrescentar abaixo da tabela a frase literal:
`Origem: Phase 1, criterio 2. Diferido conforme a politica de validacao fisica do ROADMAP (2026-07-28).`

**3) `docs/backlog/manutencao-toolchain.md` — NOVO arquivo.** Registrar os 3 itens adiados,
cada um com o que se sabe e a justificativa do adiamento:
- **`gradlew.bat` ausente** — so `gradlew` esta versionado; clone em Windows nao builda. Gerar
  o `.bat` exige rodar a task `wrapper`, que pode mexer na versao validada (9.6.1). Relevante
  porque o produto sera divulgado como open source (UIX-13).
- **AGP 9.3.1** — o check `AndroidGradlePluginVersion` do lint reporta a existencia da 9.3.1,
  mas a pagina oficial de release notes lista apenas 9.3.0 como stable (20/07/2026). Nao bumpar
  ate haver release notes. Reavaliar na Phase 9 (release).
- **Reavaliar `disable "UnusedResources"`** — as 132 ocorrencias sao strings pre-escritas para
  as telas das Fases 5-9. Quando as telas existirem (Phase 9), a regra deve voltar a ser sinal
  legitimo de recurso orfao. Registrar como pendencia explicita da Phase 9.
- **Aviso de deprecation do Gradle 10** — origem confirmada: `ReportingExtension.file(String)`
  usada pelo plugin detekt 1.23.8, nao por script deste repo. Zero impacto no Gradle 9.6.1;
  some quando o detekt 2.x sair do alpha.

**4) `docs/INDEX.md`** — adicionar a entrada do arquivo novo seguindo o padrao da linha 33:
`- [`backlog/manutencao-toolchain.md`](backlog/manutencao-toolchain.md) — itens de manutencao de toolchain adiados (gradlew.bat, AGP 9.3.1, revisao dos disable de lint)`
Exigencia do `CLAUDE.md`: todo `.md` novo entra no INDEX.

**5) `CHANGELOG.md`** — em `## [Unreleased]` > `### 🔧 Tecnico`, acrescentar itens em checkbox
`- [x]` descrevendo o fechamento da Phase 1: politica de lint declarada (QLT-02), correcao do
`ObsoleteSdkInt` por remocao do qualificador `-v26`, script de invariantes de manifest/rebranding
e teste dos tokens do tema. Manter o formato Release Notes — NUNCA Keep a Changelog, itens
SEMPRE com checkbox.

NAO fazer:
- NAO remover `POST_NOTIFICATIONS` do `AndroidManifest.xml`.
- NAO criar `checkpoint:human-*` nem pedir ao usuario que instale o APK — os itens 31-34 sao
  registro de pendencia, executados pelo mantenedor na Phase 9.
- NAO criar arquivos `.TODO`, `.BUGS*` ou `.SPRINT*` na raiz.
- NAO alterar nenhum arquivo de codigo.
  </action>
  <verify>
    <automated>grep -c "Reconciliacao POST_NOTIFICATIONS\|Reconciliação POST_NOTIFICATIONS" .planning/phases/01-fundacao-compilavel/01-CONTEXT.md && grep -c "Pendencias herdadas da Phase 1\|Pendências herdadas da Phase 1" docs/TESTE-FISICO-SAMSUNG.md && test -f docs/backlog/manutencao-toolchain.md && grep -c "manutencao-toolchain" docs/INDEX.md && grep -c "android.permission.POST_NOTIFICATIONS" app/src/main/AndroidManifest.xml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "POST_NOTIFICATIONS" .planning/phases/01-fundacao-compilavel/01-CONTEXT.md` retorna `>= 2` e o texto cita `docs/PERMISSOES.md` como fonte canonica
    - `grep -c "android.permission.POST_NOTIFICATIONS" app/src/main/AndroidManifest.xml` continua retornando `1` (permissao NAO removida)
    - `git diff app/src/main/AndroidManifest.xml` esta vazio
    - `grep -cE "^\| 3[1-4] \|" docs/TESTE-FISICO-SAMSUNG.md` retorna `4` (cenarios 31 a 34 adicionados)
    - `grep -c "Phase 1" docs/TESTE-FISICO-SAMSUNG.md` retorna `>= 1`
    - `test -f docs/backlog/manutencao-toolchain.md` passa
    - `grep -c "gradlew.bat" docs/backlog/manutencao-toolchain.md` retorna `>= 1`
    - `grep -c "9.3.1" docs/backlog/manutencao-toolchain.md` retorna `>= 1`
    - `grep -c "UnusedResources" docs/backlog/manutencao-toolchain.md` retorna `>= 1`
    - `grep -c "manutencao-toolchain" docs/INDEX.md` retorna `1`
    - `grep -c "^- \[x\]" CHANGELOG.md` aumentou em relacao ao HEAD (`git diff CHANGELOG.md` mostra apenas linhas `- [x]` adicionadas na secao `### 🔧 Tecnico` de `## [Unreleased]`)
    - `grep -c "Keep a Changelog\|## Added\|## Changed" CHANGELOG.md` retorna `0`
    - `git status --porcelain` nao lista nenhum arquivo `.kt`, `.kts`, `.xml` de `app/` como modificado
  </acceptance_criteria>
  <done>O conflito `POST_NOTIFICATIONS` esta resolvido por escrito a favor de `docs/PERMISSOES.md` com a permissao mantida no manifest; os 4 cenarios fisicos da Phase 1 estao no roteiro Samsung numerados 31-34; `docs/backlog/manutencao-toolchain.md` existe e esta no INDEX; CHANGELOG atualizado no formato Release Notes; nenhum arquivo de codigo tocado.</done>
</task>

<task type="auto">
  <name>Task 2: Rodar a suite completa pos-clean e arquivar 01-EVIDENCE.md</name>
  <files>.planning/phases/01-fundacao-compilavel/01-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/01-fundacao-compilavel/01-RESEARCH.md (Code Examples §5 — comando de validacao; Pitfall 3 — UP-TO-DATE nao prova nada; Pitfall 4 — ANDROID_HOME; secao "Validation Architecture" — mapa criterio -> comando)
    - .planning/ROADMAP.md (Phase 1 — os 5 criterios de sucesso, texto literal)
    - .planning/phases/01-fundacao-compilavel/01-01-SUMMARY.md (o que o plano 01 mudou)
    - .planning/phases/01-fundacao-compilavel/01-02-SUMMARY.md (o que o plano 02 criou)
    - scripts/verify-invariants.sh (script a executar)
  </read_first>
  <action>
Executar, nesta ordem exata, capturando a saida integral:

```bash
export ANDROID_HOME="${ANDROID_HOME:-$HOME/Library/Android/sdk}"
./gradlew clean
./gradlew assembleDebug testDebugUnitTest lint detekt
bash scripts/verify-invariants.sh
```

O `clean` e OBRIGATORIO. Um run incremental devolve `BUILD SUCCESSFUL` com tudo `UP-TO-DATE`
sem compilar, testar ou analisar nada — evidencia invalida. O log so vale se terminar em
`N actionable tasks: M executed` com **M > 0**.

Coletar tambem:
```bash
grep -o 'tests="[0-9]*" skipped="[0-9]*" failures="[0-9]*" errors="[0-9]*"' app/build/test-results/testDebugUnitTest/*.xml
grep -c "<issue" app/build/reports/lint-results-debug.xml      # esperado 0
grep -c "<error" app/build/reports/detekt/detekt.xml           # esperado 0
ls -la app/build/outputs/apk/debug/app-debug.apk
grep -o 'uses-permission android:name="[^"]*"' app/build/intermediates/merged_manifest/debug/processDebugMainManifest/AndroidManifest.xml
```

Criar `.planning/phases/01-fundacao-compilavel/01-EVIDENCE.md` com:

1. **Frontmatter** com `phase: 01-fundacao-compilavel`, `date`, `build_status`,
   `actionable_tasks`, `tasks_executed`, `tests_total`, `tests_failures`, `lint_issues`,
   `detekt_issues`, `apk_bytes`.
2. **Secao "Comando executado"** — os 3 comandos acima verbatim.
3. **Secao "Saida do build"** — o trecho final do log em bloco de codigo, incluindo
   obrigatoriamente a linha `BUILD SUCCESSFUL in Xs` E a linha
   `N actionable tasks: M executed` (colar literal, sem editar).
4. **Secao "Criterios de sucesso da Phase 1"** — tabela com os 5 criterios do ROADMAP,
   cada um mapeado a comando + resultado observado + veredito:

| # | Criterio (ROADMAP) | Comando de prova | Resultado | Veredito |
|---|--------------------|------------------|-----------|----------|
| 1 | `assembleDebug testDebugUnitTest lint detekt` sem erro | suite pos-clean | ... | ✅ |
| 2 | APK instalavel + tema Silent Guardian aplicado | `test -f .../app-debug.apk` + `ThemeTokensTest` + `grep SentinelaTheme MainActivity.kt` | ... | ✅ parcial — renderizacao real diferida (cenarios 31-34 da Phase 9) |
| 3 | Manifest sem INTERNET + Service com BIND_SCREENING_SERVICE | `scripts/verify-invariants.sh` (bloco 1, allowlist sobre o mergeado) | ... | ✅ |
| 4 | `CallDecisionEngine` puro com precedencia testada | `testDebugUnitTest` + grep de `^import android` no dominio | ... | ✅ |
| 5 | Nome/applicationId/cores/strings centralizados | `scripts/verify-invariants.sh` (bloco 2) | ... | ✅ |

5. **Secao "Saida do verify-invariants.sh"** — saida integral do script em bloco de codigo,
   incluindo o conjunto real de `uses-permission` do manifest mergeado.
6. **Secao "Diferido para a Phase 9"** — lista dos cenarios 31-34 de
   `docs/TESTE-FISICO-SAMSUNG.md`, com a nota literal:
   `Diferido conforme a politica de validacao fisica do ROADMAP — o verifier desta fase deve tratar como "deferred to Phase 9", nunca como gap.`

Se QUALQUER comando falhar, NAO maquiar o EVIDENCE: registrar a falha real, parar e reportar.
Um EVIDENCE.md com falha honesta vale mais que um verde fabricado.

NAO fazer: NAO usar `--offline`, `--rerun-tasks` no lugar do `clean` (o `clean` e o padrao
decidido), nem editar o log colado. NAO criar `checkpoint:human-*`. NAO instalar o APK em
aparelho.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-fundacao-compilavel/01-EVIDENCE.md && grep -q "BUILD SUCCESSFUL" .planning/phases/01-fundacao-compilavel/01-EVIDENCE.md && grep -qE "actionable tasks: [1-9][0-9]* executed" .planning/phases/01-fundacao-compilavel/01-EVIDENCE.md && grep -q "todos os invariantes OK" .planning/phases/01-fundacao-compilavel/01-EVIDENCE.md && echo EVIDENCE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/01-fundacao-compilavel/01-EVIDENCE.md` passa
    - `grep -c "BUILD SUCCESSFUL" .planning/phases/01-fundacao-compilavel/01-EVIDENCE.md` retorna `>= 1`
    - `grep -cE "actionable tasks: [1-9][0-9]* executed" .planning/phases/01-fundacao-compilavel/01-EVIDENCE.md` retorna `>= 1` — prova de execucao real, nao UP-TO-DATE
    - `grep -c "up-to-date$" .planning/phases/01-fundacao-compilavel/01-EVIDENCE.md` NAO e a unica evidencia de execucao (a linha `M executed` com M > 0 e obrigatoria)
    - `grep -c "todos os invariantes OK" .planning/phases/01-fundacao-compilavel/01-EVIDENCE.md` retorna `>= 1`
    - `grep -c "DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION" .planning/phases/01-fundacao-compilavel/01-EVIDENCE.md` retorna `>= 1` (conjunto real do mergeado colado)
    - `grep -c "INTERNET" .planning/phases/01-fundacao-compilavel/01-EVIDENCE.md` retorna `>= 1` com resultado `0` registrado
    - A tabela de criterios tem 5 linhas numeradas de 1 a 5, cada uma com comando e resultado observado
    - `grep -c "deferred to Phase 9" .planning/phases/01-fundacao-compilavel/01-EVIDENCE.md` retorna `>= 1`
    - Metricas registradas no frontmatter batem com os relatorios: `lint_issues: 0`, `detekt_issues: 0`, `tests_failures: 0`, `tests_total >= 27`
    - `test -f app/build/outputs/apk/debug/app-debug.apk` passa
    - `git status --porcelain` nao lista nenhum arquivo de `app/src/` como modificado por esta task
  </acceptance_criteria>
  <done>`01-EVIDENCE.md` existe com o log pos-`clean` mostrando `BUILD SUCCESSFUL` e `M executed` (M > 0), a saida integral do `verify-invariants.sh`, a tabela dos 5 criterios do ROADMAP com veredito e a lista de itens diferidos para a Phase 9.</done>
</task>

</tasks>

<verification>
```bash
export ANDROID_HOME="${ANDROID_HOME:-$HOME/Library/Android/sdk}"

# Evidencia real, nao UP-TO-DATE
grep -E "BUILD SUCCESSFUL|actionable tasks: [0-9]+ executed" \
  .planning/phases/01-fundacao-compilavel/01-EVIDENCE.md

# Reconciliacao documental feita e permissao preservada
grep -n "POST_NOTIFICATIONS" .planning/phases/01-fundacao-compilavel/01-CONTEXT.md
grep -c "android.permission.POST_NOTIFICATIONS" app/src/main/AndroidManifest.xml   # 1

# Pendencias fisicas registradas (nao pedidas ao usuario)
grep -cE "^\| 3[1-4] \|" docs/TESTE-FISICO-SAMSUNG.md    # 4

# Backlog indexado
test -f docs/backlog/manutencao-toolchain.md && grep -c "manutencao-toolchain" docs/INDEX.md  # 1

# Nenhum codigo tocado neste plano
git status --porcelain app/ | grep -v "^$" || echo "OK: app/ intocado"
```
</verification>

<success_criteria>
- `01-EVIDENCE.md` arquiva um build POS-`clean` com `BUILD SUCCESSFUL` e `M executed` (M > 0),
  lint 0 issues, detekt 0 issues, >= 27 testes com 0 failures e APK debug presente.
- Os 5 criterios de sucesso do ROADMAP estao mapeados a comando + resultado + veredito.
- `POST_NOTIFICATIONS` permanece no manifest e a reconciliacao esta escrita no CONTEXT.md
  citando `docs/PERMISSOES.md` como fonte canonica.
- Cenarios fisicos 31-34 registrados em `docs/TESTE-FISICO-SAMSUNG.md` como pendencia da Phase 9.
- `docs/backlog/manutencao-toolchain.md` criado e indexado em `docs/INDEX.md`.
- CHANGELOG atualizado no formato Release Notes com itens em checkbox.
- Nenhum arquivo de `app/` modificado por este plano.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundacao-compilavel/01-03-SUMMARY.md`
</output>
