---
phase: 01-fundacao-compilavel
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-invariants.sh
  - app/src/main/java/org/sentinela/app/ui/theme/Theme.kt
  - app/src/test/java/org/sentinela/app/ui/theme/ThemeTokensTest.kt
  - app/src/test/java/org/sentinela/app/domain/CallDecisionEngineTest.kt
autonomous: true
requirements: [PRV-01, QLT-02, UIX-08, UIX-12]

must_haves:
  truths:
    - "Um unico comando reexecutavel prova que o app nao tem INTERNET, registra o CallScreeningService e nao antecipa permissao de fase futura"
    - "A checagem de permissoes roda sobre o manifest MERGEADO e usa allowlist, entao nao quebra quando o androidx injeta permissao propria"
    - "Uma regressao de rebranding (applicationId literal, string hardcoded, cor fora do design system) faz o script falhar"
    - "Uma regressao nos tokens Silent Guardian do esquema dark e detectada por teste unitario, nao por inspecao visual"
    - "As 4 politicas de OriginPolicy estao cobertas por teste nas 3 origens (contato, whitelist, desconhecido) — a matriz de precedencia do criterio 4 nao tem buraco"
  artifacts:
    - path: "scripts/verify-invariants.sh"
      provides: "Verificacao mecanica dos criterios 3, 4 e 5 da fase (PRV-01, UIX-12, dominio puro)"
      contains: "merged_manifest"
    - path: "app/src/test/java/org/sentinela/app/domain/CallDecisionEngineTest.kt"
      provides: "Matriz OriginPolicy x origem completa + as 3 variantes de bloqueio"
      contains: "NEVER_SILENCE"
    - path: "app/src/test/java/org/sentinela/app/ui/theme/ThemeTokensTest.kt"
      provides: "Asserções JVM sobre os 26 tokens Silent Guardian e o wiring do darkColorScheme"
      contains: "0xFF081425"
  key_links:
    - from: "scripts/verify-invariants.sh"
      to: "app/build/intermediates/merged_manifest/debug/processDebugMainManifest/AndroidManifest.xml"
      via: "grep sobre o manifest mergeado gerado pelo AGP"
      pattern: "merged_manifest/debug/processDebugMainManifest"
    - from: "app/src/test/java/org/sentinela/app/ui/theme/ThemeTokensTest.kt"
      to: "app/src/main/java/org/sentinela/app/ui/theme/Theme.kt"
      via: "assercao sobre DarkColors (visibilidade internal)"
      pattern: "DarkColors"
---

<objective>
Converter os criterios 3, 4 e 5 da fase — hoje verificados por greps ad-hoc digitados a mao —
em DOIS artefatos reexecutaveis: um script de invariantes (`scripts/verify-invariants.sh`) e um
teste unitario dos tokens do tema (`ThemeTokensTest`).

Purpose: PRV-01 ("sem INTERNET"), UIX-12 ("rebranding centralizado") e UIX-08 ("tokens Silent
Guardian") sao verdade HOJE, mas sem artefato ninguem sabe se continuam verdade depois da
Phase 4 (READ_CONTACTS) ou da Phase 6 (ROLE_DIALER). O script existe justamente para FALHAR
quando alguem antecipar permissao de fase futura.
Output: script executavel + teste unitario, ambos verdes.

NAO faz parte deste plano: mexer no `CallDecisionEngine`, no catalogo de versoes, no bloco lint
(plano 01), instalar APK em aparelho, ou expandir `ContactLookupRepository` (Phase 4).
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fundacao-compilavel/01-CONTEXT.md
@.planning/phases/01-fundacao-compilavel/01-RESEARCH.md
@CLAUDE.md
@docs/PERMISSOES.md
@app/src/main/java/org/sentinela/app/ui/theme/Theme.kt
@app/src/main/java/org/sentinela/app/ui/theme/Color.kt

<interfaces>
<!-- Estado atual verificado em 2026-07-29. O executor NAO precisa explorar o codebase. -->

**Manifest mergeado (artefato do AGP, gerado por assembleDebug):**
`app/build/intermediates/merged_manifest/debug/processDebugMainManifest/AndroidManifest.xml`

Conjunto REAL de `uses-permission` no mergeado hoje (allowlist autorizada desta fase):
```
android.permission.POST_NOTIFICATIONS
org.sentinela.app.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION
```
`POST_NOTIFICATIONS` e AUTORIZADA por `docs/PERMISSOES.md` linha 13: "Fase 1 (manifest) /
Fase 5 (pedido)". `DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION` e injetada pelo androidx-core
via manifest merger, e permissao de assinatura, nao concede capacidade e NAO e removivel.
Por isso a checagem e por ALLOWLIST, nunca por contagem exata.

`BIND_SCREENING_SERVICE` aparece como atributo `android:permission` do `<service>`, junto com
a action `android.telecom.CallScreeningService`.

**Tokens Silent Guardian publicos em `Color.kt`** (todos `val ... = Color(0x...)`):
```kotlin
val Surface = Color(0xFF081425)
val SurfaceContainerLowest = Color(0xFF040E1F)
val SurfaceContainerLow = Color(0xFF111C2D)
val SurfaceContainer = Color(0xFF152031)
val SurfaceContainerHigh = Color(0xFF1F2A3C)
val SurfaceContainerHighest = Color(0xFF2A3548)
val OnSurface = Color(0xFFD8E3FB)
val OnSurfaceVariant = Color(0xFFC2C6D6)
val Outline = Color(0xFF8C909F)
val OutlineVariant = Color(0xFF424754)
val Primary = Color(0xFFADC6FF)
val OnPrimary = Color(0xFF002E6A)
val PrimaryContainer = Color(0xFF4D8EFF)
val OnPrimaryContainer = Color(0xFF00285D)
val Error = Color(0xFFFFB4AB)
```

**`Theme.kt` hoje** (linha 14): `private val DarkColors = darkColorScheme( ... )` com 26
tokens mapeados; `SentinelaTheme` usa guard `Build.VERSION.SDK_INT >= Build.VERSION_CODES.S`
para Dynamic Color. `DarkColors` e `private` — precisa virar `internal` para o teste assertar.

`androidx.compose.ui.graphics.Color` e uma `value class` pura de Kotlin: comparacao funciona em
teste JVM puro, SEM Robolectric. Os 20 testes atuais tambem sao JVM puros.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar scripts/verify-invariants.sh com allowlist de permissoes e greps de rebranding</name>
  <files>scripts/verify-invariants.sh</files>
  <read_first>
    - .planning/phases/01-fundacao-compilavel/01-RESEARCH.md (Code Examples §1, §2 e §3 — comandos exatos; Architecture Patterns §1 e §2 — manifest mergeado + allowlist; Pitfall 1 e Pitfall 4)
    - docs/PERMISSOES.md (fonte canonica — leitura BLOQUEANTE para qualquer trabalho de manifest)
    - app/src/main/AndroidManifest.xml (manifest fonte, para conferir o que o merge recebe)
    - build.sh (padrao de export de ANDROID_HOME/JAVA_HOME ja usado no projeto)
  </read_first>
  <action>
Criar `scripts/verify-invariants.sh` (novo diretorio `scripts/`), executavel (`chmod +x`),
com `#!/usr/bin/env bash` e `set -uo pipefail` — **NAO usar `set -e`**: `grep -c` sai com
codigo 1 quando o resultado e 0, o que abortaria o script no caso de sucesso (Pitfall do
RESEARCH §1). O script acumula falhas numa variavel e sai 1 no final se houver alguma.

Estrutura obrigatoria (implementar exatamente estes checks, com estes valores literais):

```bash
#!/usr/bin/env bash
# Invariantes verificaveis do Sentinela (criterios 3, 4 e 5 da Phase 1).
# Reexecutavel a cada fase: falha quando alguem antecipa permissao de fase futura
# ou quebra a centralizacao de rebranding.
set -uo pipefail

export ANDROID_HOME="${ANDROID_HOME:-$HOME/Library/Android/sdk}"
FAILURES=0
fail() { echo "FAIL: $*"; FAILURES=$((FAILURES + 1)); }
ok()   { echo "ok:   $*"; }

M=app/build/intermediates/merged_manifest/debug/processDebugMainManifest/AndroidManifest.xml
if [ ! -f "$M" ]; then
  echo "Manifest mergeado ausente. Rode ./gradlew assembleDebug antes." >&2
  exit 2
fi
```

**Bloco 1 — PRV-01 / criterio 3 (manifest MERGEADO):**
- `grep -c "android.permission.INTERNET" "$M"` deve ser `0` -> senao `fail`.
- Extrair o conjunto real com
  `grep -o 'uses-permission android:name="[^"]*"' "$M" | sed 's/.*name="//;s/"//' | sort -u`
  e comparar contra a ALLOWLIST literal (nao contagem):
  ```
  android.permission.POST_NOTIFICATIONS
  org.sentinela.app.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION
  ```
  Qualquer permissao fora dessa lista -> `fail` nomeando a permissao intrusa.
- Bloqueio explicito e ruidoso de permissoes de fase futura:
  `grep -cE "READ_CONTACTS|READ_CALL_LOG|READ_PHONE_STATE|READ_SMS|CALL_PHONE|BIND_INCALL_SERVICE|SYSTEM_ALERT_WINDOW" "$M"`
  deve ser `0` -> senao `fail` com a mensagem
  `"permissao de fase futura antecipada — ver docs/PERMISSOES.md"`.
- `grep -c "BIND_SCREENING_SERVICE" "$M"` deve ser `>= 1`.
- `grep -c "android.telecom.CallScreeningService" "$M"` deve ser `>= 1`.

**Bloco 2 — UIX-12 / criterio 5 (centralizacao para rebranding):** cada grep abaixo deve
produzir SAIDA VAZIA; qualquer linha e `fail` (imprimir as linhas ofensoras):
```bash
grep -rn "org.sentinela.app" app/src/main/java --include="*.kt" | grep -v ":package \|:import "
grep -rn 'text = "' app/src/main/java --include="*.kt"
grep -rn "Color(0x" app/src/main/java --include="*.kt" | grep -v "/ui/theme/"
```
E duas presencas obrigatorias:
```bash
grep -c "sentinelaApplicationId" app/build.gradle.kts   # deve ser 3
grep -c "app_name" app/src/main/res/values/strings.xml  # deve ser >= 1
```

**Bloco 3 — criterio 4 (dominio puro):** saida vazia obrigatoria para
`grep -rn "^import android" app/src/main/java/org/sentinela/app/domain/`.

**Bloco 4 — QLT-02 (relatorios, se existirem):** se
`app/build/reports/detekt/detekt.xml` existe, `grep -c "<error"` deve ser `0`; se
`app/build/reports/lint-results-debug.xml` existe, `grep -c "<issue"` deve ser `0`.
Se o arquivo nao existir, imprimir `skip:` (nao falhar) — o gate real e o plano 03.

**Encerramento:**
```bash
if [ "$FAILURES" -gt 0 ]; then
  echo "== $FAILURES invariante(s) violado(s) =="
  exit 1
fi
echo "== todos os invariantes OK =="
```

NAO fazer:
- NAO ler `app/src/main/AndroidManifest.xml` (o fonte) no lugar do mergeado — o fonte mente
  por omissao, quem decide o conteudo do APK e o merger.
- NAO checar cardinalidade ("exatamente N permissoes") — quebra no proximo bump de androidx.
- NAO remover `POST_NOTIFICATIONS` do manifest: e autorizada por `docs/PERMISSOES.md` e
  remove-la quebraria a Phase 5.
- NAO escrever parser de XML proprio.
  </action>
  <verify>
    <automated>export ANDROID_HOME="${ANDROID_HOME:-$HOME/Library/Android/sdk}"; ./gradlew assembleDebug && bash scripts/verify-invariants.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -x scripts/verify-invariants.sh` passa (bit de execucao setado)
    - `head -1 scripts/verify-invariants.sh` e `#!/usr/bin/env bash`
    - `grep -c "set -e$\|set -e " scripts/verify-invariants.sh` retorna `0` (nao usa `set -e`)
    - `grep -c "merged_manifest/debug/processDebugMainManifest" scripts/verify-invariants.sh` retorna `>= 1`
    - `grep -c "DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION" scripts/verify-invariants.sh` retorna `>= 1` (allowlist presente)
    - `grep -c "READ_CONTACTS" scripts/verify-invariants.sh` retorna `>= 1` (bloqueio de fase futura presente)
    - `grep -c "android.permission.INTERNET" scripts/verify-invariants.sh` retorna `>= 1`
    - `grep -c "sentinelaApplicationId" scripts/verify-invariants.sh` retorna `>= 1`
    - `bash scripts/verify-invariants.sh` (apos `./gradlew assembleDebug`) sai com codigo `0` e imprime `== todos os invariantes OK ==`
    - Teste negativo obrigatorio: adicionar temporariamente `<uses-permission android:name="android.permission.READ_CONTACTS" />` a `app/src/main/AndroidManifest.xml`, rodar `./gradlew assembleDebug`, confirmar que o script sai com codigo `1`, e REVERTER a edicao (`git checkout app/src/main/AndroidManifest.xml`) — registrar o resultado no SUMMARY
    - `git diff app/src/main/AndroidManifest.xml` esta vazio ao final da task
  </acceptance_criteria>
  <done>`scripts/verify-invariants.sh` existe, e executavel, sai 0 no estado atual do repo e sai 1 quando uma permissao de fase futura e injetada no manifest (comprovado pelo teste negativo, revertido em seguida).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cobrir os tokens Silent Guardian com ThemeTokensTest</name>
  <files>app/src/test/java/org/sentinela/app/ui/theme/ThemeTokensTest.kt, app/src/main/java/org/sentinela/app/ui/theme/Theme.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/ui/theme/Theme.kt (arquivo a modificar — `private val DarkColors` na linha 14)
    - app/src/main/java/org/sentinela/app/ui/theme/Color.kt (fonte dos 26 tokens)
    - docs/design/DESIGN.md (fonte canonica dos tokens Silent Guardian — confirmar os valores hex antes de assertar)
    - .planning/phases/01-fundacao-compilavel/01-RESEARCH.md (Architecture Patterns §3 — o que "tema aplicado" significa SEM aparelho; nota sobre ThemeTokensTest)
  </read_first>
  <behavior>
    - Test 1: `Surface` vale `Color(0xFF081425)` — token de fundo do Silent Guardian
    - Test 2: `Primary` vale `Color(0xFFADC6FF)` e `PrimaryContainer` vale `Color(0xFF4D8EFF)`
    - Test 3: `DarkColors.surface == Surface` e `DarkColors.onSurface == OnSurface` — o esquema dark realmente consome os tokens, nao valores soltos
    - Test 4: `DarkColors.primary == Primary`, `DarkColors.error == Error`, `DarkColors.outline == Outline` — wiring dos grupos primary/error/outline
    - Edge case: os tokens sao `value class Color`, comparados por igualdade estrutural em JVM pura, sem Robolectric e sem `@Config`
  </behavior>
  <action>
1) Em `app/src/main/java/org/sentinela/app/ui/theme/Theme.kt`, trocar a visibilidade de
   `DarkColors` de `private` para `internal`:
   ```kotlin
   internal val DarkColors = darkColorScheme(
   ```
   Essa e a UNICA alteracao permitida em `Theme.kt`. NAO mexer em `LightColors`, no guard
   `Build.VERSION.SDK_INT >= Build.VERSION_CODES.S`, nos parametros de `SentinelaTheme`, nem
   em qualquer valor de cor.

2) Criar `app/src/test/java/org/sentinela/app/ui/theme/ThemeTokensTest.kt`, package
   `org.sentinela.app.ui.theme`, JUnit4 puro (`org.junit.Test`, `org.junit.Assert.assertEquals`),
   SEM Robolectric e SEM `@Config` (nao ha nenhum teste Robolectric no projeto hoje e o
   Robolectric 4.16.1 nao suporta SDK 37).

   Asserções obrigatorias, com os valores literais:
   ```kotlin
   assertEquals(Color(0xFF081425), Surface)
   assertEquals(Color(0xFFD8E3FB), OnSurface)
   assertEquals(Color(0xFFADC6FF), Primary)
   assertEquals(Color(0xFF4D8EFF), PrimaryContainer)
   assertEquals(Color(0xFFFFB4AB), Error)
   assertEquals(Color(0xFF8C909F), Outline)
   // wiring: o darkColorScheme consome os tokens, nao literais soltos
   assertEquals(Surface, DarkColors.surface)
   assertEquals(OnSurface, DarkColors.onSurface)
   assertEquals(Primary, DarkColors.primary)
   assertEquals(Error, DarkColors.error)
   assertEquals(Outline, DarkColors.outline)
   assertEquals(SurfaceContainerLowest, DarkColors.surfaceContainerLowest)
   assertEquals(SurfaceContainerHighest, DarkColors.surfaceContainerHighest)
   ```
   Organizar em 3-4 metodos `@Test` com nomes descritivos em ingles (padrao dos testes atuais),
   ex.: `surfaceTokenMatchesSilentGuardianPalette`, `darkSchemeWiresSilentGuardianTokens`.

3) Antes de rodar, conferir cada hex contra `docs/design/DESIGN.md`. Se algum valor de
   `Color.kt` DIVERGIR do design system, **nao alterar `Color.kt`** — registrar a divergencia
   no SUMMARY e assertar o valor do `Color.kt` (a fase e de verificacao, nao de redesign).

NAO fazer: NAO adicionar dependencia nova; NAO usar `createComposeRule` (teste instrumentado
esta fora desta fase); NAO tentar assertar renderizacao — aparencia real e Phase 9.
  </action>
  <verify>
    <automated>export ANDROID_HOME="${ANDROID_HOME:-$HOME/Library/Android/sdk}"; ./gradlew testDebugUnitTest detekt && grep -o 'tests="[0-9]*" skipped="[0-9]*" failures="[0-9]*" errors="[0-9]*"' app/build/test-results/testDebugUnitTest/*.xml</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/src/test/java/org/sentinela/app/ui/theme/ThemeTokensTest.kt` passa
    - `grep -c "internal val DarkColors" app/src/main/java/org/sentinela/app/ui/theme/Theme.kt` retorna `1`
    - `grep -c "private val DarkColors" app/src/main/java/org/sentinela/app/ui/theme/Theme.kt` retorna `0`
    - `git diff app/src/main/java/org/sentinela/app/ui/theme/Theme.kt` mostra exatamente 1 linha alterada (`private` -> `internal`)
    - `git diff app/src/main/java/org/sentinela/app/ui/theme/Color.kt` esta vazio (nenhum token alterado)
    - `grep -c "0xFF081425" app/src/test/java/org/sentinela/app/ui/theme/ThemeTokensTest.kt` retorna `>= 1`
    - `grep -c "DarkColors" app/src/test/java/org/sentinela/app/ui/theme/ThemeTokensTest.kt` retorna `>= 5`
    - `grep -c "Robolectric\|@Config" app/src/test/java/org/sentinela/app/ui/theme/ThemeTokensTest.kt` retorna `0`
    - Soma de `tests=` nos XMLs de `app/build/test-results/testDebugUnitTest/` e `>= 27` (20 atuais + ThemeTokensTest + 4 de precedencia), com `failures="0" errors="0"` em todos
    - `grep -c "<error" app/build/reports/detekt/detekt.xml` retorna `0`
  </acceptance_criteria>
  <done>`ThemeTokensTest` roda em JVM pura, assertando os tokens Silent Guardian e o wiring do `darkColorScheme`; a suite passa de 20 para >= 23 testes, todos verdes, e o detekt continua em 0 issues.</done>

</task>
<task type="auto" tdd="true">
  <name>Task 3: Fechar a matriz de precedencia do CallDecisionEngine (criterio 4)</name>
  <files>app/src/test/java/org/sentinela/app/domain/CallDecisionEngineTest.kt</files>
  <read_first>
    - app/src/test/java/org/sentinela/app/domain/CallDecisionEngineTest.kt (20 testes atuais; helpers `engine`, `defaults`, `incoming()` e `decide(...)` nas linhas 18-29)
    - app/src/main/java/org/sentinela/app/domain/CallDecisionEngine.kt (precedencia de 7 passos — NAO modificar)
    - app/src/main/java/org/sentinela/app/settings/ScreeningSettings.kt (`OriginPolicy`, `BlockMode`, `FallbackPolicy` e os defaults do MVP)
    - app/src/main/java/org/sentinela/app/domain/CallDecision.kt (Allow/Silence/Reject/SendSilentlyToVoicemail/BlockWithoutTrace)
  </read_first>
  <behavior>
    Estado medido em 2026-07-29: 20 `@Test` verdes cobrem saida, protecao off, privado (bloqueado/permitido),
    contato RING/BLOCK/SILENCE, contato > whitelist, whitelist default/SILENCE, fallback ALLOW/BLOCK,
    contatos UNAVAILABLE, whitelist hit com contatos indisponiveis, desconhecido sem rastro / reject
    simples / voicemail / SILENCE / RING, e numero invalido.

    LACUNAS reais da matriz `OriginPolicy` (4) x origem (3) — cobrir cada uma com um `@Test`:
    - `contato com politica nunca silenciar toca`: contacts=NEVER_SILENCE, ContactLookup.HIT
      -> `CallDecision.Allow` com `DecisionReason.CONTACT`
    - `whitelist com politica tocar e permitida`: whitelist=RING, WhitelistLookup.HIT
      -> `Allow` com `DecisionReason.PERSONAL_WHITELIST`
    - `whitelist com politica bloquear e bloqueada`: whitelist=BLOCK, WhitelistLookup.HIT,
      `hideFromNativeCallLog = true` (default) -> `BlockWithoutTrace` com `PERSONAL_WHITELIST`
    - `desconhecido com politica nunca silenciar toca`: unknown=NEVER_SILENCE
      -> `Allow` com `DecisionReason.UNKNOWN_NUMBER`
    Se algum desses casos ja existir com outro nome, NAO duplicar — registrar no SUMMARY qual ja cobria.
  </behavior>
  <action>
Acrescentar ao `CallDecisionEngineTest.kt` os `@Test` descritos em `<behavior>` que ainda nao
existem, reaproveitando os helpers ja presentes (`defaults`, `incoming()`, `decide(...)`) e
mantendo o estilo do arquivo: nome em backticks, em portugues SEM acento (identificador),
inseridos na secao da precedencia correspondente (contato, whitelist, desconhecido).

Fecha o criterio 4 do ROADMAP na letra: "precedencia (incluindo politicas por origem) coberta
por testes unitarios" — hoje `NEVER_SILENCE` em contatos/desconhecidos e `RING`/`BLOCK` na
whitelist nao tem nenhuma assercao. Lembrar do contrato do motor: `RING` e `NEVER_SILENCE`
produzem AMBOS `Allow` (o bypass de Nao Perturbe e da camada telecom, nao do motor).

Regras nao negociaveis desta task:
- NAO alterar `CallDecisionEngine.kt`, `CallDecision.kt`, `DecisionReason.kt`, `ScreenedCall.kt`
  nem `ScreeningSettings.kt`. Se um teste novo falhar, o teste esta errado — o motor tem 20
  testes verdes e mudanca de comportamento e escopo da Phase 2.
- NAO mockar o motor: `CallDecisionEngine` e puro, instanciar direto (o `engine` ja existe).
- NAO importar nada de `android.*` no teste — o dominio nao conhece Telecom (CLAUDE.md).
- NAO usar numero de telefone real: manter o fake `+5511912341234` ja usado no arquivo.
- NAO adicionar dependencia ao `build.gradle.kts` nem ao `gradle/libs.versions.toml`.
  </action>
  <verify>
    <automated>export ANDROID_HOME="${ANDROID_HOME:-$HOME/Library/Android/sdk}"; ./gradlew testDebugUnitTest --tests "*CallDecisionEngine*" detekt && test "$(grep -c '@Test' app/src/test/java/org/sentinela/app/domain/CallDecisionEngineTest.kt)" -ge 24</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew testDebugUnitTest --tests "*CallDecisionEngine*"` termina `BUILD SUCCESSFUL`
    - `grep -c '@Test' app/src/test/java/org/sentinela/app/domain/CallDecisionEngineTest.kt` retorna `>= 24` (eram 20)
    - `grep -c 'NEVER_SILENCE' app/src/test/java/org/sentinela/app/domain/CallDecisionEngineTest.kt` retorna `>= 2`
    - `grep -c 'whitelistPolicy = OriginPolicy.RING\|whitelistPolicy = OriginPolicy.BLOCK' app/src/test/java/org/sentinela/app/domain/CallDecisionEngineTest.kt` retorna `>= 2`
    - `grep -c 'BlockWithoutTrace' app/src/test/java/org/sentinela/app/domain/CallDecisionEngineTest.kt` retorna `>= 1`
    - `git diff --stat app/src/main/java/org/sentinela/app/domain/` esta vazio (motor intocado)
    - `git diff --stat app/src/main/java/org/sentinela/app/settings/` esta vazio
    - `grep -c 'import android' app/src/test/java/org/sentinela/app/domain/CallDecisionEngineTest.kt` retorna `0`
    - `grep -c '<error' app/build/reports/detekt/detekt.xml` retorna `0`
  </acceptance_criteria>
  <done>As 4 politicas de `OriginPolicy` estao cobertas nas tres origens e as variantes de bloqueio incluem `BlockWithoutTrace` na whitelist; >= 24 `@Test` verdes, motor e settings intocados, detekt em 0 issues.</done>
</task>

</tasks>


<verification>
```bash
export ANDROID_HOME="${ANDROID_HOME:-$HOME/Library/Android/sdk}"
./gradlew assembleDebug testDebugUnitTest detekt

# Invariantes de manifest, rebranding e dominio puro
bash scripts/verify-invariants.sh          # exit 0, "== todos os invariantes OK =="

# Tokens do tema cobertos por teste
grep -o 'tests="[0-9]*" skipped="[0-9]*" failures="[0-9]*" errors="[0-9]*"' \
  app/build/test-results/testDebugUnitTest/*.xml
```
</verification>

<success_criteria>
- `scripts/verify-invariants.sh` executavel, sai 0 no repo limpo e 1 com permissao de fase
  futura injetada (teste negativo comprovado e revertido).
- A checagem de permissoes usa allowlist sobre o manifest MERGEADO, nunca contagem sobre o fonte.
- `POST_NOTIFICATIONS` permanece no manifest (autorizada por `docs/PERMISSOES.md`).
- `ThemeTokensTest` verde e matriz de precedencia completa; suite total >= 27 testes, 0 failures, 0 errors.
- `Theme.kt` alterado em exatamente uma linha (`private` -> `internal`); `Color.kt` intocado.
- detekt continua em 0 issues.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundacao-compilavel/01-02-SUMMARY.md`
</output>
