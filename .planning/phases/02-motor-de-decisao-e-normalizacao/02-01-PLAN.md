---
phase: 02-motor-de-decisao-e-normalizacao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gradle.properties
  - gradle/libs.versions.toml
  - app/build.gradle.kts
  - app/src/test/java/org/sentinela/app/phone/TestMetadata.kt
  - app/src/test/java/org/sentinela/app/phone/TestMetadataSentinelTest.kt
  - scripts/verify-invariants.sh
autonomous: true
requirements: [QLT-07, NRM-01, DEC-01]

must_haves:
  truths:
    - "O build sobrevive ao plugin Kover sem estourar Metaspace"
    - "Um teste JVM puro consegue construir PhoneNumberUtil com metadados reais, sem Robolectric"
    - "O invariante de pureza cobre phone/ além de domain/"
  artifacts:
    - path: "gradle.properties"
      provides: "MaxMetaspaceSize=1g"
      contains: "MaxMetaspaceSize=1g"
    - path: "app/src/test/java/org/sentinela/app/phone/TestMetadata.kt"
      provides: "MetadataLoader de teste + fábrica de PhoneNumberUtil"
      exports: ["TestMetadata"]
    - path: "scripts/verify-invariants.sh"
      provides: "Bloco 3 estendido a phone/"
      contains: "phone/"
  key_links:
    - from: "app/build.gradle.kts"
      to: "libs.plugins.kover"
      via: "alias no bloco plugins"
      pattern: "alias\\(libs\\.plugins\\.kover\\)"
    - from: "app/src/test/java/org/sentinela/app/phone/TestMetadata.kt"
      to: "com/android/tools/test_config.properties"
      via: "classLoader.getResourceAsStream + chave android_merged_assets"
      pattern: "android_merged_assets"
---

<objective>
Preparar a infraestrutura de validação da Fase 2: Kover instalado (sem gate ainda),
Metaspace suficiente, fixture de metadados do libphonenumber para testes JVM puros, e o
invariante de pureza estendido ao pacote `phone/`.

Purpose: nenhum teste de normalização roda sem o `MetadataLoader` de teste, e o plugin Kover
mata o build com 512m de Metaspace. Este plano é o desbloqueio de tudo o mais.
Output: build com Kover, `TestMetadata` verde e `verify-invariants.sh` cobrindo `phone/`.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-motor-de-decisao-e-normalizacao/02-CONTEXT.md
@.planning/phases/02-motor-de-decisao-e-normalizacao/02-RESEARCH.md
@.planning/phases/02-motor-de-decisao-e-normalizacao/02-VALIDATION.md

<interfaces>
<!-- Assinaturas do artefato io.michaelrocks:libphonenumber-android:9.0.34 (verificadas por javap na pesquisa) -->
```kotlin
package io.michaelrocks.libphonenumber.android

fun interface MetadataLoader { fun loadMetadata(phoneMetadataResource: String): java.io.InputStream? }

object PhoneNumberUtil {
    @JvmStatic fun createInstance(metadataLoader: MetadataLoader): PhoneNumberUtil
    @JvmStatic fun createInstance(context: android.content.Context): PhoneNumberUtil
}
```
`ShortNumberInfo` NÃO tem factory pública neste artefato — não tente usá-lo.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kover 0.9.9 no catálogo + build, com Metaspace de 1g</name>
  <files>gradle.properties, gradle/libs.versions.toml, app/build.gradle.kts</files>
  <read_first>
    - gradle.properties (linha `org.gradle.jvmargs`)
    - gradle/libs.versions.toml (blocos `[versions]` e `[plugins]`)
    - app/build.gradle.kts (bloco `plugins`, bloco `lint`)
    - .planning/phases/02-motor-de-decisao-e-normalizacao/02-RESEARCH.md §"Pattern 4" e §"Pitfall 1"
  </read_first>
  <action>
    1. `gradle.properties`: trocar a linha
       `org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError`
       por
       `org.gradle.jvmargs=-Xmx3072m -XX:MaxMetaspaceSize=1g -XX:+HeapDumpOnOutOfMemoryError`
       Acrescentar comentário acima: `# 1g de Metaspace e obrigatorio com o plugin Kover (Fase 2) — com 512m o build morre em "Failed to notify build listener > Metaspace"`.
    2. `gradle/libs.versions.toml`: em `[versions]` adicionar `kover = "0.9.9"`; em `[plugins]`
       adicionar `kover = { id = "org.jetbrains.kotlinx.kover", version.ref = "kover" }`.
       NÃO adicionar `org.jetbrains.kotlin.android` (AGP 9 tem Kotlin embutido — quebra o build).
    3. `app/build.gradle.kts`: adicionar `alias(libs.plugins.kover)` como última linha do bloco
       `plugins { }`. Adicionar, após o bloco `detekt { }`, o bloco:
       ```kotlin
       // Cobertura da Fase 2. O gate (verify { rule { minBound(80) } }) so entra no plano 02-05,
       // depois que os testes de normalizacao existirem — habilitar antes falharia o build.
       kover {
           reports {
               filters {
                   includes { classes("org.sentinela.app.domain.*", "org.sentinela.app.phone.*") }
               }
           }
       }
       ```
       Usar exatamente a DSL 0.9.x (`kover { reports { ... } }`); a DSL antiga `koverReport {}` não compila.
    4. NÃO remover `testOptions.unitTests.isIncludeAndroidResources = true` — é pré-requisito do
       carregamento de metadados em teste.
  </action>
  <verify>
    <automated>export ANDROID_HOME="${ANDROID_HOME:-$HOME/Library/Android/sdk}"; ./gradlew testDebugUnitTest koverLog</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'MaxMetaspaceSize=1g' gradle.properties` retorna 0
    - `grep -q '^kover = "0.9.9"' gradle/libs.versions.toml` retorna 0
    - `grep -q 'org.jetbrains.kotlinx.kover' gradle/libs.versions.toml` retorna 0
    - `grep -q 'alias(libs.plugins.kover)' app/build.gradle.kts` retorna 0
    - `grep -q 'org.sentinela.app.phone.\*' app/build.gradle.kts` retorna 0
    - `grep -q 'org.jetbrains.kotlin.android' gradle/libs.versions.toml app/build.gradle.kts` retorna 1 (ausente)
    - `./gradlew koverLog` termina com `BUILD SUCCESSFUL` e imprime uma linha de percentual de cobertura
    - `./gradlew tasks --all | grep -q koverVerify` retorna 0
  </acceptance_criteria>
  <done>`./gradlew testDebugUnitTest koverLog` verde, sem erro de Metaspace, com o filtro cobrindo domain + phone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TestMetadata — MetadataLoader de teste em JVM puro</name>
  <files>app/src/test/java/org/sentinela/app/phone/TestMetadata.kt, app/src/test/java/org/sentinela/app/phone/TestMetadataSentinelTest.kt</files>
  <read_first>
    - .planning/phases/02-motor-de-decisao-e-normalizacao/02-RESEARCH.md §"Pattern 1" (código medido) e §"Pitfall 2"
    - app/build.gradle.kts (`testOptions.unitTests.isIncludeAndroidResources`)
    - app/src/main/java/org/sentinela/app/phone/PhoneNumberNormalizer.kt
  </read_first>
  <behavior>
    - `TestMetadata.util()` devolve um `PhoneNumberUtil` com metadados reais carregados.
    - Sentinela: `util.isValidNumber(util.parse("+5511987654321", null))` == true.
    - Sentinela: `util.format(util.parse("11987654321", "BR"), E164)` == `"+5511987654321"`.
    - Se os metadados não forem encontrados, o helper falha alto (`IllegalStateException` com
      mensagem explícita), nunca devolve loader vazio silencioso.
  </behavior>
  <action>
    Criar `app/src/test/java/org/sentinela/app/phone/TestMetadata.kt` (package `org.sentinela.app.phone`):
    ```kotlin
    object TestMetadata {
        private const val CONFIG = "com/android/tools/test_config.properties"
        private const val FALLBACK_ASSETS = "build/intermediates/assets/debug/mergeDebugAssets"
        private const val SENTINEL = "io/michaelrocks/libphonenumber/android/data/PhoneNumberMetadataProto_BR"

        private val assetsDir: File by lazy {
            val stream = TestMetadata::class.java.classLoader?.getResourceAsStream(CONFIG)
            val dir = if (stream != null) {
                val props = java.util.Properties()
                stream.use { props.load(it) }
                File(checkNotNull(props.getProperty("android_merged_assets")) {
                    "android_merged_assets ausente em $CONFIG (AGP mudou a chave?)"
                })
            } else {
                File(FALLBACK_ASSETS)
            }
            check(File(dir, SENTINEL).exists()) {
                "Metadados do libphonenumber nao encontrados em $dir. " +
                    "Exige testOptions.unitTests.isIncludeAndroidResources = true e ./gradlew mergeDebugAssets."
            }
            dir
        }

        val loader = MetadataLoader { name ->
            File(assetsDir, name.removePrefix("/")).takeIf { it.exists() }?.inputStream()
        }

        fun util(): PhoneNumberUtil = PhoneNumberUtil.createInstance(loader)
    }
    ```
    Observações obrigatórias: `android_merged_assets` vem **relativo ao diretório do módulo**
    (`app/`), que é o working dir do teste — não converter para absoluto assumindo a raiz do repo.
    Criar `TestMetadataSentinelTest.kt` com os 3 asserts do bloco `<behavior>` (incluindo o
    formato E164 a partir de `("11987654321", "BR")`).
  </action>
  <verify>
    <automated>export ANDROID_HOME="${ANDROID_HOME:-$HOME/Library/Android/sdk}"; ./gradlew testDebugUnitTest --tests "*TestMetadataSentinelTest"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/src/test/java/org/sentinela/app/phone/TestMetadata.kt`
    - `grep -q 'android_merged_assets' app/src/test/java/org/sentinela/app/phone/TestMetadata.kt`
    - `grep -q 'PhoneNumberMetadataProto_BR' app/src/test/java/org/sentinela/app/phone/TestMetadata.kt`
    - `grep -q 'org.robolectric' app/src/test/java/org/sentinela/app/phone/TestMetadata.kt` retorna 1 (ausente)
    - `grep -q 'createInstance(context' app/src/test/java/org/sentinela/app/phone/TestMetadata.kt` retorna 1 (ausente)
    - `[ "$(grep -c '@Test' app/src/test/java/org/sentinela/app/phone/TestMetadataSentinelTest.kt)" -ge 3 ]`
    - `./gradlew testDebugUnitTest --tests "*TestMetadataSentinelTest"` termina `BUILD SUCCESSFUL`
    - `app/build/test-results/testDebugUnitTest/TEST-org.sentinela.app.phone.TestMetadataSentinelTest.xml` existe com `failures="0"`
  </acceptance_criteria>
  <done>Fixture compartilhada disponível para todos os testes de normalização; sentinela prova que os metadados reais carregaram.</done>
</task>

<task type="auto">
  <name>Task 3: Invariante de pureza estendido a phone/</name>
  <files>scripts/verify-invariants.sh</files>
  <read_first>
    - scripts/verify-invariants.sh (Bloco 3 — dominio puro; e o comentário de topo sobre `grep -c` sair 1)
    - .planning/phases/02-motor-de-decisao-e-normalizacao/02-RESEARCH.md §"Estrutura de arquivos proposta"
    - docs/PERMISSOES.md
  </read_first>
  <action>
    No Bloco 3 de `scripts/verify-invariants.sh`, substituir a checagem única de `domain/` por um
    laço sobre `domain/` **e** `phone/`:
    ```bash
    for pkg in domain phone; do
      DIR="app/src/main/java/org/sentinela/app/$pkg"
      [ -d "$DIR" ] || { skip "pacote $pkg ausente"; continue; }
      IMPORTS=$(grep -rn "^import android" "$DIR")
      if [ -z "$IMPORTS" ]; then
        ok "$pkg sem import de android.*"
      else
        echo "$IMPORTS" | sed 's/^/      /'
        fail "$pkg importa android.* — regra de decisao e normalizacao devem ser JVM puras"
      fi
    done
    ```
    Atualizar o cabeçalho do Bloco 3 para `== Bloco 3: dominio e normalizacao puros ==`.
    NÃO usar `set -e` nem `grep -c ... || echo 0` (armadilhas documentadas no topo do script).
    Não alterar a allowlist de permissões: esta fase não adiciona permissão alguma
    (a cascata de região usa `simCountryIso`/`networkCountryIso`, que não exigem `READ_PHONE_STATE`).
  </action>
  <verify>
    <automated>export ANDROID_HOME="${ANDROID_HOME:-$HOME/Library/Android/sdk}"; ./gradlew assembleDebug lint detekt && bash scripts/verify-invariants.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'for pkg in domain phone' scripts/verify-invariants.sh`
    - `bash scripts/verify-invariants.sh` sai com código 0 e imprime `ok:   domain sem import de android.*` e `ok:   phone sem import de android.*`
    - `bash scripts/verify-invariants.sh` imprime `== todos os invariantes OK ==`
    - Teste negativo: criar `app/src/main/java/org/sentinela/app/phone/_Tmp.kt` com `import android.content.Context` faz o script sair com código 1; apagar o arquivo em seguida
    - `grep -q 'READ_PHONE_STATE' app/src/main/AndroidManifest.xml` retorna 1 (ausente)
  </acceptance_criteria>
  <done>O script falha de verdade se alguém puser Android dentro de `phone/`, comprovado pelo teste negativo.</done>
</task>

</tasks>

<verification>
```bash
export ANDROID_HOME="${ANDROID_HOME:-$HOME/Library/Android/sdk}"
./gradlew testDebugUnitTest koverLog lint detekt && bash scripts/verify-invariants.sh
```
</verification>

<success_criteria>
- Kover 0.9.9 aplicado, `koverLog` imprime cobertura de `domain` + `phone` (baseline esperado ~94%)
- `MaxMetaspaceSize=1g` no `gradle.properties`
- `TestMetadataSentinelTest` verde sem Robolectric
- `verify-invariants.sh` cobre `phone/` e falha no teste negativo
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/02-motor-de-decisao-e-normalizacao/02-01-SUMMARY.md`
</output>
