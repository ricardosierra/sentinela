---
phase: 02-motor-de-decisao-e-normalizacao
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-03"]
files_modified:
  - app/src/main/java/org/sentinela/app/phone/PhoneNumbers.kt
  - app/src/main/java/org/sentinela/app/phone/PhoneMask.kt
  - app/src/main/java/org/sentinela/app/phone/PhoneNumberNormalizer.kt
  - app/src/main/java/org/sentinela/app/phone/LibPhoneNumberNormalizer.kt
  - app/src/test/java/org/sentinela/app/phone/PhoneMaskTest.kt
  - app/src/test/java/org/sentinela/app/phone/LibPhoneNumberNormalizerTest.kt
  - app/src/test/java/org/sentinela/app/phone/BrazilianRulesNormalizerTest.kt
autonomous: true
requirements: [NRM-01, NRM-02, NRM-03, NRM-04, QLT-01]

must_haves:
  truths:
    - "Número BR e internacional viram E.164 correto; número sem DDD é rejeitado com razão explícita"
    - "Celular BR antigo de 8 dígitos ganha o 9 e só é aceito se libphonenumber revalidar"
    - "Número curto (190) é Valid carregando os dígitos crus, nunca um E.164 falso"
    - "A máscara nunca revela o número nacional completo e nunca lança exceção"
  artifacts:
    - path: "app/src/main/java/org/sentinela/app/phone/PhoneNumbers.kt"
      provides: "LIMIAR_CURTO — fonte única compartilhada por normalização e máscara"
      contains: "LIMIAR_CURTO"
    - path: "app/src/main/java/org/sentinela/app/phone/PhoneMask.kt"
      provides: "Máscara única para log e UI"
      contains: "fun mask"
    - path: "app/src/main/java/org/sentinela/app/phone/LibPhoneNumberNormalizer.kt"
      provides: "Implementação real do PhoneNumberNormalizer"
      exports: ["LibPhoneNumberNormalizer"]
      min_lines: 60
  key_links:
    - from: "app/src/main/java/org/sentinela/app/phone/LibPhoneNumberNormalizer.kt"
      to: "io.michaelrocks.libphonenumber.android.PhoneNumberUtil"
      via: "instância injetada no construtor (nunca createInstance(Context))"
      pattern: "PhoneNumberUtil"
    - from: "app/src/main/java/org/sentinela/app/phone/LibPhoneNumberNormalizer.kt"
      to: "org.sentinela.app.phone.RegionProvider"
      via: "resolução da região quando o número vem sem DDI"
      pattern: "regionProvider"
    - from: "app/src/main/java/org/sentinela/app/phone/LibPhoneNumberNormalizer.kt"
      to: "org.sentinela.app.phone.PhoneMask"
      via: "delegação de mask()"
      pattern: "PhoneMask"
---

<objective>
Substituir o stub de `PhoneNumberNormalizer` pela implementação real com libphonenumber-android:
normalização E.164 (BR e internacional), a regra brasileira do 9º dígito feita à mão com
guarda-corpo, o tratamento de números curtos por dígitos crus, e a máscara única de exibição.

Purpose: é o coração técnico da fase. libphonenumber resolve 90% dos casos; os 10% restantes
(9º dígito e números curtos) foram provados na pesquisa como responsabilidade da aplicação.
Output: `PhoneNumbers`, `PhoneMask`, `LibPhoneNumberNormalizer` + 3 suítes de teste.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-motor-de-decisao-e-normalizacao/02-CONTEXT.md
@.planning/phases/02-motor-de-decisao-e-normalizacao/02-RESEARCH.md
@app/src/main/java/org/sentinela/app/phone/PhoneNumberNormalizer.kt
@app/src/test/java/org/sentinela/app/phone/TestMetadata.kt
@CLAUDE.md

<interfaces>
<!-- Existente (a ser evoluído por este plano) -->
```kotlin
package org.sentinela.app.phone
interface PhoneNumberNormalizer {
    fun normalize(raw: String, defaultRegion: String = "BR"): NormalizationResult
    fun mask(e164: String): String
}
sealed interface NormalizationResult {
    data class Valid(val e164: String) : NormalizationResult
    data class Invalid(val reason: String) : NormalizationResult
}
```
<!-- Criado no plano 02-03 -->
```kotlin
fun interface RegionProvider { fun currentRegion(): String? }
class CascadingRegionProvider(...) : RegionProvider { companion object { const val DEFAULT_REGION = "BR" } }
```
<!-- Fixture criada no plano 02-01 -->
```kotlin
object TestMetadata { val loader: MetadataLoader; fun util(): PhoneNumberUtil }
```
<!-- API do libphonenumber-android 9.0.34 usada aqui -->
```java
PhoneNumber parse(CharSequence numberToParse, String defaultRegion) throws NumberParseException
boolean isValidNumber(PhoneNumber n)
boolean isPossibleNumber(PhoneNumber n)
PhoneNumberUtil.ValidationResult isPossibleNumberWithReason(PhoneNumber n)   // TOO_SHORT
PhoneNumberType getNumberType(PhoneNumber n)                                 // MOBILE, TOLL_FREE...
String format(PhoneNumber n, PhoneNumberFormat.E164)
String getNationalSignificantNumber(PhoneNumber n)
int getLengthOfNationalDestinationCode(PhoneNumber n)   // 0 em numero invalido
static String normalizeDigitsOnly(CharSequence number)
```
</interfaces>

<measured_behavior>
<!-- Tabela MEDIDA na pesquisa (2026-07-29) com este artefato. É a fonte dos casos de teste. -->
| Entrada | Região | E.164 | isValid | numberType |
|---|---|---|---|---|
| `11987654321` | BR | `+5511987654321` | true | MOBILE |
| `1133334444` | BR | `+551133334444` | true | FIXED_LINE |
| `987654321` (sem DDD) | BR | `+55987654321` | **false** | UNKNOWN |
| `1187654321` (celular sem o 9) | BR | `+551187654321` | **false** | UNKNOWN |
| `190` | BR | `+55190` | **false** (TOO_SHORT, possible=false) | UNKNOWN |
| `0800 123 4567` | BR | `+558001234567` | true | TOLL_FREE |
| `40041234` | BR | `+5540041234` | true | SHARED_COST |
| `+1 212 555 0123` | BR | `+12125550123` | true | FIXED_LINE_OR_MOBILE |
| `+44 20 7183 8750` | BR | `+442071838750` | true | FIXED_LINE |
| `2125550123` | US | `+12125550123` | true | FIXED_LINE_OR_MOBILE |
| `abc` / `""` | BR | — | NumberParseException(NOT_A_NUMBER) | — |
| `+999999` | BR | — | NumberParseException(INVALID_COUNTRY_CODE) | — |

ndcLen medido: `+5511987654321`→2, `+551133334444`→2, `+12125550123`→3, `+442071838750`→2,
`+558001234567`→3, `+5540041234`→4, qualquer inválido→**0**.
</measured_behavior>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PhoneNumbers.LIMIAR_CURTO + PhoneMask (máscara única para log e UI)</name>
  <files>app/src/main/java/org/sentinela/app/phone/PhoneNumbers.kt, app/src/main/java/org/sentinela/app/phone/PhoneMask.kt, app/src/test/java/org/sentinela/app/phone/PhoneMaskTest.kt</files>
  <read_first>
    - CLAUDE.md §"Privacidade" (formato canônico `+55 11 9****-1234`; nunca número completo em log)
    - .planning/phases/02-motor-de-decisao-e-normalizacao/02-CONTEXT.md §"Máscara de exibição"
    - .planning/phases/02-motor-de-decisao-e-normalizacao/02-RESEARCH.md §"Pattern 3" e §"Pitfall 3"
    - app/src/test/java/org/sentinela/app/phone/TestMetadata.kt
  </read_first>
  <behavior>
    Constante única: `PhoneNumbers.LIMIAR_CURTO = 6` (dígitos), usada por máscara **e** normalização.
    `PhoneMask.mask(util, value)`:
    - `+5511987654321` → `"+55 11 9****-1234"` (formato canônico do CLAUDE.md, exato)
    - `+551133334444` → `"+55 11 3****-4444"`
    - `+12125550123` → `"+1 212 5****-0123"`
    - `+442071838750` → `"+44 20 2****-8750"`
    - `+558001234567` (0800) → `"+55 800 1****-4567"`
    - `"190"` (< LIMIAR_CURTO dígitos) → `"190"` — **exibido na íntegra, decisão do usuário**
    - `"911"` → `"911"`
    - `"123456"` (6 dígitos == LIMIAR_CURTO, **não** é `<`) → NÃO exibido na íntegra; segue o
      caminho normal de máscara. Teste de fronteira obrigatório, espelhando o equivalente em
      `BrazilianRulesNormalizerTest`.
    - `+55987654321` (ndc == 0, inválido) → forma degradada `"+55 ****-4321"`, sem exceção
    - `"abc"`, `""`, `"+999999"`, `null`-ish → `PhoneMask.MASCARA_GENERICA` = `"+** ****"`; nunca ecoa a entrada crua
    Propriedade obrigatória, aplicada a TODAS as entradas de teste com mais de `LIMIAR_CURTO` dígitos:
    - `assertFalse(masked.contains(nsnCompleto))`
    - a quantidade de dígitos expostos em `masked` é ≤ (dígitos do country code + ndcLen + 5)
    Invariante: `mask` NUNCA lança — teste que percorre uma lista de entradas hostis
    (`""`, `" "`, `"+"`, `"++55"`, `"55 11"`, `"٠١"`, string de 400 caracteres) e apenas
    afirma que não houve exceção e que a saída não contém a entrada crua.
  </behavior>
  <action>
    Criar `phone/PhoneNumbers.kt`:
    ```kotlin
    package org.sentinela.app.phone

    /** Constantes compartilhadas por normalizacao e mascara. Fonte unica — nao duplicar. */
    object PhoneNumbers {
        /**
         * Abaixo deste numero de digitos o valor e tratado como codigo curto de servico
         * (190, 911): a chave e o digito cru, nao um E.164, e a exibicao e integral.
         * Decisao do usuario (02-CONTEXT.md): "essas mascaras nao podem atrapalhar o usuario" —
         * numero publico de servico nao e dado pessoal.
         */
        const val LIMIAR_CURTO = 6
    }
    ```
    Criar `phone/PhoneMask.kt` como `object PhoneMask` com
    `const val MASCARA_GENERICA = "+** ****"` e
    `fun mask(util: PhoneNumberUtil, value: String): String`, implementando o algoritmo do
    §Pattern 3 da pesquisa, nesta ordem:
    1. `runCatching { util.parse(value, null) }` — em falha, devolver `MASCARA_GENERICA`.
    2. `digits = util.getNationalSignificantNumber(n)`; vazio → `MASCARA_GENERICA`.
    3. Se `util.normalizeDigitsOnly(value).length < PhoneNumbers.LIMIAR_CURTO` → devolver os
       dígitos crus na íntegra. Comparação **estritamente menor** (`<`), idêntica à do
       normalizer em Task 3 — a constante só é fonte única se os dois lados usarem o mesmo
       operador. `"123456"` (6 dígitos) **não** é curto e é mascarado normalmente.
    4. `ndc = util.getLengthOfNationalDestinationCode(n)`; se `ndc == 0` → `"+${n.countryCode} ****-${digits.takeLast(4)}"`.
    5. `resto = digits.substring(ndc)`; se `resto.length < 5` → mesma forma degradada do passo 4.
    6. `"+${n.countryCode} ${digits.substring(0, ndc)} ${resto.first()}****-${resto.takeLast(4)}"`.
    O corpo inteiro dentro de um `runCatching { ... }.getOrDefault(MASCARA_GENERICA)` — a máscara
    roda em caminho de log e uma exceção lá derruba o Service (§Anti-Patterns).
    UMA única função de máscara serve log e UI (decisão do usuário) — não criar variante.
    Criar `PhoneMaskTest.kt` usando `TestMetadata.util()`, com um `@Test` por linha do
    `<behavior>` mais o teste de propriedade e o de entradas hostis.
  </action>
  <verify>
    <automated>export ANDROID_HOME="${ANDROID_HOME:-$HOME/Library/Android/sdk}"; ./gradlew testDebugUnitTest --tests "*PhoneMaskTest" detekt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'const val LIMIAR_CURTO = 6' app/src/main/java/org/sentinela/app/phone/PhoneNumbers.kt`
    - `grep -q 'PhoneNumbers.LIMIAR_CURTO' app/src/main/java/org/sentinela/app/phone/PhoneMask.kt` (usa a constante, não o literal 6)
    - `grep -q 'MASCARA_GENERICA' app/src/main/java/org/sentinela/app/phone/PhoneMask.kt`
    - `grep -q 'getLengthOfNationalDestinationCode' app/src/main/java/org/sentinela/app/phone/PhoneMask.kt`
    - `grep -rq '^import android' app/src/main/java/org/sentinela/app/phone/` retorna 1 (ausente)
    - `[ "$(grep -c '@Test' app/src/test/java/org/sentinela/app/phone/PhoneMaskTest.kt)" -ge 12 ]`
    - `grep -q '123456' app/src/test/java/org/sentinela/app/phone/PhoneMaskTest.kt` (teste de fronteira do limiar)
    - `grep -q '< PhoneNumbers.LIMIAR_CURTO' app/src/main/java/org/sentinela/app/phone/PhoneMask.kt` (estritamente menor, igual ao normalizer)
    - `grep -q '+55 11 9\*\*\*\*-1234' app/src/test/java/org/sentinela/app/phone/PhoneMaskTest.kt` (formato canônico assertado literalmente)
    - `TEST-org.sentinela.app.phone.PhoneMaskTest.xml` com `failures="0"` `errors="0"`
  </acceptance_criteria>
  <done>Máscara única, generalizada a qualquer DDI, sem exceção em nenhuma entrada e provada por teste de propriedade.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LibPhoneNumberNormalizer — normalização E.164 BR e internacional</name>
  <files>app/src/main/java/org/sentinela/app/phone/PhoneNumberNormalizer.kt, app/src/main/java/org/sentinela/app/phone/LibPhoneNumberNormalizer.kt, app/src/test/java/org/sentinela/app/phone/LibPhoneNumberNormalizerTest.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/phone/PhoneNumberNormalizer.kt (interface atual, com `defaultRegion: String = "BR"`)
    - app/src/main/java/org/sentinela/app/phone/CascadingRegionProvider.kt (plano 02-03)
    - app/src/main/java/org/sentinela/app/phone/PhoneMask.kt (Task 1)
    - .planning/phases/02-motor-de-decisao-e-normalizacao/02-RESEARCH.md §"Pattern 1", §"Anti-Patterns", tabela medida
  </read_first>
  <behavior>
    Assinatura evoluída (a região deixa de ser hardcoded em `"BR"`):
    `fun normalize(raw: String, region: String? = null): NormalizationResult` — quando `region`
    é null, usa `regionProvider.currentRegion()`.
    Casos (todos da tabela medida):
    - `("11987654321")` região BR → `Valid("+5511987654321")`
    - `("+55 11 98765-4321")` → `Valid("+5511987654321")`
    - `("1133334444")` BR → `Valid("+551133334444")`
    - `("0800 123 4567")` BR → `Valid("+558001234567")`
    - `("40041234")` BR → `Valid("+5540041234")`
    - `("+1 212 555 0123")` região BR → `Valid("+12125550123")` (DDI explícito vence a região)
    - `("+44 20 7183 8750")` → `Valid("+442071838750")`
    - `("2125550123", region = "US")` → `Valid("+12125550123")`
    - `("987654321")` BR (sem DDD) → `Invalid` com `reason` contendo `"sem_ddd"` ou `"invalido"`,
      **nunca** `Valid` — inferir DDD envenena a whitelist
    - `("abc")` / `("")` → `Invalid(reason contendo "nao_e_numero")`
    - `("+999999")` → `Invalid(reason contendo "ddi_invalido")`
    - Região resolvida pelo provider: fake devolvendo `"US"` + `("2125550123")` → `Valid("+12125550123")`
    - `mask()` delega a `PhoneMask` e devolve o formato canônico para `+5511987654321`
    Regra estrutural: `parse()` ter sucesso NUNCA é critério de validade — o gate é `isValidNumber`.
    `reason` de `Invalid` NUNCA contém o número (é usado em log): apenas códigos `[a-z_]+`.
  </behavior>
  <action>
    1. Editar `phone/PhoneNumberNormalizer.kt`: trocar
       `fun normalize(raw: String, defaultRegion: String = "BR"): NormalizationResult`
       por `fun normalize(raw: String, region: String? = null): NormalizationResult`, com KDoc
       explicando que `null` delega ao `RegionProvider`. Acrescentar ao KDoc de
       `NormalizationResult.Valid` que a chave é E.164 **exceto para códigos curtos**
       (< `PhoneNumbers.LIMIAR_CURTO` dígitos), cujo valor são os dígitos crus — contrato de dados
       que a Fase 3 vai persistir.
    2. Criar `phone/LibPhoneNumberNormalizer.kt`:
       ```kotlin
       class LibPhoneNumberNormalizer(
           private val util: PhoneNumberUtil,
           private val regionProvider: RegionProvider,
       ) : PhoneNumberNormalizer
       ```
       e a fábrica pura de nível de arquivo `fun phoneNumberUtil(loader: MetadataLoader): PhoneNumberUtil = PhoneNumberUtil.createInstance(loader)`.
       PROIBIDO: `PhoneNumberUtil.createInstance(context)` aqui (importaria `android.content.Context`
       e quebraria a pureza de `phone/`); PROIBIDO singleton global (a instância é criada e
       guardada pelo `AppContainer` no plano 02-05 — carregar metadados custa dezenas de ms e a
       Fase 5 tem orçamento p95 < 200 ms).
       `normalize`: resolver região (`region ?: regionProvider.currentRegion() ?: CascadingRegionProvider.DEFAULT_REGION`),
       `parse` dentro de try/catch de `NumberParseException` mapeando `errorType` para reason code
       (`NOT_A_NUMBER` → `"nao_e_numero"`, `INVALID_COUNTRY_CODE` → `"ddi_invalido"`,
       `TOO_SHORT_NSN`/demais → `"invalido"`), depois `isValidNumber` como gate e
       `util.format(n, E164)` no caminho feliz. Reason `"sem_ddd"` quando `isValidNumber` é false,
       a região resolvida é `"BR"` e o NSN tem menos dígitos que um número BR completo.
       Os ramos do 9º dígito e dos números curtos são da Task 3 — aqui deixar o caminho `Invalid`
       e o TODO explícito apontando para a Task 3.
       `mask(e164)` → `PhoneMask.mask(util, e164)`.
    3. Criar `LibPhoneNumberNormalizerTest.kt` com `TestMetadata.util()` e um fake
       `RegionProvider { "BR" }`, um `@Test` por caso do `<behavior>`, mais um teste que percorre
       todos os `Invalid` produzidos e assere `reason.matches(Regex("[a-z_]+"))`.
  </action>
  <verify>
    <automated>export ANDROID_HOME="${ANDROID_HOME:-$HOME/Library/Android/sdk}"; ./gradlew testDebugUnitTest --tests "*LibPhoneNumberNormalizerTest" detekt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'region: String? = null' app/src/main/java/org/sentinela/app/phone/PhoneNumberNormalizer.kt`
    - `grep -q 'defaultRegion' app/src/main/java/org/sentinela/app/phone/PhoneNumberNormalizer.kt` retorna 1 (assinatura antiga removida)
    - `grep -q 'createInstance(context' app/src/main/java/org/sentinela/app/phone/LibPhoneNumberNormalizer.kt` retorna 1 (ausente)
    - `grep -q 'isValidNumber' app/src/main/java/org/sentinela/app/phone/LibPhoneNumberNormalizer.kt`
    - `grep -q 'PhoneMask.mask' app/src/main/java/org/sentinela/app/phone/LibPhoneNumberNormalizer.kt`
    - `grep -rq '^import android' app/src/main/java/org/sentinela/app/phone/` retorna 1 (ausente)
    - `[ "$(grep -c '@Test' app/src/test/java/org/sentinela/app/phone/LibPhoneNumberNormalizerTest.kt)" -ge 14 ]`
    - `TEST-org.sentinela.app.phone.LibPhoneNumberNormalizerTest.xml` com `failures="0"` `errors="0"`
    - `bash scripts/verify-invariants.sh` sai 0 (após `./gradlew assembleDebug lint detekt`)
  </acceptance_criteria>
  <done>Normalização real substitui o stub: BR e internacional viram E.164 correto; sem DDD é rejeitado com razão sem dado pessoal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Regras brasileiras à mão — 9º dígito e códigos curtos</name>
  <files>app/src/main/java/org/sentinela/app/phone/LibPhoneNumberNormalizer.kt, app/src/test/java/org/sentinela/app/phone/BrazilianRulesNormalizerTest.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/phone/LibPhoneNumberNormalizer.kt (Task 2)
    - app/src/main/java/org/sentinela/app/phone/PhoneNumbers.kt (LIMIAR_CURTO)
    - .planning/phases/02-motor-de-decisao-e-normalizacao/02-CONTEXT.md §"Normalização" (as DUAS decisões do usuário tomadas após a pesquisa)
    - .planning/phases/02-motor-de-decisao-e-normalizacao/02-RESEARCH.md §"Open Questions" 1 e 2
  </read_first>
  <behavior>
    **Regra do 9º dígito (decisão do usuário: implementar à mão, com guarda-corpo).**
    Aplicar SOMENTE quando: `countryCode == 55` **e** NSN tem 10 dígitos **e** o primeiro dígito
    do assinante (NSN[2]) ∈ `6..9`. Inserir `9` após o DDD, re-parsear, e aceitar **apenas se**
    `isValidNumber == true` **e** `getNumberType == MOBILE`. Caso contrário → `Invalid`.
    - `("1187654321")` BR → `Valid("+5511987654321")`
    - `("+55 11 8765-4321")` → `Valid("+5511987654321")`
    - `("+55 11 2765-4321")` (assinante começa em 2, fixo) → NÃO corrigir; segue o caminho normal
    - Um NSN de 10 dígitos cuja correção **não** revalida → `Invalid(reason = "nono_digito_nao_revalida")`
      (teste explícito exigido pelo usuário)
    - `("+1 212 555 0123")` → a regra NÃO dispara (countryCode != 55)
    **Regra dos códigos curtos (decisão do usuário: dígitos crus, nunca E.164 falso).**
    Quando `util.normalizeDigitsOnly(raw).length < PhoneNumbers.LIMIAR_CURTO` e a entrada é só
    dígitos → `Valid(<dígitos crus>)`, antes de qualquer tentativa de E.164.
    - `("190")` BR → `Valid("190")` — nunca `Valid("+55190")`
    - `("911", region = "US")` → `Valid("911")`
    - `("193")` → `Valid("193")` e o resultado é whitelistável (mesma chave que o usuário digitou)
    - `("123456")` (6 dígitos == LIMIAR_CURTO, não é `<`) → NÃO é código curto; segue o caminho normal
    - `PhoneMask.mask` do mesmo valor devolve `"190"` na íntegra — **mesma constante** nos dois lados
    Teste de consistência obrigatório: para `"190"`, `normalize().e164` e `mask()` produzem a
    mesma string, provando que normalização e máscara compartilham `PhoneNumbers.LIMIAR_CURTO`.
  </behavior>
  <action>
    Em `LibPhoneNumberNormalizer.kt`, extrair duas funções privadas NOMEADAS, cada uma com KDoc
    explicando **por que existem** (a biblioteca não cobre o caso — comprovado por medição em
    02-RESEARCH.md):
    ```kotlin
    /**
     * Insere o 9 em celular BR antigo de 8 digitos. Existe porque os metadados BR atuais do
     * libphonenumber removeram o padrao de 8 digitos: `+55 11 8765-4321` retorna
     * isValid=false / type=UNKNOWN (medido em 2026-07-29). Guarda-corpo obrigatorio: o
     * resultado corrigido so e aceito se libphonenumber revalidar como MOBILE.
     */
    private fun corrigirNonoDigitoBr(parsed: PhoneNumber): NormalizationResult?

    /**
     * Codigos curtos (190, 911) nao tem E.164: `190`/BR vira `+55190`, isValid=false, TOO_SHORT,
     * e ShortNumberInfo e inconstruivel no port -android (construtor package-private).
     * Chave = digitos crus. Limiar compartilhado com a mascara: PhoneNumbers.LIMIAR_CURTO.
     */
    private fun codigoCurto(raw: String): NormalizationResult?
    ```
    Ordem em `normalize`: (1) `codigoCurto(raw)` antes de qualquer parse; (2) parse + `isValidNumber`;
    (3) se inválido, `corrigirNonoDigitoBr`; (4) `Invalid` com reason apropriado.
    Usar `PhoneNumbers.LIMIAR_CURTO` — nunca o literal `6`. Usar `util.normalizeDigitsOnly` para
    contar dígitos (trata dígitos não-ASCII), não `filter { it.isDigit() }`.
    Criar `BrazilianRulesNormalizerTest.kt` com um `@Test` por caso do `<behavior>`, incluindo o
    caso negativo do 9º dígito e o teste de consistência normalize×mask para `"190"`.
    Registrar no SUMMARY, para a Fase 3: **o formato de chave persistido é E.164, exceto códigos
    curtos, que são dígitos crus.**
  </action>
  <verify>
    <automated>export ANDROID_HOME="${ANDROID_HOME:-$HOME/Library/Android/sdk}"; ./gradlew testDebugUnitTest --tests "*BrazilianRulesNormalizerTest" --tests "*LibPhoneNumberNormalizerTest" --tests "*PhoneMaskTest" detekt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'corrigirNonoDigitoBr' app/src/main/java/org/sentinela/app/phone/LibPhoneNumberNormalizer.kt`
    - `grep -q 'codigoCurto' app/src/main/java/org/sentinela/app/phone/LibPhoneNumberNormalizer.kt`
    - `grep -q 'PhoneNumbers.LIMIAR_CURTO' app/src/main/java/org/sentinela/app/phone/LibPhoneNumberNormalizer.kt`
    - `grep -q 'ShortNumberInfo' app/src/main/java/org/sentinela/app/phone/LibPhoneNumberNormalizer.kt` só aparece em comentário (nunca como chamada) — confirmar por inspeção do match
    - `grep -q 'MOBILE' app/src/main/java/org/sentinela/app/phone/LibPhoneNumberNormalizer.kt` (guarda-corpo de revalidação)
    - `[ "$(grep -c '@Test' app/src/test/java/org/sentinela/app/phone/BrazilianRulesNormalizerTest.kt)" -ge 10 ]`
    - `grep -q '"+55190"' app/src/test/java/org/sentinela/app/phone/BrazilianRulesNormalizerTest.kt` (assert negativo explícito de que o E.164 falso NÃO é produzido)
    - `TEST-org.sentinela.app.phone.BrazilianRulesNormalizerTest.xml` com `failures="0"` `errors="0"`
    - `./gradlew koverLog` imprime percentual de linhas ≥ 80
  </acceptance_criteria>
  <done>As duas regras que libphonenumber não cobre estão isoladas, comentadas e testadas — inclusive nos casos negativos.</done>
</task>

</tasks>

<verification>
```bash
export ANDROID_HOME="${ANDROID_HOME:-$HOME/Library/Android/sdk}"
./gradlew assembleDebug testDebugUnitTest koverLog lint detekt && bash scripts/verify-invariants.sh
```
</verification>

<success_criteria>
- `PhoneNumberNormalizer` tem implementação real; o stub deixou de existir
- Todos os casos da tabela medida passam, incluindo internacional (+1, +44) e US como região
- 9º dígito corrigido só com revalidação MOBILE; caso negativo testado
- `190` é `Valid("190")`, whitelistável, exibido na íntegra pela mesma constante
- `phone/` sem `import android.`; máscara nunca lança nem revela o NSN completo
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/02-motor-de-decisao-e-normalizacao/02-04-SUMMARY.md`
</output>
