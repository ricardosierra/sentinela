---
phase: 02-motor-de-decisao-e-normalizacao
plan: 05
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03", "02-04"]
files_modified:
  - app/src/main/java/org/sentinela/app/platform/AssetsPhoneMetadataLoader.kt
  - app/src/main/java/org/sentinela/app/AppContainer.kt
  - app/build.gradle.kts
  - .planning/phases/02-motor-de-decisao-e-normalizacao/02-EVIDENCE.md
autonomous: true
requirements: [NRM-01, QLT-07, DEC-01]

must_haves:
  truths:
    - "O AppContainer entrega um PhoneNumberNormalizer real, pronto para a Fase 5 consumir"
    - "A construção do PhoneNumberUtil acontece fora do caminho quente da decisão"
    - "O build falha se a cobertura de domain/ + phone/ cair abaixo de 80%"
    - "A suíte completa passa pós-clean, com evidência arquivada"
  artifacts:
    - path: "app/src/main/java/org/sentinela/app/platform/AssetsPhoneMetadataLoader.kt"
      provides: "MetadataLoader de produção (assets do AAR)"
      contains: "AssetsMetadataLoader"
    - path: "app/src/main/java/org/sentinela/app/AppContainer.kt"
      provides: "phoneNumberNormalizer e regionProvider por lazy"
      contains: "phoneNumberNormalizer"
    - path: ".planning/phases/02-motor-de-decisao-e-normalizacao/02-EVIDENCE.md"
      provides: "Log pós-clean da suíte completa com contagem de tasks executadas"
      contains: "actionable tasks"
  key_links:
    - from: "app/src/main/java/org/sentinela/app/AppContainer.kt"
      to: "org.sentinela.app.phone.LibPhoneNumberNormalizer"
      via: "by lazy { LibPhoneNumberNormalizer(util, regionProvider) }"
      pattern: "LibPhoneNumberNormalizer\\("
    - from: "app/build.gradle.kts"
      to: ":app:koverVerify"
      via: "kover { reports { verify { rule { minBound(80) } } } }"
      pattern: "minBound\\(80\\)"
---

<objective>
Fechar a fase: ligar o normalizer real e a cascata de região no `AppContainer` (com o único
ponto Android de carregamento de metadados isolado em `platform/`), ativar o gate de cobertura
Kover em 80% e arquivar a evidência pós-`clean` da suíte completa.

Purpose: o gate só pode ser ligado agora — antes dos testes de normalização existirem ele
falharia o build. E sem o wiring, a Fase 5 herdaria um normalizer inalcançável.
Output: `AssetsPhoneMetadataLoader`, `AppContainer` completo, `koverVerify` como gate real e
`02-EVIDENCE.md`.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-motor-de-decisao-e-normalizacao/02-CONTEXT.md
@.planning/phases/02-motor-de-decisao-e-normalizacao/02-RESEARCH.md
@.planning/phases/02-motor-de-decisao-e-normalizacao/02-VALIDATION.md
@app/src/main/java/org/sentinela/app/AppContainer.kt
@app/src/main/java/org/sentinela/app/SentinelaApp.kt
@app/src/main/java/org/sentinela/app/telecom/UnknownCallScreeningService.kt

<interfaces>
<!-- Criados nos planos 02-03 e 02-04 -->
```kotlin
package org.sentinela.app.phone
fun interface RegionProvider { fun currentRegion(): String? }
class CascadingRegionProvider(device: RegionProvider, userPreference: RegionProvider, fallback: String = DEFAULT_REGION) : RegionProvider
class LibPhoneNumberNormalizer(util: PhoneNumberUtil, regionProvider: RegionProvider) : PhoneNumberNormalizer
fun phoneNumberUtil(loader: MetadataLoader): PhoneNumberUtil
object PhoneNumbers { const val LIMIAR_CURTO = 6 }
```
```kotlin
package org.sentinela.app.platform
class AndroidRegionProvider(telephonyManager: android.telephony.TelephonyManager?) : RegionProvider
```
<!-- API do port -android para produção -->
```kotlin
io.michaelrocks.libphonenumber.android.metadata.source.AssetsMetadataLoader(assetManager: android.content.res.AssetManager)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wiring no AppContainer com loader de assets isolado em platform/</name>
  <files>app/src/main/java/org/sentinela/app/platform/AssetsPhoneMetadataLoader.kt, app/src/main/java/org/sentinela/app/AppContainer.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/AppContainer.kt (TODOs de fase, `@Suppress("UnusedPrivateProperty")`)
    - app/src/main/java/org/sentinela/app/SentinelaApp.kt (onde o container é criado)
    - app/src/main/java/org/sentinela/app/platform/AndroidRegionProvider.kt (plano 02-03)
    - .planning/phases/02-motor-de-decisao-e-normalizacao/02-RESEARCH.md §"Pitfall 6" (não construir no caminho quente)
  </read_first>
  <action>
    1. Criar `app/src/main/java/org/sentinela/app/platform/AssetsPhoneMetadataLoader.kt`:
       ```kotlin
       package org.sentinela.app.platform

       import android.content.Context
       import io.michaelrocks.libphonenumber.android.MetadataLoader
       import io.michaelrocks.libphonenumber.android.metadata.source.AssetsMetadataLoader

       /** Carrega os metadados do libphonenumber dos assets do AAR. Unico ponto Android do caminho. */
       fun assetsPhoneMetadataLoader(context: Context): MetadataLoader =
           AssetsMetadataLoader(context.assets)
       ```
    2. Em `AppContainer.kt`: remover o `@Suppress("UnusedPrivateProperty")` do `appContext`
       (agora é usado), remover o `TODO(Fase 2)` e adicionar:
       ```kotlin
       private val regionProvider: RegionProvider by lazy {
           CascadingRegionProvider(
               device = AndroidRegionProvider(
                   appContext.getSystemService(TelephonyManager::class.java),
               ),
               // Degrau 2 (preferencia do usuario) so ganha persistencia na Fase 3;
               // aqui o contrato existe com fallback em memoria.
               userPreference = RegionProvider { null },
           )
       }

       /**
        * Instancia unica. createInstance desserializa metadados (dezenas de ms): construir aqui,
        * NUNCA dentro de onScreenCall — a Fase 5 tem orcamento p95 < 200 ms.
        */
       val phoneNumberNormalizer: PhoneNumberNormalizer by lazy {
           LibPhoneNumberNormalizer(
               util = phoneNumberUtil(assetsPhoneMetadataLoader(appContext)),
               regionProvider = regionProvider,
           )
       }
       ```
       Manter os TODOs das Fases 3–6 intactos.
    3. NÃO alterar `UnknownCallScreeningService` — ele permanece pass-through até a Fase 5
       (o consumo real do normalizer é escopo diferido).
    4. Nenhuma permissão nova no manifest.
  </action>
  <verify>
    <automated>export ANDROID_HOME="${ANDROID_HOME:-$HOME/Library/Android/sdk}"; ./gradlew assembleDebug testDebugUnitTest lint detekt && bash scripts/verify-invariants.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/src/main/java/org/sentinela/app/platform/AssetsPhoneMetadataLoader.kt`
    - `grep -q 'phoneNumberNormalizer' app/src/main/java/org/sentinela/app/AppContainer.kt`
    - `grep -q 'CascadingRegionProvider' app/src/main/java/org/sentinela/app/AppContainer.kt`
    - `grep -q 'TODO(Fase 2)' app/src/main/java/org/sentinela/app/AppContainer.kt` retorna 1 (removido)
    - `grep -q 'TODO(Fase 3)' app/src/main/java/org/sentinela/app/AppContainer.kt` retorna 0 (TODOs futuros preservados)
    - `grep -rq 'PhoneNumberUtil' app/src/main/java/org/sentinela/app/telecom/` retorna 1 (Service segue pass-through, sem construir nada no caminho quente)
    - `grep -rq '^import android' app/src/main/java/org/sentinela/app/phone/` retorna 1 (ausente)
    - `bash scripts/verify-invariants.sh` sai 0 e imprime `== todos os invariantes OK ==`
    - `./gradlew assembleDebug lint detekt` termina `BUILD SUCCESSFUL`
  </acceptance_criteria>
  <done>`AppContainer.phoneNumberNormalizer` existe e é construído fora do caminho quente; o único código Android do caminho está em `platform/`.</done>
</task>

<task type="auto">
  <name>Task 2: Ativar o gate koverVerify em 80% e arquivar evidência pós-clean</name>
  <files>app/build.gradle.kts, .planning/phases/02-motor-de-decisao-e-normalizacao/02-EVIDENCE.md</files>
  <read_first>
    - app/build.gradle.kts (bloco `kover { reports { filters { ... } } }` criado no plano 02-01)
    - .planning/phases/02-motor-de-decisao-e-normalizacao/02-RESEARCH.md §"Pattern 4" (evidência do gate falhando de verdade)
    - .planning/phases/01-fundacao-compilavel/01-EVIDENCE.md (formato de evidência já aceito no projeto)
    - .planning/phases/02-motor-de-decisao-e-normalizacao/02-VALIDATION.md (comando de phase gate)
  </read_first>
  <action>
    1. Em `app/build.gradle.kts`, acrescentar ao bloco `kover { reports { ... } }` existente:
       ```kotlin
       verify {
           rule("Cobertura minima de dominio e normalizacao") {
               minBound(80)
           }
       }
       ```
       Remover o comentário do plano 02-01 que dizia que o gate entraria depois.
    2. Provar que o gate FALHA de verdade: subir temporariamente o bound para `99`, rodar
       `./gradlew koverVerify`, capturar a mensagem
       `Rule 'Cobertura minima de dominio e normalizacao' violated: lines covered percentage is ...`,
       e restaurar para `80`. O log dos dois runs vai para a evidência.
    3. Rodar a suíte completa **pós-`clean`** (um run incremental devolve `UP-TO-DATE` e não prova
       nada — regra probatória fixada na Phase 1; `--no-build-cache` é obrigatório porque
       `FROM-CACHE` tem o mesmo defeito):
       ```bash
       export ANDROID_HOME="${ANDROID_HOME:-$HOME/Library/Android/sdk}"
       ./gradlew clean
       ./gradlew --no-build-cache assembleDebug testDebugUnitTest koverVerify lint detekt
       bash scripts/verify-invariants.sh
       ```
    4. Criar `.planning/phases/02-motor-de-decisao-e-normalizacao/02-EVIDENCE.md` seguindo o
       formato do `01-EVIDENCE.md`, contendo: o comando exato, a linha `N actionable tasks: M executed`
       com M > 0, o percentual final do `koverLog`, o total de testes da suíte
       (somar os atributos `tests` dos XML em `app/build/test-results/testDebugUnitTest/`),
       a prova do gate falhando em 99, e a saída completa do `verify-invariants.sh`.
    5. Registrar na seção final da evidência os itens **diferidos para a Phase 9** (nada desta
       fase exige aparelho físico — declarar explicitamente "nenhuma pendência física nova").
  </action>
  <verify>
    <automated>export ANDROID_HOME="${ANDROID_HOME:-$HOME/Library/Android/sdk}"; ./gradlew clean && ./gradlew --no-build-cache assembleDebug testDebugUnitTest koverVerify lint detekt && bash scripts/verify-invariants.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'minBound(80)' app/build.gradle.kts`
    - `grep -q 'minBound(99)' app/build.gradle.kts` retorna 1 (bound temporário revertido)
    - `./gradlew koverVerify` termina `BUILD SUCCESSFUL`
    - `test -f .planning/phases/02-motor-de-decisao-e-normalizacao/02-EVIDENCE.md`
    - `grep -q 'actionable tasks' .planning/phases/02-motor-de-decisao-e-normalizacao/02-EVIDENCE.md`
    - `grep -q "Rule 'Cobertura minima de dominio e normalizacao' violated" .planning/phases/02-motor-de-decisao-e-normalizacao/02-EVIDENCE.md` (prova de que o gate falha de verdade)
    - `grep -q 'todos os invariantes OK' .planning/phases/02-motor-de-decisao-e-normalizacao/02-EVIDENCE.md`
    - `grep -q 'checkpoint' .planning/phases/02-motor-de-decisao-e-normalizacao/02-EVIDENCE.md` retorna 1 (nenhum checkpoint humano nesta fase)
  </acceptance_criteria>
  <done>Gate de 80% ativo e comprovadamente capaz de quebrar o build; suíte completa verde pós-`clean` com evidência arquivada.</done>
</task>

</tasks>

<verification>
```bash
export ANDROID_HOME="${ANDROID_HOME:-$HOME/Library/Android/sdk}"
./gradlew clean
./gradlew --no-build-cache assembleDebug testDebugUnitTest koverVerify lint detekt
bash scripts/verify-invariants.sh
```
</verification>

<success_criteria>
- `AppContainer.phoneNumberNormalizer` disponível para a Fase 5, construído fora do caminho quente
- `koverVerify` falha o build abaixo de 80% em `domain/` + `phone/` (provado empiricamente)
- Suíte completa verde pós-`clean` com `M executed > 0` registrado em `02-EVIDENCE.md`
- Nenhuma permissão nova, nenhum `import android.` em `domain/` ou `phone/`
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/02-motor-de-decisao-e-normalizacao/02-05-SUMMARY.md`
</output>
