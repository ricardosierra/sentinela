---
phase: 04-contatos-do-aparelho
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/org/sentinela/app/data/contacts/ContactsPermissionState.kt
  - app/src/main/java/org/sentinela/app/platform/ContactsPermissionChecker.kt
  - app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt
  - app/src/test/java/org/sentinela/app/data/contacts/ContactsPermissionStateTest.kt
  - app/src/test/java/org/sentinela/app/settings/DataStoreSettingsRepositoryTest.kt
autonomous: true
requirements: [CTT-01]

must_haves:
  truths:
    - "O app distingue NEVER_ASKED de DENIED_PERMANENTLY, entao nunca repergunta a quem negou de vez"
    - "So no estado DENIED_PERMANENTLY o app oferece atalho para as Configuracoes do sistema"
    - "O flag de ja-perguntamos persiste no DataStore de configuracoes que ja existe"
  artifacts:
    - path: "app/src/main/java/org/sentinela/app/data/contacts/ContactsPermissionState.kt"
      provides: "enum de 4 estados + funcao PURA de decisao (JVM-testavel, medida pelo Kover)"
      min_lines: 40
    - path: "app/src/main/java/org/sentinela/app/platform/ContactsPermissionChecker.kt"
      provides: "camada fina que chama ContextCompat/ActivityCompat"
    - path: "app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt"
      provides: "contactsPermissionAsked: Flow<Boolean> + markContactsPermissionAsked()"
      contains: "contacts_permission_asked"
  key_links:
    - from: "ContactsPermissionChecker"
      to: "contactsPermissionState(...)"
      via: "delega a decisao a funcao pura"
      pattern: "contactsPermissionState\\("
    - from: "DataStoreSettingsRepository"
      to: "DataStore de configuracoes existente"
      via: "booleanPreferencesKey(\"contacts_permission_asked\")"
      pattern: "contacts_permission_asked"
---

<objective>
Maquina de estado da permissao de contatos: enum de 4 estados, funcao PURA de decisao e o flag
persistido que a Fase 7 vai consumir na tela de onboarding.

Purpose: `shouldShowRequestPermissionRationale` devolve `false` nos DOIS extremos (antes do
primeiro pedido e apos a negacao permanente). Sem o flag persistido, o app ou repergunta para
sempre, ou nunca oferece o atalho para as Configuracoes.
Output: contrato de permissao pronto para a Phase 7, 100% testado em JVM.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-contatos-do-aparelho/04-CONTEXT.md
@.planning/phases/04-contatos-do-aparelho/04-RESEARCH.md
@app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt
@app/src/test/java/org/sentinela/app/settings/DataStoreSettingsRepositoryTest.kt

<interfaces>
Padrao ja estabelecido no `DataStoreSettingsRepository` (Fase 3) — seguir a letra:

```kotlin
private object Keys {
    val APP_OPEN_COUNT = intPreferencesKey("app_open_count")   // nome textual = contrato com o disco
}

val appOpenCount: Flow<Int> =
    dataStore.data
        .catch { e -> if (e is IOException) emit(emptyPreferences()) else throw e }
        .map { it[Keys.APP_OPEN_COUNT] ?: 0 }

suspend fun incrementAppOpenCount(): Int { ... dataStore.edit { ... } }
```

Algoritmo alvo (pesquisa, HIGH — doc oficial + trap confirmada):
```
GRANTED               <- granted
NEVER_ASKED           <- !granted && !alreadyAsked
DENIED_ONCE           <- !granted && alreadyAsked && rationale
DENIED_PERMANENTLY    <- !granted && alreadyAsked && !rationale
```

String ja existente e suficiente (NAO criar string nova):
`R.string.contacts_permission_rationale` em `app/src/main/res/values/strings.xml`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: enum + funcao pura de estado da permissao</name>
  <files>app/src/main/java/org/sentinela/app/data/contacts/ContactsPermissionState.kt, app/src/test/java/org/sentinela/app/data/contacts/ContactsPermissionStateTest.kt</files>
  <read_first>
    - .planning/phases/04-contatos-do-aparelho/04-RESEARCH.md secao "Maquina de estado de permissao"
    - app/src/main/java/org/sentinela/app/domain/ScreenedCall.kt (estilo de enum do projeto)
  </read_first>
  <behavior>
    - granted=true, alreadyAsked=false, rationale=false -> GRANTED
    - granted=true, alreadyAsked=true, rationale=true -> GRANTED (granted vence tudo)
    - granted=false, alreadyAsked=false, rationale=false -> NEVER_ASKED
    - granted=false, alreadyAsked=false, rationale=true -> NEVER_ASKED (flag vence o rationale)
    - granted=false, alreadyAsked=true, rationale=true -> DENIED_ONCE
    - granted=false, alreadyAsked=true, rationale=false -> DENIED_PERMANENTLY
    - `canRequest` e true so em NEVER_ASKED e DENIED_ONCE
    - `shouldOfferSystemSettings` e true SO em DENIED_PERMANENTLY
    - o enum tem exatamente 4 entradas (lista travada por teste, como DecisionReasonTest da Fase 2)
  </behavior>
  <action>
Criar `app/src/main/java/org/sentinela/app/data/contacts/ContactsPermissionState.kt`, **sem
nenhum `import android.*`**:

```kotlin
enum class ContactsPermissionState { GRANTED, NEVER_ASKED, DENIED_ONCE, DENIED_PERMANENTLY }

val ContactsPermissionState.canRequest: Boolean
    get() = this == NEVER_ASKED || this == DENIED_ONCE

val ContactsPermissionState.shouldOfferSystemSettings: Boolean
    get() = this == DENIED_PERMANENTLY

fun contactsPermissionState(
    granted: Boolean,
    alreadyAsked: Boolean,
    rationale: Boolean,
): ContactsPermissionState = when {
    granted -> GRANTED
    !alreadyAsked -> NEVER_ASKED
    rationale -> DENIED_ONCE
    else -> DENIED_PERMANENTLY
}
```

KDoc obrigatorio explicando: (a) por que o flag persistido e indispensavel; (b) que
`alreadyAsked` e gravado no momento em que o launcher e disparado, nunca no callback (o usuario
pode matar o app com o dialogo aberto); (c) que a Fase 7 consome isto e nenhuma tela nasce aqui.

Escrever `app/src/test/java/org/sentinela/app/data/contacts/ContactsPermissionStateTest.kt`
com >= 8 `@Test` cobrindo a tabela de `<behavior>`, incluindo um teste que trava
`ContactsPermissionState.entries.size == 4` e a lista de nomes escrita a mao.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*ContactsPermissionStateTest" detekt lint</automated>
  </verify>
  <acceptance_criteria>
    - `[ "$(grep -c '^import android' app/src/main/java/org/sentinela/app/data/contacts/ContactsPermissionState.kt)" -eq 0 ]`
    - `[ "$(grep -c '@Test' app/src/test/java/org/sentinela/app/data/contacts/ContactsPermissionStateTest.kt)" -ge 8 ]`
    - `grep -q "entries.size" app/src/test/java/org/sentinela/app/data/contacts/ContactsPermissionStateTest.kt`
    - `./gradlew testDebugUnitTest --tests "*ContactsPermissionStateTest"` termina BUILD SUCCESSFUL
    - `detekt` e `lint` sem issue novo (`app/build/reports/detekt/detekt.xml` sem `<error`)
  </acceptance_criteria>
  <done>Os 4 estados sao derivados por funcao pura determinística e cobertos caso a caso.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: flag contacts_permission_asked no DataStore existente</name>
  <files>app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt, app/src/test/java/org/sentinela/app/settings/DataStoreSettingsRepositoryTest.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt (objeto Keys, padrao catch/IOException)
    - app/src/test/java/org/sentinela/app/settings/DataStoreSettingsRepositoryTest.kt (TemporaryFolder, sem @JvmField na rule)
  </read_first>
  <behavior>
    - Valor inicial de `contactsPermissionAsked` e `false`
    - Apos `markContactsPermissionAsked()` o Flow emite `true`
    - Chamar `markContactsPermissionAsked()` duas vezes mantem `true` (idempotente)
    - O valor persiste entre duas instancias de repositorio sobre o MESMO arquivo
    - Arquivo corrompido (IOException) cai em `false`, nunca derruba o Flow
  </behavior>
  <action>
Editar `app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt`:

1. Em `private object Keys`, acrescentar:
   `val CONTACTS_PERMISSION_ASKED = booleanPreferencesKey("contacts_permission_asked")`
2. Expor, no mesmo estilo de `appOpenCount`:
   ```kotlin
   /** CTT-01: distingue "nunca pedimos" de "negada permanentemente" — ver ContactsPermissionState. */
   val contactsPermissionAsked: Flow<Boolean> =
       dataStore.data
           .catch { e -> if (e is IOException) emit(emptyPreferences()) else throw e }
           .map { it[Keys.CONTACTS_PERMISSION_ASKED] ?: false }

   suspend fun markContactsPermissionAsked() {
       dataStore.edit { it[Keys.CONTACTS_PERMISSION_ASKED] = true }
   }
   ```
3. **Nao** incluir o flag em `ScreeningSettings` nem em `writeInto`: nao e configuracao de
   triagem e nao pode entrar no caminho quente do snapshot.

Estender `DataStoreSettingsRepositoryTest` com >= 5 `@Test` novos cobrindo `<behavior>`,
reaproveitando o `TemporaryFolder` ja usado no arquivo (lembrete da Fase 3: `@get:Rule`
combinado com `@JvmField` desliga a rule).
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*DataStoreSettingsRepositoryTest"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'booleanPreferencesKey("contacts_permission_asked")' app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt`
    - `grep -q "markContactsPermissionAsked" app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt`
    - `[ "$(grep -c 'CONTACTS_PERMISSION_ASKED' app/src/main/java/org/sentinela/app/settings/ScreeningSettings.kt)" -eq 0 ]`
    - `[ "$(grep -c 'contactsPermissionAsked\|markContactsPermissionAsked' app/src/test/java/org/sentinela/app/settings/DataStoreSettingsRepositoryTest.kt)" -ge 5 ]`
    - `./gradlew testDebugUnitTest --tests "*DataStoreSettingsRepositoryTest"` BUILD SUCCESSFUL com todos os testes preexistentes ainda verdes
  </acceptance_criteria>
  <done>O flag persiste no DataStore unico da Fase 3, sem tabela nova e sem segunda instancia.</done>
</task>

<task type="auto">
  <name>Task 3: ContactsPermissionChecker (camada fina de plataforma)</name>
  <files>app/src/main/java/org/sentinela/app/platform/ContactsPermissionChecker.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/platform/AndroidRegionProvider.kt (padrao de isolamento de plataforma da Fase 2)
    - app/src/main/java/org/sentinela/app/data/contacts/ContactsPermissionState.kt (criado na Task 1)
  </read_first>
  <action>
Criar `app/src/main/java/org/sentinela/app/platform/ContactsPermissionChecker.kt`. Vive em
`platform/` de proposito: e a unica camada que toca `ContextCompat`/`ActivityCompat`, fica fora
do filtro do Kover (que cobre `domain`, `phone`, `data`, `settings`) e por isso nao derruba o
gate por nao ter teste JVM.

```kotlin
class ContactsPermissionChecker {

    fun isGranted(context: Context): Boolean =
        ContextCompat.checkSelfPermission(context, Manifest.permission.READ_CONTACTS) ==
            PackageManager.PERMISSION_GRANTED

    /** Precisa de Activity: shouldShowRequestPermissionRationale nao existe em Context. */
    fun state(activity: Activity, alreadyAsked: Boolean): ContactsPermissionState =
        contactsPermissionState(
            granted = isGranted(activity),
            alreadyAsked = alreadyAsked,
            rationale = ActivityCompat.shouldShowRequestPermissionRationale(
                activity, Manifest.permission.READ_CONTACTS,
            ),
        )

    /** Atalho para as Configuracoes do app — usar SO quando shouldOfferSystemSettings for true. */
    fun appSettingsIntent(packageName: String): Intent =
        Intent(
            Settings.ACTION_APPLICATION_DETAILS_SETTINGS,
            Uri.fromParts("package", packageName, null),
        )
}
```

Nenhuma decisao mora aqui — toda a regra e delegada a `contactsPermissionState`. Nao criar
tela, nao chamar `requestPermissions`, nao criar string nova (a explicacao ja existe em
`R.string.contacts_permission_rationale` e e consumida pela Fase 7).
  </action>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin testDebugUnitTest detekt lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "contactsPermissionState(" app/src/main/java/org/sentinela/app/platform/ContactsPermissionChecker.kt`
    - `[ "$(grep -cE 'when \{|if \(' app/src/main/java/org/sentinela/app/platform/ContactsPermissionChecker.kt)" -eq 0 ]` (zero ramo de decisao na camada fina)
    - `[ "$(grep -c 'requestPermissions' app/src/main/java/org/sentinela/app/platform/ContactsPermissionChecker.kt)" -eq 0 ]`
    - `git diff --exit-code app/src/main/res/values/strings.xml` (nenhuma string nova nesta fase)
    - `[ "$(grep -rc 'androidx.compose' app/src/main/java/org/sentinela/app/platform/ContactsPermissionChecker.kt)" -eq 0 ]`
    - `./gradlew :app:compileDebugKotlin testDebugUnitTest detekt lint` BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>A Fase 7 tem um contrato pronto: estado, atalho de Configuracoes e explicacao ja em resources.</done>
</task>

</tasks>

<verification>
```bash
./gradlew testDebugUnitTest detekt lint
./gradlew koverLog   # cobertura nao pode cair: a logica nova e toda JVM-testavel
```
</verification>

<success_criteria>
- 4 estados derivados por funcao pura, cobertos caso a caso, sem `import android.*`
- Flag `contacts_permission_asked` persistido no DataStore unico existente
- Camada de plataforma sem nenhuma decisao propria e sem UI
</success_criteria>

<output>
Criar `.planning/phases/04-contatos-do-aparelho/04-02-SUMMARY.md`
</output>
