---
phase: 04-contatos-do-aparelho
plan: 05
type: execute
wave: 4
depends_on: ["04-02", "04-04"]
files_modified:
  - scripts/verify-invariants.sh
  - app/src/test/java/org/sentinela/app/data/local/db/SchemaExportTest.kt
  - app/src/main/java/org/sentinela/app/AppContainer.kt
  - app/build.gradle.kts
  - docs/TESTE-FISICO-SAMSUNG.md
  - .planning/phases/04-contatos-do-aparelho/04-EVIDENCE.md
autonomous: true
requirements: [CTT-01, CTT-02, CTT-04]

must_haves:
  truths:
    - "Nenhum nome ou dado de contato aparece em banco, log ou backup — provado por teste e por invariante, nao por revisao"
    - "O repositorio de contatos e instancia unica do processo, sem nada em Application.onCreate"
    - "O gate de cobertura continua verde com a logica pura de contatos dentro do denominador"
    - "A cauda de latencia e o comportamento em aparelho real estao registrados como pendencia da Phase 9"
  artifacts:
    - path: "scripts/verify-invariants.sh"
      provides: "Bloco 6 — contatos apenas em memoria"
      contains: "Bloco 6"
    - path: "app/src/test/java/org/sentinela/app/data/local/db/SchemaExportTest.kt"
      provides: "caso que le os columnName do schema exportado e falha se vazar dado de contato"
    - path: "app/src/main/java/org/sentinela/app/AppContainer.kt"
      provides: "contactLookupRepository by lazy"
      contains: "contactLookupRepository"
    - path: ".planning/phases/04-contatos-do-aparelho/04-EVIDENCE.md"
      provides: "evidencia pos-clean com --no-build-cache"
  key_links:
    - from: "scripts/verify-invariants.sh"
      to: "app/schemas/*/*.json"
      via: "grep sobre os VALORES de columnName"
      pattern: "columnName"
    - from: "AppContainer"
      to: "DefaultContactLookupRepository"
      via: "by lazy, fora de Application.onCreate"
      pattern: "contactLookupRepository"
---

<objective>
Fechar a fase: transformar "nome de contato nao vaza" em guarda-corpo automatico, ligar o
repositorio no container, ampliar o Kover com UMA classe excluida e arquivar a evidencia
pos-`clean`.

Purpose: o risco desta fase e privacidade, nao performance — e a primeira vez que o app toca em
dado pessoal de terceiros. Revisao de codigo nao conta como prova.
Output: Bloco 6 de invariantes, teste de schema estendido, wiring, gate verde e evidencia.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-contatos-do-aparelho/04-CONTEXT.md
@.planning/phases/04-contatos-do-aparelho/04-RESEARCH.md
@scripts/verify-invariants.sh
@app/src/main/java/org/sentinela/app/AppContainer.kt
@app/src/test/java/org/sentinela/app/data/local/db/SchemaExportTest.kt

<interfaces>
Filtro atual do Kover em `app/build.gradle.kts` (linhas ~119–146) — `data.*` ja inclui
`data.contacts.*`, entao **nenhuma edicao de `includes`**:
```kotlin
excludes {
    classes("org.sentinela.app.data.local.db.*")
    classes("*_Impl", "*_Impl\$*")
    annotatedBy("androidx.room.Dao", "androidx.room.Database")
}
```

Padrao de log de invariante ja usado pelo script: `ok "..."` e `fail "..."`, com `FAILURES`
incrementado por `fail`. `set -e` e proibido no arquivo (`grep -c` sai 1 com zero matches).

Valores de `columnName` do schema atual (medidos, nenhum casa com o padrao de vazamento):
`id, number_key, description, enabled, created_at_utc_millis, masked_number, number_e164,
timestamp_utc_millis, reason_code, notification_shown, classification`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bloco 6 de invariantes + SchemaExportTest de vazamento, com vermelho demonstrado</name>
  <files>scripts/verify-invariants.sh, app/src/test/java/org/sentinela/app/data/local/db/SchemaExportTest.kt</files>
  <read_first>
    - scripts/verify-invariants.sh (Bloco 5, funcoes fail/ok, variavel M)
    - app/src/test/java/org/sentinela/app/data/local/db/SchemaExportTest.kt
    - .planning/phases/04-contatos-do-aparelho/04-RESEARCH.md secao "Provar que nada de contato e persistido"
    - app/build.gradle.kts (declaracao de `schemas/` como input das Test tasks — Fase 3)
  </read_first>
  <action>
1. Acrescentar ao fim de `scripts/verify-invariants.sh`, **antes** do bloco final de
   `FAILURES`, o "Bloco 6 — Fase 4: dado de contato nunca sai da memoria" com quatro checagens
   (o quinto item da pesquisa — barrar a permissao de gravacao de agenda — **ja esta coberto**
   pela variavel `FUTURE` editada no plano 04-01; **nao duplicar**):

   - **6.1** `LEAK_PAT='(^|_)(name|display|contact|photo|lookup|nome|agenda)'` aplicado aos
     **VALORES** de `"columnName"` do schema exportado:
     ```bash
     LEAKED=$(grep -ohE '"columnName": "[^"]*"' app/schemas/*/*.json \
       | sed 's/.*: "//;s/"//' | sort -u | grep -E "$LEAK_PAT")
     ```
     Comentario obrigatorio explicando que aplicar as CHAVES falsaria positivo (o JSON e cheio
     de `"name"`).
   - **6.2** fronteira: apenas `app/src/main/java/org/sentinela/app/data/contacts/` pode
     mencionar o provider de contatos.
   - **6.3** nenhuma coluna de identidade do contato e projetada em `app/src/main/java`
     (nome de exibicao, foto, miniatura de foto, chave de lookup).
   - **6.4** `app/src/main/java/org/sentinela/app/data/contacts` nao pode conter
     `@Entity|@Dao|Room\.|DataStore|edit \{|openFileOutput|SharedPreferences`.

   **ARMADILHA DE AUTO-SABOTAGEM — obrigatorio:** os proprios padroes de 6.2, 6.3 e 6.4 sao
   literais que os greps procuram. Todo grep deste bloco DEVE excluir o proprio script do
   escopo (os alvos ja sao `app/schemas` e `app/src/main/java`, nunca `scripts/`), e nenhum
   comentario dentro de `app/src/main/java` pode repetir esses literais. Se o script for
   incluido no escopo por engano, ele falha sozinho.

2. Estender `SchemaExportTest` com um `@Test schemaNaoTemColunaDeDadoDeContato()` que le todos
   os `1.json` de `schemas/`, extrai os valores de `"columnName"` por regex sobre o texto e
   falha se algum casar com o mesmo padrao de vazamento. Confirmar que `schemas/` continua
   declarado como `inputs.dir` das `Test` tasks — sem isso o teste vai `UP-TO-DATE` e passa
   verde ate com o schema apagado (licao literal da Fase 3).

3. **Prova de vermelho obrigatoria:** acrescentar temporariamente uma coluna
   `display_name` a uma `@Entity`, rodar `./gradlew :app:kspDebugKotlin testDebugUnitTest` e
   `bash scripts/verify-invariants.sh`, observar **6.1 e o teste de schema vermelhos**,
   reverter (inclusive o `1.json` regenerado, via `git checkout`) e reconfirmar o verde.
   Registrar as duas saidas no SUMMARY.
  </action>
  <verify>
    <automated>./gradlew assembleDebug testDebugUnitTest --tests "*SchemaExportTest" lint detekt &amp;&amp; bash scripts/verify-invariants.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Bloco 6" scripts/verify-invariants.sh`
    - `grep -q "LEAK_PAT" scripts/verify-invariants.sh`
    - `bash scripts/verify-invariants.sh` termina exit 0 e imprime as 4 linhas `ok:` do Bloco 6
    - `grep -q "columnName" app/src/test/java/org/sentinela/app/data/local/db/SchemaExportTest.kt`
    - `./gradlew testDebugUnitTest --tests "*SchemaExportTest"` BUILD SUCCESSFUL com `N actionable tasks: M executed` e M > 0
    - `git diff --exit-code app/schemas` (nenhuma alteracao de schema sobrou da prova de vermelho)
    - SUMMARY traz a saida do script e do teste no estado VERMELHO (com a coluna simulada) e no estado verde restaurado
  </acceptance_criteria>
  <done>Vazamento de dado de contato para o banco passa a quebrar o build automaticamente.</done>
</task>

<task type="auto">
  <name>Task 2: wiring no AppContainer + exclude nomeado no Kover</name>
  <files>app/src/main/java/org/sentinela/app/AppContainer.kt, app/build.gradle.kts</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/AppContainer.kt (padrao by lazy, appScope, TODO da Fase 4 na linha ~127)
    - app/build.gradle.kts (bloco kover, linhas ~119–146)
    - app/src/main/java/org/sentinela/app/data/contacts/DefaultContactLookupRepository.kt
  </read_first>
  <action>
1. `AppContainer.kt`: substituir o `TODO(Fase 4)` por
   ```kotlin
   /**
    * Instancia UNICA do processo. Nada aqui e construido em Application.onCreate: o cache e
    * o registro do observer sao PREGUICOSOS, na primeira consulta — o bind do provider custa
    * ~6 ms medidos e nao entra no cold start do Service (orcamento p95 < 200 ms da Fase 5).
    * `close()` existe para os testes instrumentados; em producao nunca e chamado, porque o
    * registro morre com o processo.
    */
   val contactLookupRepository: ContactLookupRepository by lazy {
       DefaultContactLookupRepository(
           source = ContactsContractLookupSource(appContext),
           normalizer = phoneNumberNormalizer,
           scope = appScope,
       )
   }
   ```
   **Nao** chamar nada de contatos dentro de `onAppOpened()`.

2. `app/build.gradle.kts`, bloco `kover.reports.filters.excludes`: acrescentar **exatamente uma
   classe nomeada**:
   ```kotlin
   // Fase 4: a fonte do provider de contatos so executa em teste INSTRUMENTADO (mesma razao
   // do gerado pelo Room). A logica pura — estado de permissao, cache e decisao
   // HIT/MISS/UNAVAILABLE — fica FORA deste exclude e continua medida pelo gate de 80%.
   classes("org.sentinela.app.data.contacts.ContactsContractLookupSource")
   ```
   **Proibido** excluir `data.contacts.*` inteiro. **Proibido** mexer em `includes`
   (`data.*` ja cobre `data.contacts.*`).

3. **Prova de vermelho do gate:** subir `minBound` temporariamente para 99, rodar
   `./gradlew koverVerify`, observar a falha, restaurar 80 e reconfirmar o verde. Registrar
   ambas as saidas e o percentual real no SUMMARY.
  </action>
  <verify>
    <automated>./gradlew assembleDebug testDebugUnitTest koverVerify lint detekt &amp;&amp; bash scripts/verify-invariants.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "contactLookupRepository" app/src/main/java/org/sentinela/app/AppContainer.kt`
    - `[ "$(grep -c 'TODO(Fase 4)' app/src/main/java/org/sentinela/app/AppContainer.kt)" -eq 0 ]`
    - `[ "$(grep -c 'contactLookupRepository' app/src/main/java/org/sentinela/app/SentinelaApp.kt)" -eq 0 ]` (nada de contatos no onCreate da Application)
    - `grep -q 'classes("org.sentinela.app.data.contacts.ContactsContractLookupSource")' app/build.gradle.kts`
    - `[ "$(grep -c 'data.contacts.\*' app/build.gradle.kts)" -eq 0 ]`
    - `./gradlew koverVerify` BUILD SUCCESSFUL; `./gradlew koverLog` reporta >= 80%
    - SUMMARY traz a saida do `koverVerify` FALHANDO com `minBound(99)` e passando com 80
  </acceptance_criteria>
  <done>O repositorio esta disponivel para a Fase 5 e o gate de cobertura cobre a logica pura de contatos.</done>
</task>

<task type="auto">
  <name>Task 3: pendencias da Phase 9 + evidencia pos-clean</name>
  <files>docs/TESTE-FISICO-SAMSUNG.md, .planning/phases/04-contatos-do-aparelho/04-EVIDENCE.md</files>
  <read_first>
    - docs/TESTE-FISICO-SAMSUNG.md (cenarios 31–35 ja registrados)
    - .planning/phases/03-dados-locais/03-EVIDENCE.md (formato de evidencia da fase anterior)
    - .planning/ROADMAP.md secao "Politica de validacao fisica"
  </read_first>
  <action>
1. Acrescentar a `docs/TESTE-FISICO-SAMSUNG.md`, na numeracao seguinte a existente, os cenarios
   diferidos desta fase (nenhum vira `checkpoint:human-*` aqui — a politica do ROADMAP proibe
   nas Fases 1–8):
   - agenda REAL do usuario em Samsung: contato importado de vCard **sem DDI** e reconhecido
     como HIT (a pesquisa mediu o mecanismo, nao a frequencia em aparelho BR)
   - percentis do lookup com a agenda real (p95 e max), comparados aos numeros do emulador
   - negacao permanente de verdade: negar duas vezes, confirmar que o app oferece o atalho para
     as Configuracoes e **nao** repergunta na abertura seguinte
     (`adb shell dumpsys package org.sentinela.app` mostra `USER_SET` / `USER_FIXED`;
     reset por `adb shell pm clear-permission-flags org.sentinela.app android.permission.READ_CONTACTS user-set user-fixed`)
   - comportamento do provider de contatos do One UI (Samsung substitui o app, mas o provider e
     AOSP — risco baixo, nao zero; **nenhum hack preventivo**)

2. Criar `.planning/phases/04-contatos-do-aparelho/04-EVIDENCE.md` com a saida integral de:
   ```bash
   ./gradlew clean
   ./gradlew --no-build-cache assembleDebug testDebugUnitTest koverVerify lint detekt
   bash scripts/verify-invariants.sh
   bash scripts/run-instrumented-tests.sh
   ```
   Arquivar: a linha `N actionable tasks: M executed` (com M > 0 — `FROM-CACHE` tem o mesmo
   defeito probatorio de `UP-TO-DATE`), o percentual do `koverVerify`, as linhas `ok:` do
   Bloco 6, o `TEST-*.xml` do androidTest com `failures="0"`, e as linhas
   `SENTINELA|contacts|` do logcat com p50/p95/max.

3. Registrar no SUMMARY, para o `/gsd:verify-work`, que a tela de onboarding (CTT-01, criterio 1
   do ROADMAP, parte de UI) e **deliberadamente da Phase 7** e nao e gap desta fase — esta fase
   entrega manifest, maquina de estado e a string de explicacao ja existente.

**ARMADILHA:** ao escrever este item no SUMMARY, descreva "parada humana" em prosa. Escrever o
literal da declaracao de uma task de parada humana dentro de um arquivo de plano faz o validador
de estrutura do GSD acusar o plano de ter uma — mesmo quando o texto e so documentacao.
  </action>
  <verify>
    <automated>./gradlew clean &amp;&amp; ./gradlew --no-build-cache assembleDebug testDebugUnitTest koverVerify lint detekt &amp;&amp; bash scripts/verify-invariants.sh &amp;&amp; bash scripts/run-instrumented-tests.sh</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04-contatos-do-aparelho/04-EVIDENCE.md` existe e contem a string `--no-build-cache`
    - O arquivo de evidencia contem uma linha `actionable tasks:` com numero de `executed` maior que zero
    - O arquivo de evidencia contem `== todos os invariantes OK ==`
    - O arquivo de evidencia contem `failures="0"` do XML instrumentado e ao menos uma linha `SENTINELA|contacts|`
    - `[ "$(grep -c 'vCard' docs/TESTE-FISICO-SAMSUNG.md)" -ge 1 ]` e `grep -q "clear-permission-flags" docs/TESTE-FISICO-SAMSUNG.md`
    - Todos os 5 planos da fase sao autonomos: `[ "$(grep -c '^autonomous: true' .planning/phases/04-contatos-do-aparelho/*-PLAN.md | grep -vc ':1')" -eq 0 ]` — a politica de validacao fisica do ROADMAP proibe parada humana nas Fases 1-8, e este criterio le o frontmatter em vez de procurar o literal da declaracao (que derrubaria a si mesmo)
  </acceptance_criteria>
  <done>Fase auditavel: evidencia pos-clean arquivada e toda pendencia fisica registrada na Phase 9.</done>
</task>

</tasks>

<verification>
```bash
./gradlew clean
./gradlew --no-build-cache assembleDebug testDebugUnitTest koverVerify lint detekt
bash scripts/verify-invariants.sh
bash scripts/run-instrumented-tests.sh
```
</verification>

<success_criteria>
- Bloco 6 verde, com vermelho demonstrado por coluna de vazamento simulada
- `contactLookupRepository` disponivel como singleton preguicoso, nada em `Application.onCreate`
- `koverVerify` verde com um unico exclude nomeado, gate demonstrado falhando em 99
- `04-EVIDENCE.md` arquivado pos-`clean` com `--no-build-cache`
</success_criteria>

<output>
Criar `.planning/phases/04-contatos-do-aparelho/04-05-SUMMARY.md`
</output>
