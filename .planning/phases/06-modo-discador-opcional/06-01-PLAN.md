---
phase: 06-modo-discador-opcional
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/org/sentinela/app/telecom/call/CallUiState.kt
  - app/src/main/java/org/sentinela/app/telecom/call/CallStateMapper.kt
  - app/src/main/java/org/sentinela/app/telecom/call/CallControls.kt
  - app/src/main/java/org/sentinela/app/telecom/call/CallSessionCoordinator.kt
  - app/src/test/java/org/sentinela/app/telecom/call/CallStateMapperTest.kt
  - app/src/test/java/org/sentinela/app/telecom/call/CallSessionCoordinatorTest.kt
  - app/src/test/java/org/sentinela/app/telecom/call/CallSessionFailureTest.kt
  - app/src/test/java/org/sentinela/app/telecom/call/DtmfPairingTest.kt
  - app/src/test/java/org/sentinela/app/telecom/call/CallSessionWatchdogTest.kt
autonomous: true
requirements: [DIA-02]

must_haves:
  truths:
    - "Todo estado de chamada que a plataforma entrega produz um estado de interface nomeado, nunca silencio"
    - "Atender, recusar, encerrar, mudo, viva-voz e teclado chegam a uma costura observavel e verificavel"
    - "Todo tom de teclado enviado tem o encerramento pareado, e nenhum tom novo comeca antes do anterior parar"
    - "Uma chamada tocando que nao consegue apresentar interface no prazo curto falha alto, em vez de ficar em silencio"
  artifacts:
    - path: "app/src/main/java/org/sentinela/app/telecom/call/CallUiState.kt"
      provides: "estados de dominio da chamada, sem tipo da plataforma"
      min_lines: 40
    - path: "app/src/main/java/org/sentinela/app/telecom/call/CallStateMapper.kt"
      provides: "traducao exaustiva dos 13 estados da plataforma"
    - path: "app/src/main/java/org/sentinela/app/telecom/call/CallControls.kt"
      provides: "costura de comandos da interface para a telefonia"
    - path: "app/src/main/java/org/sentinela/app/telecom/call/CallSessionCoordinator.kt"
      provides: "maquina de estado pura da sessao de chamada, com prazo de apresentacao"
      min_lines: 90
  key_links:
    - from: "app/src/main/java/org/sentinela/app/telecom/call/CallSessionCoordinator.kt"
      to: "app/src/main/java/org/sentinela/app/telecom/call/CallControls.kt"
      via: "delegacao de cada comando da interface"
      pattern: "CallControls"
    - from: "app/src/main/java/org/sentinela/app/telecom/call/CallSessionCoordinator.kt"
      to: "app/src/main/java/org/sentinela/app/telecom/call/CallUiState.kt"
      via: "fluxo observavel de estado"
      pattern: "StateFlow"
---

<objective>
Construir o nucleo PURO do modo discador: os estados de dominio da chamada, a traducao exaustiva
dos estados da plataforma, a costura de comandos e o coordenador de sessao — tudo sem uma unica
importacao de `android.*`, testado em JVM.

Purpose: a pesquisa mediu que o modo de falha perigoso desta fase nao e morrer, e sim ficar
vivo e travado. O guarda-corpo contra isso e uma maquina de estado exaustiva com prazo de
apresentacao, e ela so e testavel de verdade se for pura. Este plano tambem entrega a costura
onde mudo e viva-voz podem ser PROVADOS — chamar o metodo no servico da plataforma e um no-op
silencioso quando nao existe telefone vinculado (medido na pesquisa).

Output: quatro arquivos de producao em `telecom/call/` e cinco classes de teste em JVM.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-modo-discador-opcional/06-CONTEXT.md
@.planning/phases/06-modo-discador-opcional/06-RESEARCH.md
@.planning/phases/06-modo-discador-opcional/06-VALIDATION.md
@CLAUDE.md

<interfaces>
Precedente arquitetural desta fase e o `ScreeningCoordinator` da Fase 5: puro, com prazo interno,
guarda atomica local e costura de saida. Leia-o antes de escrever este coordenador.

Estados da plataforma a mapear (valores fixos, android-35 — copiar desta lista, nao adivinhar):

```
STATE_NEW = 0
STATE_DIALING = 1
STATE_RINGING = 2
STATE_HOLDING = 3
STATE_ACTIVE = 4
STATE_DISCONNECTED = 7
STATE_SELECT_PHONE_ACCOUNT = 8
STATE_CONNECTING = 9
STATE_DISCONNECTING = 10
STATE_PULLING_CALL = 11
STATE_AUDIO_PROCESSING = 12
STATE_SIMULATED_RINGING = 13
```

Existe tambem o valor 5/6 vago na numeracao publica do android-35 (`STATE_PRE_DIAL_WAIT` e
`STATE_CONNECTING` antigos, hoje reservados): o mapa recebe um inteiro e nao um enum, portanto
precisa de um ramo nomeado para inteiro desconhecido — que NAO e um ramo mudo, e sim o estado
"nao suportado" visivel.

Assinatura minima da costura (o executor pode renomear, nao pode reduzir):

```kotlin
interface CallControls {
    fun answer()
    fun reject()
    fun hangUp()
    fun setMuted(muted: Boolean)
    fun setSpeakerOn(on: Boolean)
    fun playDtmf(digit: Char)
    fun stopDtmf()
}
```

Rotulos de estado ja contratados pelo `06-UI-SPEC.md` (as strings entram no plano 06-02; aqui
so o estado de dominio existe): chamada recebida, chamando, tocando, em chamada, encerrada,
falhou, nao suportado.

Nada aqui usa cronometro como prova de estrutura. O prazo de apresentacao e testado com
`runTest` e despachante de teste, avancando o relogio virtual — nunca `Thread.sleep`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estados de dominio e traducao exaustiva</name>
  <read_first>
    - app/src/main/java/org/sentinela/app/domain/CallDecision.kt
    - app/src/main/java/org/sentinela/app/telecom/ScreeningCoordinator.kt
    - .planning/phases/06-modo-discador-opcional/06-UI-SPEC.md
  </read_first>
  <files>
    app/src/main/java/org/sentinela/app/telecom/call/CallUiState.kt,
    app/src/main/java/org/sentinela/app/telecom/call/CallStateMapper.kt,
    app/src/test/java/org/sentinela/app/telecom/call/CallStateMapperTest.kt
  </files>
  <behavior>
    - cada um dos 12 valores listados no bloco de interfaces produz um estado nomeado
    - um inteiro que a plataforma nao documenta (por exemplo 99) produz o estado "nao suportado"
    - os estados que a interface desta fase nao desenha (em espera, escolha de chip, processamento
      de audio) produzem "nao suportado" com o comando de encerrar HABILITADO
    - o estado "nao suportado" carrega o codigo bruto recebido, para diagnostico sem dado pessoal
    - nenhum estado produz nulo
  </behavior>
  <action>
    1. Criar `CallUiState` como hierarquia selada com, no minimo: `Incoming`, `Dialing`, `Ringing`,
       `Active`, `Ended`, `Failed`, `Unsupported(rawState: Int)`. Campos de identidade
       (`displayName: String?`, `fullNumber: String?`, `origin: CallOrigin`) e de controle
       (`muted: Boolean`, `speakerOn: Boolean`, `keypadOpen: Boolean`, `hangUpEnabled: Boolean`,
       `startedAtMillis: Long?`) vivem num `CallSnapshot` que envolve o estado, ou como campos do
       proprio estado — a escolha e do executor, desde que `hangUpEnabled` seja `true` tambem em
       `Unsupported`. `CallOrigin` e enum de quatro entradas: contato, permitido, desconhecido,
       privado — espelho dos quatro chips do contrato de design.
    2. Criar `CallStateMapper` com uma unica funcao `fun map(rawState: Int): CallUiState`
       implementada por `when` sobre os valores inteiros, com ramo final nomeado devolvendo
       `Unsupported`. Proibido ramo final vazio.
    3. `CallStateMapperTest` parametrizado com uma tabela ESCRITA A MAO (nunca derivada do mapa),
       cobrindo os 12 valores + um valor desconhecido + o valor 0 e o valor 99, com >= 14 casos.
    4. KDoc explicando, em prosa portuguesa e sem escrever nenhum identificador da plataforma
       vigiado por script, por que o ramo final devolve um estado visivel em vez de nada: a
       pesquisa mediu que o sistema de telefonia NAO detecta interface viva e travada e nao troca
       para o discador do aparelho, entao tela em branco e a pior falha da fase.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*CallStateMapperTest" --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `[ "$(grep -c '@Test\|@Parameters' app/src/test/java/org/sentinela/app/telecom/call/CallStateMapperTest.kt)" -ge 1 ]` e a tabela de casos tem >= 14 linhas
    - `[ "$(grep -c 'import android\.' app/src/main/java/org/sentinela/app/telecom/call/CallStateMapper.kt)" -eq 0 ]`
    - `[ "$(grep -c 'import android\.' app/src/main/java/org/sentinela/app/telecom/call/CallUiState.kt)" -eq 0 ]`
    - `[ "$(grep -cE 'else\s*->\s*\{\s*\}|else\s*->\s*Unit' app/src/main/java/org/sentinela/app/telecom/call/CallStateMapper.kt)" -eq 0 ]`
    - Prova de vermelho registrada no SUMMARY: trocar o ramo final por um estado `Ended` deixa
      pelo menos 1 caso VERMELHO; restaurado depois
  </acceptance_criteria>
  <done>Todo inteiro de estado produz um estado de interface nomeado e visivel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Costura de comandos e coordenador da sessao</name>
  <read_first>
    - app/src/main/java/org/sentinela/app/telecom/ScreeningCoordinator.kt
    - app/src/main/java/org/sentinela/app/telecom/call/CallUiState.kt
    - app/src/main/java/org/sentinela/app/telecom/call/CallStateMapper.kt
  </read_first>
  <files>
    app/src/main/java/org/sentinela/app/telecom/call/CallControls.kt,
    app/src/main/java/org/sentinela/app/telecom/call/CallSessionCoordinator.kt,
    app/src/test/java/org/sentinela/app/telecom/call/CallSessionCoordinatorTest.kt,
    app/src/test/java/org/sentinela/app/telecom/call/DtmfPairingTest.kt
  </files>
  <behavior>
    - atender, recusar e encerrar chegam UMA vez cada a costura, com o estado observavel mudando
    - mudo ligado e desligado altera o estado observavel E chega a costura; mudo ja ligado nao
      reenvia comando
    - viva-voz ligado e desligado idem; quando a lista de rotas suportadas tem uma unica entrada,
      o controle de viva-voz e reportado como indisponivel no estado
    - abrir e fechar o teclado altera o estado observavel e nao envia nenhum tom
    - cada digito enviado produz o par tom-inicia/tom-encerra na lista ordenada de eventos
    - um digito novo enviado sem encerrar o anterior encerra o anterior primeiro
    - sair da sessao com um tom em curso encerra o tom
    - atender numa chamada que nao esta tocando nao chega a costura
  </behavior>
  <action>
    1. Criar `CallControls` exatamente com a assinatura do bloco de interfaces.
    2. Criar `CallSessionCoordinator` puro recebendo `controls: CallControls`, `mapper`,
       `clock: () -> Long` (relogio injetado, regra da Fase 3) e um `scope`/despachante para o
       prazo. Expor `val state: StateFlow<CallUiState>`. Metodos de entrada:
       `onCallAdded(rawState, identity)`, `onStateChanged(rawState)`, `onAudioStateChanged(
       muted, speakerOn, supportedRoutes: Set<CallAudioRoute>)`, `onCallRemoved()`, e os comandos
       da interface `answer/reject/hangUp/toggleMute/toggleSpeaker/toggleKeypad/pressDigit/
       releaseDigit`.
    3. Pareamento do teclado: manter o digito em curso em campo local; `pressDigit` encerra um
       tom pendente antes de iniciar o novo; `releaseDigit`, `onCallRemoved` e qualquer transicao
       para encerrado encerram o tom pendente. Este e um invariante de fase, no mesmo nivel da
       resposta unica da Fase 5.
    4. Guarda de comando: `answer` so delega quando o estado corrente e chamada recebida;
       `reject` idem; `hangUp` delega em qualquer estado com encerrar habilitado. Guarda por
       comparacao do estado corrente, nunca por flag de classe compartilhado entre chamadas.
    5. `CallSessionCoordinatorTest` com dublê de `CallControls` que registra eventos numa LISTA
       ORDENADA (nunca contadores soltos), >= 10 `@Test` cobrindo `<behavior>`. `DtmfPairingTest`
       dedicado ao pareamento, >= 5 `@Test`, assercoes sobre a sequencia de eventos por indice.
    6. Comentar, em prosa, por que mudo e viva-voz sao provados aqui e nao no servico da
       plataforma: a pesquisa mediu que o metodo do servico nao lanca e nao faz nada quando nao
       ha telefone vinculado, o que produz teste verde sem prova.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*CallSessionCoordinatorTest" --tests "*DtmfPairingTest" --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `[ "$(grep -c '@Test' app/src/test/java/org/sentinela/app/telecom/call/CallSessionCoordinatorTest.kt)" -ge 10 ]`
    - `[ "$(grep -c '@Test' app/src/test/java/org/sentinela/app/telecom/call/DtmfPairingTest.kt)" -ge 5 ]`
    - `[ "$(grep -c 'import android\.' app/src/main/java/org/sentinela/app/telecom/call/CallSessionCoordinator.kt)" -eq 0 ]`
    - `[ "$(grep -c 'import android\.' app/src/main/java/org/sentinela/app/telecom/call/CallControls.kt)" -eq 0 ]`
    - `[ "$(grep -c 'Thread.sleep' app/src/test/java/org/sentinela/app/telecom/call/CallSessionCoordinatorTest.kt)" -eq 0 ]`
    - Prova de vermelho registrada no SUMMARY: remover o encerramento do tom pendente em
      `pressDigit` deixa >= 1 caso de `DtmfPairingTest` VERMELHO; restaurado depois
  </acceptance_criteria>
  <done>Cada comando da interface tem destino unico, verificavel e com pareamento de tom garantido.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Falhar alto e prazo de apresentacao</name>
  <read_first>
    - app/src/main/java/org/sentinela/app/telecom/call/CallSessionCoordinator.kt
    - app/src/test/java/org/sentinela/app/telecom/ScreeningCoordinatorFailureTest.kt
    - .planning/phases/06-modo-discador-opcional/06-RESEARCH.md
  </read_first>
  <files>
    app/src/test/java/org/sentinela/app/telecom/call/CallSessionFailureTest.kt,
    app/src/test/java/org/sentinela/app/telecom/call/CallSessionWatchdogTest.kt
  </files>
  <behavior>
    - excecao lancada pela costura em cada um dos comandos PROPAGA para quem chamou; o coordenador
      nao a captura e nao segue em frente com estado inconsistente
    - excecao lancada pela traducao de estado tambem propaga
    - uma chamada entregue em estado de chamada recebida sem que a apresentacao seja confirmada
      dentro do prazo curto declarado produz falha alta (excecao), com o relogio virtual avancado
      por despachante de teste
    - a confirmacao de apresentacao dentro do prazo NAO produz falha
    - o prazo e uma constante nomeada no arquivo do coordenador, nunca um literal espalhado
  </behavior>
  <action>
    1. Escrever `CallSessionFailureTest` no molde do `ScreeningCoordinatorFailureTest` da Fase 5:
       matriz de injecao de defeito em CADA metodo da costura, >= 7 `@Test`, cada um afirmando que
       a excecao ESCAPA (`assertFailsWith`). Este e o inverso deliberado da rede permissiva da
       Fase 5 e o KDoc precisa dizer por que, em prosa: quando o processo morre, o sistema de
       telefonia detecta e assume a chamada com o discador do aparelho — medido na pesquisa —,
       enquanto interface viva e congelada nao e detectada por ninguem.
    2. Acrescentar ao coordenador (arquivo do plano, task 2) o prazo de apresentacao: constante
       nomeada com valor **2000** milissegundos, um contrato `confirmPresented()` chamado pela
       camada de interface, e falha alta se o prazo vencer sem confirmacao para uma chamada
       recebida. Usar `runTest` + `TestDispatcher` + avanco de relogio virtual.
    3. `CallSessionWatchdogTest` com >= 4 `@Test`: vence o prazo e falha; confirma dentro do prazo
       e nao falha; chamada nao-recebida (saida) nao arma o prazo; encerramento antes do prazo
       desarma o prazo.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*CallSessionFailureTest" --tests "*CallSessionWatchdogTest" --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `[ "$(grep -c '@Test' app/src/test/java/org/sentinela/app/telecom/call/CallSessionFailureTest.kt)" -ge 7 ]`
    - `[ "$(grep -c '@Test' app/src/test/java/org/sentinela/app/telecom/call/CallSessionWatchdogTest.kt)" -ge 4 ]`
    - `[ "$(grep -c 'assertFailsWith\|assertThrows' app/src/test/java/org/sentinela/app/telecom/call/CallSessionFailureTest.kt)" -ge 7 ]`
    - `[ "$(grep -c 'runCatching\|try {' app/src/main/java/org/sentinela/app/telecom/call/CallSessionCoordinator.kt)" -eq 0 ]`
    - `[ "$(grep -c '2_000\|2000' app/src/main/java/org/sentinela/app/telecom/call/CallSessionCoordinator.kt)" -ge 1 ]`
    - Prova de vermelho registrada no SUMMARY: envolver um comando da costura em captura de
      excecao deixa >= 1 caso de `CallSessionFailureTest` VERMELHO; restaurado depois
  </acceptance_criteria>
  <done>Falha no caminho da chamada nunca fica silenciosa: ou propaga, ou estoura o prazo.</done>
</task>

</tasks>

<verification>
```bash
./gradlew --rerun-tasks testDebugUnitTest detekt lint
./gradlew koverLog
bash scripts/verify-invariants.sh
```
As tres classes novas de producao ficam em `telecom.*`, que JA esta no denominador do Kover — e
por serem puras entram sem exclude, como o coordenador da Fase 5. O gate `koverVerify` volta a
ser cobrado no ultimo plano da fase.
</verification>

<success_criteria>
- 12 estados da plataforma + inteiro desconhecido produzem estado nomeado (>= 14 casos verdes)
- >= 26 `@Test` novos verdes em JVM nas cinco classes deste plano
- Zero importacao da plataforma nos quatro arquivos de producao
- Tres provas de vermelho executadas e transcritas no SUMMARY
</success_criteria>

<output>
Criar `.planning/phases/06-modo-discador-opcional/06-01-SUMMARY.md`
</output>
