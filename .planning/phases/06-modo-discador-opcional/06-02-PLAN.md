---
phase: 06-modo-discador-opcional
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/org/sentinela/app/ui/theme/Type.kt
  - app/src/main/java/org/sentinela/app/ui/theme/Color.kt
  - app/src/main/java/org/sentinela/app/ui/theme/Shape.kt
  - app/src/main/res/values/strings.xml
  - app/src/main/java/org/sentinela/app/ui/components/SentinelaWatermark.kt
  - app/src/main/java/org/sentinela/app/ui/components/HonestyCard.kt
  - app/src/main/java/org/sentinela/app/ui/components/InfoBanner.kt
  - app/src/main/java/org/sentinela/app/ui/call/CallActionButton.kt
  - app/src/main/java/org/sentinela/app/ui/call/CallControlButton.kt
  - app/src/main/java/org/sentinela/app/ui/call/CallOriginChip.kt
  - app/src/main/java/org/sentinela/app/ui/call/CallTimer.kt
  - app/src/main/java/org/sentinela/app/ui/dialer/DialpadGrid.kt
  - app/src/main/java/org/sentinela/app/ui/dialer/DialpadKey.kt
  - app/src/test/java/org/sentinela/app/ui/theme/ThemeTokensTest.kt
  - app/src/test/java/org/sentinela/app/ui/CallStringsTest.kt
autonomous: true
requirements: [DIA-02]

must_haves:
  truths:
    - "As telas de chamada tem tipografia propria para numero e cronometro, com figuras de largura fixa"
    - "As cores de atender e de recusar/encerrar sao fixas e nao dependem do papel de parede do usuario"
    - "Todo texto novo das telas desta fase existe em recurso pt-BR, nenhum literal em Kotlin"
    - "Todo controle das telas de chamada tem alvo de toque de pelo menos 48dp e descricao de conteudo"
  artifacts:
    - path: "app/src/main/java/org/sentinela/app/ui/theme/Type.kt"
      provides: "estilos numberXl, numberLg e timer com figuras tabulares"
      contains: "tnum"
    - path: "app/src/main/java/org/sentinela/app/ui/theme/Color.kt"
      provides: "cores funcionais fixas de atender"
      contains: "CallAccept"
    - path: "app/src/main/java/org/sentinela/app/ui/theme/Shape.kt"
      provides: "formas 8/16/24 e pilula centralizadas"
    - path: "app/src/main/res/values/strings.xml"
      provides: "as 46 strings do contrato de design"
      contains: "dialer_activation_cta"
    - path: "app/src/main/java/org/sentinela/app/ui/call/CallControlButton.kt"
      provides: "controle de 56dp com estado ligado/desligado anunciado"
  key_links:
    - from: "app/src/main/java/org/sentinela/app/ui/call/CallActionButton.kt"
      to: "app/src/main/java/org/sentinela/app/ui/theme/Color.kt"
      via: "cor fixa recebida por parametro, fora do esquema dinamico"
      pattern: "CallAccept"
    - from: "app/src/main/java/org/sentinela/app/ui/call/CallTimer.kt"
      to: "app/src/main/java/org/sentinela/app/ui/theme/Type.kt"
      via: "estilo de cronometro com figuras de largura fixa"
      pattern: "timer"
---

<objective>
Entregar a fundacao visual das telas desta fase com acabamento equivalente ao dos mockups
existentes: tipografia com estilos proprios de numero e cronometro, cores funcionais fixas de
atender/recusar, formas centralizadas, as 46 strings pt-BR do contrato e os oito componentes
reutilizaveis que as quatro telas de chamada e as duas de discagem consomem.

Purpose: decisao travada do usuario — a interface desta fase e completa e polida desde o inicio.
Alem disso duas regras de seguranca dependem daqui: as cores de atender e recusar ficam FORA da
cor dinamica do papel de parede (senao um papel de parede pode aproximar as duas e produzir recusa
acidental de uma chamada real) e o cronometro precisa de figuras de largura fixa (sem isso o numero
pula a cada segundo).

Output: tres arquivos de tema, o arquivo de strings ampliado, oito componentes e duas classes de
teste.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-modo-discador-opcional/06-CONTEXT.md
@.planning/phases/06-modo-discador-opcional/06-UI-SPEC.md
@.planning/phases/06-modo-discador-opcional/06-VALIDATION.md
@docs/design/DESIGN.md
@CLAUDE.md

<interfaces>
`ThemeTokensTest` (Fase 1) TRAVA os 26 tokens atuais de `Color.kt` e a escala atual. A extensao
deste plano nao pode alterar nenhum valor existente — apenas acrescentar.

Valores exatos a acrescentar em `Color.kt`:
```
CallAccept    = 0xFF1E6E42
OnCallAccept  = 0xFFD9F2E3
```
Recusar e encerrar reutilizam os tokens de erro existentes (`ErrorContainer` `#93000A` /
`OnErrorContainer` `#FFDAD6`) — nao criar cor nova para eles.

Estilos exatos a acrescentar em `Type.kt` (extensoes nomeadas de `SentinelaTypography`, nunca
estilo inline no ponto de uso):
```
numberXl : 32sp, peso 500, altura de linha 40sp, espacamento 0.5
numberLg : 24sp, peso 500, altura de linha 32sp, espacamento 0.5
timer    : 16sp, peso 500, altura de linha 24sp, FontFeatureSettings("tnum")
```

Fontes Inter e Geist: se os arquivos de fonte estiverem disponiveis localmente, empacotar em
`app/src/main/res/font/` (licenca SIL OFL) e usar. **Se nao estiverem**, usar
`FontFamily.SansSerif` para texto e `FontFamily.Monospace` para os tres estilos numericos — o
monoespacado ja entrega largura fixa e cumpre o requisito funcional do cronometro — e registrar a
troca de fonte como pendencia em `docs/backlog/`. O app nao tem permissao de rede: **nenhuma fonte
pode ser baixada em tempo de execucao**, nem por provedor de fontes do sistema.

Formas de `Shape.kt` (novo): 8dp, 16dp, 24dp e pilula (`RoundedCornerShape(50)`).

Dimensoes travadas pelo contrato de design, a serem literais nomeados nos componentes:
atender/recusar 72dp · encerrar 64dp · tecla 72dp com espaco 8dp · controle secundario 56dp ·
apagar 56dp · fechar teclado 48dp · avatar 96dp.

A tabela completa das 46 strings esta na secao "Copywriting Contract" de
`.planning/phases/06-modo-discador-opcional/06-UI-SPEC.md`. Copiar chave e texto exatamente como
estao la, incluindo o bloco de honestidade da ativacao. As telas que consomem essas strings vem
nos planos 06-04 e 06-05: aqui elas apenas passam a existir.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tema — tipografia numerica, cores funcionais fixas e formas</name>
  <read_first>
    - app/src/main/java/org/sentinela/app/ui/theme/Type.kt
    - app/src/main/java/org/sentinela/app/ui/theme/Color.kt
    - app/src/main/java/org/sentinela/app/ui/theme/Theme.kt
    - app/src/test/java/org/sentinela/app/ui/theme/ThemeTokensTest.kt
    - .planning/phases/06-modo-discador-opcional/06-UI-SPEC.md
  </read_first>
  <files>
    app/src/main/java/org/sentinela/app/ui/theme/Type.kt,
    app/src/main/java/org/sentinela/app/ui/theme/Color.kt,
    app/src/main/java/org/sentinela/app/ui/theme/Shape.kt,
    app/src/test/java/org/sentinela/app/ui/theme/ThemeTokensTest.kt
  </files>
  <behavior>
    - os 26 tokens de cor ja travados continuam com exatamente os mesmos valores
    - `CallAccept` vale 0xFF1E6E42 e `OnCallAccept` vale 0xFFD9F2E3
    - os estilos numberXl, numberLg e timer existem com os tamanhos, pesos e alturas de linha do
      bloco de interfaces
    - o estilo do cronometro declara o ajuste de figuras de largura fixa
    - as quatro formas de `Shape.kt` existem com 8dp, 16dp, 24dp e pilula
  </behavior>
  <action>
    1. Acrescentar as duas cores a `Color.kt` sem tocar em nenhuma existente.
    2. Reescrever `Type.kt` mantendo a escala Material atual como base e acrescentando as tres
       extensoes nomeadas com os valores exatos do bloco de interfaces. KDoc registrando, em prosa,
       por que as cores de atender e de recusar nao vem do esquema dinamico: um papel de parede
       pode aproximar as duas e a consequencia de errar e irreversivel.
    3. Criar `Shape.kt` com as quatro formas e passa-las ao `MaterialTheme` dentro de
       `SentinelaTheme`.
    4. Resolver as fontes conforme a regra do bloco de interfaces; se cair no caminho de reserva,
       criar `docs/backlog/fontes-inter-geist.md` descrevendo a pendencia e acrescenta-lo a
       `docs/INDEX.md`.
    5. Estender `ThemeTokensTest` com >= 6 `@Test` novos cobrindo `<behavior>`, incluindo um caso
       que afirma os valores das duas cores novas por inteiro literal.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*ThemeTokensTest" --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `[ "$(grep -c 'CallAccept' app/src/main/java/org/sentinela/app/ui/theme/Color.kt)" -ge 1 ]`
    - `[ "$(grep -c 'tnum' app/src/main/java/org/sentinela/app/ui/theme/Type.kt)" -ge 1 ]`
    - `[ "$(grep -c 'numberXl' app/src/main/java/org/sentinela/app/ui/theme/Type.kt)" -ge 1 ]` e idem para `numberLg` e `timer`
    - `[ "$(grep -c 'downloadable\|GoogleFont\|FontRequest' app/src/main/java/org/sentinela/app/ui/theme/Type.kt)" -eq 0 ]`
    - `./gradlew testDebugUnitTest --tests "*ThemeTokensTest"` verde com >= 6 `@Test` novos
    - Prova de vermelho registrada no SUMMARY: alterar um digito de `CallAccept` deixa >= 1 caso
      VERMELHO; restaurado depois
  </acceptance_criteria>
  <done>O tema tem tipografia numerica, cores funcionais fixas e formas centralizadas.</done>
</task>

<task type="auto">
  <name>Task 2: As 46 strings pt-BR do contrato, com o bloco de honestidade</name>
  <read_first>
    - app/src/main/res/values/strings.xml
    - .planning/phases/06-modo-discador-opcional/06-UI-SPEC.md
    - docs/LIMITACOES.md
  </read_first>
  <files>
    app/src/main/res/values/strings.xml,
    app/src/test/java/org/sentinela/app/ui/CallStringsTest.kt
  </files>
  <behavior>
    - as 46 chaves da secao "Copywriting Contract" existem em pt-BR
    - nenhuma string desta fase contem promessa de bloqueio garantido, total ou infalivel
    - nenhuma string desta fase promete filtragem de aplicativo de mensagem por internet
    - nenhuma string desta fase sugere que o registro no historico do telefone deixa de acontecer
    - nenhuma string desta fase contem superlativo de marketing nem pressao de urgencia
    - as strings de descricao de conteudo de atender e recusar existem e sao distintas
  </behavior>
  <action>
    1. Acrescentar as 46 strings a `app/src/main/res/values/strings.xml` com as chaves e os textos
       exatos da tabela do contrato de design, mais as descricoes de conteudo dos controles
       (atender, recusar, encerrar, mudo ligado/desligado, viva-voz ligado/desligado, teclado
       aberto/fechado, fechar teclado, apagar, ligar, cada tecla do teclado).
    2. Criar `CallStringsTest` (Robolectric, `@Config(sdk = [35])`) que le os recursos de verdade e
       afirma `<behavior>`. A varredura de honestidade opera sobre o TEXTO DOS RECURSOS lidos em
       tempo de teste, nunca sobre o arquivo fonte — assim nenhum comentario deste plano pode
       derrubar o proprio criterio. A lista de expressoes proibidas fica declarada dentro do teste,
       em portugues, como dados do teste.
    3. Declarar `app/src/main/res` como input da task de teste se ainda nao estiver, para o teste
       nao ir a UP-TO-DATE quando so o arquivo de strings muda.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*CallStringsTest" --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `[ "$(grep -c 'name="dialer_activation_unchanged_1"' app/src/main/res/values/strings.xml)" -eq 1 ]`
    - `[ "$(grep -c 'name="call_action_answer"\|name="call_action_reject"\|name="call_action_hangup"' app/src/main/res/values/strings.xml)" -eq 3 ]`
    - O total de chaves novas acrescentadas neste plano e >= 46, conferido pelo diff do arquivo
    - `CallStringsTest` tem >= 8 `@Test` e todos verdes
    - Prova de vermelho registrada no SUMMARY: acrescentar temporariamente uma string com promessa
      de bloqueio total deixa >= 1 caso VERMELHO; removida depois
  </acceptance_criteria>
  <done>Todo texto das telas desta fase existe em recurso e passa pela varredura de honestidade.</done>
</task>

<task type="auto">
  <name>Task 3: Oito componentes reutilizaveis com acessibilidade real</name>
  <read_first>
    - app/src/main/java/org/sentinela/app/ui/theme/Theme.kt
    - app/src/main/java/org/sentinela/app/ui/theme/Type.kt
    - app/src/main/res/values/strings.xml
    - .planning/phases/06-modo-discador-opcional/06-UI-SPEC.md
  </read_first>
  <files>
    app/src/main/java/org/sentinela/app/ui/components/SentinelaWatermark.kt,
    app/src/main/java/org/sentinela/app/ui/components/HonestyCard.kt,
    app/src/main/java/org/sentinela/app/ui/components/InfoBanner.kt,
    app/src/main/java/org/sentinela/app/ui/call/CallActionButton.kt,
    app/src/main/java/org/sentinela/app/ui/call/CallControlButton.kt,
    app/src/main/java/org/sentinela/app/ui/call/CallOriginChip.kt,
    app/src/main/java/org/sentinela/app/ui/call/CallTimer.kt,
    app/src/main/java/org/sentinela/app/ui/dialer/DialpadGrid.kt,
    app/src/main/java/org/sentinela/app/ui/dialer/DialpadKey.kt
  </files>
  <action>
    1. `SentinelaWatermark`: escudo 24dp + "Sentinela" em label 12sp, cor de variante sobre a
       superficie a 60% de opacidade, `clearAndSetSemantics {}` (decorativo, nao focavel).
    2. `HonestyCard(title, items, iconPerItem)`: superficie de container baixo, raio 16dp, padding
       16dp, borda 1dp de variante de contorno. Os cards "o que muda" e "o que nao muda" usam
       ESTILO IDENTICO por decisao de produto — custo e beneficio com o mesmo peso visual.
    3. `InfoBanner(text, actionLabel, onAction)`: container alto, borda esquerda 4dp na cor de
       acento, icone informativo, corpo 14sp. Tom informativo; nunca cor de erro.
    4. `CallActionButton(diameter, containerColor, contentColor, icon, label, contentDescription,
       onClick)`: circulo, escala 0.95 em 120ms no toque, retorno tatil de confirmacao, label
       textual SOB o botao. A cor vem por parametro — o componente nao le o esquema dinamico.
    5. `CallControlButton(on, enabled, iconOff, iconOn, label, onToggle)`: circulo 56dp;
       desligado com container mais alto e icone de variante; ligado com container de acento,
       icone sobre acento e anel externo 2dp a 40%; desabilitado com container baixo, icone a 38%
       e `stateDescription` de indisponivel. Usa `Modifier.toggleable` com `Role.Switch` para o
       leitor de tela anunciar ligado/desligado sozinho. Transicao de cor em 150ms.
    6. `CallOriginChip(origin)`: pilula com os quatro pares de cor/label da tabela de chips do
       contrato (contato, permitido, desconhecido, privado). Passivo: sem `onClick`.
    7. `CallTimer(startedAtMillis, clock)`: formato MM:SS e H:MM:SS depois de uma hora, estilo
       `timer` do tema, `liveRegion` educado.
    8. `DialpadKey` (circulo 72dp, digito em `numberXl`, letras em label 12sp sob o digito, `0`
       com `+` como secundaria, escala 0.92 em 100ms + retorno tatil de tecla) e `DialpadGrid`
       (grade 3x4 com espaco 8dp, ordem 1 2 3 / 4 5 6 / 7 8 9 / * 0 #, callbacks de pressionar e
       soltar separados para o pareamento de tom do plano 06-01, toque longo em `0` inserindo `+`).
       Cada tecla recebe descricao de conteudo com o digito falado; asterisco e sustenido usam as
       strings dedicadas.
    9. Todo alvo de toque declarado com `Modifier.size` >= 48dp; nenhum icone acionavel sem
       descricao de conteudo vinda de recurso.
  </action>
  <verify>
    <automated>./gradlew assembleDebug lint detekt --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - Os nove arquivos existem e `./gradlew assembleDebug lint detekt` termina sem erro
    - `[ "$(grep -rc 'Role.Switch' app/src/main/java/org/sentinela/app/ui/call/CallControlButton.kt)" -ge 1 ]`
    - `[ "$(grep -c 'clearAndSetSemantics' app/src/main/java/org/sentinela/app/ui/components/SentinelaWatermark.kt)" -ge 1 ]`
    - Nenhum literal de texto visivel em Kotlin: `[ "$(grep -rcE '(Text|contentDescription)\s*=\s*"' app/src/main/java/org/sentinela/app/ui/call app/src/main/java/org/sentinela/app/ui/dialer app/src/main/java/org/sentinela/app/ui/components | grep -v ':0' | wc -l)" -eq 0 ]`
    - `[ "$(grep -c 'colorScheme' app/src/main/java/org/sentinela/app/ui/call/CallActionButton.kt)" -eq 0 ]`
    - Cada um dos oito componentes tem uma `@Preview` de conferencia visual
  </acceptance_criteria>
  <done>Os componentes das telas de chamada existem com acabamento e acessibilidade reais.</done>
</task>

</tasks>

<verification>
```bash
./gradlew --rerun-tasks assembleDebug testDebugUnitTest lint detekt
bash scripts/verify-invariants.sh
```
`ui.*` NAO esta no denominador do Kover (so domain, phone, data, settings, telecom, notifications
e permissions). Componentes de tela nao mexem no percentual de cobertura e nao precisam de exclude.
</verification>

<success_criteria>
- Nenhum valor de token existente mudou (`ThemeTokensTest` verde)
- 46 strings novas em pt-BR, varridas por teste contra promessa desonesta
- Nove componentes compilando, com alvo >= 48dp e descricao de conteudo em recurso
- Duas provas de vermelho executadas e transcritas no SUMMARY
</success_criteria>

<output>
Criar `.planning/phases/06-modo-discador-opcional/06-02-SUMMARY.md`
</output>
