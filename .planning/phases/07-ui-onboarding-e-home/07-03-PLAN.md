---
phase: 07-ui-onboarding-e-home
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - app/src/main/java/org/sentinela/app/ui/components/OptionCard.kt
  - app/src/main/java/org/sentinela/app/ui/components/StepHeader.kt
  - app/src/main/java/org/sentinela/app/ui/components/SentinelaTopBar.kt
  - app/src/main/java/org/sentinela/app/ui/components/SettingSwitchRow.kt
  - app/src/main/java/org/sentinela/app/ui/components/CheckRow.kt
  - app/src/main/java/org/sentinela/app/ui/components/SentinelaBottomBar.kt
  - app/src/test/java/org/sentinela/app/ui/components/Phase7ComponentSemanticsTest.kt
autonomous: true
requirements: [UIX-08, UIX-09, UIX-10]

must_haves:
  truths:
    - "Um controle desabilitado desta fase é anunciado como desabilitado, com o motivo em texto"
    - "Todo controle compartilhado da fase tem alvo de toque E tamanho desenhado acima do mínimo"
    - "Seleção, estado ligado/desligado e aba ativa são anunciados por papel e por estado, nunca só por cor"
  artifacts:
    - path: "app/src/main/java/org/sentinela/app/ui/components/OptionCard.kt"
      provides: "o componente-chave da fase: cartão de opção com papel de botão de rádio"
      min_lines: 90
    - path: "app/src/main/java/org/sentinela/app/ui/components/SettingSwitchRow.kt"
      provides: "rótulo + explicação permanente + interruptor em nó próprio"
      min_lines: 60
    - path: "app/src/test/java/org/sentinela/app/ui/components/Phase7ComponentSemanticsTest.kt"
      provides: "dois eixos de toque, papéis, descrição de estado e os pontos de risco de semântica mesclada"
      min_lines: 150
  key_links:
    - from: "app/src/test/java/org/sentinela/app/ui/components/Phase7ComponentSemanticsTest.kt"
      to: "app/src/test/java/org/sentinela/app/ui/TouchTargetAsserts.kt"
      via: "import dos três asserts extraídos em 07-02"
      pattern: "assertLayoutHeightIsAtLeast"
    - from: "app/src/main/java/org/sentinela/app/ui/components/OptionCard.kt"
      to: "app/src/main/res/values/strings.xml"
      via: "todo texto chega por parâmetro String vindo de stringResource na tela"
      pattern: "title: String"
---

<objective>
Os seis componentes compartilhados da fase, com a semântica de acessibilidade correta desde o
nascimento, e o teste que trava os dois eixos de toque e os pontos de risco de semântica mesclada.

Purpose: as cinco telas de onboarding, a home e a Proteção consomem estes seis componentes; escrever
cada tela com o seu próprio cartão de opção multiplicaria por cinco a chance de perder um estado na
semântica mesclada — que é o defeito que a Fase 6 mediu.
Output: `OptionCard`, `StepHeader`, `SentinelaTopBar`, `SettingSwitchRow`, `CheckRow`,
`SentinelaBottomBar` e `Phase7ComponentSemanticsTest`.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md
@.planning/phases/07-ui-onboarding-e-home/07-01-SUMMARY.md
@.planning/phases/07-ui-onboarding-e-home/07-02-SUMMARY.md
@CLAUDE.md

<interfaces>
Componentes prontos da Fase 6, a consumir como estão e NUNCA reimplementar:

```kotlin
fun HonestyCard(title: String, items: List<String>, itemIcon: ImageVector,
                modifier: Modifier = Modifier, itemIconTint: Color = MaterialTheme.colorScheme.onSurfaceVariant)
fun InfoBanner(text: String, modifier: Modifier = Modifier,
               actionLabel: String? = null, onAction: (() -> Unit)? = null)
```

Formas: `ShapeSmall` 8dp, `ShapeMedium` 16dp, `ShapeLarge` 24dp, `ShapePill` — de `ui/theme/Shape.kt`.
Cores novas de 07-01: `StatusAttention`, `OnStatusAttention`, `StatusBlocked`; reusadas
`CallAccept`/`OnCallAccept`.

Asserts de dois eixos, de 07-02, pacote `org.sentinela.app.ui`:
```kotlin
internal fun SemanticsNodeInteraction.assertTouchHeightIsAtLeast(minimo: Dp): SemanticsNodeInteraction
internal fun SemanticsNodeInteraction.assertTouchWidthIsAtLeast(minimo: Dp): SemanticsNodeInteraction
internal fun SemanticsNodeInteraction.assertLayoutHeightIsAtLeast(minimo: Dp): SemanticsNodeInteraction
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OptionCard e StepHeader</name>
  <files>app/src/main/java/org/sentinela/app/ui/components/OptionCard.kt, app/src/main/java/org/sentinela/app/ui/components/StepHeader.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/ui/components/HonestyCard.kt (o estilo de arquivo de componente deste projeto)
    - app/src/main/java/org/sentinela/app/ui/theme/Shape.kt
    - app/src/main/java/org/sentinela/app/ui/theme/Color.kt
    - .planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md (§6.0.1 e §6.0, cabeçalho de progresso)
  </read_first>
  <action>
    **`ui/components/OptionCard.kt`.** Assinatura:
    `OptionCard(title: String, description: String, icon: ImageVector, selected: Boolean,
    onClick: () -> Unit, modifier: Modifier = Modifier, iconContainerColor: Color? = null,
    iconTint: Color? = null, badge: String? = null, enabled: Boolean = true,
    unavailableReason: String? = null)`. Nenhum texto nasce dentro do componente — tudo chega por
    parâmetro `String`.

    Layout, valores exatos da §6.0.1: `Row` com altura mínima 72dp por `heightIn(min = 72.dp)`,
    padding 16dp, `ShapeMedium`; ícone-container circular de 40dp; 16dp entre ícone e coluna de
    texto; `Column` com título em `titleMedium` `OnSurface` e descrição em `bodyMedium`
    `OnSurfaceVariant` — descrição PERMANENTE, nunca tooltip e nunca elipsada; `weight(1f)`; ícone
    de confirmação de 24dp no fim.

    Estados: não selecionado usa fundo `SurfaceContainerLow`, borda 1dp `OutlineVariant`,
    ícone-container `SurfaceContainerHighest` com ícone `OnSurfaceVariant`, e o ícone de confirmação
    em contorno com alfa 0 — ele OCUPA espaço e fica invisível, para o layout não pular na seleção.
    Selecionado usa fundo `SurfaceContainerHigh`, borda **2dp `Primary`**, ícone-container
    `SecondaryContainer` com ícone `OnSecondaryContainer`, e ícone de confirmação preenchido
    `Primary`. Pressionado: ondulação padrão mais escala 0,98 por 100 ms, suprimida quando a
    redução de movimento está ligada.

    Badge opcional: pílula `SecondaryContainer`/`OnSecondaryContainer`, `labelSmall`, 8dp à direita
    do título.

    Semântica, e é aqui que a fase inteira ganha ou perde acessibilidade: o `Row` INTEIRO é um alvo
    único, por `Modifier.selectable(selected = selected, enabled = enabled, role = Role.RadioButton,
    onClick = onClick)`. O ícone de confirmação é decorativo, `contentDescription = null` — quem
    anuncia a seleção é o papel de botão de rádio, não o ícone. Quando `enabled` é falso, o
    `enabled = false` vai no modificador do PRÓPRIO `Row` e `unavailableReason` entra como
    `stateDescription` do mesmo nó. Registrar em KDoc, em português, as duas consequências medidas
    na Fase 6: (a) controle interativo nunca pode ser filho deste cartão, porque o cartão é o
    controle; (b) declarar o estado desabilitado envolvendo o componente com semântica de mesclagem
    NÃO funciona — o nó interno já mescla e é ele que responde às buscas.

    **`ui/components/StepHeader.kt`.** `StepHeader(step: Int, total: Int, modifier: Modifier = Modifier)`.
    Texto do contador vindo de `onboarding_step_indicator` formatado com os dois argumentos, em
    `labelMedium` `Primary`; barra de 4dp de altura por 96dp de largura, trilho
    `SurfaceContainerHighest`, preenchimento `Primary`, `RoundedCornerShape(50)`. A barra é
    DECORATIVA: `Modifier.clearAndSetSemantics {}` — a informação está no texto, que o leitor de
    tela lê. `total` chega por parâmetro e as telas passam 6 (§12.1 do contrato de design resolveu
    a contradição dos mockups em favor de um contador único de 6 passos).

    Uma pré-visualização por estado em cada arquivo, no molde da Fase 6, com
    `SentinelaTheme(darkTheme = true, dynamicColor = false)`.
  </action>
  <verify>
    <automated>./gradlew assembleDebug lint detekt --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Role.RadioButton' app/src/main/java/org/sentinela/app/ui/components/OptionCard.kt` é maior que 0
    - `grep -c 'heightIn' app/src/main/java/org/sentinela/app/ui/components/OptionCard.kt` é maior que 0 e o mínimo escrito ali é `72.dp`
    - `grep -c 'clearAndSetSemantics' app/src/main/java/org/sentinela/app/ui/components/StepHeader.kt` é maior que 0
    - `grep -cE '"[A-Za-zÀ-ú ]{4,}"' app/src/main/java/org/sentinela/app/ui/components/OptionCard.kt` restrito às linhas fora de KDoc devolve 0: nenhum texto de interface embutido em Kotlin
    - `./gradlew assembleDebug lint detekt --rerun-tasks` verde
  </acceptance_criteria>
  <done>O cartão de opção é um alvo único com papel de botão de rádio, descrição permanente e estado desabilitado no próprio nó; o cabeçalho de passo tem a barra declarada decorativa.</done>
</task>

<task type="auto">
  <name>Task 2: SentinelaTopBar, SettingSwitchRow e CheckRow</name>
  <files>app/src/main/java/org/sentinela/app/ui/components/SentinelaTopBar.kt, app/src/main/java/org/sentinela/app/ui/components/SettingSwitchRow.kt, app/src/main/java/org/sentinela/app/ui/components/CheckRow.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/ui/components/InfoBanner.kt
    - app/src/main/java/org/sentinela/app/ui/components/OptionCard.kt (criado na Task 1)
    - .planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md (§6.1, §6.7, §9.3, §11)
  </read_first>
  <action>
    **`ui/components/SentinelaTopBar.kt`.** `SentinelaTopBar(modifier: Modifier = Modifier,
    center: (@Composable () -> Unit)? = null, actions: (@Composable RowScope.() -> Unit)? = null)`.
    Altura 64dp, fundo transparente sobre `Surface`. À esquerda a marca: ícone de escudo 28dp
    `Primary` mais o nome do aplicativo vindo de `app_name` em `headlineSmall` negrito `Primary`.
    A marca é um nó único de texto; o ícone é decorativo. O slot central recebe o `StepHeader` no
    onboarding, e o slot de ações recebe o texto de pular ou o ícone de ajustes. Toda ação do slot
    tem alvo de 48dp.

    **`ui/components/SettingSwitchRow.kt`.** `SettingSwitchRow(label: String, description: String,
    checked: Boolean, onCheckedChange: (Boolean) -> Unit, modifier: Modifier = Modifier,
    enabled: Boolean = true, unavailableReason: String? = null)`. Altura mínima 56dp, padding
    vertical 12dp quando há explicação, margem lateral 16dp. Rótulo em `titleMedium` `OnSurface`,
    explicação PERMANENTE em `bodyMedium` `OnSurfaceVariant` logo abaixo, interruptor Material 3
    alinhado ao topo com alvo de 48dp.

    Ponto de risco nomeado (b) da §13.6 do contrato: a linha tem TRÊS nós — rótulo, interruptor e
    explicação — e o interruptor NÃO entra em nenhum container com semântica de mesclagem, sob pena
    de perder o estado ligado/desligado e o habilitado. O interruptor recebe `Role.Switch` e
    `stateDescription` explícito vindo por parâmetro implícito do próprio estado; quando `enabled`
    é falso, `unavailableReason` é o `stateDescription`. Registrar em KDoc, em prosa portuguesa,
    que esta é a razão de a explicação ser IRMÃ e não filha: mesclada, o leitor de tela produz um
    bloco único ilegível em vez de "rótulo, desativado" seguido da explicação.

    **`ui/components/CheckRow.kt`.** `CheckRow(label: String, stateText: String, ok: Boolean,
    modifier: Modifier = Modifier, actionLabel: String? = null, onAction: (() -> Unit)? = null)`.
    Altura 56dp. Ícone de confirmação `CallAccept` quando `ok`, ícone de erro `StatusAttention`
    quando não — sempre acompanhado de `stateText`, porque estado nunca é comunicado só por cor.
    Ponto de risco nomeado (e): quando existe ação, o botão dela é um nó FOCÁVEL SEPARADO, fora do
    nó mesclado da linha. `contentDescription` da linha é o rótulo seguido do estado.

    Pré-visualizações por estado em cada arquivo.
  </action>
  <verify>
    <automated>./gradlew assembleDebug lint detekt --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Role.Switch' app/src/main/java/org/sentinela/app/ui/components/SettingSwitchRow.kt` é maior que 0
    - `grep -c 'stateDescription' app/src/main/java/org/sentinela/app/ui/components/SettingSwitchRow.kt` é maior que 0
    - `grep -c 'mergeDescendants' app/src/main/java/org/sentinela/app/ui/components/SettingSwitchRow.kt` devolve 0
    - `grep -c 'app_name' app/src/main/java/org/sentinela/app/ui/components/SentinelaTopBar.kt` é maior que 0 — a marca vem de recurso, invariante de rebranding
    - Nenhum dos três arquivos contém texto de interface embutido em Kotlin fora de KDoc
    - `./gradlew assembleDebug lint detekt --rerun-tasks` verde
  </acceptance_criteria>
  <done>Barra superior com a marca vinda de recurso; linha de interruptor com três nós e nenhuma mesclagem; linha de verificação com a ação em nó separado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SentinelaBottomBar e o teste de semântica dos componentes</name>
  <files>app/src/main/java/org/sentinela/app/ui/components/SentinelaBottomBar.kt, app/src/test/java/org/sentinela/app/ui/components/Phase7ComponentSemanticsTest.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/ui/components/OptionCard.kt
    - app/src/main/java/org/sentinela/app/ui/components/SettingSwitchRow.kt
    - app/src/main/java/org/sentinela/app/ui/components/CheckRow.kt
    - app/src/test/java/org/sentinela/app/ui/TouchTargetAsserts.kt
    - app/src/test/java/org/sentinela/app/ui/call/CallScreenSemanticsTest.kt (o molde de teste de composição deste projeto)
    - .planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md (§12.10 e §13)
  </read_first>
  <behavior>
    - Cartão de opção selecionado anuncia papel de botão de rádio e estado selecionado
    - Cartão de opção desabilitado é anunciado como desabilitado E carrega o motivo em texto
    - Linha de interruptor anuncia o estado ligado e o desligado, e não perde o habilitado
    - Item de aba ativa anuncia papel de aba e seleção
    - Cada um dos cinco controles tem alvo de toque e tamanho desenhado acima do mínimo
    - Nenhum controle comunica estado apenas por cor: todos têm descrição de estado ou texto
  </behavior>
  <action>
    **`ui/components/SentinelaBottomBar.kt`.** Quatro itens, fundo `SurfaceContainer`, cantos
    superiores de 16dp. Rótulos de `nav_home`, `nav_whitelist`, `nav_history`, `nav_settings`.
    Decisão desta fase para a questão aberta da pesquisa: a barra entrega os QUATRO destinos porque
    é o que o mockup mostra, e os dois que pertencem à Phase 8 chegam como
    `enabled = false` com `stateDescription` explicando que a tela ainda não existe — aba que leva
    a tela em branco sem explicação é proibida por `UIX-10`. A assinatura recebe
    `items: List<BottomBarItem>` com rótulo, ícone, `selected`, `enabled`, `unavailableReason` e
    `onClick`, para a Phase 8 apenas habilitar dois itens sem tocar no componente.
    Dimensões corrigidas do mockup (§12.10): altura de item 56dp por `requiredHeight`, ícone 24dp,
    rótulo 12sp, alvo garantido de 48dp por 48dp. `requiredHeight` e não `height`: `size()` negocia
    com o pai, e na Fase 6 o pai comprimiu um círculo de 72dp para 23dp em silêncio.
    Semântica: `Role.Tab` mais `selected` em cada item.

    **`app/src/test/java/org/sentinela/app/ui/components/Phase7ComponentSemanticsTest.kt`**,
    `@RunWith(RobolectricTestRunner::class)`,
    `@Config(sdk = [35], qualifiers = "w411dp-h891dp-xxhdpi")`, `createComposeRule`. Os
    qualificadores de tela reais são obrigatórios: o aparelho padrão do Robolectric é pequeno demais
    e reprova por motivo falso.

    Casos, no mínimo 14:
    - cartão de opção não selecionado e selecionado: papel de botão de rádio presente, `selected`
      correto, título e descrição exibidos, e os DOIS eixos de toque acima de 72dp
    - cartão de opção desabilitado: `assertIsNotEnabled()` no nó do cartão E o `stateDescription`
      igual ao motivo passado. Usar `useUnmergedTree` se necessário para alcançar o nó
    - grupo de cartões: o container declara agrupamento selecionável, e apenas um está selecionado
    - cabeçalho de passo: o texto do contador está visível e casa o recurso formatado; a barra NÃO
      aparece como nó com informação
    - linha de interruptor ligada e desligada: papel de interruptor, `stateDescription` distinto
      nos dois casos, explicação legível como nó separado, dois eixos acima de 48dp
    - linha de interruptor desabilitada: `assertIsNotEnabled()` no nó do interruptor
    - linha de verificação com ação: o botão de ação é nó focável separado, com dois eixos acima de
      48dp; e a linha anuncia rótulo seguido do estado
    - barra inferior: quatro itens, papel de aba, um selecionado, os dois da Phase 8 desabilitados
      com motivo textual, e dois eixos acima de 48dp em cada item
    - barra superior: a marca é lida do recurso do nome do aplicativo, e a ação do slot tem dois
      eixos acima de 48dp

    Todo texto usado nos asserts vem de `stringResource`/`context.getString` — nunca literal no
    teste, o que também é o que permite a varredura de honestidade de 07-01 continuar sendo a única
    dona do texto.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*Phase7ComponentSemanticsTest" --rerun-tasks && ./gradlew assembleDebug lint detekt --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew testDebugUnitTest --tests "*Phase7ComponentSemanticsTest" --rerun-tasks` verde com no mínimo 14 `@Test`
    - `grep -c 'assertLayoutHeightIsAtLeast' app/src/test/java/org/sentinela/app/ui/components/Phase7ComponentSemanticsTest.kt` é no mínimo 6, e para cada uso existe um `assertTouchHeightIsAtLeast` no mesmo caso
    - `grep -c 'requiredHeight' app/src/main/java/org/sentinela/app/ui/components/SentinelaBottomBar.kt` é maior que 0
    - PROVA DE VERMELHO 1 (eixo desenhado): reduzir a altura do item da barra inferior de 56dp para 40dp ⇒ SÓ o eixo desenhado fica vermelho; transcrever no SUMMARY que o eixo de toque continuou verde, que é a lição medida na Fase 6; restaurar
    - PROVA DE VERMELHO 2 (semântica mesclada): envolver o interruptor da linha de configuração num container com semântica de mesclagem ⇒ o caso de desabilitado fica vermelho; restaurar
    - PROVA DE VERMELHO 3 (estado só por cor): remover o `stateDescription` do cartão de opção desabilitado ⇒ o caso correspondente fica vermelho; restaurar
    - As três provas com saída transcrita no SUMMARY, todas sobre código já commitado nesta task
  </acceptance_criteria>
  <done>Os seis componentes existem, e cada ponto de risco de semântica mesclada nomeado no contrato de design tem um caso vermelho demonstrado.</done>
</task>

</tasks>

<verification>
```bash
./gradlew --rerun-tasks assembleDebug testDebugUnitTest lint detekt
bash scripts/verify-invariants.sh
./gradlew koverLog
```
</verification>

<success_criteria>
- Seis componentes compartilhados, todos com alvo em dois eixos e semântica correta
- Quatro dos cinco pontos de risco de semântica mesclada do contrato cobertos aqui (o quinto, o
  cartão hero da home com o interruptor dentro, fica em 07-08)
- Três provas de vermelho transcritas
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-ui-onboarding-e-home/07-03-SUMMARY.md`
</output>
