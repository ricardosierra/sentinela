---
phase: 07-ui-onboarding-e-home
plan: 08
type: execute
wave: 3
depends_on: ["07-03", "07-04"]
files_modified:
  - app/src/main/java/org/sentinela/app/ui/home/StatusHeroCard.kt
  - app/src/main/java/org/sentinela/app/ui/home/StatCard.kt
  - app/src/main/java/org/sentinela/app/ui/home/LastBlockedCard.kt
  - app/src/main/java/org/sentinela/app/ui/home/QuickActionRow.kt
  - app/src/main/java/org/sentinela/app/ui/home/HomeScreen.kt
  - app/src/test/java/org/sentinela/app/ui/home/HomeScreenStateTest.kt
  - app/src/test/java/org/sentinela/app/ui/home/HomePrivacyTest.kt
autonomous: true
requirements: [SCR-02, UIX-02, UIX-09, UIX-10, UIX-11]

must_haves:
  truths:
    - "A home mostra o estado real do papel e um botão de correção que abre o seletor do sistema"
    - "O interruptor do cartão principal alterna a preferência de proteção, nunca o papel do sistema"
    - "A home nunca renderiza zero bloqueadas quando o histórico está desligado, carregando ou em erro"
    - "A última chamada bloqueada aparece mascarada, e o número completo não existe nesta tela"
    - "Proteção ligada sem leitura da agenda produz um aviso explícito com atalho para conceder"
    - "Nenhum rótulo de risco ou classificação de spam aparece na home"
  artifacts:
    - path: "app/src/main/java/org/sentinela/app/ui/home/StatCard.kt"
      provides: "cartão de estatística que aceita apenas o tipo fechado de valor"
      contains: "StatValue"
    - path: "app/src/main/java/org/sentinela/app/ui/home/HomeScreen.kt"
      provides: "a home inteira, com os oito estados degradados"
      min_lines: 220
    - path: "app/src/test/java/org/sentinela/app/ui/home/HomeScreenStateTest.kt"
      provides: "um caso por estado degradado, incluindo os três em que zero é proibido"
      min_lines: 180
  key_links:
    - from: "app/src/main/java/org/sentinela/app/ui/home/StatCard.kt"
      to: "app/src/main/java/org/sentinela/app/ui/home/HomeUiState.kt"
      via: "parâmetro do tipo fechado, sem sobrecarga numérica"
      pattern: "value: StatValue"
    - from: "app/src/main/java/org/sentinela/app/ui/home/LastBlockedCard.kt"
      to: "app/src/main/java/org/sentinela/app/ui/home/HomeUiState.kt"
      via: "recebe apenas o número já mascarado, produzido pelo dono de estado"
      pattern: "maskedNumber"
---

<objective>
A home inteira: cartão principal de proteção, banners de estado, estatísticas, última bloqueada
mascarada, atalhos e barra inferior — com os oito estados degradados que nenhum mockup previu.

Purpose: é o critério de sucesso 2 da fase (home mostra status real do papel e proteção com botão de
correção que funciona) e é a tela onde as três decisões mais delicadas do usuário se materializam: o
interruptor alterna a preferência e não o papel, o zero é impossível quando o histórico está
desligado, e a última bloqueada é mascarada.
Output: quatro componentes de home, `HomeScreen` e dois testes.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md
@.planning/phases/07-ui-onboarding-e-home/07-CONTEXT.md
@.planning/phases/07-ui-onboarding-e-home/07-03-SUMMARY.md
@.planning/phases/07-ui-onboarding-e-home/07-04-SUMMARY.md
@docs/design/telas/dashboard/code.html
@CLAUDE.md

<interfaces>
De 07-04:
```kotlin
sealed interface StatValue {
    data class Loaded(val count: Long) : StatValue
    data object Unavailable : StatValue
    data object Loading : StatValue
}
data class HomeUiState(
    val protectionEnabled: Boolean, val screeningRoleHeld: Boolean, val screeningRoleAvailable: Boolean,
    val contactsPermission: ContactsPermissionState, val dialerMode: DialerModeState,
    val totalBlocked: StatValue, val blockedToday: StatValue,
    val lastBlocked: LastBlockedUi?, val historyEnabled: Boolean, val readError: Boolean,
)
data class LastBlockedUi(val maskedNumber: String, val reasonLabelRes: Int, val timestampUtcMillis: Long)
```
De 07-03: `SentinelaTopBar`, `SentinelaBottomBar`, `BottomBarItem`.
Da Fase 6: `InfoBanner(text, modifier, actionLabel, onAction)`.
De 07-01: `StatusAttention`, `OnStatusAttention`, `StatusBlocked`; reusados `CallAccept`/`OnCallAccept`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StatusHeroCard, StatCard, LastBlockedCard e QuickActionRow</name>
  <files>app/src/main/java/org/sentinela/app/ui/home/StatusHeroCard.kt, app/src/main/java/org/sentinela/app/ui/home/StatCard.kt, app/src/main/java/org/sentinela/app/ui/home/LastBlockedCard.kt, app/src/main/java/org/sentinela/app/ui/home/QuickActionRow.kt</files>
  <read_first>
    - docs/design/telas/dashboard/code.html
    - app/src/main/java/org/sentinela/app/ui/home/HomeUiState.kt
    - app/src/main/java/org/sentinela/app/ui/theme/Color.kt
    - app/src/main/java/org/sentinela/app/phone/PhoneMask.kt
    - .planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md (§4.2, §5, §7, §12.5, §12.8, §12.10)
  </read_first>
  <action>
    **`ui/home/StatusHeroCard.kt`.**
    `StatusHeroCard(protectionEnabled: Boolean, onProtectionChange: (Boolean) -> Unit,
    modifier: Modifier = Modifier)`. `ShapeLarge` de 24dp, padding 24dp. Ligado: fundo
    `PrimaryContainer` e conteúdo `OnPrimaryContainer`. Desligado: fundo `StatusAttention` FIXO e
    conteúdo `OnStatusAttention` FIXO — literais, nunca lidos do esquema, porque a partir do nível 31
    o esquema inteiro vem do papel de parede e um papel de parede laranja tornaria ligado e desligado
    indistinguíveis.
    Dentro: `titleLarge` negrito com `dashboard_protection_active` ou
    `dashboard_protection_inactive`; 4dp; linha de estado com ponto de 12dp — `CallAccept` FIXO com
    pulsação quando ligado, `Outline` estático quando desligado — mais `labelMedium` com
    `dashboard_monitoring` ou `dashboard_protection_off_hint`; à direita e alinhado ao topo, o
    interruptor Material 3 com alvo de 48dp; 16dp; faixa interna com fundo do conteúdo a 10%,
    `ShapeMedium`, padding 16dp, ícone de escudo verificado preenchido e `bodyMedium` com
    `dashboard_device_safe`.

    **O interruptor alterna a PREFERÊNCIA de proteção, não o papel do sistema.** KDoc em português
    registrando o motivo concreto: revogar um papel encerra o processo, e o usuário veria o
    aplicativo fechar sozinho ao desligar um interruptor; o papel é estado somente-leitura e vive no
    aviso separado.

    **Ponto de risco (a) da semântica mesclada:** o interruptor é nó PRÓPRIO, com papel de
    interruptor e descrição de estado, e fica FORA do nó mesclado do cartão. Mesclar o cartão com o
    interruptor dentro faz o leitor de tela perder o ligado/desligado e o habilitado — medido na
    Fase 6. O cartão em si tem descrição de conteúdo como TEXTO ("proteção ativa" ou "proteção
    desativada"); o ponto e a pulsação recebem `clearAndSetSemantics {}`.

    **`ui/home/StatCard.kt`.**
    `StatCard(label: String, value: StatValue, icon: ImageVector, accent: Color,
    modifier: Modifier = Modifier)`. Altura 128dp, `ShapeLarge`, padding 20dp, fundo
    `SurfaceContainerLow`, borda de 1dp `OutlineVariant` — o vidro do mockup vira camada tonal,
    porque desfoque em tempo real na home custa quadro e a interface só existe a partir do nível 31.
    Ícone no topo; rótulo `labelMedium` `OnSurfaceVariant`; o valor em `headlineLarge` 32sp com
    entrelinha 40 e peso 600 — o mockup usa uma classe de tipografia que NÃO existe na configuração
    dele, e o desenho saiu com tamanho de reserva pequeno; esta é a correção.

    O `when` sobre `StatValue` é EXAUSTIVO e o parâmetro não tem sobrecarga numérica:
    `Loaded` mostra o número; `Unavailable` mostra `dashboard_history_off_value` em `titleMedium`
    `OnSurfaceVariant`; `Loading` mostra esqueleto tonal `SurfaceContainerLow` com
    `state_loading` como descrição de conteúdo. KDoc registrando que é o TIPO que impede renderizar
    zero como reserva de "ainda não sei", e que acrescentar aqui um parâmetro numérico reabriria o
    defeito. Descrição de conteúdo do cartão é rótulo seguido de valor — ler o número solto não
    significa nada.

    **`ui/home/LastBlockedCard.kt`.**
    `LastBlockedCard(maskedNumber: String, reasonLabel: String, relativeTime: String,
    onClick: () -> Unit, modifier: Modifier = Modifier)`. Recebe o número JÁ MASCARADO — a assinatura
    não tem parâmetro de número em bruto, o que é a fronteira herdada da Fase 6 expressa no tipo.
    `ShapeMedium`, padding 16dp, `SurfaceContainerLow`, borda de 1dp `OutlineVariant`; avatar circular
    de 48dp em `StatusBlocked` a 30% com ícone de telefone desabilitado `StatusBlocked` FIXO; 16dp;
    coluna com o número mascarado em `bodyLarge` `OnSurface` e o motivo REAL em `labelMedium`
    `OnSurfaceVariant`; peso; ícone de seta `Outline`. O cartão inteiro é clicável e leva ao
    histórico.

    O ícone de informação do mockup SAI, e o rótulo de risco do mockup SAI: o aplicativo não
    classifica spam, não conhece base de spam e não tem esse dado. Os únicos rótulos permitidos vêm
    de `history_unknown_number`, `history_private_number`, `history_private_id`,
    `call_origin_contact` e `call_origin_whitelist`. Registrar a proibição em prosa portuguesa.

    Descrição de conteúdo: última chamada bloqueada, a máscara, o motivo, o tempo relativo. Nunca a
    sequência completa.

    Tempo relativo, granularidade da §12.10: "agora" abaixde um minuto, "há N min" abaixo de uma
    hora, "há N h" abaixo de um dia, "ontem", depois data curta. Plurais por recurso de plural, nunca
    por concatenação — se faltar chave de plural, criá-la agora neste arquivo de recursos e registrar
    no SUMMARY que a contagem de 07-01 subiu.

    **`ui/home/QuickActionRow.kt`.**
    `QuickActionRow(label: String, icon: ImageVector, iconContainerColor: Color, onClick: () -> Unit,
    modifier: Modifier = Modifier, enabled: Boolean = true, unavailableReason: String? = null)`.
    Altura 72dp por `requiredHeight`, `ShapeMedium`, padding 20dp, fundo `SurfaceContainer`, borda de
    1dp `OutlineVariant`, ícone-container de 40dp, seta ao fim. Alvo em dois eixos acima de 48dp.

    Pré-visualizações por estado em cada arquivo.
  </action>
  <verify>
    <automated>./gradlew assembleDebug lint detekt --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'value: StatValue' app/src/main/java/org/sentinela/app/ui/home/StatCard.kt` é 1 e `grep -cE 'value: (Int|Long)' ` no mesmo arquivo devolve 0
    - `grep -cE 'numberE164|fullNumber|maskNumber\(' app/src/main/java/org/sentinela/app/ui/home/LastBlockedCard.kt` devolve 0 — o cartão recebe já mascarado e não mascara nada
    - `grep -c 'StatusAttention' app/src/main/java/org/sentinela/app/ui/home/StatusHeroCard.kt` é maior que 0 e `grep -c 'colorScheme.errorContainer'` no mesmo arquivo devolve 0
    - `grep -c 'mergeDescendants' app/src/main/java/org/sentinela/app/ui/home/StatusHeroCard.kt` devolve 0
    - `grep -c 'requiredHeight' app/src/main/java/org/sentinela/app/ui/home/QuickActionRow.kt` é maior que 0
    - Nenhum texto de interface embutido em Kotlin em nenhum dos quatro arquivos
    - `./gradlew assembleDebug lint detekt --rerun-tasks` verde
  </acceptance_criteria>
  <done>Quatro componentes de home, com o interruptor fora do nó mesclado, o valor da estatística fechado por tipo, o número recebido já mascarado e as cores semânticas fixas.</done>
</task>

<task type="auto">
  <name>Task 2: HomeScreen com os oito estados degradados</name>
  <files>app/src/main/java/org/sentinela/app/ui/home/HomeScreen.kt</files>
  <read_first>
    - docs/design/telas/dashboard/code.html
    - app/src/main/java/org/sentinela/app/ui/home/StatusHeroCard.kt
    - app/src/main/java/org/sentinela/app/ui/home/StatCard.kt
    - app/src/main/java/org/sentinela/app/ui/components/SentinelaBottomBar.kt
    - app/src/main/java/org/sentinela/app/ui/components/InfoBanner.kt
    - .planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md (§7 e §8 inteiras)
  </read_first>
  <action>
    Criar `ui/home/HomeScreen.kt`, composta PURA:
    `HomeScreen(state: HomeUiState, onProtectionChange: (Boolean) -> Unit, onFixRole: () -> Unit,
    onGrantContacts: () -> Unit, onOpenAppSettings: () -> Unit, onEnableHistory: () -> Unit,
    onRetryHistory: () -> Unit, onOpenSettings: () -> Unit, onOpenWhitelist: () -> Unit,
    onOpenHistory: () -> Unit, onOpenDialerActivation: () -> Unit, modifier: Modifier = Modifier)`.

    Estrutura da §7, na ordem e com os espaçamentos ditados: barra superior de 64dp com a marca e o
    ícone de ajustes de 48dp levando à Proteção, com elevação tonal ao rolar — o desfoque do mockup
    vira elevação tonal Material 3; 24dp; o cartão principal; 24dp; os banners; 24dp; duas colunas de
    estatística com 16dp de espaçamento; 24dp; o bloco da última bloqueada com cabeçalho
    `dashboard_last_blocked` e o tempo relativo à direita; 24dp; os dois atalhos com 12dp de
    espaçamento, `dashboard_quick_whitelist` e `dashboard_quick_history`; 32dp mais o recorte;
    e a barra inferior de quatro itens. Toda a tela é ROLÁVEL — nenhuma tela desta fase depende de
    caber, porque a escala de fonte pode dobrar.

    **Os oito estados da §8, cada um com tratamento explícito:**
    1. papel de triagem ausente: cartão principal em desligado mais `InfoBanner` com
       `dashboard_role_missing` e ação `dashboard_fix_configuration` chamando `onFixRole`. Se
       `screeningRoleAvailable` é falso, o botão de correção NÃO aparece — a intenção de pedido não
       existe no aparelho e o toque não resolveria nada
    2. proteção desligada pelo usuário: cartão em desligado, SEM banner (é escolha, não erro), e as
       estatísticas continuam visíveis
    3. leitura da agenda negada: `InfoBanner` com `dashboard_contacts_missing`; negação simples oferece
       `dialer_activation_grant_contacts`; negação definitiva oferece `about_open_app_settings`
    4. histórico desligado: os dois cartões recebem `StatValue.Unavailable`, e abaixo um `InfoBanner`
       com `dashboard_history_off` e ação `dashboard_history_off_action` chamando `onEnableHistory`.
       O bloco da última bloqueada NÃO é renderizado
    5. histórico ligado e nenhum bloqueio ainda: estatísticas com zero — ali zero é VERDADE — e o
       bloco da última bloqueada vira estado vazio com ícone de escudo e `history_empty`
    6. papel de discador perdido: `InfoBanner` informativo de tom NEUTRO, sem vermelho porque nada
       quebrou, com `dialer_role_lost_title`/`dialer_role_lost_body` e ação `dialer_role_lost_action`
       chamando `onOpenDialerActivation`
    7. carregando: os dois cartões com `StatValue.Loading` e esqueleto tonal. Nunca zero como reserva
    8. falha de leitura: `InfoBanner` com `state_error` e ação de tentar de novo; estatísticas em
       indisponível

    **Precedência dos banners**, e ela é obrigatória: papel de triagem ausente, depois agenda negada,
    depois histórico desligado, depois papel de discador perdido. No MÁXIMO DOIS banners simultâneos;
    o excedente vira uma linha que leva à Proteção, porque três banners empilhados empurram o
    conteúdo real da home fora da tela.

    Semântica: índice de travessia explícito na ordem marca, cartão principal como um nó, interruptor,
    banners, estatísticas, última bloqueada, atalhos, barra inferior. Cada estatística é um nó único.

    Pré-visualizações: uma por estado degradado, oito no total.
  </action>
  <verify>
    <automated>./gradlew assembleDebug lint detekt --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'fraude|alto risco|spam' app/src/main/java/org/sentinela/app/ui/home/HomeScreen.kt` devolve 0
    - `grep -c 'dashboard_history_off' app/src/main/java/org/sentinela/app/ui/home/HomeScreen.kt` é maior que 0
    - `grep -c 'screeningRoleAvailable' app/src/main/java/org/sentinela/app/ui/home/HomeScreen.kt` é maior que 0 — o botão de correção depende da disponibilidade do papel
    - `grep -cE 'AppContainer|ViewModel|LifecycleResumeEffect' app/src/main/java/org/sentinela/app/ui/home/HomeScreen.kt` devolve 0 — a tela é pura; a reconsulta na retomada vive na camada de rota, em 07-10
    - `grep -c 'verticalScroll|LazyColumn' app/src/main/java/org/sentinela/app/ui/home/HomeScreen.kt` é maior que 0
    - Nenhum texto de interface embutido em Kotlin
    - `./gradlew assembleDebug lint detekt --rerun-tasks` verde
  </acceptance_criteria>
  <done>A home tem os oito estados, a precedência dos banners, o teto de dois banners e zero rótulo de risco.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Testes de estado e de privacidade da home</name>
  <files>app/src/test/java/org/sentinela/app/ui/home/HomeScreenStateTest.kt, app/src/test/java/org/sentinela/app/ui/home/HomePrivacyTest.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/ui/home/HomeScreen.kt
    - app/src/test/java/org/sentinela/app/ui/TouchTargetAsserts.kt
    - app/src/main/java/org/sentinela/app/phone/PhoneMask.kt
    - app/src/test/java/org/sentinela/app/ui/home/HomeViewModelTest.kt (de 07-04, para não duplicar cobertura)
  </read_first>
  <behavior>
    - Histórico desligado não renderiza o caractere zero em nenhum cartão de estatística
    - Carregando não renderiza o caractere zero em nenhum cartão de estatística
    - Falha de leitura não renderiza o caractere zero em nenhum cartão de estatística
    - Histórico ligado com contagem zero renderiza zero, e o bloco vazio da última bloqueada aparece
    - Histórico desligado não renderiza o bloco da última bloqueada
    - Papel ausente com papel disponível exibe o aviso e o botão de correção, e tocar nele emite uma vez
    - Papel ausente com papel indisponível no aparelho NÃO exibe botão de correção
    - Proteção desligada exibe o cartão em desligado e nenhum aviso de erro
    - O interruptor do cartão é anunciado com papel de interruptor e descrição de estado distinta
    - Nunca aparecem mais de dois avisos simultâneos
    - A última bloqueada exibe o texto mascarado, e a tela não exibe em nenhum nó a sequência completa de dígitos
    - Cada controle da home passa os dois eixos de toque
  </behavior>
  <action>
    **`app/src/test/java/org/sentinela/app/ui/home/HomeScreenStateTest.kt`**,
    `@RunWith(RobolectricTestRunner::class)`,
    `@Config(sdk = [35], qualifiers = "w411dp-h891dp-xxhdpi")`, `createComposeRule`.

    Um caso por estado degradado, mais os de semântica e de alvo. No mínimo 18 casos.

    O caso do zero proibido é o mais importante da fase e precisa ser escrito com dentes: varrer a
    ÁRVORE SEMÂNTICA da região das estatísticas e afirmar que nenhum nó de texto ali é igual ao
    caractere zero. Afirmar apenas que o traço aparece seria fraco — a tela poderia mostrar os dois.
    Escrever os três casos (desligado, carregando, erro) e o caso do zero verdadeiro LADO A LADO na
    mesma classe, para que ninguém conserte um quebrando o outro.

    O caso do teto de banners conta os nós de aviso montando um estado com quatro condições ruins
    simultâneas e afirmando que aparecem exatamente dois mais a linha que leva à Proteção.

    **`app/src/test/java/org/sentinela/app/ui/home/HomePrivacyTest.kt`**, mesma configuração. A
    fronteira do número: montar o estado com uma última bloqueada cuja máscara foi produzida por
    `PhoneMask.mask` sobre um número conhecido, compor a home, e afirmar que (a) o texto mascarado
    aparece e (b) varrendo a árvore semântica INTEIRA, incluindo descrições de conteúdo, nenhum nó
    contém a sequência completa de dígitos daquele número. O número de teste é dado do teste, e a
    varredura é sobre nós compostos — nunca sobre texto de fonte.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*HomeScreenStateTest" --tests "*HomePrivacyTest" --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew testDebugUnitTest --tests "*HomeScreenStateTest" --tests "*HomePrivacyTest" --rerun-tasks` verde com no mínimo 20 `@Test` somados
    - `grep -c 'assertLayoutHeightIsAtLeast' app/src/test/java/org/sentinela/app/ui/home/HomeScreenStateTest.kt` é no mínimo 4, cada um com `assertTouchHeightIsAtLeast` no mesmo caso
    - PROVA DE VERMELHO 1 (zero mentiroso na tela): fazer o cartão de estatística renderizar zero no ramo indisponível ⇒ os três casos ficam vermelhos; restaurar
    - PROVA DE VERMELHO 2 (fronteira do número): fazer a home receber e exibir o número sem máscara ⇒ o teste de privacidade fica vermelho; restaurar
    - PROVA DE VERMELHO 3 (interruptor no nó mesclado): envolver o cartão principal com semântica de mesclagem contendo o interruptor ⇒ o caso de descrição de estado fica vermelho; restaurar
    - PROVA DE VERMELHO 4 (botão de correção sem papel disponível): fazer o botão aparecer mesmo quando o aparelho não oferece o papel ⇒ vermelho; restaurar
    - As quatro provas transcritas no SUMMARY com a saída real, todas sobre código já commitado
  </acceptance_criteria>
  <done>Os oito estados, o teto de banners, a fronteira do número e o quinto ponto de risco de semântica mesclada estão travados, com quatro provas de vermelho.</done>
</task>

</tasks>

<verification>
```bash
./gradlew --rerun-tasks assembleDebug testDebugUnitTest lint detekt
bash scripts/verify-invariants.sh
./gradlew koverLog
```
</verification>

<success_criteria>
- Home fiel ao layout do mockup, com copy honesta e zero rótulo de risco
- Zero mentiroso impossível, provado por varredura da árvore semântica e não por convenção
- Última bloqueada mascarada, provada por varredura de todos os nós incluindo descrições
- Vinte casos verdes e quatro provas de vermelho transcritas
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-ui-onboarding-e-home/07-08-SUMMARY.md`
</output>
