---
phase: 07-ui-onboarding-e-home
plan: 05
type: execute
wave: 3
depends_on: ["07-03", "07-04"]
files_modified:
  - app/src/main/java/org/sentinela/app/ui/onboarding/WelcomeScreen.kt
  - app/src/main/java/org/sentinela/app/ui/onboarding/RoleStepScreen.kt
  - app/src/main/java/org/sentinela/app/ui/onboarding/UnknownPolicyStepScreen.kt
  - app/src/test/java/org/sentinela/app/ui/onboarding/WelcomeAndRoleStepTest.kt
autonomous: true
requirements: [SCR-01, UIX-01, UIX-09, UIX-11]

must_haves:
  truths:
    - "A tela de boas-vindas afirma apenas o que o aplicativo faz: local, silencioso e sem internet"
    - "O passo do papel carrega o aviso de que só chamada de telefone é filtrada, com o mesmo peso visual do resto"
    - "Papel negado não trava o passo nem repete o diálogo do sistema sem toque explícito"
    - "O botão em 'solicitando permissão' é anunciado como desabilitado"
  artifacts:
    - path: "app/src/main/java/org/sentinela/app/ui/onboarding/WelcomeScreen.kt"
      provides: "tela 0 com os três cartões honestos e o selo de código aberto"
      min_lines: 120
    - path: "app/src/main/java/org/sentinela/app/ui/onboarding/RoleStepScreen.kt"
      provides: "passo 1 de 6 com o cartão de honestidade obrigatório e os três ramos de estado do papel"
      min_lines: 140
    - path: "app/src/main/java/org/sentinela/app/ui/onboarding/UnknownPolicyStepScreen.kt"
      provides: "passo 2 de 6 com três cartões de opção e o padrão bloquear"
      min_lines: 90
  key_links:
    - from: "app/src/main/java/org/sentinela/app/ui/onboarding/RoleStepScreen.kt"
      to: "app/src/main/java/org/sentinela/app/ui/components/HonestyCard.kt"
      via: "as três frases honestas herdadas das Fases 5 e 6, por identificador de recurso"
      pattern: "HonestyCard"
    - from: "app/src/main/java/org/sentinela/app/ui/onboarding/UnknownPolicyStepScreen.kt"
      to: "app/src/main/java/org/sentinela/app/ui/components/OptionCard.kt"
      via: "grupo selecionável de três opções"
      pattern: "OptionCard"
---

<objective>
Tela de boas-vindas e os dois primeiros passos do onboarding, com fidelidade aos mockups no layout e
copy honesta no texto — a substituição decidida pelo usuário.

Purpose: é aqui que o critério de sucesso 1 começa (o onboarding concede o papel) e é aqui que o
aviso obrigatório de escopo tem de aparecer com peso visual, não como rodapé em cinza.
Output: `WelcomeScreen`, `RoleStepScreen`, `UnknownPolicyStepScreen` e o teste de composição dos três.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md
@.planning/phases/07-ui-onboarding-e-home/07-CONTEXT.md
@.planning/phases/07-ui-onboarding-e-home/07-03-SUMMARY.md
@.planning/phases/07-ui-onboarding-e-home/07-04-SUMMARY.md
@docs/design/telas/boas_vindas_ao_sentinela/code.html
@docs/design/telas/onboarding/code.html
@docs/design/telas/configura_o_desconhecidos/code.html
@CLAUDE.md

<interfaces>
Componentes de 07-03 (assinaturas exatas a consumir):
```kotlin
fun OptionCard(title: String, description: String, icon: ImageVector, selected: Boolean,
               onClick: () -> Unit, modifier: Modifier = Modifier,
               iconContainerColor: Color? = null, iconTint: Color? = null,
               badge: String? = null, enabled: Boolean = true, unavailableReason: String? = null)
fun StepHeader(step: Int, total: Int, modifier: Modifier = Modifier)
fun SentinelaTopBar(modifier: Modifier = Modifier, center: (@Composable () -> Unit)? = null,
                    actions: (@Composable RowScope.() -> Unit)? = null)
```
Da Fase 6, consumidos como estão:
```kotlin
fun HonestyCard(title: String, items: List<String>, itemIcon: ImageVector,
                modifier: Modifier = Modifier, itemIconTint: Color = ...)
fun InfoBanner(text: String, modifier: Modifier = Modifier,
               actionLabel: String? = null, onAction: (() -> Unit)? = null)
```
De 07-04: `OnboardingUiState` com `step`, `totalSteps`, `screeningRoleHeld`,
`roleRequestInFlight`, `roleDenied`, `settings`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WelcomeScreen — tela 0</name>
  <files>app/src/main/java/org/sentinela/app/ui/onboarding/WelcomeScreen.kt</files>
  <read_first>
    - docs/design/telas/boas_vindas_ao_sentinela/code.html
    - docs/design/telas/onboarding/code.html (a fonte dos três cartões honestos)
    - app/src/main/java/org/sentinela/app/ui/components/SentinelaTopBar.kt
    - .planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md (§6.1, §12.3, §12.7, §12.8)
  </read_first>
  <action>
    Criar `ui/onboarding/WelcomeScreen.kt`, composta PURA:
    `WelcomeScreen(onStart: () -> Unit, onAbout: () -> Unit, modifier: Modifier = Modifier)`.
    Nenhum container, nenhum dono de estado.

    Estrutura exata da §6.1, de cima para baixo, com os espaçamentos ditados:
    barra superior de 64dp com a marca e o ícone de informação de 48dp cuja descrição de conteúdo é
    `about_title`; 32dp; hero de círculo de 128dp `SurfaceContainer` com borda de 2dp `Primary` a
    30% de alfa e ícone de escudo de 64dp `Primary`; 32dp; título em `headlineMedium` 28sp com
    entrelinha 36 e peso 600, `OnSurface`, centralizado, largura máxima 280dp, texto
    `welcome_headline`; 16dp; subtítulo `bodyLarge` `OnSurfaceVariant` centralizado, máximo 320dp,
    `welcome_subtitle`; 48dp; três cartões em arranjo assimétrico — um largo ocupando as duas colunas
    e dois de meia largura, espaçamento de 16dp — com `welcome_feature_local_*`,
    `welcome_feature_silent_*` e `welcome_feature_offline_*`, cada cartão em `SurfaceContainerLow`,
    `ShapeMedium`, borda de 1dp `OutlineVariant`, padding de 16dp, ícone-container de 40dp, título
    `labelLarge` e descrição `labelMedium`; 24dp; chip pílula `SurfaceContainerHighest` com ícone de
    16dp e o texto `welcome_open_source`; peso restante; botão pílula de 56dp
    `Primary`/`OnPrimary` de largura total com `welcome_cta` e seta, com gradiente de `Surface` a
    transparente de 32dp acima; 16dp; microcopy `labelMedium` `OnSurfaceVariant` centralizada,
    `welcome_cta_hint`; 32dp mais o recorte da barra de navegação.

    Três adaptações obrigatórias, decisão do usuário: (a) o cartão de base global do mockup NÃO
    entra e o selo de código aberto ocupa o lugar do selo de proteção ativa; (b) a imagem
    fotográfica remota do mockup vira superfície tonal em gradiente de `SurfaceContainerLow` a
    `PrimaryContainer` a 20% de alfa mais ícone vetorial, porque o aplicativo não declara acesso à
    internet e imagem remota é impossível; (c) a sobreposição de "preparando escudo" do mockup sai —
    não há trabalho a fazer entre esta tela e o passo 1, e barra de progresso falsa é padrão
    escuro. Registrar as três em KDoc, apontando o arquivo de registro pós-lançamento
    `docs/backlog/capacidades-prometidas-nos-mockups.md`.

    A animação de flutuação do mockup fica com amplitude reduzida a 4dp e é suprimida quando a
    redução de movimento está ligada; o hero e a animação recebem
    `Modifier.clearAndSetSemantics {}`. O paralaxe do mockup sai: não existe cursor em Android.

    Semântica: ordem marca, título, subtítulo, cartões, selo, botão, microcopy. O título recebe
    `semantics { heading() }`.

    Pré-visualizações: uma clara e uma escura, ambas com `dynamicColor = false`.
  </action>
  <verify>
    <automated>./gradlew assembleDebug lint detekt --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'welcome_open_source' app/src/main/java/org/sentinela/app/ui/onboarding/WelcomeScreen.kt` é maior que 0
    - `grep -ciE 'googleusercontent|https?://' app/src/main/java/org/sentinela/app/ui/onboarding/WelcomeScreen.kt` devolve 0
    - `grep -c 'heading()' app/src/main/java/org/sentinela/app/ui/onboarding/WelcomeScreen.kt` é maior que 0
    - `grep -c 'clearAndSetSemantics' app/src/main/java/org/sentinela/app/ui/onboarding/WelcomeScreen.kt` é maior que 0
    - Nenhum texto de interface embutido em Kotlin: todo texto chega por `stringResource`
    - `grep -cE 'AppContainer|ViewModel' app/src/main/java/org/sentinela/app/ui/onboarding/WelcomeScreen.kt` devolve 0
    - `./gradlew assembleDebug lint detekt --rerun-tasks` verde
  </acceptance_criteria>
  <done>Boas-vindas fiel ao layout do mockup, com os três cartões honestos, o selo de código aberto, zero imagem remota e zero progresso falso.</done>
</task>

<task type="auto">
  <name>Task 2: RoleStepScreen — passo 1 de 6, com o aviso obrigatório de escopo</name>
  <files>app/src/main/java/org/sentinela/app/ui/onboarding/RoleStepScreen.kt</files>
  <read_first>
    - docs/design/telas/onboarding/code.html
    - app/src/main/java/org/sentinela/app/ui/components/HonestyCard.kt
    - app/src/main/java/org/sentinela/app/ui/components/InfoBanner.kt
    - app/src/main/java/org/sentinela/app/ui/dialer/DialerActivationScreen.kt (o precedente de dois cartões com peso igual)
    - .planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md (§6.0, §6.2)
  </read_first>
  <action>
    Criar `ui/onboarding/RoleStepScreen.kt`, composta pura:
    `RoleStepScreen(state: OnboardingUiState, onRequestRole: () -> Unit, onNext: () -> Unit,
    onSkip: () -> Unit, modifier: Modifier = Modifier)`.

    Estrutura da §6.2: barra superior com marca, `StepHeader(step = 1, total = 6)` no centro e a ação
    de texto `onboarding_skip` com alvo de 48dp e cor `OnSurfaceVariant` à direita; 32dp; hero de
    círculo de 96dp em `PrimaryContainer` a 20% com escudo preenchido de 64dp `Primary` e flutuação
    de 4dp; 24dp; título `headlineMedium` `OnSurface` centralizado com `onboarding_role_title`;
    16dp; corpo `bodyLarge` `OnSurfaceVariant` centralizado a 85% da largura com
    `onboarding_role_intro`; 24dp; faixa de contexto de 80dp em `SurfaceContainerLow`,
    `ShapeMedium`, gradiente tonal de `SurfaceContainerLow` a `PrimaryContainer` a 20%, com ícone de
    16dp e `welcome_badge_native` em `labelMedium` `Primary`; 24dp; o cartão de honestidade.

    O CARTÃO DE HONESTIDADE é o aviso obrigatório da fase e não é rodapé em cinza: mesmo peso visual
    do resto da tela, exatamente como a tela de ativação do discador faz. `title` é
    `onboarding_scope_title`, ícone de informação com tinta `OnSurfaceVariant`, e os itens são, nesta
    ordem, `dialer_activation_unchanged_3`, `onboarding_scope_dnd` e
    `settings_hide_native_log_desc`. As três frases já existem em recurso, escritas nas Fases 5 e 6 a
    partir da fonte do Android — REESCREVÊ-LAS é o caminho de volta à promessa falsa e é proibido.

    Rodapé fixo: botão pílula de 56dp `Primary`/`OnPrimary`. Três ramos:
    - papel não detido e sem pedido em curso: rótulo `onboarding_role_cta`, ação `onRequestRole`
    - pedido em curso (`roleRequestInFlight`): rótulo `onboarding_role_requesting`, indicador
      circular de 20dp à esquerda, `enabled = false`. O botão fica FORA de qualquer container com
      semântica de mesclagem — este é o ponto de risco (d) nomeado no contrato, e mesclado o estado
      desabilitado desapareceria em silêncio
    - papel detido: rótulo `onboarding_next`, ação `onNext`; e sob o título aparece um chip pílula
      `CallAccept` a 20% com ícone de confirmação e `dialer_active_chip`. O avanço NÃO é automático:
      o usuário lê a confirmação e toca
    Quando `roleDenied`: `InfoBanner(text = onboarding_role_denied, actionLabel = onboarding_role_retry,
    onAction = onRequestRole)` acima do botão, e o botão passa a `onboarding_next`. Nunca travar
    aqui, e nunca repetir o diálogo do sistema sem toque explícito.

    Abaixo do botão, 16dp, o esclarecimento `onboarding_role_disclaimer` em `bodyMedium`
    `OnSurfaceVariant` centralizado.

    O chip e o banner de resultado recebem `liveRegion = LiveRegionMode.Polite`, para anunciar a
    transição concedido/negado sem roubar o foco do botão. Nunca `Assertive`.

    O rodapé "Sentinela Guardian" do mockup SAI: a marca é exatamente Sentinela.

    Transição de passo: deslizamento horizontal mais dissolução, 250 ms, curva de saída rápida e
    entrada lenta; espelhada no retorno; troca instantânea quando a redução de movimento está ligada.
    Implementar como parâmetro do envelope de navegação, não dentro desta tela, se for mais simples —
    a decisão é do executor, desde que a supressão por redução de movimento exista.

    Pré-visualizações: um por ramo de estado, três no total.
  </action>
  <verify>
    <automated>./gradlew assembleDebug lint detekt --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'dialer_activation_unchanged_3' app/src/main/java/org/sentinela/app/ui/onboarding/RoleStepScreen.kt` é 1
    - `grep -c 'onboarding_scope_dnd' app/src/main/java/org/sentinela/app/ui/onboarding/RoleStepScreen.kt` é 1
    - `grep -c 'settings_hide_native_log_desc' app/src/main/java/org/sentinela/app/ui/onboarding/RoleStepScreen.kt` é 1
    - `grep -c 'HonestyCard' app/src/main/java/org/sentinela/app/ui/onboarding/RoleStepScreen.kt` é maior que 0
    - `grep -c 'Polite' app/src/main/java/org/sentinela/app/ui/onboarding/RoleStepScreen.kt` é maior que 0 e `grep -c 'Assertive'` devolve 0
    - `git diff app/src/main/res/values/strings.xml` não mostra alteração no VALOR de `dialer_activation_unchanged_3` nem de `settings_hide_native_log_desc`
    - Nenhum texto de interface embutido em Kotlin
    - `./gradlew assembleDebug lint detekt --rerun-tasks` verde
  </acceptance_criteria>
  <done>O passo do papel pede o papel com explicação, carrega o aviso obrigatório de escopo com peso visual, e não trava nem repete o diálogo quando negado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: UnknownPolicyStepScreen e o teste de composição dos três primeiros ecrãs</name>
  <files>app/src/main/java/org/sentinela/app/ui/onboarding/UnknownPolicyStepScreen.kt, app/src/test/java/org/sentinela/app/ui/onboarding/WelcomeAndRoleStepTest.kt</files>
  <read_first>
    - docs/design/telas/configura_o_desconhecidos/code.html
    - app/src/main/java/org/sentinela/app/ui/components/OptionCard.kt
    - app/src/main/java/org/sentinela/app/ui/onboarding/RoleStepScreen.kt
    - app/src/test/java/org/sentinela/app/ui/TouchTargetAsserts.kt
    - .planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md (§6.3)
  </read_first>
  <behavior>
    - Boas-vindas exibe título, subtítulo, os três cartões honestos, o selo e o botão
    - Passo do papel exibe as três frases do cartão de honestidade, lidas do recurso
    - Papel negado exibe o aviso com ação, e o botão passa a Próximo sem ficar desabilitado
    - Pedido em curso deixa o botão anunciado como desabilitado
    - Passo dos desconhecidos vem com Bloquear pré-selecionado
    - Escolher Silenciar move a seleção e emite exatamente uma vez a política escolhida
    - Nenhuma das três telas oferece Nunca Silenciar para desconhecidos
    - Cada botão e cada cartão de opção passa os dois eixos de toque
  </behavior>
  <action>
    Criar `ui/onboarding/UnknownPolicyStepScreen.kt`, composta pura:
    `UnknownPolicyStepScreen(selected: OriginPolicy, onSelect: (OriginPolicy) -> Unit,
    onNext: () -> Unit, onSkip: () -> Unit, modifier: Modifier = Modifier)`.

    Estrutura da §6.3: barra superior com `StepHeader(step = 2, total = 6)` e a ação de pular;
    conteúdo centralizado verticalmente num cartão de largura máxima 400dp — o cartão flutuante É a
    assinatura visual deste passo e se mantém — em `SurfaceContainerLow`, `ShapeMedium`, borda de 1dp
    `OutlineVariant` a 30% e padding de 16dp. Dentro: círculo de 64dp em `PrimaryContainer` a 20%
    com ícone de 32dp `Primary`; 4dp; título `headlineMedium` `OnSurface` centralizado com
    `unknown_title`; 4dp; `bodyMedium` `OnSurfaceVariant` centralizado com `unknown_question`;
    24dp; três `OptionCard` com 8dp de espaçamento num container com
    `Modifier.semantics { selectableGroup() }`:
    bloquear com ícone de bloqueio, `ErrorContainer` a 20% e tinta `Error`, textos
    `unknown_option_block`/`_desc`, mapeando para bloquear — é o PADRÃO;
    silenciar com ícone de notificação desativada, `SecondaryContainer` a 20% e tinta `Secondary`,
    `unknown_option_silence`/`_desc`;
    permitir com ícone de chamada, `TertiaryContainer` a 20% e tinta `Tertiary`,
    `unknown_option_allow`/`_desc`, mapeando para tocar.
    Depois 32dp; botão pílula de 56dp com `onboarding_next` e seta; 16dp; microcopy `labelMedium`
    `OnSurfaceVariant` a 60% com `onboarding_change_later`.

    Duas restrições de domínio a registrar em KDoc: "nunca silenciar" NÃO é oferecida para
    desconhecidos, porque não faria sentido e o mockup também não a oferece; e o estilo do bloqueio
    (rejeitar contra caixa postal) não aparece aqui — vive na tela Proteção.

    Criar `app/src/test/java/org/sentinela/app/ui/onboarding/WelcomeAndRoleStepTest.kt`,
    `@RunWith(RobolectricTestRunner::class)`,
    `@Config(sdk = [35], qualifiers = "w411dp-h891dp-xxhdpi")`, `createComposeRule`, cobrindo os oito
    comportamentos acima, com no mínimo 12 casos. Todo texto de assert vem do recurso lido em tempo
    de teste, nunca literal no teste. Os dois eixos de toque afirmados com os asserts de 07-02 em
    cada botão e em cada cartão de opção.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*WelcomeAndRoleStepTest" --rerun-tasks && ./gradlew assembleDebug lint detekt --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew testDebugUnitTest --tests "*WelcomeAndRoleStepTest" --rerun-tasks` verde com no mínimo 12 `@Test`
    - `grep -c 'NEVER_SILENCE' app/src/main/java/org/sentinela/app/ui/onboarding/UnknownPolicyStepScreen.kt` devolve 0
    - `grep -c 'selectableGroup' app/src/main/java/org/sentinela/app/ui/onboarding/UnknownPolicyStepScreen.kt` é maior que 0
    - `grep -c 'assertLayoutHeightIsAtLeast' app/src/test/java/org/sentinela/app/ui/onboarding/WelcomeAndRoleStepTest.kt` é no mínimo 3, cada um acompanhado de `assertTouchHeightIsAtLeast`
    - PROVA DE VERMELHO 1: trocar o padrão do passo 2 de bloquear para tocar ⇒ o caso de padrão fica vermelho; restaurar
    - PROVA DE VERMELHO 2: remover uma das três frases do cartão de honestidade do passo 1 ⇒ o caso correspondente fica vermelho; restaurar
    - PROVA DE VERMELHO 3: envolver o botão do passo 1 num container com semântica de mesclagem ⇒ o caso de desabilitado em pedido em curso fica vermelho; restaurar
  </acceptance_criteria>
  <done>Os três primeiros ecrãs existem, com padrão correto, aviso de escopo presente e os dois eixos de toque afirmados; três provas de vermelho transcritas.</done>
</task>

</tasks>

<verification>
```bash
./gradlew --rerun-tasks assembleDebug testDebugUnitTest lint detekt
bash scripts/verify-invariants.sh
./gradlew koverLog
```
</verification>

<success_criteria>
- Três ecrãs compostos puros, sem container e sem dono de estado dentro
- Zero texto de interface em Kotlin, zero imagem remota, zero afirmação de capacidade inexistente
- Doze casos verdes e três provas de vermelho transcritas
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-ui-onboarding-e-home/07-05-SUMMARY.md`
</output>
