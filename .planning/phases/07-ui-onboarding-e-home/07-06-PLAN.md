---
phase: 07-ui-onboarding-e-home
plan: 06
type: execute
wave: 3
depends_on: ["07-03", "07-04"]
files_modified:
  - app/src/main/java/org/sentinela/app/ui/onboarding/ContactsPolicyStepScreen.kt
  - app/src/main/java/org/sentinela/app/ui/onboarding/WhitelistPolicyStepScreen.kt
  - app/src/test/java/org/sentinela/app/ui/onboarding/ContactsAndWhitelistStepTest.kt
autonomous: true
requirements: [UIX-01, UIX-09, UIX-11]

must_haves:
  truths:
    - "O passo de contatos mostra, na tela e não em rodapé, que sem a leitura da agenda contatos podem cair como desconhecidos"
    - "Os quatro estados da permissão da agenda têm tratamento distinto, e a negação definitiva não oferece diálogo que não aparece mais"
    - "As quatro opções continuam editáveis mesmo sem a permissão concedida"
    - "A descrição de nunca silenciar diz que o Não Perturbe continua valendo"
  artifacts:
    - path: "app/src/main/java/org/sentinela/app/ui/onboarding/ContactsPolicyStepScreen.kt"
      provides: "passo 3 de 6: quatro políticas, cartão de permissão em quatro ramos e o interruptor de privados"
      min_lines: 160
    - path: "app/src/main/java/org/sentinela/app/ui/onboarding/WhitelistPolicyStepScreen.kt"
      provides: "passo 4 de 6: quatro políticas com nunca silenciar como padrão e hint permanente"
      min_lines: 120
  key_links:
    - from: "app/src/main/java/org/sentinela/app/ui/onboarding/ContactsPolicyStepScreen.kt"
      to: "app/src/main/java/org/sentinela/app/data/contacts/ContactsPermissionState.kt"
      via: "ramo por estado com quatro tratamentos distintos"
      pattern: "ContactsPermissionState"
---

<objective>
Passos 3 e 4 do onboarding: política de contatos com o pedido de leitura da agenda em quatro ramos, e
tratamento da whitelist.

Purpose: fecha a metade central do critério de sucesso 1 (política de contatos com pedido de
`READ_CONTACTS`, tratamento da whitelist) e é onde a consequência honesta de negar a agenda precisa
aparecer na tela.
Output: `ContactsPolicyStepScreen`, `WhitelistPolicyStepScreen` e o teste de composição dos dois.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md
@.planning/phases/07-ui-onboarding-e-home/07-03-SUMMARY.md
@.planning/phases/07-ui-onboarding-e-home/07-04-SUMMARY.md
@docs/design/telas/configura_o_contatos/code.html
@docs/design/telas/configura_o_whitelist/code.html
@CLAUDE.md

<interfaces>
```kotlin
enum class ContactsPermissionState { GRANTED, NEVER_ASKED, DENIED_ONCE, DENIED_PERMANENTLY }
val ContactsPermissionState.canRequest: Boolean            // NEVER_ASKED ou DENIED_ONCE
val ContactsPermissionState.shouldOfferSystemSettings: Boolean  // só DENIED_PERMANENTLY
enum class OriginPolicy { RING, BLOCK, SILENCE, NEVER_SILENCE }
```
Componentes: `OptionCard`, `StepHeader`, `SentinelaTopBar`, `SettingSwitchRow` (07-03);
`HonestyCard`, `InfoBanner` (Fase 6) — assinaturas em 07-03-SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContactsPolicyStepScreen — passo 3 de 6</name>
  <files>app/src/main/java/org/sentinela/app/ui/onboarding/ContactsPolicyStepScreen.kt</files>
  <read_first>
    - docs/design/telas/configura_o_contatos/code.html
    - app/src/main/java/org/sentinela/app/data/contacts/ContactsPermissionState.kt
    - app/src/main/java/org/sentinela/app/ui/components/SettingSwitchRow.kt
    - app/src/main/java/org/sentinela/app/ui/components/OptionCard.kt
    - .planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md (§6.4, §12.4)
  </read_first>
  <action>
    Criar `ui/onboarding/ContactsPolicyStepScreen.kt`, composta pura:
    `ContactsPolicyStepScreen(permission: ContactsPermissionState, selected: OriginPolicy,
    blockPrivate: Boolean, onSelect: (OriginPolicy) -> Unit, onBlockPrivateChange: (Boolean) -> Unit,
    onGrantContacts: () -> Unit, onOpenAppSettings: () -> Unit, onNext: () -> Unit,
    onSkip: () -> Unit, modifier: Modifier = Modifier)`.

    Estrutura da §6.4: barra superior com `StepHeader(step = 3, total = 6)` e a ação de pular. Este
    mockup usa página cheia, sem cartão central, e o contador do mockup vai para a barra superior
    como em todos os passos — a unificação decidida no contrato de design. Depois: 24dp; título
    `headlineMedium` `OnSurface` alinhado à ESQUERDA com `contacts_title`; 16dp; `bodyLarge`
    `OnSurfaceVariant` com `contacts_explainer`; 24dp; o cartão de permissão; 24dp; quatro
    `OptionCard` com 16dp de espaçamento num container com agrupamento selecionável:
    tocar com ícone de notificação ativa e badge `contacts_default_badge` — é o PADRÃO;
    bloquear com ícone de bloqueio; silenciar com ícone de notificação desativada; nunca silenciar
    com ícone de prioridade alta. Textos de `contacts_option_*` e `contacts_option_*_desc`.
    Depois 24dp; linha de interruptor com `settings_block_private` e explicação permanente
    `settings_block_private_desc`, LIGADO por padrão; peso restante; botão pílula de 56dp com
    `onboarding_next`.

    **Cartão de permissão, quatro ramos, um por estado — nenhum ramo mudo:**
    - nunca perguntado: cartão em `SurfaceContainerLow` com ícone de contatos `Primary`, texto
      `contacts_permission_rationale` e botão tonal `SecondaryContainer` com
      `dialer_activation_grant_contacts`, chamando `onGrantContacts`. O diálogo do sistema é
      disparado UMA vez
    - concedido: colapsa em chip pílula `CallAccept` a 20% com ícone de confirmação e
      `contacts_permission_granted`
    - negado uma vez: `InfoBanner(text = contacts_permission_denied,
      actionLabel = dialer_activation_grant_contacts, onAction = onGrantContacts)`
    - negado definitivamente: `InfoBanner(text = contacts_permission_blocked,
      actionLabel = about_open_app_settings, onAction = onOpenAppSettings)`. SEM novo pedido do
      sistema: a plataforma já não mostra o diálogo, e oferecê-lo seria um toque que não faz nada

    Decidir o ramo pelo `when` exaustivo sobre o enum, e usar `canRequest` e
    `shouldOfferSystemSettings` — as propriedades já existem e não devem ser reimplementadas como
    condição própria.

    **A consequência honesta fica NA TELA, não em rodapé:** sem a leitura da agenda a consulta
    responde indisponível e a chamada cai na política de erro, ou seja, contatos podem ser tratados
    como desconhecidos. É exatamente o que `contacts_permission_denied` diz, e por isso ele aparece
    como aviso e não como nota. As quatro opções permanecem HABILITADAS e editáveis sem a permissão —
    a escolha é preferência persistida e passa a valer no instante em que a permissão for concedida.
    Registrar em KDoc a alternativa considerada e rejeitada: desabilitar os cartões, o que seria pior
    (desabilitar sem explicar) e recairia justamente onde o estado se perde na semântica mesclada.

    A descrição de nunca silenciar é `contacts_option_never_silence_desc`, já corrigida na Fase 1 —
    o mockup afirma que a opção ignora o Não Perturbe, e isso é FALSO, medido na fonte do Android na
    Fase 5. Não reescrever a string.

    O interruptor de bloquear privados não está em nenhum mockup e é exigência do documento de telas;
    ele existe aqui e repete na Proteção, com a mesma explicação.

    Pré-visualizações: uma por estado de permissão, quatro no total.
  </action>
  <verify>
    <automated>./gradlew assembleDebug lint detekt --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ContactsPermissionState.GRANTED' app/src/main/java/org/sentinela/app/ui/onboarding/ContactsPolicyStepScreen.kt` é maior que 0 e as quatro constantes do enum aparecem no arquivo
    - `grep -c 'contacts_permission_denied' app/src/main/java/org/sentinela/app/ui/onboarding/ContactsPolicyStepScreen.kt` é 1
    - `grep -c 'contacts_option_never_silence_desc' app/src/main/java/org/sentinela/app/ui/onboarding/ContactsPolicyStepScreen.kt` é 1, e `git diff app/src/main/res/values/strings.xml` não altera o valor dessa chave
    - `grep -c 'enabled = false' app/src/main/java/org/sentinela/app/ui/onboarding/ContactsPolicyStepScreen.kt` devolve 0 — as quatro opções nunca são desabilitadas por falta de permissão
    - Nenhum texto de interface embutido em Kotlin
    - `./gradlew assembleDebug lint detekt --rerun-tasks` verde
  </acceptance_criteria>
  <done>Quatro ramos de permissão distintos, consequência honesta na tela, opções sempre editáveis e a descrição de nunca silenciar preservada.</done>
</task>

<task type="auto">
  <name>Task 2: WhitelistPolicyStepScreen — passo 4 de 6</name>
  <files>app/src/main/java/org/sentinela/app/ui/onboarding/WhitelistPolicyStepScreen.kt</files>
  <read_first>
    - docs/design/telas/configura_o_whitelist/code.html
    - app/src/main/java/org/sentinela/app/ui/components/OptionCard.kt
    - .planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md (§6.5, §12.1, §12.7)
  </read_first>
  <action>
    Criar `ui/onboarding/WhitelistPolicyStepScreen.kt`, composta pura:
    `WhitelistPolicyStepScreen(selected: OriginPolicy, onSelect: (OriginPolicy) -> Unit,
    onNext: () -> Unit, onBack: () -> Unit, onSkip: () -> Unit, modifier: Modifier = Modifier)`.

    Estrutura da §6.5: barra superior com `StepHeader(step = 4, total = 6)` e a ação de pular; 24dp;
    título `headlineMedium` `OnSurface` com `whitelist_setup_title`; 8dp; `bodyMedium`
    `OnSurfaceVariant` com `whitelist_setup_desc`; 24dp; cartão explicativo em `SurfaceContainerLow`,
    `ShapeMedium`, padding de 24dp, conteúdo centralizado, com círculo de 80dp `PrimaryContainer` e
    ícone de escudo preenchido de 40dp `OnPrimaryContainer`, 16dp, `titleLarge` `OnSurface` com
    `whitelist_setup_what_title`, 8dp, `bodyMedium` `OnSurfaceVariant` com
    `whitelist_setup_what_desc`; 24dp; `labelLarge` `OnSurfaceVariant` com
    `whitelist_setup_question`; 16dp; quatro `OptionCard` com 16dp de espaçamento e agrupamento
    selecionável: nunca silenciar com ícone de notificação ativa e badge `contacts_default_badge` —
    é o PADRÃO; tocar com ícone de volume; bloquear com ícone de bloqueio; silenciar com ícone de
    notificação desativada. Textos de `whitelist_option_*` e `_desc`. Depois 32dp; botão pílula de
    56dp com `onboarding_next`; 8dp; botão de texto de 48dp `Primary` com `onboarding_back`; 16dp;
    hint FIXO em `bodyMedium` `OnSurfaceVariant` com `whitelist_setup_hint`.

    Três adaptações do mockup, cada uma registrada em KDoc:
    - o quadro ilustrado remoto do mockup é substituído pelo cartão explicativo acima, porque o
      aplicativo não declara acesso à internet
    - o hint do mockup é um aviso temporário que desliza depois de um segundo; ele vira TEXTO
      PERMANENTE no rodapé, porque informação que desaparece por conta própria é informação perdida,
      e aviso temporizado é hostil ao leitor de tela
    - o botão deste passo é "Próximo", NÃO "Finalizar Configuração": o contador único de seis passos
      resolveu a contradição dos três mockups, e finalizar só existe no passo 6
    - o mockup rotula o padrão como padrão do sistema, o que é impreciso — é o padrão do Sentinela;
      usar o badge de padrão e a descrição honesta já existente, que diz que o Não Perturbe do
      sistema continua valendo

    Pré-visualizações: uma por opção selecionada, quatro no total.
  </action>
  <verify>
    <automated>./gradlew assembleDebug lint detekt --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'onboarding_next' app/src/main/java/org/sentinela/app/ui/onboarding/WhitelistPolicyStepScreen.kt` é maior que 0 e `grep -c 'onboarding_finish'` devolve 0
    - `grep -ciE 'googleusercontent|https?://' app/src/main/java/org/sentinela/app/ui/onboarding/WhitelistPolicyStepScreen.kt` devolve 0
    - `grep -ciE 'Snackbar' app/src/main/java/org/sentinela/app/ui/onboarding/WhitelistPolicyStepScreen.kt` devolve 0 — o hint é permanente
    - `grep -c 'whitelist_setup_hint' app/src/main/java/org/sentinela/app/ui/onboarding/WhitelistPolicyStepScreen.kt` é 1
    - Nenhum texto de interface embutido em Kotlin
    - `./gradlew assembleDebug lint detekt --rerun-tasks` verde
  </acceptance_criteria>
  <done>Passo 4 com nunca silenciar como padrão, imagem remota substituída por cartão tonal, hint permanente e botão Próximo em vez de Finalizar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Teste de composição dos passos 3 e 4</name>
  <files>app/src/test/java/org/sentinela/app/ui/onboarding/ContactsAndWhitelistStepTest.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/ui/onboarding/ContactsPolicyStepScreen.kt
    - app/src/main/java/org/sentinela/app/ui/onboarding/WhitelistPolicyStepScreen.kt
    - app/src/test/java/org/sentinela/app/ui/TouchTargetAsserts.kt
    - app/src/test/java/org/sentinela/app/ui/components/Phase7ComponentSemanticsTest.kt (o molde)
  </read_first>
  <behavior>
    - Nunca perguntado exibe a justificativa e o botão de permitir; tocar nele emite o pedido uma vez
    - Concedido exibe o chip de concedido e NÃO exibe botão de permitir
    - Negado uma vez exibe o aviso de consequência com ação de permitir
    - Negado definitivamente exibe o aviso de bloqueio com ação de abrir configurações, e nenhum pedido do sistema
    - Em todos os quatro estados as quatro opções continuam habilitadas
    - Passo 3 vem com Tocar pré-selecionado e o interruptor de privados ligado
    - Passo 4 vem com Nunca Silenciar pré-selecionado
    - Passo 4 exibe o hint como texto presente na tela, e o botão principal é Próximo
    - Cada cartão de opção e cada botão passa os dois eixos de toque
  </behavior>
  <action>
    Criar `app/src/test/java/org/sentinela/app/ui/onboarding/ContactsAndWhitelistStepTest.kt`,
    `@RunWith(RobolectricTestRunner::class)`,
    `@Config(sdk = [35], qualifiers = "w411dp-h891dp-xxhdpi")`, `createComposeRule`.

    No mínimo 14 casos cobrindo os nove comportamentos, com um caso por estado de permissão. Todo
    texto de assert lido do recurso em tempo de teste. Os dois eixos afirmados com os asserts de
    07-02 em cada cartão de opção, no interruptor e nos dois botões.

    Um caso específico para o interruptor de privados: papel de interruptor presente, descrição de
    estado distinta entre ligado e desligado, e a explicação permanente visível como nó separado —
    é o ponto de risco (b) da semântica mesclada, e ele reaparece aqui.

    Um caso de contagem: o passo 3 oferece exatamente quatro opções e o passo 4 exatamente quatro.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*ContactsAndWhitelistStepTest" --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew testDebugUnitTest --tests "*ContactsAndWhitelistStepTest" --rerun-tasks` verde com no mínimo 14 `@Test`
    - `grep -c 'assertLayoutHeightIsAtLeast' app/src/test/java/org/sentinela/app/ui/onboarding/ContactsAndWhitelistStepTest.kt` é no mínimo 4, cada um com `assertTouchHeightIsAtLeast` no mesmo caso
    - PROVA DE VERMELHO 1: fazer o ramo de negação definitiva oferecer o botão de permitir em vez do atalho de configurações ⇒ o caso correspondente fica vermelho; restaurar
    - PROVA DE VERMELHO 2: desabilitar as quatro opções quando a permissão não está concedida ⇒ o caso de opções sempre editáveis fica vermelho; restaurar
    - PROVA DE VERMELHO 3: trocar o padrão do passo 4 de nunca silenciar para tocar ⇒ vermelho; restaurar
  </acceptance_criteria>
  <done>Os quatro ramos de permissão, os dois padrões e os dois eixos de toque estão travados, com três provas de vermelho transcritas.</done>
</task>

</tasks>

<verification>
```bash
./gradlew --rerun-tasks assembleDebug testDebugUnitTest lint detekt
bash scripts/verify-invariants.sh
./gradlew koverLog
```
</verification>

<success_criteria>
- Passos 3 e 4 compostos puros, com os quatro ramos de permissão e os dois padrões corretos
- Consequência honesta de negar a agenda visível na tela
- Catorze casos verdes e três provas de vermelho transcritas
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-ui-onboarding-e-home/07-06-SUMMARY.md`
</output>
