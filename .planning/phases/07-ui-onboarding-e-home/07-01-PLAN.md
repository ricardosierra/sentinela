---
phase: 07-ui-onboarding-e-home
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/org/sentinela/app/ui/theme/Color.kt
  - app/src/main/res/values/strings.xml
  - app/src/test/java/org/sentinela/app/ui/theme/ThemeTokensTest.kt
  - app/src/test/java/org/sentinela/app/ui/theme/CallColorFixationTest.kt
  - app/src/test/java/org/sentinela/app/ui/Phase7StringsTest.kt
autonomous: true
requirements: [UIX-07, UIX-08, UIX-11]

must_haves:
  truths:
    - "Toda tela desta fase pode ser escrita sem literal de texto em Kotlin, porque a chave já existe em pt-BR"
    - "As cinco capacidades desonestas dos mockups não podem entrar na copy sem um teste ficar vermelho"
    - "Proteção ativa e proteção desligada continuam distinguíveis com qualquer papel de parede"
  artifacts:
    - path: "app/src/main/res/values/strings.xml"
      provides: "as 43 chaves novas da fase, em pt-BR"
      contains: "welcome_open_source"
    - path: "app/src/main/java/org/sentinela/app/ui/theme/Color.kt"
      provides: "StatusAttention, OnStatusAttention, StatusBlocked como literais"
      contains: "StatusAttention"
    - path: "app/src/test/java/org/sentinela/app/ui/Phase7StringsTest.kt"
      provides: "varredura de honestidade sobre o TEXTO DOS RECURSOS das chaves desta fase"
      min_lines: 80
  key_links:
    - from: "app/src/test/java/org/sentinela/app/ui/Phase7StringsTest.kt"
      to: "app/src/main/res/values/strings.xml"
      via: "leitura por Context.getString em tempo de teste, nunca leitura do arquivo fonte"
      pattern: "getString"
    - from: "app/src/test/java/org/sentinela/app/ui/theme/CallColorFixationTest.kt"
      to: "app/src/main/java/org/sentinela/app/ui/theme/Color.kt"
      via: "fixação dos três valores novos nos três esquemas"
      pattern: "StatusAttention"
---

<objective>
Fundação de texto e cor da fase: as três cores semânticas fixas fora do Dynamic Color, as 43
chaves pt-BR que faltam, a varredura de honestidade que impede a copy dos mockups voltar, e a
correção do `%` cru medido em `strings.xml`.

Purpose: nenhuma tela da fase pode ser escrita antes disto — texto embutido em Kotlin é proibido
pelo `CLAUDE.md`, e a varredura precisa existir ANTES das telas (precedente de 06-02).
Output: `Color.kt` estendido, `strings.xml` completo, `Phase7StringsTest.kt` novo, dois testes de
tema estendidos.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md
@.planning/phases/07-ui-onboarding-e-home/07-CONTEXT.md
@CLAUDE.md

<interfaces>
Tokens já existentes em `ui/theme/Color.kt` (nenhum deles muda nesta fase):

```kotlin
val Surface = Color(0xFF081425)
val SurfaceContainerLow = Color(0xFF111C2D)
val SurfaceContainer = Color(0xFF152031)
val SurfaceContainerHigh = Color(0xFF1F2A3C)
val SurfaceContainerHighest = Color(0xFF2A3548)
val Primary = Color(0xFFADC6FF)
val PrimaryContainer = Color(0xFF4D8EFF)
val Error = Color(0xFFFFB4AB)
val ErrorContainer = Color(0xFF93000A)
val OnErrorContainer = Color(0xFFFFDAD6)
val CallAccept = Color(0xFF1E6E42)      // reusado por esta fase
val OnCallAccept = Color(0xFFD9F2E3)    // reusado por esta fase
val CallReject = Color(0xFF93000A)
val OnCallReject = Color(0xFFFFDAD6)
```

Precedente da varredura de honestidade (Fase 6), a imitar e não a reescrever:
`app/src/test/java/org/sentinela/app/ui/CallStringsTest.kt` — `@RunWith(RobolectricTestRunner)`,
`@Config(sdk = [35])`, `ApplicationProvider.getApplicationContext<Context>()`, prefixos da fase
numa lista, frases proibidas como dados do teste em português.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: As três cores semânticas fixas fora do Dynamic Color</name>
  <files>app/src/main/java/org/sentinela/app/ui/theme/Color.kt, app/src/test/java/org/sentinela/app/ui/theme/ThemeTokensTest.kt, app/src/test/java/org/sentinela/app/ui/theme/CallColorFixationTest.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/ui/theme/Color.kt
    - app/src/main/java/org/sentinela/app/ui/theme/Theme.kt
    - app/src/test/java/org/sentinela/app/ui/theme/ThemeTokensTest.kt
    - app/src/test/java/org/sentinela/app/ui/theme/CallColorFixationTest.kt
    - .planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md (§4.2)
  </read_first>
  <action>
    Acrescentar ao FINAL de `ui/theme/Color.kt`, sem tocar em nenhum token existente:

    ```kotlin
    val StatusAttention = Color(0xFF93000A)
    val OnStatusAttention = Color(0xFFFFDAD6)
    val StatusBlocked = Color(0xFFFFB4AB)
    ```

    KDoc de bloco acima dos três, em português, explicando o motivo como requisito de segurança
    de leitura e não como preferência estética: o tema substitui o esquema INTEIRO por um esquema
    derivado do papel de parede a partir do nível 31, então uma cor lida do esquema deixaria o
    papel de parede decidir a diferença entre "proteção ativa" e "proteção desligada".
    Registrar também que `CallAccept`/`OnCallAccept` são reusados por esta fase para o estado
    ativo, e que os três valores acima são digito a digito iguais a `ErrorContainer`,
    `OnErrorContainer` e `Error` de propósito — são apelidos SEMÂNTICOS, e a igualdade é afirmada
    por teste para que apelido novo nunca vire cor nova disfarçada (precedente de 06-02 com
    `CallReject`).

    Estender `ThemeTokensTest.kt` (JVM pura, sem Robolectric — decisão da Fase 1) com três casos:
    (a) cada um dos três novos tem exatamente o valor acima; (b) a igualdade digito a digito com
    o token destrutivo correspondente; (c) nenhum dos três é igual a `CallAccept` (se fossem,
    ativo e desligado colapsariam).

    Estender `CallColorFixationTest.kt` (Robolectric, `@Config(sdk = [35])`) com um caso por
    esquema (claro dinâmico, escuro dinâmico, escuro fixo) afirmando que os três valores lidos das
    constantes NÃO mudam com o esquema — a fixação é o ponto, e ela só se prova comparando a
    constante contra o esquema montado.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*ThemeTokensTest" --tests "*CallColorFixationTest" --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'val StatusAttention = Color(0xFF93000A)' app/src/main/java/org/sentinela/app/ui/theme/Color.kt` devolve 1
    - `grep -c 'val OnStatusAttention = Color(0xFFFFDAD6)' app/src/main/java/org/sentinela/app/ui/theme/Color.kt` devolve 1
    - `grep -c 'val StatusBlocked = Color(0xFFFFB4AB)' app/src/main/java/org/sentinela/app/ui/theme/Color.kt` devolve 1
    - `git diff --stat app/src/main/java/org/sentinela/app/ui/theme/Theme.kt` está vazio: nenhum token existente muda e o tema não é editado
    - `./gradlew testDebugUnitTest --tests "*ThemeTokensTest" --rerun-tasks` verde com no mínimo 3 `@Test` novos
    - PROVA DE VERMELHO (sobre código já commitado, nunca por `git checkout` de trabalho novo): alterar um dígito de `StatusAttention` em `Color.kt`, rodar o comando, transcrever a falha no SUMMARY, restaurar por edição manual
  </acceptance_criteria>
  <done>Os três valores existem como literais, estão travados por teste em JVM pura e fixados nos três esquemas; nenhum token anterior mudou.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: As 43 chaves pt-BR novas e a correção do porcento cru</name>
  <files>app/src/main/res/values/strings.xml</files>
  <read_first>
    - app/src/main/res/values/strings.xml
    - .planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md (§10.2 — o bloco XML é ditado ali, chave por chave)
    - .planning/phases/07-ui-onboarding-e-home/07-RESEARCH.md (Pitfall 8)
  </read_first>
  <behavior>
    - Cada uma das 43 chaves de §10.2 resolve por `Context.getString` sem lançar
    - `settings_clear_history_confirm` aceita um argumento inteiro e formata sem lançar
    - `dialer_activation_unchanged_4` formata sem lançar quando passada por `getString` com argumento
  </behavior>
  <action>
    Copiar para `app/src/main/res/values/strings.xml` o bloco XML da §10.2 do `07-UI-SPEC.md`
    LITERALMENTE — as 43 chaves, o texto exato, os comentários de agrupamento. Não reescrever
    nenhuma frase: elas foram revisadas contra as proibições de copy da §10.3 e reescrever é o
    caminho de volta à promessa falsa. Inserir agrupado por tela, seguindo a ordem de seções que o
    arquivo já usa.

    Além das 43, uma chave a mais para o estado vazio dos destinos que só existem na Phase 8, caso
    o executor da barra inferior precise: NÃO criar agora. Chave sem uso é achado de lint que o
    último plano da fase teria de silenciar.

    Corrigir o defeito latente medido: em `dialer_activation_unchanged_4` o `%` está cru, o que faz
    o lint ler um especificador de formato e faria a string lançar no dia em que alguém lhe passar
    argumento. Escapar duplicando o sinal de porcento no valor da string. O TEXTO VISÍVEL ao
    usuário não muda — é escapamento, não reescrita, e a Phase 6 já aprovou aquela frase.

    Acrescentar um caso ao `Phase7StringsTest` da Task 3 (ou, se preferir, à Task 3 direto) que
    formate `dialer_activation_unchanged_4` com um argumento e afirme que não lança e que o texto
    resultante contém o sinal de porcento uma única vez.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*Phase7StringsTest" --rerun-tasks && ./gradlew :app:lintDebug --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '<string name=' app/src/main/res/values/strings.xml` devolve 269 (226 + 43)
    - Cada uma destas chaves existe exatamente uma vez: `welcome_open_source`, `onboarding_skip`, `onboarding_role_title`, `onboarding_role_intro`, `onboarding_role_denied`, `onboarding_role_retry`, `onboarding_scope_title`, `onboarding_scope_dnd`, `contacts_permission_granted`, `contacts_permission_denied`, `contacts_permission_blocked`, `settings_block_private_desc`, `whitelist_setup_desc`, `onboarding_summary_title_ok`, `onboarding_summary_title_partial`, `onboarding_summary_body`, `onboarding_check_role`, `onboarding_check_contacts`, `onboarding_check_unknown`, `onboarding_check_origins`, `onboarding_check_granted`, `onboarding_check_missing`, `dashboard_protection_off_hint`, `dashboard_contacts_missing`, `dashboard_history_off`, `dashboard_history_off_value`, `dashboard_history_off_action`, `settings_protection_toggle_desc`, `settings_contacts_policy_note`, `settings_block_mode_desc`, `settings_repeated_call`, `settings_repeated_call_desc`, `settings_history_enabled`, `settings_history_enabled_desc`, `settings_history_off_kept`, `settings_retention_title`, `settings_retention_never`, `settings_retention_7`, `settings_retention_30`, `settings_retention_90`, `settings_retention_manual`, `settings_retention_never_confirm`, `settings_clear_history_confirm`, `settings_fallback_desc`
    - `./gradlew :app:lintDebug --rerun-tasks` não relata mais `PluralsCandidate` em `strings.xml`
    - Nenhuma chave existente teve o VALOR alterado: `git diff app/src/main/res/values/strings.xml` mostra apenas linhas adicionadas, exceto a única linha de `dialer_activation_unchanged_4`, cuja mudança é só o escapamento do sinal de porcento
  </acceptance_criteria>
  <done>As 43 chaves resolvem em pt-BR, a contagem fecha em 269, o lint não relata mais o porcento cru e nenhum texto anterior foi reescrito.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Varredura de honestidade das chaves desta fase</name>
  <files>app/src/test/java/org/sentinela/app/ui/Phase7StringsTest.kt</files>
  <read_first>
    - app/src/test/java/org/sentinela/app/ui/CallStringsTest.kt (o precedente inteiro)
    - app/src/main/res/values/strings.xml
    - .planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md (§10.3 — as 11 proibições)
    - .planning/phases/07-ui-onboarding-e-home/07-CONTEXT.md (as cinco capacidades desonestas)
  </read_first>
  <behavior>
    - Nenhuma chave dos prefixos da fase contém promessa de bloqueio total ou garantido
    - Nenhuma chave dos prefixos da fase contém rótulo de risco ou classificação de spam
    - Nenhuma chave dos prefixos da fase menciona base de números, nuvem ou processamento cifrado
    - Nenhuma chave dos prefixos da fase promete filtragem de mensageiro ou de chamada por internet
    - Nenhuma chave dos prefixos da fase pressiona o usuário a aceitar um opt-in
    - Toda chave dos prefixos da fase resolve para texto não vazio
  </behavior>
  <action>
    Criar `app/src/test/java/org/sentinela/app/ui/Phase7StringsTest.kt`, no molde exato do
    `CallStringsTest`: `@RunWith(RobolectricTestRunner::class)`, `@Config(sdk = [35])`,
    `ApplicationProvider.getApplicationContext<Context>()`.

    A varredura opera sobre o TEXTO DOS RECURSOS lido em tempo de teste — jamais sobre o arquivo
    fonte. Isso é o que impede que um comentário ou um KDoc derrube o próprio critério, armadilha
    que já pegou seis executores neste projeto.

    Enumerar as chaves da fase por reflexão sobre `R.string`, filtrando pelos prefixos:
    `welcome_`, `onboarding_`, `unknown_`, `contacts_`, `dashboard_`, `settings_`, `nav_`,
    `action_`, `state_`. Restringir por prefixo é obrigatório: rótulos legítimos de outras fases
    usam vocabulário aqui proibido.

    Listas de dados do teste, em português minúsculo, comparadas contra o texto do recurso também
    em minúsculo:

    - promessas de bloqueio: "bloqueio garantido", "garantia de bloqueio", "100% garantido",
      "bloqueio total", "totalmente bloqueado", "bloqueio completo", "infalível", "infalivel",
      "bloqueia tudo", "à prova de falhas", "nunca mais será incomodado"
    - rótulo de risco / spam (as duas afirmações desonestas do dashboard): "fraude", "alto risco",
      "spam conhecido", "número denunciado", "provável fraude", "seguro contra spam"
    - base de dados / nuvem (a afirmação desonesta das boas-vindas): "base global",
      "milhões de números", "banco de números", "na nuvem", "servidor", "base de dados"
    - processamento cifrado: "criptografado", "criptografada", "encriptado"
    - filtro inteligente / superlativo: "filtros inteligentes", "filtro inteligente",
      "inteligente", "o melhor", "a melhor", "definitivo", "poderoso", "revolucionário"
    - mensageiro e chamada por internet PROMETIDOS: afirmar apenas quando a frase os cita SEM a
      palavra "não" e sem "fora do alcance" — as chaves honestas da Fase 6 os citam justamente
      para dizer que ficam fora. Implementar como: se o texto cita o mensageiro, ele TEM de citar
      também negação ou ausência de alcance
    - pressão de opt-in: "ative agora", "ativar agora", "recomendado", "não perca", "urgente"

    Duas exceções nomeadas e justificadas em KDoc, porque são verdadeiras e verificáveis no
    manifest: "100% offline" e "100% open source". Implementar removendo essas duas expressões do
    texto ANTES de procurar as proibidas — nunca isentando a chave inteira, o que abriria porta
    para promessa nova na mesma string.

    Um caso a mais, o da Task 2: formatar `dialer_activation_unchanged_4` com um argumento inteiro
    e afirmar que não lança.

    Um caso a mais de completude: toda chave dos prefixos resolve para texto com pelo menos um
    caractere. Isto é o que pega chave criada vazia por engano. Exceção nominal:
    `support_bitcoin_address`, que está vazia de propósito enquanto o mantenedor não fornecer o
    endereço real — mas ela não casa nenhum prefixo desta fase, então não precisa de isenção.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*Phase7StringsTest" --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `app/src/test/java/org/sentinela/app/ui/Phase7StringsTest.kt` existe com no mínimo 8 `@Test`
    - `grep -c 'getString' app/src/test/java/org/sentinela/app/ui/Phase7StringsTest.kt` é maior que 0 e `grep -c 'strings.xml' app/src/test/java/org/sentinela/app/ui/Phase7StringsTest.kt` é 0 fora de KDoc: a varredura lê recurso, não arquivo
    - `./gradlew testDebugUnitTest --tests "*Phase7StringsTest" --rerun-tasks` verde
    - PROVA DE VERMELHO 1: acrescentar em `strings.xml` uma chave `dashboard_teste_desonesto` cujo texto afirme classificação de fraude ⇒ a varredura fica vermelha; remover a chave
    - PROVA DE VERMELHO 2: acrescentar uma chave `welcome_teste_desonesto` que prometa base global de milhões de números ⇒ vermelha; remover
    - PROVA DE VERMELHO 3: acrescentar uma chave `settings_teste_desonesto` que afirme processamento local criptografado ⇒ vermelha; remover
    - As três provas transcritas no SUMMARY com a saída real
  </acceptance_criteria>
  <done>As cinco capacidades desonestas dos mockups não conseguem entrar na copy sem um teste vermelho, e a prova é sobre recurso lido, nunca sobre texto de fonte.</done>
</task>

</tasks>

<verification>
```bash
./gradlew --rerun-tasks assembleDebug testDebugUnitTest lint detekt
bash scripts/verify-invariants.sh
./gradlew koverLog
```
`koverVerify` NÃO é cobrado neste plano — precedente de 06-08/06-09: o gate volta no último plano
da fase.
</verification>

<success_criteria>
- 269 chaves em `strings.xml`, as 43 novas em pt-BR e nenhum valor anterior reescrito
- Três cores semânticas fixas, travadas em JVM pura e fixadas nos três esquemas
- `Phase7StringsTest` com 3 provas de vermelho transcritas
- Lint sem `PluralsCandidate`; `lint` e `detekt` zerados
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-ui-onboarding-e-home/07-01-SUMMARY.md`
</output>
