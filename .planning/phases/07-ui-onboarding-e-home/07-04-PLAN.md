---
phase: 07-ui-onboarding-e-home
plan: 04
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - app/src/main/java/org/sentinela/app/ui/home/HomeUiState.kt
  - app/src/main/java/org/sentinela/app/ui/home/HomeViewModel.kt
  - app/src/main/java/org/sentinela/app/ui/onboarding/OnboardingUiState.kt
  - app/src/main/java/org/sentinela/app/ui/onboarding/OnboardingViewModel.kt
  - app/src/main/java/org/sentinela/app/ui/settings/SettingsViewModel.kt
  - app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt
  - app/src/test/java/org/sentinela/app/ui/home/HomeViewModelTest.kt
  - app/src/test/java/org/sentinela/app/ui/home/RoleLiveStateTest.kt
  - app/src/test/java/org/sentinela/app/ui/onboarding/PermissionAskOrderTest.kt
  - app/src/test/java/org/sentinela/app/ui/settings/SettingsViewModelTest.kt
autonomous: true
requirements: [SCR-02, UIX-02, UIX-03, UIX-10]

must_haves:
  truths:
    - "A home nunca mostra zero bloqueadas quando o histórico está desligado, carregando ou em erro"
    - "O papel do sistema é reconsultado a cada retomada e nunca lido de valor guardado"
    - "Trocar uma configuração na Proteção vale na triagem sem nenhum botão salvar"
    - "A marca de já ter perguntado uma permissão é gravada ao disparar o pedido, nunca no retorno"
  artifacts:
    - path: "app/src/main/java/org/sentinela/app/ui/home/HomeUiState.kt"
      provides: "StatValue como tipo fechado — Loaded, Unavailable, Loading — que torna o zero mentiroso impossível"
      contains: "sealed interface StatValue"
    - path: "app/src/main/java/org/sentinela/app/ui/home/HomeViewModel.kt"
      provides: "estado vivo do papel, contagens, última bloqueada mascarada"
      min_lines: 100
    - path: "app/src/main/java/org/sentinela/app/ui/settings/SettingsViewModel.kt"
      provides: "efeito imediato por gravação no repositório, sem acumular"
      min_lines: 70
  key_links:
    - from: "app/src/main/java/org/sentinela/app/ui/home/HomeViewModel.kt"
      to: "app/src/main/java/org/sentinela/app/telecom/SystemRoleGate.kt"
      via: "função de reconsulta chamada pela retomada da tela, sem campo de cache"
      pattern: "isRoleHeld"
    - from: "app/src/main/java/org/sentinela/app/ui/home/HomeViewModel.kt"
      to: "app/src/main/java/org/sentinela/app/phone/PhoneMask.kt"
      via: "máscara injetada como função, aplicada à última bloqueada"
      pattern: "mask"
    - from: "app/src/main/java/org/sentinela/app/ui/settings/SettingsViewModel.kt"
      to: "app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt"
      via: "update transformando o registro de configurações"
      pattern: "update"
---

<objective>
Os três donos de estado da fase e os tipos de estado de interface que eles publicam — incluindo o
tipo que torna o "zero mentiroso" impossível — mais os quatro testes que travam as decisões do
usuário: papel vivo, zero impossível, efeito imediato e marca gravada ao disparar.

Purpose: as telas desta fase são compostas puras que recebem estado e retorno de chamada; sem os
tipos de estado nenhuma delas é testável sem construir container, e a Fase 5 mediu que um segundo
container no mesmo processo derruba a aplicação.
Output: `HomeUiState`, `HomeViewModel`, `OnboardingUiState`, `OnboardingViewModel`,
`SettingsViewModel`, chave nova de onboarding concluído, quatro classes de teste.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-onboarding-e-home/07-RESEARCH.md
@.planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md
@.planning/phases/07-ui-onboarding-e-home/07-CONTEXT.md
@CLAUDE.md

<interfaces>
Fontes reais, assinaturas lidas do código:

```kotlin
// telecom/SystemRoleGate.kt
abstract class SystemRoleGate(context: Context, role: String) {
    fun isRoleAvailable(): Boolean
    fun isRoleHeld(): Boolean
    fun buildRequestIntent(): Intent?      // nulo quando o aparelho não oferece o papel
}
class ScreeningRoleManager(context: Context) : SystemRoleGate(context, RoleManager.ROLE_CALL_SCREENING)

// telecom/call/DialerModeState.kt
enum class DialerModeState { UNAVAILABLE, BLOCKED_BY_CONTACTS, OFFERED, ACTIVE, ROLE_LOST }
fun dialerModeState(roleAvailable: Boolean, roleHeld: Boolean, contactsGranted: Boolean, userOptedIn: Boolean): DialerModeState

// settings/ScreeningSettings.kt
data class ScreeningSettings(
    val protectionEnabled: Boolean = true,
    val unknownPolicy: OriginPolicy = OriginPolicy.BLOCK,
    val contactsPolicy: OriginPolicy = OriginPolicy.RING,
    val whitelistPolicy: OriginPolicy = OriginPolicy.NEVER_SILENCE,
    val blockPrivateNumbers: Boolean = true,
    val blockMode: BlockMode = BlockMode.REJECT,
    val hideFromNativeCallLog: Boolean = true,
    val showOwnNotification: Boolean = false,
    val fallbackPolicy: FallbackPolicy = FallbackPolicy.ALLOW,
    val historyEnabled: Boolean = true,
    val retentionPolicy: RetentionPolicy = RetentionPolicy.DAYS_30,
    val repeatedCallBypassEnabled: Boolean = true,
    val notificationIdentification: NotificationIdentification = NotificationIdentification.MASKED,
)
enum class OriginPolicy { RING, BLOCK, SILENCE, NEVER_SILENCE }
enum class BlockMode { REJECT, SILENT_VOICEMAIL }
enum class FallbackPolicy { ALLOW, BLOCK }
enum class NotificationIdentification { MASKED, ANONYMOUS }

// settings/DataStoreSettingsRepository.kt
val settings: Flow<ScreeningSettings>
val appOpenCount: Flow<Int>
val contactsPermissionAsked: Flow<Boolean>
val notificationPermissionAsked: Flow<Boolean>
suspend fun markContactsPermissionAsked()
suspend fun markNotificationPermissionAsked()
suspend fun snapshot(): ScreeningSettings
fun cachedSnapshot(): ScreeningSettings
suspend fun update(transform: (ScreeningSettings) -> ScreeningSettings)

// data/local/BlockedCallRepository.kt
data class BlockedCallEntry(
    val id: Long = 0, val maskedNumber: String, val numberE164: String?,
    val timestampUtcMillis: Long, val reason: DecisionReason,
    val notificationShown: Boolean, val classification: CallClassification = CallClassification.UNCLASSIFIED,
)
fun observeRecent(): Flow<List<BlockedCallEntry>>
fun observeTotalCount(): Flow<Long>
suspend fun clearAll()

// data/contacts/ContactsPermissionState.kt
enum class ContactsPermissionState { GRANTED, NEVER_ASKED, DENIED_ONCE, DENIED_PERMANENTLY }
val ContactsPermissionState.canRequest: Boolean
val ContactsPermissionState.shouldOfferSystemSettings: Boolean
fun contactsPermissionState(granted: Boolean, alreadyAsked: Boolean, rationale: Boolean): ContactsPermissionState
```

Fábrica manual, sobrecarga NÃO deprecada, verificada a compilar neste repositório:
```kotlin
override fun <T : ViewModel> create(modelClass: Class<T>, extras: CreationExtras): T
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HomeUiState com o zero impossível por tipo, e HomeViewModel com papel vivo</name>
  <files>app/src/main/java/org/sentinela/app/ui/home/HomeUiState.kt, app/src/main/java/org/sentinela/app/ui/home/HomeViewModel.kt, app/src/test/java/org/sentinela/app/ui/home/HomeViewModelTest.kt, app/src/test/java/org/sentinela/app/ui/home/RoleLiveStateTest.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt
    - app/src/main/java/org/sentinela/app/data/local/BlockedCallRepository.kt
    - app/src/main/java/org/sentinela/app/telecom/SystemRoleGate.kt
    - app/src/main/java/org/sentinela/app/telecom/call/DialerModeState.kt
    - app/src/main/java/org/sentinela/app/AppContainer.kt
    - .planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md (§7 e §8 inteiras)
  </read_first>
  <behavior>
    - Histórico desligado ⇒ as duas contagens são Unavailable, e a última bloqueada não é publicada
    - Retenção "não guardar" ⇒ as duas contagens são Unavailable
    - Antes da primeira emissão ⇒ as contagens são Loading
    - Falha ao ler o histórico ⇒ as contagens são Unavailable e o estado carrega a marca de erro
    - Histórico ligado com contagem zero ⇒ Loaded com zero, porque ali zero é verdade
    - Última bloqueada é publicada MASCARADA, e o número completo não existe no estado
    - Reconsultar duas vezes produz duas consultas ao sistema; nenhuma resposta é guardada
  </behavior>
  <action>
    **`ui/home/HomeUiState.kt`.** Declarar:

    ```kotlin
    sealed interface StatValue {
        data class Loaded(val count: Long) : StatValue
        data object Unavailable : StatValue
        data object Loading : StatValue
    }
    ```

    Este tipo é o guarda-corpo do "zero mentiroso" e o KDoc precisa dizer isso em português: não
    existe caminho em que a interface renderize zero sem que a contagem tenha sido efetivamente
    carregada, porque não existe `Int` na assinatura para renderizar. A regra deixa de ser convenção
    e passa a ser tipo. Proibido para sempre acrescentar aqui um construtor que aceite número sem
    a garantia de carregamento.

    `data class HomeUiState` com: `protectionEnabled: Boolean`, `screeningRoleHeld: Boolean`,
    `screeningRoleAvailable: Boolean`, `contactsPermission: ContactsPermissionState`,
    `dialerMode: DialerModeState`, `totalBlocked: StatValue`, `blockedToday: StatValue`,
    `lastBlocked: LastBlockedUi?`, `historyEnabled: Boolean`, `readError: Boolean`.
    `LastBlockedUi` carrega `maskedNumber: String`, `reasonLabelRes: Int` e
    `timestampUtcMillis: Long` — nunca `numberE164`. Nenhum campo do estado pode carregar a
    sequência completa de dígitos; registrar isso em KDoc como a fronteira herdada da Fase 6.

    O rótulo do motivo é resolvido para um identificador de recurso a partir do motivo real da
    decisão, no mapeamento da §7: desconhecido, privado, identificação oculta, contato, whitelist.
    Nenhum rótulo de risco ou de classificação de spam existe como opção — não existe esse dado no
    aplicativo.

    **`ui/home/HomeViewModel.kt`.** Construtor recebendo colaboradores por PARÂMETRO, jamais
    container: `settings: DataStoreSettingsRepository`, `history: BlockedCallRepository`,
    `roleHeld: () -> Boolean`, `roleAvailable: () -> Boolean`, `requestRoleIntent: () -> Intent?`,
    `contactsState: () -> ContactsPermissionState`, `dialerRoleHeld: () -> Boolean`,
    `mask: (String) -> String`, `clock: () -> Long = System::currentTimeMillis`.
    As consultas de papel chegam como FUNÇÕES para que o teste as conte — é o contador de consultas
    que prova o estado vivo, jamais um cronômetro.

    `estado: StateFlow<HomeUiState>` combinando as configurações, a contagem total, os recentes e o
    resultado da última reconsulta de papel. Valor inicial explícito com as contagens em `Loading`:
    `collectAsStateWithLifecycle` exige valor inicial, e o primeiro quadro NUNCA espera disco —
    proibido `runBlocking` sobre o retrato.

    `fun reconsultarPapel()` chama as funções de papel na hora e publica o resultado. NENHUM campo
    guarda a resposta entre chamadas: KDoc registra que perder um papel encerra o processo (medido
    três vezes na Fase 6), que o retorno do seletor do sistema por isso nunca é a fonte da verdade,
    e que o trio de consultas custa a metade de um milésimo de milissegundo, medido — não existe
    argumento de desempenho para cachear.

    `fun intencaoDePedidoDoPapel(): Intent?` delega e devolve nulo quando o aparelho não oferece o
    papel; a interface não pode nem mostrar o botão nesse caso.

    A leitura do histórico usa `catch` no fluxo, publicando `Unavailable` mais a marca de erro — e
    NÃO propaga: aqui a tela é o consumidor, e uma exceção derrubaria a home. Registrar que isso é
    o oposto deliberado do caminho da chamada da Fase 6, onde a exceção propaga, e o motivo é que
    ninguém detecta uma home congelada exceto o usuário.

    A contagem do dia é derivada do relógio injetado sobre os recentes.

    PROIBIDO tocar `container.contactLookupRepository`: ele registra observador da agenda e dispara
    construção de cache medida em mais de dois segundos e meio. O estado da permissão chega pela
    função `contactsState`.

    `companion object { fun factory(...) }` com a sobrecarga `create(modelClass, extras)`.

    **`app/src/test/java/org/sentinela/app/ui/home/HomeViewModelTest.kt`** com dublês e
    `kotlinx-coroutines-test`. Cobrir os sete comportamentos acima. O caso do zero verdadeiro e o do
    zero proibido ficam LADO A LADO na mesma classe, para que ninguém "conserte" um quebrando o
    outro.

    **`app/src/test/java/org/sentinela/app/ui/home/RoleLiveStateTest.kt`**: contador de invocações
    das funções de papel; duas reconsultas produzem duas invocações de cada; e um caso afirmando que
    o estado publicado MUDA quando a função passa a responder o contrário — o que é o que pega um
    cache introduzido depois.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*HomeViewModelTest" --tests "*RoleLiveStateTest" --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'sealed interface StatValue' app/src/main/java/org/sentinela/app/ui/home/HomeUiState.kt` devolve 1
    - `grep -cE 'numberE164|fullNumber' app/src/main/java/org/sentinela/app/ui/home/HomeUiState.kt` devolve 0
    - `grep -cE 'contactLookupRepository|runBlocking' app/src/main/java/org/sentinela/app/ui/home/HomeViewModel.kt` devolve 0
    - `grep -cE 'AppContainer' app/src/main/java/org/sentinela/app/ui/home/HomeViewModel.kt` devolve no máximo 1, e essa única ocorrência está na fábrica — nunca no construtor da classe
    - `./gradlew testDebugUnitTest --tests "*HomeViewModelTest" --tests "*RoleLiveStateTest" --rerun-tasks` verde com no mínimo 10 `@Test` somados
    - PROVA DE VERMELHO 1 (zero mentiroso): fazer o dono do estado publicar `Loaded(0)` com o histórico desligado ⇒ o caso correspondente fica vermelho; restaurar
    - PROVA DE VERMELHO 2 (papel guardado): acrescentar um campo que memorize a resposta do papel na primeira consulta ⇒ o contador de consultas por reconsulta cai e `RoleLiveStateTest` fica vermelho; restaurar
    - PROVA DE VERMELHO 3 (fronteira do número): fazer a última bloqueada publicar o número sem máscara ⇒ o caso de privacidade fica vermelho; restaurar
  </acceptance_criteria>
  <done>O zero mentiroso é impossível por tipo, o papel é consultado vivo e a última bloqueada é mascarada — os três com vermelho demonstrado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OnboardingViewModel, a chave de onboarding concluído e a ordem da marca de permissão</name>
  <files>app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt, app/src/main/java/org/sentinela/app/ui/onboarding/OnboardingUiState.kt, app/src/main/java/org/sentinela/app/ui/onboarding/OnboardingViewModel.kt, app/src/test/java/org/sentinela/app/ui/onboarding/PermissionAskOrderTest.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt (inteiro, incluindo o objeto de chaves)
    - app/src/main/java/org/sentinela/app/data/contacts/ContactsPermissionState.kt
    - app/src/main/java/org/sentinela/app/permissions/RuntimePermissionAsk.kt
    - app/src/main/java/org/sentinela/app/ui/dialer/DialerActivity.kt (o precedente em produção de marca gravada ao disparar)
    - .planning/phases/07-ui-onboarding-e-home/07-RESEARCH.md (§Q4 e Open Question 3)
  </read_first>
  <behavior>
    - A marca de agenda já perguntada é gravada ANTES de o pedido ser disparado
    - A marca de notificação já perguntada é gravada ANTES de o pedido ser disparado
    - Concluir ou pular o onboarding grava a chave de onboarding concluído
    - A chave de onboarding concluído tem valor falso por padrão numa instalação nova
    - O passo corrente sobrevive a uma recriação do dono de estado
    - Cada passo tem um padrão pré-selecionado vindo do repositório, não redefinido pela tela
  </behavior>
  <action>
    **Chave nova no `DataStoreSettingsRepository`.** Acrescentar `onboarding_completed` como
    preferência booleana, FORA de `ScreeningSettings` — não é configuração de triagem e não pode
    pesar no retrato do caminho quente, exatamente como as três marcas de permissão. Expor
    `val onboardingCompleted: Flow<Boolean>` com padrão falso e
    `suspend fun markOnboardingCompleted()`. KDoc registrando por que ela não reaproveita o contador
    de aberturas: aquele contador já tem outro dono, o convite de avaliação da Phase 9, e amarrar
    duas decisões de produto ao mesmo número é dívida garantida.

    **`ui/onboarding/OnboardingUiState.kt`.** `data class OnboardingUiState` com `step: Int`,
    `totalSteps: Int = 6`, `screeningRoleHeld: Boolean`, `roleRequestInFlight: Boolean`,
    `roleDenied: Boolean`, `contactsPermission: ContactsPermissionState`,
    `notificationPermission: RuntimePermissionAsk`, `settings: ScreeningSettings`.
    O padrão de cada passo NÃO é redefinido aqui: ele vem de `settings`, cujos valores de fábrica já
    são exatamente os dos mockups — bloquear desconhecidos, tocar para contatos, nunca silenciar a
    whitelist, bloquear privados ligado, notificação desligada. Registrar isso em KDoc: o onboarding
    REFLETE o repositório em vez de redefinir padrão, e é por isso que pular aplica os padrões
    corretos sem escrever nada.

    **`ui/onboarding/OnboardingViewModel.kt`.** Colaboradores por parâmetro:
    `settings: DataStoreSettingsRepository`, `roleHeld: () -> Boolean`, `roleAvailable: () -> Boolean`,
    `requestRoleIntent: () -> Intent?`, `contactsState: () -> ContactsPermissionState`,
    `notificationState: () -> RuntimePermissionAsk`. Funções: `avancar()`, `voltar()`,
    `pular()`, `concluir()`, `reconsultarPapel()`, `pedirAgenda(dispararLauncher: () -> Unit)`,
    `pedirNotificacao(dispararLauncher: () -> Unit)`, e uma função de gravação por configuração dos
    passos 2 a 5.

    A ORDEM da marca é contrato, não estilo, e as Fases 4, 5 e 6 já a estabeleceram três vezes:
    `pedirAgenda` grava a marca e SÓ ENTÃO chama o disparo, na sequência, sem esperar a escrita
    concluir — é o padrão que a tela de ativação do discador já usa em produção. O motivo, em KDoc e
    em português: a consulta de justificativa da plataforma responde igual nos dois extremos, antes
    do primeiro pedido e depois da negação definitiva, e o usuário pode encerrar o aplicativo com o
    diálogo do sistema aberto — se a marca dependesse do retorno, o aplicativo voltaria achando que
    nunca perguntou.

    `pular()` e `concluir()` gravam a chave de onboarding concluído. Nenhum passo é bloqueante:
    papel negado, agenda negada ou notificação negada avançam normalmente, e o pedido do sistema
    acontece no máximo uma vez por passo por sessão de onboarding.

    O passo corrente é estado do dono, o que já cobre rotação e recriação de Activity pelo diálogo
    do sistema. `SavedStateHandle` fica FORA por decisão deste plano: acrescentar cobertura de morte
    de processo antes de haver um caso concreto de perda de progresso é escopo novo, e a pesquisa
    recomendou começar sem.

    **`app/src/test/java/org/sentinela/app/ui/onboarding/PermissionAskOrderTest.kt`.** O
    guarda-corpo é uma LISTA ORDENADA DE EVENTOS, no molde de 05-03 e 06-01: o dublê de repositório
    registra "marcou agenda" e o dublê de disparo registra "disparou agenda"; o assert é sobre a
    ordem exata. Igual para notificação. Mais os casos de onboarding concluído e de padrão vindo do
    repositório.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*PermissionAskOrderTest" --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'onboarding_completed' app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt` devolve 1
    - `grep -c 'onboarding' app/src/main/java/org/sentinela/app/settings/ScreeningSettings.kt` devolve 0 — a chave fica fora do retrato do caminho quente
    - `./gradlew testDebugUnitTest --tests "*PermissionAskOrderTest" --tests "*DataStoreSettingsRepositoryTest" --rerun-tasks` verde com no mínimo 6 `@Test` novos
    - `grep -cE 'AppContainer' app/src/main/java/org/sentinela/app/ui/onboarding/OnboardingViewModel.kt` devolve no máximo 1, e só na fábrica
    - PROVA DE VERMELHO (marca no retorno): mover a gravação da marca de agenda para depois do disparo ⇒ a lista ordenada de eventos inverte e o caso fica vermelho; restaurar
  </acceptance_criteria>
  <done>A chave de onboarding concluído existe fora do retrato de triagem, e a ordem "marca antes do disparo" está travada por lista ordenada de eventos com vermelho demonstrado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SettingsViewModel com efeito imediato e sem botão salvar</name>
  <files>app/src/main/java/org/sentinela/app/ui/settings/SettingsViewModel.kt, app/src/test/java/org/sentinela/app/ui/settings/SettingsViewModelTest.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt
    - app/src/main/java/org/sentinela/app/data/local/RoomBlockedCallRepository.kt
    - app/src/main/java/org/sentinela/app/telecom/call/DialerModeState.kt
    - .planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md (§9, §9.1, §9.2)
  </read_first>
  <behavior>
    - Trocar a política de desconhecidos grava na hora e o retrato do repositório passa a reportá-la
    - Nenhuma função acumula mudança para gravar depois: não existe função de salvar
    - Escolher "não guardar" na retenção poda os registros existentes
    - Limpar histórico apaga todos os registros e reporta quantos foram apagados antes de confirmar
    - Desligar o histórico NÃO apaga registro algum
    - O estado do modo discador é derivado das consultas ao sistema, nunca de valor gravado
  </behavior>
  <action>
    **`ui/settings/SettingsViewModel.kt`.** Colaboradores por parâmetro:
    `settings: DataStoreSettingsRepository`, `history: BlockedCallRepository`,
    `roleHeld: () -> Boolean`, `roleAvailable: () -> Boolean`, `requestRoleIntent: () -> Intent?`,
    `dialerRoleHeld: () -> Boolean`, `dialerRoleAvailable: () -> Boolean`,
    `contactsGranted: () -> Boolean`, `dialerOptedIn: Flow<Boolean>`.

    `estado: StateFlow<SettingsUiState>` com o registro de configurações corrente, o estado do papel
    de triagem consultado vivo, o `DialerModeState` derivado por `dialerModeState(...)` — que já é
    função pura existente e não deve ser reescrita — e a contagem de registros do histórico, para o
    diálogo de confirmação poder dizer quantos serão apagados.

    Uma função por item da §9, cada uma no formato
    `viewModelScope.launch { settings.update { it.copy(campo = novo) } }`. É o cache mantido por
    coletor da Fase 3 que faz o valor chegar à triagem; a mudança vale na próxima chamada. NÃO
    existe função de salvar, e o KDoc registra o motivo em português: um botão salvar tornaria
    possível a interface e a triagem discordarem, e o retrato já é imediato.

    Retenção para "não guardar" grava E chama a poda, na mesma corotina e nessa ordem — o efeito de
    perder dado é exatamente o que a §9.2 manda confirmar na tela antes de chamar aqui.
    Limpar histórico chama `clearAll()`. Desligar o histórico apenas grava; nenhuma poda.

    `fun reconsultarPapeis()` reconsulta os dois papéis, sem guardar resposta — mesma doutrina da
    Task 1.

    **`app/src/test/java/org/sentinela/app/ui/settings/SettingsViewModelTest.kt`** com o repositório
    REAL sobre `TemporaryFolder` quando o caso for de efeito imediato — é o que prova que o retrato
    reporta — e dublês no resto. Armadilha herdada de 06-08 e ela é do TESTE, não do produto: gravar
    e consultar o retrato no instante seguinte é corrida, porque o retrato vem de cache mantido por
    coletor assíncrono; o caso precisa ESPERAR o valor gravado ser reportado. Atenção também à
    armadilha da Fase 3: `@get:Rule` combinado com `@JvmField` desliga a regra do JUnit e a pasta
    temporária nunca é criada.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*SettingsViewModelTest" --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ciE 'fun salvar|fun save|fun commit|fun aplicar' app/src/main/java/org/sentinela/app/ui/settings/SettingsViewModel.kt` devolve 0
    - `grep -c 'dialerModeState(' app/src/main/java/org/sentinela/app/ui/settings/SettingsViewModel.kt` é maior que 0 — a função pura existente é reusada, não reescrita
    - `./gradlew testDebugUnitTest --tests "*SettingsViewModelTest" --rerun-tasks` verde com no mínimo 8 `@Test`
    - `git diff --stat app/src/main/java/org/sentinela/app/domain/CallDecisionEngine.kt` está vazio nesta fase inteira
    - PROVA DE VERMELHO 1 (efeito imediato): fazer o dono de estado acumular a mudança num campo e só gravar numa função nova ⇒ o caso de retrato fica vermelho; restaurar
    - PROVA DE VERMELHO 2 (poda ao escolher não guardar): remover a chamada de poda ⇒ o caso fica vermelho; restaurar
  </acceptance_criteria>
  <done>Cada configuração grava na hora, o retrato reporta, e a ausência de botão salvar é verificável; o motor de decisão continua intocado.</done>
</task>

</tasks>

<verification>
```bash
./gradlew --rerun-tasks assembleDebug testDebugUnitTest lint detekt
bash scripts/verify-invariants.sh
./gradlew koverLog
```
A chave nova entra em `settings.*`, que É medido pelo Kover — os casos novos de repositório mantêm a
cobertura. Os donos de estado vivem em `ui.*`, fora do filtro: nenhum exclude novo, e nenhum deve
ser acrescentado aqui (o filtro só se alarga no último plano da fase).
</verification>

<success_criteria>
- `StatValue` fechado, com o zero mentiroso impossível por tipo e vermelho demonstrado
- Papel reconsultado a cada chamada, provado por contador e não por cronômetro
- Marca de permissão gravada ao disparar, provada por lista ordenada de eventos
- Efeito imediato provado contra o repositório real, sem função de salvar
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-ui-onboarding-e-home/07-04-SUMMARY.md`
</output>
