---
phase: 07-ui-onboarding-e-home
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/test/java/org/sentinela/app/ui/TouchTargetAsserts.kt
  - app/src/test/java/org/sentinela/app/ui/call/CallScreenSemanticsTest.kt
  - app/src/main/java/org/sentinela/app/ui/navigation/SentinelaRoutes.kt
  - app/src/test/java/org/sentinela/app/ui/navigation/NavGraphContractTest.kt
autonomous: true
requirements: [UIX-01, UIX-09]

must_haves:
  truths:
    - "Qualquer teste de acessibilidade da fase pode afirmar os dois eixos sem duplicar o eixo com dentes"
    - "Uma rota anotada em vez de rota por texto fica vermelha num teste, não verde no compilador"
    - "Pular o onboarding não deixa o gesto de voltar devolver o usuário a ele"
  artifacts:
    - path: "app/src/test/java/org/sentinela/app/ui/TouchTargetAsserts.kt"
      provides: "os três asserts de dois eixos em pacote neutro"
      exports: ["assertTouchHeightIsAtLeast", "assertTouchWidthIsAtLeast", "assertLayoutHeightIsAtLeast"]
    - path: "app/src/main/java/org/sentinela/app/ui/navigation/SentinelaRoutes.kt"
      provides: "constantes de rota por texto"
      min_lines: 20
    - path: "app/src/test/java/org/sentinela/app/ui/navigation/NavGraphContractTest.kt"
      provides: "prova em execução de que o grafo compõe e a pilha se comporta"
      min_lines: 90
  key_links:
    - from: "app/src/test/java/org/sentinela/app/ui/call/CallScreenSemanticsTest.kt"
      to: "app/src/test/java/org/sentinela/app/ui/TouchTargetAsserts.kt"
      via: "import do pacote neutro, com os helpers removidos do arquivo da Fase 6"
      pattern: "TouchTargetAsserts|assertLayoutHeightIsAtLeast"
    - from: "app/src/test/java/org/sentinela/app/ui/navigation/NavGraphContractTest.kt"
      to: "app/src/main/java/org/sentinela/app/ui/navigation/SentinelaRoutes.kt"
      via: "grafo composto de verdade a partir das constantes"
      pattern: "Rotas\\."
---

<objective>
Wave 0 de infraestrutura de teste: extrair os três asserts de dois eixos para arquivo neutro, e
travar por EXECUÇÃO o contrato de navegação da fase — rotas por texto, `popUpTo` inclusivo e pilha
sem retorno ao onboarding.

Purpose: os asserts de dois eixos vivem hoje dentro de uma classe de teste do pacote de chamada, e
as telas desta fase estão em outros pacotes; duplicá-los faria o eixo com dentes divergir entre as
cópias. E a rota tipada é falso-verde de compilação MEDIDO neste repositório — o guarda-corpo tem
de ser um teste que compõe o grafo, porque nenhum assert de compilação pega isso.
Output: `TouchTargetAsserts.kt`, `SentinelaRoutes.kt`, `NavGraphContractTest.kt`.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-onboarding-e-home/07-RESEARCH.md
@CLAUDE.md

<interfaces>
Os três helpers a extrair, hoje funções `internal` de nível de arquivo DENTRO de
`app/src/test/java/org/sentinela/app/ui/call/CallScreenSemanticsTest.kt` (L265-290):

```kotlin
internal fun SemanticsNodeInteraction.assertTouchHeightIsAtLeast(minimo: Dp): SemanticsNodeInteraction
internal fun SemanticsNodeInteraction.assertTouchWidthIsAtLeast(minimo: Dp): SemanticsNodeInteraction
internal fun SemanticsNodeInteraction.assertLayoutHeightIsAtLeast(minimo: Dp): SemanticsNodeInteraction
```

`assertLayoutHeightIsAtLeast` mede `node.size.height` convertido por `node.layoutInfo.density` — é
o eixo com dentes, acrescentado depois de uma prova de vermelho falhar na Fase 6.
Há também um privado `tambemMede(minimo, vertical)` que os dois primeiros compartilham.

Forma verificada a compilar E executar neste repositório (Nav 2.9.8, Robolectric 4.16.1):

```kotlin
@RunWith(RobolectricTestRunner::class)
@Config(sdk = [35], qualifiers = "w411dp-h891dp-xxhdpi")
```

```kotlin
NavHost(navController = nav, startDestination = Rotas.BOAS_VINDAS) {
    composable(Rotas.BOAS_VINDAS) { /* … */ }
}
nav.navigate(Rotas.HOME) { popUpTo(Rotas.BOAS_VINDAS) { inclusive = true }; launchSingleTop = true }
nav.currentBackStack.value.mapNotNull { it.destination.route }   // a entrada do NavGraph tem rota nula
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extrair os asserts de dois eixos para arquivo de apoio neutro</name>
  <files>app/src/test/java/org/sentinela/app/ui/TouchTargetAsserts.kt, app/src/test/java/org/sentinela/app/ui/call/CallScreenSemanticsTest.kt</files>
  <read_first>
    - app/src/test/java/org/sentinela/app/ui/call/CallScreenSemanticsTest.kt (inteiro; os helpers estão no fim)
    - .planning/phases/07-ui-onboarding-e-home/07-RESEARCH.md (§Q6, item de Wave 0)
  </read_first>
  <action>
    Criar `app/src/test/java/org/sentinela/app/ui/TouchTargetAsserts.kt`, pacote
    `org.sentinela.app.ui`, e MOVER para lá — sem alterar uma linha de comportamento — as três
    funções `internal` e o auxiliar privado que duas delas compartilham. Levar o KDoc inteiro
    junto, incluindo a explicação de por que o eixo desenhado existe: ele é a memória da prova de
    vermelho que falhou.

    Acrescentar ao KDoc do arquivo o segundo motivo da extração, em português: classe de teste não
    enxerga membros de outra classe de teste entre sandboxes de SDK do Robolectric (registro da
    Fase 5), e arquivo de apoio neutro é a forma que já resolveu isso antes neste projeto.
    Registrar também, em prosa, a proibição permanente de duplicar estes helpers — duas cópias
    deixariam o eixo com dentes divergir.

    Em `CallScreenSemanticsTest.kt`: REMOVER as quatro funções e acrescentar o import do pacote
    neutro. Nenhum caso de teste da Fase 6 muda de conteúdo; a suíte dela tem de continuar com o
    mesmo número de `@Test` verdes.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*CallScreenSemanticsTest" --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `app/src/test/java/org/sentinela/app/ui/TouchTargetAsserts.kt` existe e declara exatamente três funções `internal fun SemanticsNodeInteraction.`
    - `grep -c 'internal fun SemanticsNodeInteraction' app/src/test/java/org/sentinela/app/ui/call/CallScreenSemanticsTest.kt` devolve 0
    - `./gradlew testDebugUnitTest --tests "*CallScreenSemanticsTest" --rerun-tasks` verde com o MESMO número de casos de antes da extração (registrar os dois números no SUMMARY, lidos do XML de resultados)
    - `grep -rc 'assertLayoutHeightIsAtLeast' app/src/test --include=*.kt` mostra a definição em UM único arquivo
  </acceptance_criteria>
  <done>Os três asserts vivem em pacote neutro, o teste da Fase 6 continua verde com a mesma contagem, e não existe segunda cópia do eixo com dentes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Constantes de rota por texto e contrato do grafo provado em execução</name>
  <files>app/src/main/java/org/sentinela/app/ui/navigation/SentinelaRoutes.kt, app/src/test/java/org/sentinela/app/ui/navigation/NavGraphContractTest.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/ui/MainActivity.kt
    - app/src/main/java/org/sentinela/app/ui/dialer/DialerActivationScreen.kt (a assinatura, para a rota do item 9 de Proteção)
    - .planning/phases/07-ui-onboarding-e-home/07-RESEARCH.md (§Q1 inteiro e Pitfall 1)
    - .planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md (§6 — a ordem canônica dos 6 passos)
  </read_first>
  <behavior>
    - Compor um grafo com todas as rotas da fase não lança exceção alguma
    - Toda rota declarada no grafo é texto não vazio; nenhuma é nula
    - Navegar de boas-vindas para o primeiro passo troca o destino corrente
    - Ir para a home com descarte inclusivo do início deixa a pilha com um único elemento
    - Voltar do primeiro passo devolve o destino de boas-vindas
  </behavior>
  <action>
    Criar `app/src/main/java/org/sentinela/app/ui/navigation/SentinelaRoutes.kt` com um `object`
    `Rotas` de constantes `const val`, rotas por TEXTO. Destinos, um por tela da fase:
    boas-vindas; os seis passos do onboarding (papel, desconhecidos, contatos, whitelist,
    notificação, resumo); home; proteção; modo discador. Valores em minúsculas com sublinhado, sem
    barra e sem argumento — nenhuma tela desta fase recebe parâmetro de rota.

    KDoc do arquivo, em PROSA PORTUGUESA e sem escrever o nome da anotação vigiada: registrar que
    as rotas deste projeto são texto por medição, não por gosto; que a alternativa tipada da
    biblioteca de navegação compila limpa e estoura na primeira composição do grafo neste
    repositório, porque o compilador de Kotlin embutido na ferramenta de build não traz o
    complemento de serialização; e que por isso o guarda-corpo é um teste que COMPÕE o grafo,
    já que nenhum assert de compilação pega esse defeito.

    Criar `app/src/test/java/org/sentinela/app/ui/navigation/NavGraphContractTest.kt`,
    `@RunWith(RobolectricTestRunner::class)`,
    `@Config(sdk = [35], qualifiers = "w411dp-h891dp-xxhdpi")`, `createComposeRule`.

    O teste monta um `NavHost` REAL a partir de `Rotas`, com cada destino contendo apenas um `Text`
    de identificação vindo de `stringResource` (nada de literal em Kotlin). Capturar o
    `NavHostController` num `lateinit var` do próprio teste. Casos:

    1. O grafo compõe sem exceção e `nav.currentDestination?.route` é a rota de boas-vindas.
    2. Toda rota do grafo é texto não vazio: percorrer `nav.graph` e afirmar sobre cada destino.
       Este é o caso que fica vermelho se alguém trocar um destino por rota anotada — a exceção de
       serialização sobe na COMPOSIÇÃO, e o caso falha.
    3. `navigate` para o primeiro passo troca o destino corrente. Chamar `compose.waitForIdle()`
       depois de cada navegação: a troca de destino não é síncrona.
    4. `navigate` para a home com `popUpTo(boas-vindas) { inclusive = true }` e
       `launchSingleTop = true` deixa `nav.currentBackStack.value.mapNotNull { it.destination.route }`
       igual a uma lista de um elemento — a home. O `mapNotNull` não é enfeite: a entrada do próprio
       grafo tem rota nula, medido.
    5. `nav.popBackStack()` do primeiro passo devolve boas-vindas.
    6. A contagem de destinos do grafo é exatamente 10, travada por assert — destino novo exige
       revisão de navegação, no molde da contagem travada de reason codes da Fase 2.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*NavGraphContractTest" --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `app/src/main/java/org/sentinela/app/ui/navigation/SentinelaRoutes.kt` existe e `grep -c 'const val' ` nele devolve 10
    - `grep -ci 'serializable' app/src/main/java/org/sentinela/app/ui/navigation/SentinelaRoutes.kt` devolve 0
    - `./gradlew testDebugUnitTest --tests "*NavGraphContractTest" --rerun-tasks` verde com no mínimo 6 `@Test`
    - PROVA DE VERMELHO 1 (rota tipada): num arquivo de sonda descartável dentro de `app/src/test`, declarar um destino por objeto anotado e compor o grafo com ele ⇒ o caso 2 fica VERMELHO em EXECUÇÃO com a exceção de serialização, e o compilador continua verde. Transcrever no SUMMARY as duas coisas — o build verde e a falha de execução — e apagar a sonda
    - PROVA DE VERMELHO 2 (pilha): remover o descarte inclusivo do caso 4 ⇒ a lista esperada muda e o caso fica vermelho; restaurar
    - Ambas as provas incidem sobre código já commitado ou sobre sonda descartável; nenhuma usa `git checkout` sobre trabalho novo
  </acceptance_criteria>
  <done>As dez rotas existem como texto, o grafo é provado compondo de verdade, e a rota tipada tem um teste com dentes em vez de um compilador satisfeito.</done>
</task>

</tasks>

<verification>
```bash
./gradlew --rerun-tasks assembleDebug testDebugUnitTest lint detekt
bash scripts/verify-invariants.sh
```
`koverVerify` não é cobrado aqui; `./gradlew koverLog` para acompanhar. `SentinelaRoutes.kt` vive
em `ui.*`, fora dos pacotes medidos pelo Kover — nenhum exclude novo é necessário e nenhum deve ser
acrescentado.
</verification>

<success_criteria>
- Os três asserts de dois eixos em pacote neutro, sem cópia, com a suíte da Fase 6 na mesma contagem
- Dez rotas por texto, contagem travada
- Contrato do grafo provado em execução, com a prova de vermelho da rota tipada transcrita
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-ui-onboarding-e-home/07-02-SUMMARY.md`
</output>
