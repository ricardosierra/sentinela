---
phase: 07-ui-onboarding-e-home
plan: 07
type: execute
wave: 3
depends_on: ["07-03", "07-04"]
files_modified:
  - app/src/main/java/org/sentinela/app/ui/onboarding/NotificationStepScreen.kt
  - app/src/main/java/org/sentinela/app/ui/onboarding/SummaryStepScreen.kt
  - app/src/test/java/org/sentinela/app/ui/onboarding/NotificationAndSummaryStepTest.kt
autonomous: true
requirements: [UIX-01, UIX-09, UIX-10, UIX-11]

must_haves:
  truths:
    - "A notificação própria continua desligada por padrão e o passo não pressiona o usuário a ligá-la"
    - "A verificação final nunca diz que está tudo pronto quando o papel está ausente"
    - "Cada linha da verificação comunica o estado por ícone e por texto, nunca só por cor"
    - "O último ecrã antes da home repete o aviso de escopo com as mesmas frases do passo 1"
  artifacts:
    - path: "app/src/main/java/org/sentinela/app/ui/onboarding/NotificationStepScreen.kt"
      provides: "passo 5 de 6: opt-in sem pressão e as duas identificações"
      min_lines: 90
    - path: "app/src/main/java/org/sentinela/app/ui/onboarding/SummaryStepScreen.kt"
      provides: "passo 6 de 6: veredito honesto, quatro linhas de verificação e o cartão de honestidade repetido"
      min_lines: 140
  key_links:
    - from: "app/src/main/java/org/sentinela/app/ui/onboarding/SummaryStepScreen.kt"
      to: "app/src/main/java/org/sentinela/app/ui/components/CheckRow.kt"
      via: "quatro linhas de verificação com ação de correção em nó separado"
      pattern: "CheckRow"
---

<objective>
Passos 5 e 6 do onboarding: o opt-in de notificação sem pressão e a verificação final com veredito
honesto.

Purpose: fecha o critério de sucesso 1 (opt-in de notificação e verificação final) e é o ecrã onde a
regra "nenhum estado falsamente positivo" se materializa — o mesmo princípio do zero mentiroso.
Output: `NotificationStepScreen`, `SummaryStepScreen` e o teste de composição dos dois.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md
@.planning/phases/07-ui-onboarding-e-home/07-03-SUMMARY.md
@.planning/phases/07-ui-onboarding-e-home/07-04-SUMMARY.md
@CLAUDE.md

<interfaces>
```kotlin
enum class RuntimePermissionAsk { GRANTED, NEVER_ASKED, DENIED_ONCE, DENIED_PERMANENTLY }
enum class NotificationIdentification { MASKED, ANONYMOUS }
enum class OriginPolicy { RING, BLOCK, SILENCE, NEVER_SILENCE }
enum class ContactsPermissionState { GRANTED, NEVER_ASKED, DENIED_ONCE, DENIED_PERMANENTLY }
```
Componentes de 07-03: `OptionCard`, `StepHeader`, `SentinelaTopBar`, `SettingSwitchRow`, `CheckRow`.
Da Fase 6: `HonestyCard`. Cores de 07-01: `StatusAttention`, `CallAccept`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NotificationStepScreen — passo 5 de 6, opt-in sem pressão</name>
  <files>app/src/main/java/org/sentinela/app/ui/onboarding/NotificationStepScreen.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/ui/components/SettingSwitchRow.kt
    - app/src/main/java/org/sentinela/app/ui/components/OptionCard.kt
    - app/src/main/java/org/sentinela/app/permissions/RuntimePermissionAsk.kt
    - app/src/main/java/org/sentinela/app/notifications/BlockedCallNotifier.kt (para conferir que nenhuma identificação carrega dígito completo)
    - .planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md (§6.6)
  </read_first>
  <action>
    Criar `ui/onboarding/NotificationStepScreen.kt`, composta pura:
    `NotificationStepScreen(enabled: Boolean, identification: NotificationIdentification,
    permission: RuntimePermissionAsk, onEnabledChange: (Boolean) -> Unit,
    onIdentificationChange: (NotificationIdentification) -> Unit, onNext: () -> Unit,
    onSkip: () -> Unit, modifier: Modifier = Modifier)`.

    Estrutura da §6.6, mantendo o esqueleto dos passos 3 e 4: barra superior com
    `StepHeader(step = 5, total = 6)` e a ação de pular; 24dp; título `headlineMedium` com
    `settings_notification_enable`; 16dp; `bodyLarge` `OnSurfaceVariant` com
    `settings_notification_enable_desc`; 24dp; linha de interruptor de 56dp com o mesmo rótulo e a
    explicação permanente, DESLIGADO por padrão; quando o estado da permissão é negado uma vez,
    exibir `notification_permission_rationale` junto; 16dp; sub-opções visíveis SOMENTE com o
    interruptor ligado, com expansão e colapso de 200 ms suprimidos pela redução de movimento: dois
    `OptionCard` com papel de botão de rádio para
    `settings_notification_identification_masked` (padrão) e
    `settings_notification_identification_anonymous`; peso restante; botão pílula com
    `onboarding_next`.

    Regras de produto a respeitar e a registrar em KDoc: o padrão desligado é decisão já escrita na
    própria descrição da configuração, e o passo NÃO pressiona — nenhuma palavra de recomendação,
    nenhum destaque desigual entre ligar e não ligar, nenhum contador, nenhuma urgência. Nenhuma das
    duas identificações mostra a sequência completa de dígitos: a mascarada usa a máscara única do
    aplicativo e a anônima não mostra dígito algum.

    Semântica: o interruptor tem papel de interruptor e descrição de estado explícita, e a explicação
    permanente é IRMÃ e não filha mesclada, para o leitor de tela ler o rótulo com o estado e depois
    a explicação em vez de um bloco único. O container das sub-opções recebe
    `liveRegion = LiveRegionMode.Polite`, para anunciar que novas opções surgiram.

    O pedido do sistema não é feito aqui: a tela apenas chama `onEnabledChange(true)`, e é o dono de
    estado que grava a marca e dispara o pedido, nessa ordem — contrato estabelecido nas Fases 4, 5
    e 6 e travado em 07-04.

    Pré-visualizações: desligado, ligado com mascarada, ligado com anônima.
  </action>
  <verify>
    <automated>./gradlew assembleDebug lint detekt --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ciE 'recomendado|recomendamos|ative agora|não perca' app/src/main/java/org/sentinela/app/ui/onboarding/NotificationStepScreen.kt` devolve 0
    - `grep -c 'Polite' app/src/main/java/org/sentinela/app/ui/onboarding/NotificationStepScreen.kt` é maior que 0
    - `grep -cE 'requestPermission|launcher|ActivityResult' app/src/main/java/org/sentinela/app/ui/onboarding/NotificationStepScreen.kt` devolve 0 — a tela não dispara pedido do sistema
    - Nenhum texto de interface embutido em Kotlin
    - `./gradlew assembleDebug lint detekt --rerun-tasks` verde
  </acceptance_criteria>
  <done>Opt-in desligado por padrão, sem pressão, com as duas identificações anunciadas por papel de botão de rádio e as sub-opções em região viva educada.</done>
</task>

<task type="auto">
  <name>Task 2: SummaryStepScreen — passo 6 de 6, veredito honesto</name>
  <files>app/src/main/java/org/sentinela/app/ui/onboarding/SummaryStepScreen.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/ui/components/CheckRow.kt
    - app/src/main/java/org/sentinela/app/ui/components/HonestyCard.kt
    - app/src/main/java/org/sentinela/app/ui/onboarding/RoleStepScreen.kt (os itens do cartão de honestidade, a repetir idênticos)
    - .planning/phases/07-ui-onboarding-e-home/07-UI-SPEC.md (§6.7)
  </read_first>
  <action>
    Criar `ui/onboarding/SummaryStepScreen.kt`, composta pura:
    `SummaryStepScreen(roleHeld: Boolean, contactsPermission: ContactsPermissionState,
    unknownPolicy: OriginPolicy, contactsPolicy: OriginPolicy, whitelistPolicy: OriginPolicy,
    onFixRole: () -> Unit, onGrantContacts: () -> Unit, onFinish: () -> Unit,
    modifier: Modifier = Modifier)`.

    Estrutura da §6.7: barra superior com `StepHeader(step = 6, total = 6)` e SEM a ação de pular —
    já é o fim, e o único caminho é concluir; 24dp; círculo de 96dp com escudo preenchido de 48dp na
    cor do veredito; 16dp; título `headlineMedium`; 8dp; `bodyLarge` `OnSurfaceVariant` com
    `onboarding_summary_body`; 24dp; a lista de verificação de quatro linhas de 56dp; 24dp; o cartão
    de honestidade; peso restante; botão pílula de 56dp com `onboarding_finish`.

    **O veredito nunca é falsamente positivo.** Papel detido: círculo em `CallAccept` e título
    `onboarding_summary_title_ok`. Papel ausente: círculo em `StatusAttention`, título
    `onboarding_summary_title_partial`, e a primeira linha traz a ação de correção. Registrar em
    KDoc que esta é a mesma regra do zero mentiroso da home: o aplicativo não escreve que está tudo
    pronto quando não está.

    Quatro linhas, por `CheckRow`:
    1. `onboarding_check_role`, estado `onboarding_check_granted` ou `onboarding_check_missing`, com
       ação `dashboard_fix_configuration` chamando `onFixRole` quando ausente
    2. `onboarding_check_contacts`, mesmo par de estados, com ação
       `dialer_activation_grant_contacts` chamando `onGrantContacts` quando não concedida
    3. `onboarding_check_unknown`, com o rótulo da opção escolhida vindo de `unknown_option_*`
    4. `onboarding_check_origins`, com os rótulos das opções de contatos e whitelist escolhidas

    A lista recebe `semantics { collectionInfo = CollectionInfo(rowCount = 4, columnCount = 1) }`;
    cada linha é um nó cuja descrição de conteúdo é o rótulo seguido do estado; as ações de correção
    são botões focáveis SEPARADOS, fora do nó mesclado da linha — ponto de risco (e) da semântica
    mesclada.

    O cartão de honestidade repete EXATAMENTE os mesmos três itens do passo 1, na mesma ordem e com
    os mesmos identificadores de recurso: `dialer_activation_unchanged_3`, `onboarding_scope_dnd`,
    `settings_hide_native_log_desc`. Registrar em KDoc que a repetição é deliberada — é o último
    ecrã antes de o usuário confiar no aplicativo, e o único em que ele já viu o aplicativo inteiro.

    Pré-visualizações: veredito completo e veredito parcial.
  </action>
  <verify>
    <automated>./gradlew assembleDebug lint detekt --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'onboarding_summary_title_partial' app/src/main/java/org/sentinela/app/ui/onboarding/SummaryStepScreen.kt` é 1 e `grep -c 'onboarding_summary_title_ok'` é 1
    - `grep -c 'onboarding_skip' app/src/main/java/org/sentinela/app/ui/onboarding/SummaryStepScreen.kt` devolve 0
    - `grep -c 'CheckRow' app/src/main/java/org/sentinela/app/ui/onboarding/SummaryStepScreen.kt` é no mínimo 4 usos ou um laço sobre quatro dados
    - Os três identificadores do cartão de honestidade aparecem uma vez cada, idênticos aos do passo 1
    - `grep -c 'collectionInfo' app/src/main/java/org/sentinela/app/ui/onboarding/SummaryStepScreen.kt` é maior que 0
    - Nenhum texto de interface embutido em Kotlin
    - `./gradlew assembleDebug lint detekt --rerun-tasks` verde
  </acceptance_criteria>
  <done>A verificação final tem dois vereditos, quatro linhas com estado em ícone e texto, ações em nó separado e o aviso de escopo repetido.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Teste de composição dos passos 5 e 6</name>
  <files>app/src/test/java/org/sentinela/app/ui/onboarding/NotificationAndSummaryStepTest.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/ui/onboarding/NotificationStepScreen.kt
    - app/src/main/java/org/sentinela/app/ui/onboarding/SummaryStepScreen.kt
    - app/src/test/java/org/sentinela/app/ui/TouchTargetAsserts.kt
  </read_first>
  <behavior>
    - O interruptor da notificação vem desligado e as sub-opções não aparecem
    - Ligar o interruptor faz as duas sub-opções aparecerem, com mascarada selecionada
    - Negação simples da permissão exibe a justificativa
    - Papel detido produz o título de tudo pronto e nenhuma ação de correção
    - Papel ausente produz o título de quase pronto E a ação de correção na primeira linha
    - Agenda não concedida produz a ação de permitir na segunda linha
    - As linhas 3 e 4 exibem os rótulos das opções efetivamente escolhidas
    - O passo 6 não oferece a ação de pular
    - As quatro linhas anunciam rótulo e estado, e a ação de correção é nó focável separado
    - Cada controle passa os dois eixos de toque
  </behavior>
  <action>
    Criar `app/src/test/java/org/sentinela/app/ui/onboarding/NotificationAndSummaryStepTest.kt`,
    `@RunWith(RobolectricTestRunner::class)`,
    `@Config(sdk = [35], qualifiers = "w411dp-h891dp-xxhdpi")`, `createComposeRule`.

    No mínimo 14 casos cobrindo os dez comportamentos. Todo texto lido do recurso.

    Dois casos merecem cuidado especial, porque são onde o falso-verde mora:
    - o caso do veredito parcial precisa afirmar as DUAS coisas — o título de quase pronto E a
      existência da ação de correção; afirmar só o título deixaria passar uma tela que diz "quase
      pronto" e não oferece conserto
    - o caso da ação em nó separado precisa alcançar o nó da ação com `useUnmergedTree` e afirmar que
      ele tem ação de clique própria; encontrá-lo pelo texto sozinho não prova que ele é focável
      fora do nó mesclado da linha

    Os dois eixos afirmados no interruptor, nas duas sub-opções, nos dois botões de ação das linhas e
    no botão de concluir.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*NotificationAndSummaryStepTest" --rerun-tasks</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew testDebugUnitTest --tests "*NotificationAndSummaryStepTest" --rerun-tasks` verde com no mínimo 14 `@Test`
    - `grep -c 'useUnmergedTree' app/src/test/java/org/sentinela/app/ui/onboarding/NotificationAndSummaryStepTest.kt` é maior que 0
    - `grep -c 'assertLayoutHeightIsAtLeast' app/src/test/java/org/sentinela/app/ui/onboarding/NotificationAndSummaryStepTest.kt` é no mínimo 4, cada um com `assertTouchHeightIsAtLeast` no mesmo caso
    - PROVA DE VERMELHO 1 (veredito falsamente positivo): fazer o título ser sempre o de tudo pronto ⇒ o caso de veredito parcial fica vermelho; restaurar
    - PROVA DE VERMELHO 2 (padrão do opt-in): fazer o interruptor da notificação vir ligado ⇒ vermelho; restaurar
    - PROVA DE VERMELHO 3 (ação dentro do nó mesclado): mover o botão de correção para dentro do nó mesclado da linha ⇒ o caso de nó separado fica vermelho; restaurar
  </acceptance_criteria>
  <done>Os dois ecrãs estão travados contra veredito falsamente positivo, contra opt-in ligado por padrão e contra ação engolida por semântica mesclada.</done>
</task>

</tasks>

<verification>
```bash
./gradlew --rerun-tasks assembleDebug testDebugUnitTest lint detekt
bash scripts/verify-invariants.sh
./gradlew koverLog
```
</verification>

<success_criteria>
- Passo 5 sem pressão e com padrão desligado
- Passo 6 com veredito honesto nos dois ramos e aviso de escopo repetido
- Catorze casos verdes e três provas de vermelho transcritas
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-ui-onboarding-e-home/07-07-SUMMARY.md`
</output>
