---
phase: 03-dados-locais
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/src/main/java/org/sentinela/app/data/local/db/SentinelaDatabase.kt
  - app/src/main/java/org/sentinela/app/data/local/db/WhitelistEntity.kt
  - app/src/main/java/org/sentinela/app/data/local/db/WhitelistDao.kt
  - app/src/main/java/org/sentinela/app/data/local/db/BlockedCallEntity.kt
  - app/src/main/java/org/sentinela/app/data/local/db/BlockedCallDao.kt
  - app/src/main/java/org/sentinela/app/data/local/db/Converters.kt
  - app/src/main/java/org/sentinela/app/data/local/db/Migrations.kt
  - app/src/test/java/org/sentinela/app/data/local/db/SchemaExportTest.kt
  - app/src/test/java/org/sentinela/app/data/local/db/ConvertersTest.kt
  - scripts/verify-invariants.sh
  - app/schemas/
autonomous: true
requirements: [WLT-04, HST-01, HST-05, QLT-03]

must_haves:
  truths:
    - "Existe um unico banco sentinela.db com as tabelas whitelist e blocked_call"
    - "A chave da whitelist tem indice UNICO — duplicata e impossivel no banco"
    - "O schema v1 esta exportado e versionado em app/schemas/"
    - "fallbackToDestructiveMigration nao existe em lugar nenhum de app/src/main"
  artifacts:
    - path: "app/src/main/java/org/sentinela/app/data/local/db/SentinelaDatabase.kt"
      provides: "@Database version 1, exportSchema = true, duas entidades"
      contains: "exportSchema = true"
    - path: "app/src/main/java/org/sentinela/app/data/local/db/WhitelistEntity.kt"
      provides: "Tabela whitelist com indice unico em number_key"
      contains: "Index(value = [\"number_key\"], unique = true)"
    - path: "app/src/main/java/org/sentinela/app/data/local/db/WhitelistDao.kt"
      provides: "containsBlocking NAO-suspend + upsert + findIdByKey + search"
      contains: "fun containsBlocking"
    - path: "app/src/main/java/org/sentinela/app/data/local/db/Converters.kt"
      provides: "TypeConverters por name/code, nunca por ordinal"
    - path: "scripts/verify-invariants.sh"
      provides: "Invariantes de destrutivo proibido e schema v1 exportado"
      contains: "fallbackToDestructiveMigration"
  key_links:
    - from: "app/build.gradle.kts (ksp room.schemaLocation)"
      to: "app/schemas/org.sentinela.app.data.local.db.SentinelaDatabase/1.json"
      via: "geracao do KSP no build"
      pattern: "app/schemas/.*/1\\.json"
    - from: "SentinelaDatabase"
      to: "WhitelistDao e BlockedCallDao"
      via: "metodos abstratos do @Database"
      pattern: "abstract fun (whitelistDao|blockedCallDao)"
---

<objective>
Criar o schema v1 completo do `sentinela.db` — as duas tabelas, os dois DAOs, os
conversores e o arquivo de migracoes — e travar por invariante que ele nunca sera
destruido numa atualizacao.

Purpose: um unico schema e uma unica cadeia de migracao (decisao travada). Criar as duas
tabelas juntas evita churn de versao e libera os planos 04 e 05 para rodarem em paralelo.
Output: `app/schemas/<fqcn>/1.json` versionado e invariantes de protecao de dado no script.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dados-locais/03-CONTEXT.md
@.planning/phases/03-dados-locais/03-RESEARCH.md
@.planning/phases/03-dados-locais/03-01-SUMMARY.md
@app/src/main/java/org/sentinela/app/data/local/PersonalWhitelistRepository.kt
@app/src/main/java/org/sentinela/app/data/local/BlockedCallRepository.kt
@app/src/main/java/org/sentinela/app/domain/DecisionReason.kt
@scripts/verify-invariants.sh

<interfaces>
<!-- Modelos de dominio JA EXISTENTES — as entidades Room mapeiam PARA eles.
     NAO alterar estes contratos. -->

// data/local/PersonalWhitelistRepository.kt
data class WhitelistEntry(
    val id: Long = 0,
    val numberE164: String,        // chave: E.164 OU digitos crus se < LIMIAR_CURTO (6)
    val description: String? = null,
    val enabled: Boolean = true,
    val createdAtUtcMillis: Long,
)

// data/local/BlockedCallRepository.kt
data class BlockedCallEntry(
    val id: Long = 0,
    val maskedNumber: String,
    val numberE164: String?,
    val timestampUtcMillis: Long,
    val reason: DecisionReason,
    val notificationShown: Boolean,
    val classification: CallClassification = CallClassification.UNCLASSIFIED,
)
enum class CallClassification { UNCLASSIFIED, LEGITIMATE, UNWANTED }

// domain/DecisionReason.kt — 9 entradas, cada uma com `code: String` estavel:
// outgoing_call, protection_disabled, private_number, contact, personal_whitelist,
// unknown_number, invalid_number, local_lookup_failure, fallback_policy
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Entidades, DAOs, conversores e o @Database v1</name>
  <files>app/src/main/java/org/sentinela/app/data/local/db/SentinelaDatabase.kt, app/src/main/java/org/sentinela/app/data/local/db/WhitelistEntity.kt, app/src/main/java/org/sentinela/app/data/local/db/WhitelistDao.kt, app/src/main/java/org/sentinela/app/data/local/db/BlockedCallEntity.kt, app/src/main/java/org/sentinela/app/data/local/db/BlockedCallDao.kt, app/src/main/java/org/sentinela/app/data/local/db/Converters.kt, app/src/main/java/org/sentinela/app/data/local/db/Migrations.kt, app/src/test/java/org/sentinela/app/data/local/db/ConvertersTest.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/data/local/PersonalWhitelistRepository.kt
    - app/src/main/java/org/sentinela/app/data/local/BlockedCallRepository.kt
    - app/src/main/java/org/sentinela/app/domain/DecisionReason.kt
    - .planning/phases/03-dados-locais/03-RESEARCH.md secoes "Pattern 1" e "Pattern 2"
    - app/build.gradle.kts (bloco `ksp { arg("room.schemaLocation", ...) }`)
  </read_first>
  <behavior>
    - `Converters` mapeia `DecisionReason` pelo `code` e `CallClassification` pelo `name` — round-trip para todas as entradas
    - Um `code`/`name` desconhecido vindo do banco cai num fallback documentado, nunca lanca
    - `ordinal` nao aparece em nenhum conversor
  </behavior>
  <action>
Criar o pacote `app/src/main/java/org/sentinela/app/data/local/db/` com 6 arquivos.
O subpacote `db/` e obrigatorio: isola o codigo gerado pelo Room (`*_Impl`) para o
filtro do Kover poder exclui-lo por pacote (plano 03-07).

**WhitelistEntity.kt**
```kotlin
package org.sentinela.app.data.local.db

import androidx.room.ColumnInfo
import androidx.room.Entity
import androidx.room.Index
import androidx.room.PrimaryKey

@Entity(
    tableName = "whitelist",
    indices = [Index(value = ["number_key"], unique = true)],
)
data class WhitelistEntity(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    /**
     * Chave devolvida pelo PhoneNumberNormalizer (contrato fechado na Fase 2):
     * E.164 para numeros normais, DIGITOS CRUS abaixo de PhoneNumbers.LIMIAR_CURTO (6),
     * para que codigos curtos como "190" possam entrar na whitelist.
     * A normalizacao acontece ANTES de chegar ao repositorio.
     */
    @ColumnInfo(name = "number_key") val numberKey: String,
    @ColumnInfo(name = "description") val description: String? = null,
    @ColumnInfo(name = "enabled") val enabled: Boolean = true,
    @ColumnInfo(name = "created_at_utc_millis") val createdAtUtcMillis: Long,
)
```

**WhitelistDao.kt**
```kotlin
package org.sentinela.app.data.local.db

import androidx.room.Dao
import androidx.room.Delete
import androidx.room.Query
import androidx.room.Upsert
import kotlinx.coroutines.flow.Flow

@Dao
interface WhitelistDao {

    /**
     * CAMINHO QUENTE do CallScreeningService (Fase 5) — deliberadamente NAO-suspend.
     * Medido na pesquisa da Fase 3 (emulador API 35, 1.000 entradas):
     *   suspend      p50 1,46 ms | p95 9,12 ms | p99 26,39 ms  -> ESTOURA o alvo
     *   nao-suspend  p50 0,20 ms | p95 3,59 ms | p99  5,46 ms  -> dentro
     * O custo e o dispatch de corrotina do Room, nao o SQLite. O chamador e
     * responsavel por nao invocar na main thread.
     *
     * `enabled = 1`: desabilitar equivale funcionalmente a remover (decisao travada).
     */
    @Query("SELECT EXISTS(SELECT 1 FROM whitelist WHERE number_key = :key AND enabled = 1)")
    fun containsBlocking(key: String): Boolean

    @Query("SELECT id FROM whitelist WHERE number_key = :key")
    suspend fun findIdByKey(key: String): Long?

    @Upsert
    suspend fun upsert(entity: WhitelistEntity)

    @Delete
    suspend fun delete(entity: WhitelistEntity)

    @Query("DELETE FROM whitelist WHERE id = :id")
    suspend fun deleteById(id: Long)

    @Query("SELECT * FROM whitelist ORDER BY created_at_utc_millis DESC")
    fun observeAll(): Flow<List<WhitelistEntity>>

    @Query(
        "SELECT * FROM whitelist " +
            "WHERE number_key LIKE '%' || :query || '%' " +
            "OR description LIKE '%' || :query || '%' " +
            "ORDER BY created_at_utc_millis DESC",
    )
    fun search(query: String): Flow<List<WhitelistEntity>>

    @Query("SELECT * FROM whitelist WHERE number_key = :key")
    suspend fun findByKey(key: String): WhitelistEntity?
}
```

**BlockedCallEntity.kt**
```kotlin
package org.sentinela.app.data.local.db

import androidx.room.ColumnInfo
import androidx.room.Entity
import androidx.room.Index
import androidx.room.PrimaryKey

/**
 * Historico local. Guarda o E.164 completo em coluna propria porque a Fase 8
 * precisa dele para "adicionar a whitelist a partir do historico" (HST-04).
 * Isso NAO afrouxa a privacidade: log, notificacao e crash report continuam
 * usando somente `masked_number`, e o banco fica fora do backup (PRV-03).
 *
 * NUNCA adicionar coluna de nome de contato aqui.
 */
@Entity(
    tableName = "blocked_call",
    indices = [Index(value = ["timestamp_utc_millis"])],
)
data class BlockedCallEntity(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    @ColumnInfo(name = "masked_number") val maskedNumber: String,
    @ColumnInfo(name = "number_e164") val numberE164: String? = null,
    @ColumnInfo(name = "timestamp_utc_millis") val timestampUtcMillis: Long,
    @ColumnInfo(name = "reason_code") val reasonCode: String,
    @ColumnInfo(name = "notification_shown") val notificationShown: Boolean,
    @ColumnInfo(name = "classification") val classification: String,
)
```
O indice em `timestamp_utc_millis` serve a poda por retencao e a listagem ordenada.

**Converters.kt** — `DecisionReason` persistido pelo `code`, `CallClassification` pelo
`name`. NUNCA `ordinal` (reordenar o enum corromperia o historico do usuario).
Entrada desconhecida vinda do banco cai em `DecisionReason.UNKNOWN_NUMBER` /
`CallClassification.UNCLASSIFIED` com comentario explicando — nunca lanca.

**Migrations.kt**
```kotlin
package org.sentinela.app.data.local.db

import androidx.room.migration.Migration

/**
 * Cadeia explicita de migracoes. `fallbackToDestructiveMigration` e PROIBIDO
 * (apagaria a whitelist do usuario numa atualizacao) e verificado por
 * scripts/verify-invariants.sh. A v1 e a versao inicial: ainda nao ha migracao.
 * Toda versao nova DEVE acrescentar aqui e ganhar teste em MigrationHarnessTest.
 */
val SENTINELA_MIGRATIONS: Array<Migration> = emptyArray()
```

**SentinelaDatabase.kt**
```kotlin
package org.sentinela.app.data.local.db

import androidx.room.Database
import androidx.room.RoomDatabase
import androidx.room.TypeConverters

@Database(
    entities = [WhitelistEntity::class, BlockedCallEntity::class],
    version = 1,
    exportSchema = true,
)
@TypeConverters(Converters::class)
abstract class SentinelaDatabase : RoomDatabase() {
    abstract fun whitelistDao(): WhitelistDao
    abstract fun blockedCallDao(): BlockedCallDao
    companion object { const val NAME = "sentinela.db" }
}
```

**BlockedCallDao.kt** — `@Insert` suspend `record`, `observeRecent(): Flow<List<...>>`
ordenado por `timestamp_utc_millis DESC`, `observeTotalCount(): Flow<Long>`
(`SELECT COUNT(*)`), `deleteById(id)`, `clearAll()` (`DELETE FROM blocked_call`),
`pruneOlderThan(cutoffUtcMillis)` (`DELETE FROM blocked_call WHERE timestamp_utc_millis < :cutoffUtcMillis`)
e `updateClassification(id, classification: String)` (HST-05).

Criar tambem `app/src/test/java/org/sentinela/app/data/local/db/ConvertersTest.kt`:
round-trip de **todas** as 9 entradas de `DecisionReason` e das 3 de `CallClassification`,
mais um caso de valor desconhecido caindo no fallback sem lancar.

PROIBIDO neste arquivo e em qualquer outro de `app/src/main`:
`fallbackToDestructiveMigration`, `allowMainThreadQueries`, `@Insert(onConflict = REPLACE)`
(REPLACE deleta e reinsere, mudando o id) e `enum.ordinal` como valor persistido.
  </action>
  <acceptance_criteria>
    - `./gradlew :app:compileDebugKotlin` termina com `BUILD SUCCESSFUL`
    - `[ "$(ls -1 app/schemas/*/1.json 2>/dev/null | wc -l)" -ge 1 ]`
    - `grep -q '"tableName": "whitelist"' app/schemas/*/1.json`
    - `grep -q '"tableName": "blocked_call"' app/schemas/*/1.json`
    - `grep -q 'index_whitelist_number_key' app/schemas/*/1.json`
    - `grep -q '"unique": true' app/schemas/*/1.json`
    - `[ "$(grep -rc 'fallbackToDestructiveMigration' app/src/main --include='*.kt' | grep -v ':0' | wc -l)" -eq 0 ]`
    - `[ "$(grep -rn 'ordinal' app/src/main/java/org/sentinela/app/data/local/db/Converters.kt | wc -l)" -eq 0 ]`
    - `./gradlew testDebugUnitTest --tests "*ConvertersTest"` verde com `tests` >= 4
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin testDebugUnitTest --tests "*ConvertersTest" detekt &amp;&amp; ls app/schemas/*/1.json</automated>
  </verify>
  <done>Schema v1 compila, exporta `1.json` com as duas tabelas e o indice unico, e os conversores tem round-trip testado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SchemaExportTest — o schema exportado e contrato versionado</name>
  <files>app/src/test/java/org/sentinela/app/data/local/db/SchemaExportTest.kt, app/schemas/</files>
  <read_first>
    - app/schemas/ (conteudo gerado na Task 1)
    - app/build.gradle.kts (bloco `ksp`)
    - app/src/test/java/org/sentinela/app/phone/TestMetadata.kt (working dir do teste JVM = `app/`)
  </read_first>
  <behavior>
    - Existe exatamente um diretorio de schema sob `schemas/` e ele contem `1.json`
    - `1.json` nao esta vazio e declara `"version": 1`
    - `1.json` declara as tabelas `whitelist` e `blocked_call`
    - `1.json` declara o indice `index_whitelist_number_key` como `unique`
    - Desligar `exportSchema` deixa este teste vermelho
  </behavior>
  <action>
Criar `app/src/test/java/org/sentinela/app/data/local/db/SchemaExportTest.kt` (JVM puro,
sem Room, sem Android). Le `File("schemas")` — caminho relativo ao modulo `app/`.

Testes (5, com mensagens de assert descritivas):
1. `schemasExportadosExistem` — `File("schemas").listFiles()` tem ao menos um diretorio
   contendo `1.json`, e `1.json.length() > 0`.
2. `schemaV1DeclaraVersao1` — o texto do `1.json` contem `"version": 1`.
3. `schemaV1TemAsDuasTabelas` — contem `"tableName": "whitelist"` e `"tableName": "blocked_call"`.
4. `whitelistTemIndiceUnicoNaChave` — contem `index_whitelist_number_key` e `"unique": true`.
5. `nenhumSchemaDeRascunhoVersionado` — o unico diretorio sob `schemas/` e o do
   `SentinelaDatabase` (assert de nome contendo `SentinelaDatabase`); protege contra
   sobra de banco de rascunho.

Verificar tambem que `app/schemas/.gitkeep` continua existindo e que o `1.json` sera
commitado (nao pode estar em `.gitignore`) — se estiver ignorado, corrigir o `.gitignore`
no mesmo commit e registrar no SUMMARY.

**Prova de falha:** trocar temporariamente `exportSchema = true` para `false`,
rodar `./gradlew clean :app:compileDebugKotlin testDebugUnitTest --tests "*SchemaExportTest"`,
confirmar VERMELHO, reverter, confirmar VERDE. Registrar as duas saidas no SUMMARY.
  </action>
  <acceptance_criteria>
    - `./gradlew testDebugUnitTest --tests "*SchemaExportTest"` verde
    - `[ "$(grep -c '@Test' app/src/test/java/org/sentinela/app/data/local/db/SchemaExportTest.kt)" -ge 5 ]`
    - `git check-ignore -q app/schemas/*/1.json` retorna 1 (arquivo NAO ignorado)
    - `git status --porcelain app/schemas` lista o `1.json` como novo/versionado
    - SUMMARY registra a saida vermelha com `exportSchema = false`
  </acceptance_criteria>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*SchemaExportTest"</automated>
  </verify>
  <done>O schema v1 e contrato versionado no git e protegido por teste que falha se o export for desligado.</done>
</task>

<task type="auto">
  <name>Task 3: Invariantes de protecao de dado no verify-invariants.sh</name>
  <files>scripts/verify-invariants.sh</files>
  <read_first>
    - scripts/verify-invariants.sh (integral — reusar `fail`/`ok`/`skip` e o estilo de contagem seguro)
    - app/src/main/java/org/sentinela/app/data/local/db/ (arquivos da Task 1)
    - .planning/phases/03-dados-locais/03-RESEARCH.md secao "Invariante em scripts/verify-invariants.sh"
  </read_first>
  <action>
Acrescentar em `scripts/verify-invariants.sh` um **Bloco 5**, entre o Bloco 4
(relatorios de qualidade, termina na linha ~182) e o bloco final de saida
(`if [ "$FAILURES" -gt 0 ]`):

```bash
# ---------------------------------------------------------------------------
# Bloco 5 — Fase 3: integridade do dado local (o risco da fase e PERDER dado)
# ---------------------------------------------------------------------------
echo "== Bloco 5: integridade do dado local =="

# fallbackToDestructiveMigration apagaria a whitelist do usuario numa atualizacao.
DESTRUCTIVE=$(grep -rn "fallbackToDestructiveMigration" app/src/main --include="*.kt" 2>/dev/null)
if [ -z "$DESTRUCTIVE" ]; then
  ok "sem fallbackToDestructiveMigration (migracao explicita obrigatoria)"
else
  echo "$DESTRUCTIVE" | sed 's/^/      /'
  fail "fallbackToDestructiveMigration proibido (Fase 3): apagaria o dado do usuario"
fi

# allowMainThreadQueries mascara o problema de dispatch em vez de resolve-lo.
MAINTHREAD=$(grep -rn "allowMainThreadQueries" app/src/main --include="*.kt" 2>/dev/null)
if [ -z "$MAINTHREAD" ]; then
  ok "sem allowMainThreadQueries"
else
  echo "$MAINTHREAD" | sed 's/^/      /'
  fail "allowMainThreadQueries proibido (Fase 3)"
fi

# Schema v1 exportado e versionado — o JSON e o oraculo da migracao.
SCHEMAS=$(ls -1 app/schemas/*/1.json 2>/dev/null | wc -l | tr -d ' ')
if [ "${SCHEMAS:-0}" -ge 1 ]; then
  ok "schema Room v1 exportado (app/schemas/*/1.json)"
else
  fail "app/schemas/<db>/1.json ausente — exportSchema desligado?"
fi

# O historico guarda E.164, mas NUNCA nome de contato (docs/PRIVACIDADE.md).
CONTACT_COL=$(grep -rniE "contactName|contact_name|displayName|display_name" \
  app/src/main/java/org/sentinela/app/data/local 2>/dev/null)
if [ -z "$CONTACT_COL" ]; then
  ok "nenhuma coluna de nome de contato na camada de dados"
else
  echo "$CONTACT_COL" | sed 's/^/      /'
  fail "nome de contato na camada de dados — proibido (docs/PRIVACIDADE.md)"
fi
```

Regras de escrita obrigatorias (o script roda sem `set -e`):
- Nunca usar `grep -c ... || echo 0` — `grep -c` imprime `0` **e** sai 1, entao o `|| echo 0`
  duplicaria a saida. Usar captura em variavel, como acima.
- Nao mover nem reescrever os blocos 1-4 existentes.

O bloco 5 deve rodar verde no estado atual do repo (schema ja exportado pela Task 1).
**Prova de falha:** inserir temporariamente `// fallbackToDestructiveMigration` num
arquivo de `app/src/main`, rodar o script, confirmar `exit 1`, remover, confirmar verde.
  </action>
  <acceptance_criteria>
    - `bash -n scripts/verify-invariants.sh` sai 0
    - `./gradlew assembleDebug lint detekt && bash scripts/verify-invariants.sh` sai 0 e imprime `== todos os invariantes OK ==`
    - A saida contem `ok:   sem fallbackToDestructiveMigration (migracao explicita obrigatoria)`
    - A saida contem `ok:   schema Room v1 exportado (app/schemas/*/1.json)`
    - A saida contem `ok:   nenhuma coluna de nome de contato na camada de dados`
    - `[ "$(grep -c '|| echo 0' scripts/verify-invariants.sh)" -eq 0 ]`
    - SUMMARY registra o `exit 1` da prova de falha
  </acceptance_criteria>
  <verify>
    <automated>./gradlew assembleDebug lint detekt &amp;&amp; bash scripts/verify-invariants.sh</automated>
  </verify>
  <done>Quatro invariantes novos verdes; o script falha de verdade quando o destrutivo e reintroduzido.</done>
</task>

</tasks>

<verification>
```bash
./gradlew clean
./gradlew --no-build-cache assembleDebug testDebugUnitTest lint detekt
bash scripts/verify-invariants.sh
ls -l app/schemas/*/1.json
```
</verification>

<success_criteria>
- `sentinela.db` v1 com `whitelist` e `blocked_call`, indice UNICO em `number_key`
- `app/schemas/<fqcn>/1.json` gerado, versionado no git e coberto por `SchemaExportTest`
- Conversores por `code`/`name`, nunca `ordinal`, com round-trip testado
- `verify-invariants.sh` bloqueia `fallbackToDestructiveMigration`, `allowMainThreadQueries`,
  ausencia de schema e coluna de nome de contato
</success_criteria>

<output>
Criar `.planning/phases/03-dados-locais/03-03-SUMMARY.md`
</output>
