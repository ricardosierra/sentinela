---
phase: 03-dados-locais
plan: 06
type: execute
wave: 4
depends_on: ["03-05"]
files_modified:
  - app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt
  - app/src/main/java/org/sentinela/app/settings/SettingsRepository.kt
  - app/src/test/java/org/sentinela/app/settings/DataStoreSettingsRepositoryTest.kt
  - app/src/test/java/org/sentinela/app/settings/AppOpenCounterTest.kt
autonomous: true
requirements: [ENG-01, HST-02, QLT-01]

must_haves:
  truths:
    - "Arquivo novo ou vazio devolve exatamente os defaults do MVP"
    - "Toda configuracao gravada sobrevive a releitura, inclusive retencao e historyEnabled"
    - "snapshot() e servido de memoria depois do primeiro aquecimento"
    - "Arquivo corrompido cai nos defaults seguros em vez de derrubar o Flow da decisao"
    - "O contador de aberturas incrementa e persiste, comecando em zero"
  artifacts:
    - path: "app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt"
      provides: "Implementacao DataStore com cache @Volatile e contador de aberturas"
      min_lines: 70
      contains: "@Volatile"
    - path: "app/src/test/java/org/sentinela/app/settings/DataStoreSettingsRepositoryTest.kt"
      provides: "Cobertura JVM do mapeamento Preferences<->ScreeningSettings"
  key_links:
    - from: "DataStoreSettingsRepository.settings"
      to: "cache @Volatile"
      via: "onEach { cached = it } no Flow"
      pattern: "onEach"
    - from: "DataStoreSettingsRepository.settings"
      to: "emptyPreferences() em IOException"
      via: "catch antes do map — arquivo corrompido nao derruba a decisao"
      pattern: "emptyPreferences"
---

<objective>
Implementar `SettingsRepository` sobre DataStore Preferences com snapshot servido de
memoria e o contador de aberturas do app.

Purpose: fecha o criterio 1 (defaults do MVP com Flow + snapshot rapido) e o criterio 4
(contador de aberturas) do ROADMAP. O snapshot esta no caminho quente do Service: ler o
disco ali custou 10,9 ms na primeira leitura (medido) — cache e requisito, nao estilo.
Output: `DataStoreSettingsRepository` com suites JVM puras usando `TemporaryFolder`.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dados-locais/03-CONTEXT.md
@.planning/phases/03-dados-locais/03-RESEARCH.md
@.planning/phases/03-dados-locais/03-05-SUMMARY.md
@app/src/main/java/org/sentinela/app/settings/SettingsRepository.kt
@app/src/main/java/org/sentinela/app/settings/ScreeningSettings.kt
@app/src/main/java/org/sentinela/app/settings/RetentionPolicy.kt

<interfaces>
<!-- Contrato JA EXISTENTE — implementar sem alterar as 3 assinaturas: -->
interface SettingsRepository {
    val settings: Flow<ScreeningSettings>
    suspend fun snapshot(): ScreeningSettings
    suspend fun update(transform: (ScreeningSettings) -> ScreeningSettings)
}

<!-- ScreeningSettings apos o plano 03-05 (11 campos, todos com default do MVP): -->
<!-- protectionEnabled=true, unknownPolicy=BLOCK, contactsPolicy=RING,           -->
<!-- whitelistPolicy=NEVER_SILENCE, blockPrivateNumbers=true, blockMode=REJECT,  -->
<!-- hideFromNativeCallLog=true, showOwnNotification=false,                      -->
<!-- fallbackPolicy=ALLOW, historyEnabled=true, retentionPolicy=DAYS_30          -->

<!-- Medicoes da pesquisa (emulador API 35):                                      -->
<!--   dataStore.data.first() primeira leitura: 10,896 ms                         -->
<!--   dataStore.data.first() cache quente:     p50 0,470 / p95 3,870 ms          -->
<!-- Runtime lanca IllegalStateException se houver 2 DataStores sobre o mesmo     -->
<!-- arquivo — nos testes, um arquivo NOVO por teste e cancelamento do scope.     -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DataStoreSettingsRepository com cache volatil e defaults seguros</name>
  <files>app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt, app/src/main/java/org/sentinela/app/settings/SettingsRepository.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/settings/SettingsRepository.kt
    - app/src/main/java/org/sentinela/app/settings/ScreeningSettings.kt (11 campos apos 03-05)
    - app/src/main/java/org/sentinela/app/settings/RetentionPolicy.kt
    - .planning/phases/03-dados-locais/03-RESEARCH.md "Pattern 3" e "Pitfall 2/3"
  </read_first>
  <behavior>
    - DataStore vazio -> `snapshot()` devolve `ScreeningSettings()` (todos os defaults do MVP)
    - `update { it.copy(unknownPolicy = SILENCE) }` persiste e a proxima leitura reflete
    - Enums persistidos pelo `name`/`id`, nunca pelo `ordinal` — reordenar o enum nao muda o valor lido
    - Valor invalido gravado no arquivo cai no default do campo, sem lancar
    - `IOException` na leitura -> `emptyPreferences()` -> defaults; o Flow NAO morre
    - Apos o primeiro `snapshot()`, chamadas seguintes vem do cache (nao reabrem o Flow)
  </behavior>
  <action>
Criar `app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt`:

```kotlin
class DataStoreSettingsRepository(
    private val dataStore: DataStore<Preferences>,
    scope: CoroutineScope,
) : SettingsRepository {

    /**
     * Caminho quente do CallScreeningService (Fase 5). Medido na pesquisa da Fase 3:
     * a PRIMEIRA leitura do DataStore custa 10,9 ms; as seguintes p95 3,87 ms.
     * Cabe nos 200 ms, mas somado a Room + normalizacao come metade do orcamento
     * de cold start — por isso o snapshot e servido de memoria.
     */
    @Volatile private var cached: ScreeningSettings? = null

    override val settings: Flow<ScreeningSettings> =
        dataStore.data
            // Arquivo corrompido NAO pode derrubar o Flow: por tabela, derrubaria a
            // decisao. emptyPreferences() cai nos defaults SEGUROS do MVP.
            .catch { e -> if (e is IOException) emit(emptyPreferences()) else throw e }
            .map { it.toScreeningSettings() }
            .onEach { cached = it }

    init { scope.launch { settings.collect() } }

    override suspend fun snapshot(): ScreeningSettings =
        cached ?: settings.first().also { cached = it }

    override suspend fun update(transform: (ScreeningSettings) -> ScreeningSettings) {
        dataStore.edit { prefs -> transform(prefs.toScreeningSettings()).writeInto(prefs) }
    }
}
```

Mapeamento `Preferences.toScreeningSettings()` / `ScreeningSettings.writeInto(prefs)`
para os 11 campos. Chaves: `booleanPreferencesKey` para os 4 booleanos e
`stringPreferencesKey` para os enums, gravando `OriginPolicy.name`, `BlockMode.name`,
`FallbackPolicy.name` e `RetentionPolicy.id`. Leitura de enum sempre com fallback ao
default do campo (`entries.firstOrNull { it.name == raw } ?: DEFAULT`), nunca
`valueOf` cru (lancaria em dado antigo/corrompido) e nunca `ordinal`.

Acrescentar tambem, na MESMA classe, o contador de aberturas (ENG-01):
```kotlin
    /** ENG-01: contador local de aberturas, base do convite de avaliacao (Fase 9). */
    val appOpenCount: Flow<Int> =
        dataStore.data
            .catch { e -> if (e is IOException) emit(emptyPreferences()) else throw e }
            .map { it[Keys.APP_OPEN_COUNT] ?: 0 }

    /** Devolve o novo valor. Atomico: edit() do DataStore e transacional. */
    suspend fun incrementAppOpenCount(): Int {
        var updated = 0
        dataStore.edit { prefs ->
            updated = (prefs[Keys.APP_OPEN_COUNT] ?: 0) + 1
            prefs[Keys.APP_OPEN_COUNT] = updated
        }
        return updated
    }
```
Contador vive no DataStore junto das configuracoes — nao merece tabela (decisao travada).

NAO usar o delegate `by preferencesDataStore(name = ...)` como extensao de Context: o
delegate e por-Context e e facil duplicar, e o runtime lanca
`There are multiple DataStores active for the same file`. A instancia unica e criada
explicitamente no `AppContainer` (plano 03-07). NENHUMA chamada de rede. NENHUM log
de valor de configuracao com dado pessoal.
  </action>
  <acceptance_criteria>
    - `./gradlew :app:compileDebugKotlin detekt` com `BUILD SUCCESSFUL` e sem issues
    - `grep -q '@Volatile private var cached' app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt`
    - `grep -q 'emptyPreferences()' app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt`
    - `grep -q 'incrementAppOpenCount' app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt`
    - `[ "$(grep -c 'preferencesDataStore(' app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt)" -eq 0 ]`
    - `[ "$(grep -c 'ordinal' app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt)" -eq 0 ]`
    - `[ "$(grep -cE '\.valueOf\(' app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt)" -eq 0 ]`
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin detekt lint</automated>
  </verify>
  <done>Repositorio DataStore compila com cache volatil, catch de IOException, enums por name/id e contador de aberturas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Suite JVM do DataStore com TemporaryFolder</name>
  <files>app/src/test/java/org/sentinela/app/settings/DataStoreSettingsRepositoryTest.kt, app/src/test/java/org/sentinela/app/settings/AppOpenCounterTest.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt (Task 1)
    - app/src/main/java/org/sentinela/app/settings/ScreeningSettings.kt
    - .planning/phases/03-dados-locais/03-RESEARCH.md "Teste de DataStore em JVM (sem instrumentacao)"
  </read_first>
  <behavior>
    - Arquivo inexistente -> `snapshot() == ScreeningSettings()` (os 11 defaults)
    - Cada um dos 11 campos: gravar valor NAO-default, reler, conferir round-trip
    - Enum gravado com string desconhecida -> default do campo, sem excecao
    - `update` sucessivos compoem (o segundo enxerga o resultado do primeiro)
    - `settings` emite o valor novo apos `update` (coletado com turbine)
    - `snapshot()` chamado 2x nao reabre o arquivo (cache quente)
  </behavior>
  <action>
Criar `app/src/test/java/org/sentinela/app/settings/DataStoreSettingsRepositoryTest.kt`,
JVM puro — `PreferenceDataStoreFactory.create { file }` recebe um `File` arbitrario e
nao precisa de Context, entao nada de Robolectric nem de instrumentacao.

Helper obrigatorio (evita `IllegalStateException: There are multiple DataStores active
for the same file`, que e o modo de falha real reproduzido na pesquisa):
```kotlin
@get:Rule val tmp = TemporaryFolder()

private fun repo(scope: CoroutineScope): DataStoreSettingsRepository {
    val file = tmp.newFile("settings-${counter++}.preferences_pb").also { it.delete() }
    return DataStoreSettingsRepository(PreferenceDataStoreFactory.create(scope = scope) { file }, scope)
}
```
Arquivo NOVO por teste e `scope.cancel()` no `@After` — sem isso o proximo teste quebra.

Cobrir os 6 comportamentos. Minimo 16 `@Test` (os 11 round-trips de campo contam).
Usar `runTest` + `UnconfinedTestDispatcher(testScheduler)` e `turbine` (ja em
`testImplementation`) para o teste de emissao do Flow.

Criar tambem `app/src/test/java/org/sentinela/app/settings/AppOpenCounterTest.kt` (ENG-01):
- contador comeca em 0 num arquivo novo;
- `incrementAppOpenCount()` devolve 1, depois 2, depois 3;
- o valor sobrevive a recriacao do repositorio sobre o MESMO arquivo (persistencia real);
- `appOpenCount` Flow emite o valor atualizado apos o incremento.
Minimo 5 `@Test`.

Nenhum teste pode depender de `System.currentTimeMillis()` real nem de sleep.
  </action>
  <acceptance_criteria>
    - `./gradlew testDebugUnitTest --tests "*DataStoreSettingsRepositoryTest" --tests "*AppOpenCounterTest"` verde
    - `[ "$(grep -c '@Test' app/src/test/java/org/sentinela/app/settings/DataStoreSettingsRepositoryTest.kt)" -ge 16 ]`
    - `[ "$(grep -c '@Test' app/src/test/java/org/sentinela/app/settings/AppOpenCounterTest.kt)" -ge 5 ]`
    - `grep -q 'TemporaryFolder' app/src/test/java/org/sentinela/app/settings/DataStoreSettingsRepositoryTest.kt`
    - `grep -q 'scope.cancel()' app/src/test/java/org/sentinela/app/settings/DataStoreSettingsRepositoryTest.kt`
    - `[ "$(grep -c 'Robolectric\|@Config' app/src/test/java/org/sentinela/app/settings/DataStoreSettingsRepositoryTest.kt)" -eq 0 ]`
    - `[ "$(grep -c 'Thread.sleep' app/src/test/java/org/sentinela/app/settings/*.kt)" -eq 0 ]`
    - `./gradlew koverLog` imprime percentual; o executor registra o valor no SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*DataStoreSettingsRepositoryTest" --tests "*AppOpenCounterTest" detekt</automated>
  </verify>
  <done>21+ testes JVM verdes cobrindo defaults, round-trip dos 11 campos, corrupcao e contador de aberturas.</done>
</task>

</tasks>

<verification>
```bash
./gradlew testDebugUnitTest lint detekt
./gradlew koverLog
bash scripts/verify-invariants.sh
```
</verification>

<success_criteria>
- Criterio 1 do ROADMAP: Flow + snapshot rapido com os defaults do MVP
- Criterio 4 do ROADMAP: contador de aberturas persiste e incrementa
- HST-02: `retentionPolicy` e `historyEnabled` persistem e releem
- QLT-01: arquivo corrompido e valor de enum invalido caem em default seguro
- Nenhuma instancia duplicada de DataStore; nenhum delegate por-Context
</success_criteria>

<output>
Criar `.planning/phases/03-dados-locais/03-06-SUMMARY.md`
</output>
