---
phase: 03-dados-locais
plan: 07
type: execute
wave: 5
depends_on: ["03-02", "03-04", "03-06"]
files_modified:
  - app/src/main/java/org/sentinela/app/AppContainer.kt
  - app/src/main/java/org/sentinela/app/SentinelaApp.kt
  - app/src/androidTest/java/org/sentinela/app/data/local/db/MigrationHarnessTest.kt
  - app/build.gradle.kts
  - .planning/phases/03-dados-locais/03-EVIDENCE.md
autonomous: true
requirements: [WLT-07, HST-02, ENG-01, QLT-03, QLT-06, QLT-01]

must_haves:
  truths:
    - "Ha exatamente uma instancia de banco e uma de DataStore no processo"
    - "Abrir o app incrementa o contador e roda a poda de retencao"
    - "MigrationTestHelper abre o schema v1 exportado — o harness de migracao esta de pe"
    - "O gate de cobertura mede tambem data/ e settings/ sem falso-vermelho de codigo gerado"
    - "A suite completa passa pos-clean com --no-build-cache, JVM e instrumentada"
  artifacts:
    - path: "app/src/main/java/org/sentinela/app/AppContainer.kt"
      provides: "Singletons by lazy de SentinelaDatabase, DataStore e os tres repositorios"
      contains: "by lazy"
    - path: "app/src/androidTest/java/org/sentinela/app/data/local/db/MigrationHarnessTest.kt"
      provides: "MigrationTestHelper abrindo a v1 a partir dos assets"
      contains: "MigrationTestHelper"
    - path: ".planning/phases/03-dados-locais/03-EVIDENCE.md"
      provides: "Saidas reais arquivadas: XML instrumentado, percentis, EQP, koverVerify"
  key_links:
    - from: "AppContainer"
      to: "SentinelaDatabase.NAME e SENTINELA_MIGRATIONS"
      via: "Room.databaseBuilder(...).addMigrations(*SENTINELA_MIGRATIONS)"
      pattern: "addMigrations"
    - from: "SentinelaApp.onCreate"
      to: "incrementAppOpenCount + pruneNow"
      via: "coroutine de abertura, fora da main thread"
      pattern: "incrementAppOpenCount"
    - from: "app/build.gradle.kts (kover filters)"
      to: "org.sentinela.app.data.local.db.*"
      via: "exclusao do codigo gerado pelo Room"
      pattern: "data.local.db"
---

<objective>
Ligar tudo no `AppContainer`, provar o harness de migracao, ampliar o gate de cobertura
para `data/` e `settings/` e arquivar a evidencia pos-`clean` da fase.

Purpose: ultimo plano da fase. O filtro do Kover so pode ser ampliado AQUI — liga-lo antes
dos testes existirem quebra o build (licao literal da Phase 2, onde o gate foi adiado de
02-01 para 02-05).
Output: wiring completo, `koverVerify` verde com o denominador novo e `03-EVIDENCE.md`.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dados-locais/03-CONTEXT.md
@.planning/phases/03-dados-locais/03-RESEARCH.md
@.planning/phases/03-dados-locais/03-04-SUMMARY.md
@.planning/phases/03-dados-locais/03-05-SUMMARY.md
@.planning/phases/03-dados-locais/03-06-SUMMARY.md
@app/src/main/java/org/sentinela/app/AppContainer.kt
@app/src/main/java/org/sentinela/app/SentinelaApp.kt
@app/build.gradle.kts

<interfaces>
<!-- AppContainer atual (Phase 2) — os TODOs da Fase 3 sao os que este plano fecha: -->
class AppContainer(private val appContext: Context) {
    val decisionEngine: CallDecisionEngine by lazy { CallDecisionEngine() }
    private val regionProvider: RegionProvider by lazy { ... }
    val phoneNumberNormalizer: PhoneNumberNormalizer by lazy { ... }
    // TODO(Fase 3): settingsRepository (DataStore), whitelistRepository,
    //  blockedCallRepository (Room) e contador de aberturas — via interface.
}
<!-- O `regionProvider` usa hoje `userPreference = RegionProvider { null }` com o
     comentario "Degrau 2 so ganha persistencia na Fase 3". NAO ampliar o escopo:
     preferencia de regiao NAO esta nos requisitos desta fase — deixar o TODO
     apontando para a fase de UI e registrar isso no SUMMARY. -->

<!-- Kover atual (app/build.gradle.kts linhas 103-114): -->
kover { reports { filters { includes { classes("org.sentinela.app.domain.*", "org.sentinela.app.phone.*") } } }
        verify { rule("Cobertura minima de dominio e normalizacao") { minBound(80) } } }
<!-- Cobertura da Phase 2 no escopo atual: 97,619% -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wiring dos singletons no AppContainer e a abertura do app</name>
  <files>app/src/main/java/org/sentinela/app/AppContainer.kt, app/src/main/java/org/sentinela/app/SentinelaApp.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/AppContainer.kt (integral)
    - app/src/main/java/org/sentinela/app/SentinelaApp.kt (integral)
    - app/src/main/java/org/sentinela/app/data/local/db/SentinelaDatabase.kt
    - app/src/main/java/org/sentinela/app/settings/DataStoreSettingsRepository.kt
    - app/src/main/java/org/sentinela/app/data/local/RoomBlockedCallRepository.kt
    - .planning/phases/03-dados-locais/03-RESEARCH.md "Pitfall 3"
  </read_first>
  <action>
Em `AppContainer.kt`, substituir os TODOs da Fase 3 por singletons `by lazy`
(mantendo os TODOs das Fases 4-6 intactos):

```kotlin
    private val appScope: CoroutineScope =
        CoroutineScope(SupervisorJob() + Dispatchers.IO)

    /**
     * Instancia UNICA do banco. Construir mais de uma vez custaria abertura de
     * SQLite no caminho quente do Service (Fase 5).
     * `fallbackToDestructiveMigration` e PROIBIDO: apagaria a whitelist do usuario
     * numa atualizacao (verificado por scripts/verify-invariants.sh).
     */
    private val database: SentinelaDatabase by lazy {
        Room.databaseBuilder(appContext, SentinelaDatabase::class.java, SentinelaDatabase.NAME)
            .addMigrations(*SENTINELA_MIGRATIONS)
            .build()
    }

    /**
     * Instancia UNICA do DataStore. O runtime lanca
     * "There are multiple DataStores active for the same file" se houver duas
     * sobre o mesmo arquivo — reproduzido na pesquisa da Fase 3. Singleton e
     * contrato do runtime, nao estilo.
     * Caminho real: /data/data/<pkg>/files/datastore/sentinela_settings.preferences_pb
     * (excluido do backup por data_extraction_rules.xml e full_backup_content.xml).
     */
    private val settingsDataStore: DataStore<Preferences> by lazy {
        PreferenceDataStoreFactory.create(scope = appScope) {
            appContext.preferencesDataStoreFile(SETTINGS_DATASTORE_NAME)
        }
    }

    val settingsRepository: DataStoreSettingsRepository by lazy {
        DataStoreSettingsRepository(settingsDataStore, appScope)
    }

    val whitelistRepository: PersonalWhitelistRepository by lazy {
        RoomWhitelistRepository(database.whitelistDao())
    }

    val blockedCallRepository: RoomBlockedCallRepository by lazy {
        RoomBlockedCallRepository(database.blockedCallDao(), settingsRepository)
    }

    /**
     * Chamado uma vez na abertura do app: incrementa o contador (ENG-01, base do
     * convite de avaliacao da Fase 9) e aplica a retencao (HST-02).
     * Sem WorkManager — a poda de uma tabela local pequena nao justifica a
     * dependencia nem o custo de cold start.
     */
    fun onAppOpened() {
        appScope.launch {
            settingsRepository.incrementAppOpenCount()
            blockedCallRepository.pruneNow()
        }
    }

    private companion object {
        const val SETTINGS_DATASTORE_NAME = "sentinela_settings"
    }
```

Em `SentinelaApp.kt`, chamar `container.onAppOpened()` no `onCreate()`, DEPOIS da
inicializacao do container. A chamada e nao-bloqueante (lanca no `appScope` de IO):
`onCreate` nao pode ganhar I/O sincrono — e ele que define o cold start do Service.

NAO expor `database` publicamente (so os repositorios). NAO usar
`by preferencesDataStore(...)` como extensao de Context. NAO adicionar permissao nova.
NAO tocar em `regionProvider` — persistir a preferencia de regiao esta fora do escopo
da Fase 3; manter o TODO e registrar no SUMMARY.
  </action>
  <acceptance_criteria>
    - `./gradlew assembleDebug` com `BUILD SUCCESSFUL`
    - `[ "$(grep -c 'by lazy' app/src/main/java/org/sentinela/app/AppContainer.kt)" -ge 6 ]`
    - `grep -q 'addMigrations(\*SENTINELA_MIGRATIONS)' app/src/main/java/org/sentinela/app/AppContainer.kt`
    - `grep -q 'onAppOpened' app/src/main/java/org/sentinela/app/SentinelaApp.kt`
    - `[ "$(grep -c 'preferencesDataStore(' app/src/main/java/org/sentinela/app/AppContainer.kt)" -eq 0 ]`
    - `[ "$(grep -c 'TODO(Fase 3)' app/src/main/java/org/sentinela/app/AppContainer.kt)" -eq 0 ]`
    - `[ "$(grep -c 'TODO(Fase 4)' app/src/main/java/org/sentinela/app/AppContainer.kt)" -eq 1 ]`
    - `./gradlew assembleDebug lint detekt && bash scripts/verify-invariants.sh` sai 0 (allowlist de permissoes intacta)
  </acceptance_criteria>
  <verify>
    <automated>./gradlew assembleDebug testDebugUnitTest lint detekt &amp;&amp; bash scripts/verify-invariants.sh</automated>
  </verify>
  <done>Banco, DataStore e os tres repositorios sao singletons no container; abrir o app incrementa o contador e poda o historico.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MigrationHarnessTest — o harness de migracao esta de pe (QLT-03)</name>
  <files>app/src/androidTest/java/org/sentinela/app/data/local/db/MigrationHarnessTest.kt</files>
  <read_first>
    - app/src/main/java/org/sentinela/app/data/local/db/SentinelaDatabase.kt
    - app/src/main/java/org/sentinela/app/data/local/db/Migrations.kt
    - app/build.gradle.kts (sourceSets androidTest assets — plano 03-01)
    - app/schemas/ (1.json gerado no plano 03-03)
    - .planning/phases/03-dados-locais/03-RESEARCH.md "Harness de migracao" e Open Question 1
  </read_first>
  <behavior>
    - `MigrationTestHelper` cria e abre o banco na versao 1 a partir do JSON exportado
    - O banco v1 aberto pelo helper contem as tabelas `whitelist` e `blocked_call`
    - Abrir o banco de producao com `Room.databaseBuilder` + `SENTINELA_MIGRATIONS` roda sem erro de schema
  </behavior>
  <action>
Criar `app/src/androidTest/java/org/sentinela/app/data/local/db/MigrationHarnessTest.kt`:

```kotlin
@RunWith(AndroidJUnit4::class)
class MigrationHarnessTest {

    @get:Rule
    val helper = MigrationTestHelper(
        InstrumentationRegistry.getInstrumentation(),
        SentinelaDatabase::class.java,
    )

    /**
     * QLT-03. Com apenas a versao 1 nao existe migracao a testar — o que se prova
     * aqui e que o HARNESS funciona: o JSON exportado e consumivel e a primeira
     * migracao real ja nasce verificavel. Inventar uma migracao falsa nao provaria nada.
     */
    @Test
    fun schemaV1AbrePeloHelper() {
        helper.createDatabase(TEST_DB, 1).use { db ->
            val tables = mutableSetOf<String>()
            db.query("SELECT name FROM sqlite_master WHERE type='table'").use { c ->
                while (c.moveToNext()) tables += c.getString(0)
            }
            assertTrue("tabela whitelist ausente na v1: $tables", "whitelist" in tables)
            assertTrue("tabela blocked_call ausente na v1: $tables", "blocked_call" in tables)
        }
    }

    private companion object { const val TEST_DB = "migration-harness.db" }
}
```

⚠️ **A assinatura do construtor de `MigrationTestHelper` mudou entre Room 2.5/2.6/2.8**
(ha sobrecargas com `Class<out RoomDatabase>`, com `KClass` e com
`SupportSQLiteOpenHelper.Factory`). Confidence MEDIUM na pesquisa: o executor deve
confirmar contra o erro do compilador na primeira compilacao e usar a sobrecarga
NAO-deprecada de 2.8.4, registrando qual foi no SUMMARY. Feedback em segundos —
nao e risco de plano.

Se o helper falhar com "Cannot find the schema file", a causa e o `sourceSets` do
plano 03-01: conferir `getByName("androidTest").assets.srcDir("$projectDir/schemas")`.

Acrescentar um segundo teste abrindo o banco real de producao via
`Room.databaseBuilder(ctx, SentinelaDatabase::class.java, "migration-prod-check.db")
.addMigrations(*SENTINELA_MIGRATIONS).build().openHelper.writableDatabase` e afirmando
`version == 1`, com `deleteDatabase` no `@After`.
  </action>
  <acceptance_criteria>
    - `bash scripts/run-instrumented-tests.sh --tests "*MigrationHarnessTest"` termina com `BUILD SUCCESSFUL`
    - O `TEST-*.xml` correspondente tem `failures="0"`, `errors="0"` e `tests` >= 2
    - `grep -q 'MigrationTestHelper' app/src/androidTest/java/org/sentinela/app/data/local/db/MigrationHarnessTest.kt`
    - `[ "$(grep -c 'fallbackToDestructiveMigration' app/src/androidTest/java/org/sentinela/app/data/local/db/MigrationHarnessTest.kt)" -eq 0 ]`
    - SUMMARY registra qual sobrecarga de `MigrationTestHelper` foi usada no Room 2.8.4
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-instrumented-tests.sh --tests "*MigrationHarnessTest"</automated>
  </verify>
  <done>Harness de migracao verde no emulador, com as duas tabelas da v1 confirmadas pelo sqlite_master.</done>
</task>

<task type="auto">
  <name>Task 3: Ampliar o gate do Kover e arquivar a evidencia pos-clean</name>
  <files>app/build.gradle.kts, .planning/phases/03-dados-locais/03-EVIDENCE.md, .planning/phases/03-dados-locais/03-VALIDATION.md</files>
  <read_first>
    - app/build.gradle.kts (bloco `kover` linhas 101-114)
    - .planning/phases/03-dados-locais/03-RESEARCH.md secao "Kover — o que incluir sem criar falso-vermelho"
    - .planning/phases/02-motor-de-decisao-e-normalizacao/02-EVIDENCE.md (formato da evidencia)
    - .planning/phases/03-dados-locais/03-VALIDATION.md
  </read_first>
  <action>
**Passo 1 — ampliar o filtro.** Substituir o bloco `kover { }` de `app/build.gradle.kts` por:

```kotlin
// Cobertura das Fases 2-3: gate real sobre motor, normalizacao, dados e configuracoes.
// koverVerify quebra o build abaixo do minimo — o filtro define o denominador.
kover {
    reports {
        filters {
            includes {
                classes(
                    "org.sentinela.app.domain.*",
                    "org.sentinela.app.phone.*",
                    "org.sentinela.app.data.*",
                    "org.sentinela.app.settings.*",
                )
            }
            excludes {
                // Codigo gerado pelo Room (KSP) so executa em teste INSTRUMENTADO,
                // que o Kover nao mede. Incluir no denominador derrubaria o gate
                // com falso-vermelho mesmo com o codigo humano 100% coberto.
                // Fica coberto por connectedDebugAndroidTest (plano 03-04/03-05).
                classes("org.sentinela.app.data.local.db.*")
                classes("*_Impl", "*_Impl\$*")
                annotatedBy("androidx.room.Dao", "androidx.room.Database")
            }
        }
        verify {
            rule("Cobertura minima de dominio, normalizacao e dados") { minBound(80) }
        }
    }
}
```

**Passo 2 — provar o gate.** Rodar `./gradlew koverLog` e registrar o percentual.
Se cair abaixo de 80, a causa quase certamente e codigo gerado escapando das exclusoes:
inspecionar `app/build/reports/kover/html/index.html` classe a classe e ajustar a
EXCLUSAO — **nunca** afrouxar o `minBound`. Depois, subir o bound temporariamente para
99, confirmar que `koverVerify` QUEBRA o build, restaurar 80 e confirmar verde (mesma
disciplina do plano 02-05). Registrar as duas saidas.

**Passo 3 — evidencia pos-clean.** Executar, nesta ordem exata, e capturar as saidas:
```bash
./gradlew clean
./gradlew --no-build-cache assembleDebug testDebugUnitTest koverVerify lint detekt
bash scripts/verify-invariants.sh
bash scripts/run-instrumented-tests.sh
```
`--no-build-cache` alem do `clean` e obrigatorio: `FROM-CACHE` tem o mesmo defeito
probatorio que `UP-TO-DATE` (regra fixada na Phase 1).

Criar `.planning/phases/03-dados-locais/03-EVIDENCE.md` com, no minimo:
- linha `N actionable tasks: M executed` de cada comando, com **M > 0**;
- percentual final do `koverLog` e a saida do `koverVerify` verde, mais a saida vermelha
  do bound temporario em 99;
- contagem de testes JVM (`app/build/test-results/testDebugUnitTest/*.xml`);
- o caminho e o conteudo resumido do `TEST-*.xml` do androidTest (`tests`, `failures`, `errors`);
- as linhas `SENTINELA|contains|...` e `SENTINELA|EQP|...` copiadas do logcat real
  (percentis medidos e o plano de query literal);
- saida completa de `verify-invariants.sh` com os 4 invariantes do Bloco 5;
- pendencias diferidas para a Phase 9 (nenhum comportamento de OEM/telefonia foi validado
  nesta fase — apenas SQLite/DataStore em emulador, que e infraestrutura de teste).

Atualizar `.planning/phases/03-dados-locais/03-VALIDATION.md`: marcar os status da tabela
per-task de ⬜ para ✅ conforme o resultado real.
  </action>
  <acceptance_criteria>
    - `grep -q '"org.sentinela.app.data.\*"' app/build.gradle.kts` e `grep -q '"org.sentinela.app.settings.\*"' app/build.gradle.kts`
    - `grep -q 'org.sentinela.app.data.local.db' app/build.gradle.kts` dentro do bloco `excludes`
    - `grep -q 'annotatedBy("androidx.room.Dao", "androidx.room.Database")' app/build.gradle.kts`
    - `./gradlew clean && ./gradlew --no-build-cache assembleDebug testDebugUnitTest koverVerify lint detekt` termina com `BUILD SUCCESSFUL`
    - `bash scripts/run-instrumented-tests.sh` termina com `BUILD SUCCESSFUL` e todos os `TEST-*.xml` com `failures="0"`
    - `.planning/phases/03-dados-locais/03-EVIDENCE.md` existe e contem `--no-build-cache`, `SENTINELA|contains|`, `SENTINELA|EQP|` e `USING INDEX index_whitelist_number_key`
    - `[ "$(grep -c 'UP-TO-DATE\|FROM-CACHE' .planning/phases/03-dados-locais/03-EVIDENCE.md)" -eq 0 ]` nas linhas de prova de execucao
    - `.planning/phases/03-dados-locais/03-VALIDATION.md` sem nenhum `⬜ pending` restante
    - `grep -q 'minBound(80)' app/build.gradle.kts` (bound restaurado apos a prova em 99)
  </acceptance_criteria>
  <verify>
    <automated>./gradlew clean &amp;&amp; ./gradlew --no-build-cache assembleDebug testDebugUnitTest koverVerify lint detekt &amp;&amp; bash scripts/verify-invariants.sh &amp;&amp; bash scripts/run-instrumented-tests.sh</automated>
  </verify>
  <done>Gate de cobertura ampliado e provado falhando; evidencia real pos-clean arquivada em 03-EVIDENCE.md.</done>
</task>

</tasks>

<verification>
```bash
./gradlew clean
./gradlew --no-build-cache assembleDebug testDebugUnitTest koverVerify lint detekt
bash scripts/verify-invariants.sh
bash scripts/run-instrumented-tests.sh
cat .planning/phases/03-dados-locais/03-EVIDENCE.md
```
</verification>

<success_criteria>
- Criterio 1: `SettingsRepository` real ligado no container com defaults do MVP
- Criterio 2/3: whitelist e historico ligados como singletons sobre um unico `sentinela.db`
- Criterio 4: contador de aberturas incrementa na abertura do app
- Criterio 6: schemas exportados e `MigrationTestHelper` verde no emulador
- `koverVerify` verde com `data.*` + `settings.*` no denominador e o gerado excluido
- `03-EVIDENCE.md` com saidas reais pos-`clean --no-build-cache`, percentis e EQP
- Nenhum `checkpoint:human-*`; itens de OEM/telefonia registrados como diferidos para a Phase 9
</success_criteria>

<output>
Criar `.planning/phases/03-dados-locais/03-07-SUMMARY.md`
</output>
