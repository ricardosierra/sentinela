---
phase: 05-triagem-telecom-modo-filtro
plan: 06
type: execute
wave: 4
depends_on: ["05-05"]
files_modified:
  - app/src/androidTest/java/org/sentinela/app/telecom/ScreeningServiceBindTest.kt
  - app/src/androidTest/java/org/sentinela/app/telecom/DecisionPerformanceTest.kt
  - scripts/verify-invariants.sh
autonomous: true
requirements: [SCR-10, SCR-11, QLT-06]

must_haves:
  truths:
    - "O servico de triagem esta declarado de forma que o sistema consegue se vincular a ele"
    - "O caminho de decisao completo cabe com folga no orcamento, medido em aparelho virtual"
    - "Nenhuma condicao de bloqueio existe fora do motor de decisao, e o script prova isso"
  artifacts:
    - path: "app/src/androidTest/java/org/sentinela/app/telecom/ScreeningServiceBindTest.kt"
      provides: "vinculo real ao servico + prova do container unico"
    - path: "app/src/androidTest/java/org/sentinela/app/telecom/DecisionPerformanceTest.kt"
      provides: "percentis do caminho de decisao, assert na mediana"
      contains: "p50"
    - path: "scripts/verify-invariants.sh"
      provides: "Bloco 7 — regra de decisao concentrada no motor"
      contains: "Bloco 7"
  key_links:
    - from: "scripts/verify-invariants.sh"
      to: "app/src/main/java/org/sentinela/app/telecom"
      via: "busca por condicao de bloqueio fora do motor"
      pattern: "Bloco 7"
---

<objective>
Fechar a prova automatizada da fase: vinculo real do servico no aparelho virtual, percentis do
caminho de decisao com assercao na mediana, e um invariante de script que impede regra de bloqueio
de migrar para fora do motor.

Purpose: o compromisso do produto e responder muito antes do prazo do sistema, e o `CLAUDE.md`
concentra toda regra no motor. As duas coisas precisam de guarda automatica, senao regridem em
silencio.

Output: dois testes instrumentados e o Bloco 7 de `scripts/verify-invariants.sh`.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-triagem-telecom-modo-filtro/05-CONTEXT.md
@.planning/phases/05-triagem-telecom-modo-filtro/05-RESEARCH.md
@.planning/phases/05-triagem-telecom-modo-filtro/05-VALIDATION.md
@.planning/phases/05-triagem-telecom-modo-filtro/05-05-SUMMARY.md
@CLAUDE.md

<interfaces>
Comando de teste instrumentado (a task de `connectedDebugAndroidTest` **nao aceita** `--tests`):
```bash
bash scripts/run-instrumented-tests.sh --tests "*ScreeningServiceBindTest"
```
Os percentis saem em logcat por teste, em
`app/build/outputs/androidTest-results/connected/debug/Medium_Phone_API_35(AVD) - 15/logcat-<classe>-<metodo>.txt`.

Numeros medidos na pesquisa, no mesmo aparelho virtual (referencia, nao meta):
caminho de decisao completo com tudo aquecido **23,3 ms**; pior soma a frio ~50 ms; orcamento
declarado do produto 200 ms.

Precedentes do script `scripts/verify-invariants.sh`: `set -e` e **proibido**; `grep -c` sai com
codigo 1 quando o resultado e zero e **nunca** se acrescenta `|| echo 0` (duplicaria a saida);
o padrao correto e `[ "$(grep -c ...)" -eq 0 ]`. O script ja tem os Blocos 1 a 6.

`androidx.test.rule.ServiceTestRule` ja esta em `androidTestImplementation`. Ele serve para
provar o vinculo (declaracao, exportacao, binder nao nulo) — **nao** serve para exercitar a
triagem, porque o objeto de chamada exigido e interno da plataforma. O comportamento fica no
harness em JVM do plano 05-02.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vinculo real do servico e container unico</name>
  <read_first>
    - app/src/main/AndroidManifest.xml
    - app/src/main/java/org/sentinela/app/telecom/UnknownCallScreeningService.kt
    - app/src/androidTest/java/org/sentinela/app/InstrumentationSmokeTest.kt
    - scripts/run-instrumented-tests.sh
  </read_first>
  <files>
    app/src/androidTest/java/org/sentinela/app/telecom/ScreeningServiceBindTest.kt
  </files>
  <behavior>
    - vincular ao servico de triagem devolve um binder nao nulo
    - o container obtido pela aplicacao e o MESMO objeto em duas leituras seguidas
    - ler configuracoes pelo container da aplicacao funciona (nao ha instancia duplicada)
  </behavior>
  <action>
    1. Criar `ScreeningServiceBindTest` com `ServiceTestRule`, vinculando por `Intent` explicito
       ao `UnknownCallScreeningService`, com >= 3 `@Test` cobrindo `<behavior>`.
    2. O teste do container compara identidade (`===`) entre duas leituras e le um snapshot das
       configuracoes. KDoc, em prosa, registrando que construir um segundo container derruba o
       processo por haver duas instancias do armazenamento de preferencias sobre o mesmo arquivo
       — reproduzido na pesquisa.
    3. Nenhum teste aqui tenta exercitar a triagem: registrar em KDoc por que (o objeto de
       chamada exigido pelo binder e interno da plataforma) e apontar para o harness em JVM.
  </action>
  <verify>
    <automated>bash scripts/run-instrumented-tests.sh --tests "*ScreeningServiceBindTest"</automated>
  </verify>
  <acceptance_criteria>
    - `ScreeningServiceBindTest` tem >= 3 `@Test` e todos verdes no relatorio
      `app/build/outputs/androidTest-results/connected/debug/TEST-*.xml`
    - `[ "$(grep -c 'ServiceTestRule' app/src/androidTest/java/org/sentinela/app/telecom/ScreeningServiceBindTest.kt)" -ge 1 ]`
    - `[ "$(grep -c 'AppContainer(' app/src/androidTest/java/org/sentinela/app/telecom/ScreeningServiceBindTest.kt)" -eq 0 ]`
    - Se o aparelho virtual nao subir, o resultado e blocker reportado no SUMMARY — **nunca**
      troca silenciosa por teste em JVM
  </acceptance_criteria>
  <done>O sistema consegue se vincular ao servico e o processo tem um unico container.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Percentis do caminho de decisao, assert na mediana</name>
  <read_first>
    - app/src/androidTest/java/org/sentinela/app/data/local/WhitelistPerformanceTest.kt
    - app/src/androidTest/java/org/sentinela/app/data/contacts/ContactLookupPerformanceTest.kt
    - app/src/main/java/org/sentinela/app/telecom/ScreeningCoordinator.kt
    - .planning/phases/05-triagem-telecom-modo-filtro/05-RESEARCH.md (secao "Orcamento de cold start")
  </read_first>
  <files>
    app/src/androidTest/java/org/sentinela/app/telecom/DecisionPerformanceTest.kt
  </files>
  <behavior>
    - com o caminho aquecido, a MEDIANA de 100 triagens fica abaixo de 50 ms (folga de 4x sobre
      o orcamento declarado de 200 ms; medida de referencia da pesquisa: 23,3 ms)
    - a primeira triagem, com tudo frio, e medida e **reportada** em logcat, nunca afirmada
    - percentil de cauda e maximo sao reportados em logcat, nunca afirmados
    - a triagem medida exercita o caminho completo: configuracoes, agenda, whitelist, historico
      recente, motor e traducao
  </behavior>
  <action>
    1. Criar `DecisionPerformanceTest` seguindo o padrao ja estabelecido nas Fases 3 e 4:
       aquecimento explicito, 100 iteracoes, ordenacao das amostras, e uma unica linha de log com
       prefixo `SENTINELA|decision|` contendo p50, p95 e maximo.
    2. **Assert primario apenas na mediana.** Escrever em KDoc, em prosa, por que: a Fase 3
       precisou realocar um percentil de cauda instavel e a Fase 4 viu 30 ms virarem 140 ms sem
       mudanca de codigo. O veredito da cauda pertence a validacao em aparelho fisico da Fase 9.
    3. Usar o container da aplicacao (nunca construir outro) e numeros que resultem em bloqueio,
       para o caminho medido ser o mais longo.
    4. Nao usar cronometro como prova de estrutura: este teste mede tempo e so isso; nada nele
       afirma que uma dependencia foi usada.
  </action>
  <verify>
    <automated>bash scripts/run-instrumented-tests.sh --tests "*DecisionPerformanceTest"</automated>
  </verify>
  <acceptance_criteria>
    - `[ "$(grep -c 'SENTINELA|decision|' app/src/androidTest/java/org/sentinela/app/telecom/DecisionPerformanceTest.kt)" -ge 1 ]`
    - Existe exatamente um assert de tempo, e ele e sobre a mediana:
      `[ "$(grep -cE 'assert.*p95|assert.*max' app/src/androidTest/java/org/sentinela/app/telecom/DecisionPerformanceTest.kt)" -eq 0 ]`
    - `[ "$(grep -c 'p50' app/src/androidTest/java/org/sentinela/app/telecom/DecisionPerformanceTest.kt)" -ge 2 ]`
    - Os tres percentis medidos aparecem transcritos no SUMMARY, com a nota de que o veredito de
      cauda e da Phase 9
  </acceptance_criteria>
  <done>O caminho de decisao tem medida reproduzivel com assercao estavel na mediana.</done>
</task>

<task type="auto">
  <name>Task 3: Bloco 7 — regra de decisao concentrada no motor</name>
  <read_first>
    - scripts/verify-invariants.sh
    - app/src/main/java/org/sentinela/app/telecom/ScreeningCoordinator.kt
    - app/src/main/java/org/sentinela/app/telecom/UnknownCallScreeningService.kt
    - app/src/main/java/org/sentinela/app/domain/CallDecisionEngine.kt
  </read_first>
  <files>
    scripts/verify-invariants.sh
  </files>
  <action>
    1. Acrescentar o Bloco 7 ao final do script, antes do somatorio de falhas, com quatro
       checagens:
       - 7.1 — nenhum arquivo de `app/src/main/java/org/sentinela/app/telecom/` cita as
         politicas por origem ou o modo de bloqueio (o padrao busca os identificadores
         `OriginPolicy` e `BlockMode`), **exceto** o arquivo da fabrica de respostas, que
         precisa das configuracoes para traduzir. Excluir esse arquivo pelo caminho.
       - 7.2 — os construtores das decisoes de bloqueio (`CallDecision.Reject`,
         `CallDecision.Silence`, `CallDecision.BlockWithoutTrace`,
         `CallDecision.SendSilentlyToVoicemail`) so aparecem dentro de
         `app/src/main/java/org/sentinela/app/domain/`.
       - 7.3 — `app/src/main/java/org/sentinela/app/telecom/ScreeningCoordinator.kt` nao contem
         nenhum `import android.` (o coordenador precisa continuar puro e medido pela cobertura).
       - 7.4 — nenhum arquivo de `app/src/main/java` chama `respondToCall` fora do arquivo do
         servico, e no arquivo do servico ele aparece exatamente uma vez.
    2. Cada checagem usa o padrao correto de contagem do projeto:
       `[ "$(grep -c ...)" -eq 0 ]`. Nao introduzir `set -e` e nao usar `|| echo 0`.
    3. Cada mensagem de erro explica o defeito em portugues e diz onde a regra deveria estar.
    4. **Cuidado com a auto-sabotagem:** o proprio comentario do Bloco 7 nao pode conter os
       literais que ele procura em `app/src/main/java`. O script varre apenas
       `app/src/main/java`, entao comentar dentro do script e seguro — confirmar isso lendo os
       caminhos usados em cada `grep` antes de escrever o comentario.
  </action>
  <verify>
    <automated>bash scripts/verify-invariants.sh && ./gradlew assembleDebug testDebugUnitTest lint detekt</automated>
  </verify>
  <acceptance_criteria>
    - `[ "$(grep -c 'Bloco 7' scripts/verify-invariants.sh)" -ge 2 ]`
    - `[ "$(grep -c 'set -e' scripts/verify-invariants.sh)" -eq 0 ]`
    - `[ "$(grep -c '|| echo 0' scripts/verify-invariants.sh)" -eq 0 ]`
    - `bash scripts/verify-invariants.sh` termina com `== todos os invariantes OK ==`
    - Prova de vermelho obrigatoria, uma por checagem: (a) acrescentar temporariamente um `when`
      sobre a politica de origem no coordenador; (b) construir uma decisao de recusa dentro do
      servico; (c) acrescentar um import de plataforma no coordenador; (d) chamar a resposta ao
      sistema duas vezes no servico. Em cada caso o script deve reportar violacao. Restaurar tudo
      e transcrever as quatro saidas vermelhas no SUMMARY
  </acceptance_criteria>
  <done>Migrar regra de bloqueio para fora do motor passa a quebrar o build.</done>
</task>

</tasks>

<verification>
```bash
./gradlew assembleDebug testDebugUnitTest lint detekt && bash scripts/verify-invariants.sh \
  && bash scripts/run-instrumented-tests.sh
```
</verification>

<success_criteria>
- Vinculo real ao servico verde no aparelho virtual
- Mediana do caminho de decisao com folga registrada; cauda reportada e diferida
- Bloco 7 ativo com quatro provas de vermelho registradas
</success_criteria>

<output>
Criar `.planning/phases/05-triagem-telecom-modo-filtro/05-06-SUMMARY.md`.
</output>
