---
phase: 05-triagem-telecom-modo-filtro
plan: 07
type: execute
wave: 5
depends_on: ["05-06"]
files_modified:
  - docs/LIMITACOES.md
  - docs/TESTE-FISICO-SAMSUNG.md
  - docs/design/TELAS.md
  - app/src/main/res/values/strings.xml
  - app/src/main/java/org/sentinela/app/domain/ScreenedCall.kt
  - app/src/main/java/org/sentinela/app/settings/ScreeningSettings.kt
  - app/build.gradle.kts
  - .planning/STATE.md
  - .planning/phases/05-triagem-telecom-modo-filtro/05-EVIDENCE.md
autonomous: true
requirements: [SCR-04, SCR-06, QLT-01, QLT-06]

must_haves:
  truths:
    - "Nenhum texto do aplicativo ou da documentacao promete ausencia de registro no historico do telefone"
    - "Nenhum texto promete que a chamada toca durante o modo de nao perturbar"
    - "A documentacao registra que o bloqueio de numero oculto so vale no modo discador"
    - "A validacao fisica da Phase 9 sabe exatamente o que precisa medir nesta fase"
    - "A suite inteira fica verde a partir de um estado limpo, sem cache"
  artifacts:
    - path: "docs/LIMITACOES.md"
      provides: "itens 2, 3 e 7 corrigidos com a fonte"
    - path: "docs/TESTE-FISICO-SAMSUNG.md"
      provides: "cenarios 40 a 51"
      contains: "| 51 |"
    - path: ".planning/phases/05-triagem-telecom-modo-filtro/05-EVIDENCE.md"
      provides: "evidencia pos-limpeza da fase"
  key_links:
    - from: "docs/LIMITACOES.md"
      to: "docs/TESTE-FISICO-SAMSUNG.md"
      via: "cenarios de registro de comportamento"
      pattern: "TESTE-FISICO-SAMSUNG"
---

<objective>
Fechar a fase pela honestidade e pela prova: corrigir tudo que o projeto afirmava e a pesquisa
provou falso, registrar os cenarios de validacao fisica, reconciliar o gate de cobertura e
arquivar a evidencia a partir de um estado limpo.

Purpose: honestidade e feature. Tres afirmacoes do projeto morreram nesta fase — que o modo filtro
nunca recebe contatos, que pular o registro no historico do telefone e questao de fabricante, e
que a opcao de nunca silenciar fura o modo de nao perturbar. Deixar esses textos em pe faria o
produto prometer o que nao entrega.

Output: documentacao corrigida, cenarios 40-51, rotulos revistos, gate de cobertura reconciliado e
`05-EVIDENCE.md`.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-triagem-telecom-modo-filtro/05-CONTEXT.md
@.planning/phases/05-triagem-telecom-modo-filtro/05-RESEARCH.md
@.planning/phases/05-triagem-telecom-modo-filtro/05-06-SUMMARY.md
@docs/LIMITACOES.md
@docs/PERMISSOES.md
@CLAUDE.md

<interfaces>
Fatos de origem, todos lidos no codigo do proprio Android:
1. O sistema calcula `setShouldAddToCallLog(!skipCallLog || packageType != PACKAGE_TYPE_CARRIER)`.
   O Sentinela e do tipo escolhido pelo usuario, entao a expressao e sempre verdadeira: a chamada
   bloqueada **sempre** entra no registro nativo como bloqueada. Virar discador padrao na Fase 6
   **nao** destrava — so aplicativos de operadora sao isentos.
2. O sistema so pula o vinculo com o servico de triagem quando o numero esta na agenda **e** o
   aplicativo nao pode ler a agenda. Desde a Fase 4 o aplicativo pode: contatos **chegam** ao
   servico e a decisao passou a ser nossa.
3. O modo de nao perturbar e avaliado por um filtro separado e paralelo, que consulta o
   gerenciador de notificacoes. Nenhum campo da resposta de triagem o alcanca. O unico contorno
   exigiria uma permissao fora da lista permitida e mudaria a politica global do usuario.
4. `@Config(sdk = [36])` e impossivel com JDK 17 (exige Java 21). O valor correto e `[35]`.

Estado atual de `docs/LIMITACOES.md` (a corrigir): item 2 afirma que no modo filtro contatos
sempre tocam porque o filtro nem recebe essas chamadas; item 3 afirma que pular o registro e
"best-effort" e varia por fabricante; item 7 afirma que a semantica de nunca silenciar sera
confirmada na pesquisa da Fase 6.

`docs/TESTE-FISICO-SAMSUNG.md` vai hoje ate o cenario **39**. Os novos comecam em **40**.
A tabela de cenarios 40-51 proposta esta em `05-RESEARCH.md`, secao "Cenarios propostos para a
Phase 9" — transcrever integralmente.

Filtro de cobertura em `app/build.gradle.kts`: gate `minBound(80)`, `includes` com
`domain.*`, `phone.*`, `data.*`, `settings.*`; `excludes` por **nome de classe**, nunca por
pacote. O coordenador e puro e entra no denominador sem exclude.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir tudo que o projeto afirmava e nao e verdade</name>
  <read_first>
    - docs/LIMITACOES.md
    - docs/design/TELAS.md
    - app/src/main/res/values/strings.xml
    - app/src/main/java/org/sentinela/app/domain/ScreenedCall.kt
    - app/src/main/java/org/sentinela/app/settings/ScreeningSettings.kt
    - .planning/STATE.md
    - .planning/phases/05-triagem-telecom-modo-filtro/05-RESEARCH.md
  </read_first>
  <files>
    docs/LIMITACOES.md,
    docs/design/TELAS.md,
    app/src/main/res/values/strings.xml,
    app/src/main/java/org/sentinela/app/domain/ScreenedCall.kt,
    app/src/main/java/org/sentinela/app/settings/ScreeningSettings.kt,
    .planning/STATE.md
  </files>
  <action>
    1. `docs/LIMITACOES.md`:
       - **Item 2** — reescrever: as politicas por origem para contatos passam a valer no modo
         filtro **quando o aplicativo pode ler a agenda**; sem essa leitura o sistema nem vincula
         o servico e o contato toca pelo caminho nativo. Acrescentar o alerta de que revogar a
         leitura muda o comportamento sem nenhum aviso.
       - **Item 3** — reescrever: pular o registro no historico nativo **nao funciona** para
         aplicativos de terceiros; e decisao do proprio Android e nao variacao de fabricante; a
         chamada bloqueada sempre aparece como bloqueada. Registrar que virar discador padrao na
         Fase 6 nao muda isso. Registrar tambem o que **de fato** e entregue: nao toca, nao
         mostra tela de chamada e nao gera aviso de chamada perdida.
       - **Item 7** — reescrever: o aplicativo **nao consegue** contornar o modo de nao
         perturbar; a opcao chamada "Nunca Silenciar" significa que o Sentinela nunca silencia
         aquela origem, e nao que a chamada toca durante o nao perturbar. Citar que o unico
         contorno exigiria permissao fora da lista permitida.
       - Acrescentar item novo: numero oculto/restrito nao e entregue ao servico no modo filtro;
         a opcao existe e vale no modo discador (Fase 6).
       Cada item corrigido cita a origem (codigo do Android) e aponta o cenario correspondente do
       roteiro fisico.
    2. `app/src/main/res/values/strings.xml`:
       - Revisar `contacts_option_never_silence_desc` e `whitelist_option_never_silence_desc`,
         que hoje prometem ignorar o nao perturbar. Reescrever para descrever o que acontece de
         verdade: o Sentinela nunca silencia essa origem.
       - Acrescentar/ajustar o texto da opcao de ocultar do historico do telefone para descrever
         o efeito real, sem usar a expressao que sugere ausencia de registro.
    3. `docs/design/TELAS.md`: ajustar as duas descricoes de "Nunca Silenciar" e a nota do item
       "Ocultar do historico nativo" para o texto novo.
    4. KDoc de `ContactLookup` em `domain/ScreenedCall.kt`: hoje afirma que no modo filtro a
       plataforma so entrega nao-contatos e que o servico passa "ausente". Reescrever em prosa.
    5. KDoc de `ScreeningSettings`: hoje o resumo dos padroes usa a expressao que promete
       ausencia de registro. Reescrever.
    6. `.planning/STATE.md`: corrigir o bloqueio registrado sobre a versao de Android usada nos
       testes em JVM (o valor correto e 35, e o motivo e a versao do Java do projeto), e corrigir
       a decisao de bootstrap que afirma que o modo filtro so recebe nao-contatos. Marcar as duas
       como superadas por medicao, com data.
  </action>
  <verify>
    <automated>./gradlew assembleDebug testDebugUnitTest lint detekt && bash scripts/verify-invariants.sh</automated>
  </verify>
  <acceptance_criteria>
    - `[ "$(grep -c 'best-effort' docs/LIMITACOES.md)" -eq 0 ]`
    - `[ "$(grep -c 'confirmada na pesquisa da' docs/LIMITACOES.md)" -eq 0 ]`
    - `[ "$(grep -c 'Fase 6' docs/LIMITACOES.md)" -ge 1 ]` (o item novo de numero oculto aponta a fase)
    - `[ "$(grep -cE 'Ignora o modo|mesmo em .Não Perturbe' app/src/main/res/values/strings.xml)" -eq 0 ]`
    - `[ "$(grep -c 'sem rastro' app/src/main/java/org/sentinela/app/settings/ScreeningSettings.kt)" -eq 0 ]`
    - `[ "$(grep -c 'só entrega não-contatos\|so entrega nao-contatos\|só recebe não-contatos' app/src/main/java/org/sentinela/app/domain/ScreenedCall.kt)" -eq 0 ]`
    - `[ "$(grep -c 'sdk = \[36\]' .planning/STATE.md)" -eq 0 ]`
    - `[ "$(grep -cE '100% garantid' docs/LIMITACOES.md app/src/main/res/values/strings.xml)" -eq 0 ]`
  </acceptance_criteria>
  <done>Nenhum texto do produto ou da documentacao afirma algo que a pesquisa provou falso.</done>
</task>

<task type="auto">
  <name>Task 2: Cenarios 40 a 51 do roteiro fisico</name>
  <read_first>
    - docs/TESTE-FISICO-SAMSUNG.md
    - .planning/phases/05-triagem-telecom-modo-filtro/05-RESEARCH.md (secao "Cenarios propostos")
    - .planning/ROADMAP.md
  </read_first>
  <files>
    docs/TESTE-FISICO-SAMSUNG.md
  </files>
  <action>
    1. Acrescentar a secao "Pendencias herdadas da Phase 5 (triagem no modo filtro)" apos a
       secao da Phase 4, com a tabela dos cenarios **40 a 51** transcrita integralmente de
       `05-RESEARCH.md` (bloqueio real, onde a chamada bloqueada aparece no historico do
       fabricante, contato tocando, leitura de agenda revogada, caixa postal, nao perturbar,
       silenciar, percentis em aparelho, inicio a frio por chamada, perda do papel, notificacao
       na tela bloqueada, dois chips / chamada em espera).
    2. Manter o formato de coluna ja usado no arquivo (numero, cenario, acao, esperado, coluna
       de resultado em branco).
    3. Registrar explicitamente que os criterios 1, 2 e 6 do ROADMAP desta fase, e o veredito do
       percentil de cauda, sao **diferidos para a Phase 9** — nao sao lacuna desta fase.
    4. Atualizar a secao de criterios de aceite cobertos no fim do arquivo, se existir.
  </action>
  <verify>
    <automated>bash scripts/verify-invariants.sh && [ "$(grep -c '^| 51 |' docs/TESTE-FISICO-SAMSUNG.md)" -eq 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - `[ "$(grep -c '^| 40 |' docs/TESTE-FISICO-SAMSUNG.md)" -eq 1 ]`
    - `[ "$(grep -c '^| 51 |' docs/TESTE-FISICO-SAMSUNG.md)" -eq 1 ]`
    - Os doze cenarios existem sem lacuna:
      `[ "$(grep -cE '^\| (4[0-9]|5[01]) \|' docs/TESTE-FISICO-SAMSUNG.md)" -eq 12 ]`
    - `[ "$(grep -c 'Phase 5' docs/TESTE-FISICO-SAMSUNG.md)" -ge 1 ]`
  </acceptance_criteria>
  <done>Tudo que so pode ser provado em aparelho fisico esta escrito, numerado e diferido para a Phase 9.</done>
</task>

<task type="auto">
  <name>Task 3: Gate de cobertura e evidencia pos-limpeza</name>
  <read_first>
    - app/build.gradle.kts
    - .planning/phases/04-contatos-do-aparelho/04-EVIDENCE.md
    - .planning/phases/05-triagem-telecom-modo-filtro/05-VALIDATION.md
    - .planning/STATE.md
  </read_first>
  <files>
    app/build.gradle.kts,
    .planning/phases/05-triagem-telecom-modo-filtro/05-EVIDENCE.md
  </files>
  <action>
    1. Rodar `./gradlew koverLog` e registrar o percentual atual. Se `koverVerify` passar com o
       filtro atual, **nao acrescentar nenhum exclude** — o coordenador e puro e os demais
       arquivos novos rodam sob Robolectric, que e teste em JVM e portanto e medido.
    2. Se e somente se `koverVerify` falhar, acrescentar excludes por **nome de classe**
       (nunca por pacote), um por classe, com comentario justificando por que aquela classe nao
       e medida. Candidatas legitimas: apenas classes que so executam em teste instrumentado.
       O coordenador **nunca** pode ser excluido: se ele estiver fora, a fiacao ficou errada.
    3. Demonstrar o gate falhando: subir `minBound` temporariamente para 99, ver o build quebrar,
       restaurar para 80. Transcrever a saida.
    4. Criar `05-EVIDENCE.md` no formato do `04-EVIDENCE.md`, contendo:
       - o comando completo executado a partir de um estado limpo:
         ```bash
         ./gradlew clean && ./gradlew --no-build-cache assembleDebug testDebugUnitTest \
           koverVerify lint detekt && bash scripts/verify-invariants.sh \
           && bash scripts/run-instrumented-tests.sh
         ```
       - a linha `N actionable tasks: M executed` com **M maior que zero** (`FROM-CACHE` tem o
         mesmo defeito probatorio que `UP-TO-DATE`);
       - contagem de testes em JVM e instrumentados;
       - percentual de cobertura e o filtro em vigor;
       - os percentis do caminho de decisao medidos no plano 05-06;
       - a lista de todas as provas de vermelho executadas na fase, plano a plano;
       - a lista de pendencias diferidas para a Phase 9 (cenarios 40-51) e para a Phase 7
         (telas do opt-in de notificacao e do papel).
  </action>
  <verify>
    <automated>./gradlew clean && ./gradlew --no-build-cache assembleDebug testDebugUnitTest koverVerify lint detekt && bash scripts/verify-invariants.sh && bash scripts/run-instrumented-tests.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/05-triagem-telecom-modo-filtro/05-EVIDENCE.md`
    - `[ "$(grep -c 'no-build-cache' .planning/phases/05-triagem-telecom-modo-filtro/05-EVIDENCE.md)" -ge 1 ]`
    - `[ "$(grep -cE 'actionable tasks: [0-9]+ executed' .planning/phases/05-triagem-telecom-modo-filtro/05-EVIDENCE.md)" -ge 1 ]`
    - `[ "$(grep -c 'minBound(80)' app/build.gradle.kts)" -eq 1 ]`
    - Nenhum exclude por pacote novo:
      `[ "$(grep -c 'org.sentinela.app.telecom.\*' app/build.gradle.kts)" -eq 0 ]`
    - `[ "$(grep -c 'ScreeningCoordinator' app/build.gradle.kts)" -eq 0 ]`
    - `koverVerify` verde com `minBound(80)` e a demonstracao de falha com 99 transcrita
  </acceptance_criteria>
  <done>A fase inteira e reproduzivel a partir de um estado limpo, com cobertura no gate e evidencia arquivada.</done>
</task>

</tasks>

<verification>
```bash
./gradlew clean && ./gradlew --no-build-cache assembleDebug testDebugUnitTest koverVerify lint detekt \
  && bash scripts/verify-invariants.sh && bash scripts/run-instrumented-tests.sh
```
</verification>

<success_criteria>
- Itens 2, 3 e 7 de `docs/LIMITACOES.md` corrigidos com a origem citada
- Rotulos e documentos de tela sem promessa de contornar o nao perturbar ou de ausencia de registro
- Cenarios 40 a 51 no roteiro fisico, sem lacuna
- `koverVerify` verde com `minBound(80)` e falha demonstrada
- `05-EVIDENCE.md` com evidencia pos-limpeza e `M > 0`
</success_criteria>

<output>
Criar `.planning/phases/05-triagem-telecom-modo-filtro/05-07-SUMMARY.md`.
</output>
