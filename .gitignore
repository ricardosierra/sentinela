# Built application files
*.apk
*.aab

# Files for the ART/Dalvik VM
*.dex

# Java class files
*.class

# Generated files
bin/
gen/
out/

# Gradle files
.gradle/
build/
**/build/

# Kotlin compiler session files
.kotlin/

# Local configuration file (sdk path, etc)
local.properties

# APK and AAB files
releases/

# Log Files
*.log

# Android Studio Navigation editor temp files
.navigation/

# Android Studio captures folder
captures/

# IntelliJ
*.iml
.idea/

# Keystores e certificados (NUNCA commitar!)
*.keystore
*.jks
*.der
app/keystore/
keystore.properties

# Credenciais do Google Play (ficam fora do repositório e entram somente por ambiente).
.secrets/
google-play-service-account*.json
*-service-account*.json

# Arquivos de mapeamento ProGuard/R8
mapping*.txt
releases/mapping-*.txt

# External native build folder
.externalNativeBuild
.cxx/

# NDK
obj/

# JAR files (exceto o gradle wrapper)
*.jar
!gradle/wrapper/gradle-wrapper.jar

# AAR files
*.aar

# Android Profiling
*.hprof

# OS-specific files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Temporary files
*.tmp
*.temp

# Backup files
*.bak
*.backup
*~

# Arquivos privados
/private

# Planos e documentos temporários
*.plan.md

# Claude Code — o gitignore global do usuário ignora .claude/ inteiro;
# reincluir aqui para versionar as settings do projeto (travas), mantendo o local fora
!.claude/
.claude/*
!.claude/settings.json

# graphify (gerado)
/.graphify/
/graphify/
/graphify-out/
/graphify-output/
/graphify-cache/

# Metadados temporários da Play, derivados de docs/loja/ pelo script de publicação.
/app/src/main/play/

# Design de referência (zip original fica fora; extraído vive em docs/design/telas)
stitch_silent_call_blocker.zip

# As duas linhas abaixo continuam aqui como rede de seguranca, mas o par de chaves
# NAO mora mais na raiz: em 2026-08-24 ele foi movido para ~/.secrets/sentinela/ com
# permissao 600. Um .gitignore nao impede `git add -f`, e chave privada de carteira
# nao tem por que morar dentro de arvore de trabalho de repositorio publico. Se o
# arquivo reaparecer aqui, estas linhas evitam o commit acidental.
.bitcoin_private_key.pem
.bitcoin_address.txt

# Ambiente virtual Python (ferramenta local, nunca versionado)
.venv/

# Sujeira recorrente na RAIZ do repositorio. Verificado em 2026-08-23: NENHUM
# destes arquivos estava rastreado — todos ja caem nos padroes acima. A lista
# fica nominal para quem procurar pelo nome antes de acrescentar regra duplicada.
#   dialer_fail.log, test_output.log  -> *.log
#   java_pid*.hprof                   -> *.hprof (dump de OOM do daemon do Gradle,
#                                        gerado por -XX:+HeapDumpOnOutOfMemoryError)
#   sentinela-debug.apk               -> *.apk (copia da raiz feita por build.sh)
#   sentinela-v*-release.aab/.apk     -> *.aab e *.apk (artefato publicado mora em releases/)
#   stitch_silent_call_blocker.zip    -> ja listado nominalmente acima

# --- Artefatos de assinatura e segredos ---
# Chave privada e senha no mesmo repositorio = identidade de publicacao comprometida.
# Chave de assinatura Android nao pode ser trocada depois que o app esta publicado.
*.keystore
*.jks
*.p12
*.pfx
*.pkcs12
*.jceks
*.mobileprovision
*.provisionprofile
*.pem
*.key
*.ppk
key.properties
keystore.properties
signing.properties
local.properties
google-services.json
GoogleService-Info.plist
id_rsa
id_dsa
id_ecdsa
id_ed25519
.netrc
.pypirc
.env
.env.*
!.env*.example
!.env*.sample
!.env*.template
!.env*.dist
